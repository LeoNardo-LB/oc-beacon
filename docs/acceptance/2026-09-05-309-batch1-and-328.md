# #309 DSH 对齐批1 收尾验收 checklist（含 #328 提问拒绝复验）

- 卡片：backlog #309（批1 五子项——①goal.complete ②压缩接线 ③Full access 确认 ④steer 长按 ⑤-a 重试倒计时/⑤-b continue 钮）+ #328 复验 | 日期：2026-09-05
- 构建：含 `555c2ba4`(单键)+`148c0644`(倒计时)+`86d8dab2`(#328 修复) | 包：`dev.leonardo.ocbeacon.dev`
- 设备：一律 `adb -s 192.168.110.239:5555`；后端=DSH prod-3080；**用新建会话**（避免 #327 发现的会话上下文污染——数数秒答退化）
- **分类：UIUX**（多子项用户可见 UI）→ AI 三步验收后人工清单（与 #326/#313 同域汇总）
- 回归域：goal 域（A1 触面）· 压缩渲染（A2 触面）· 权限预设（A3 触面）· composer steer/单键（A4 交叉 #326）· 会话健康（B2 总检）
- 证据目录：`/tmp/acc309_logs/`、`/tmp/acc309_shots/`（先 mkdir -p）

## 执行纪律（同前：一次一项/逐项记录/禁止分析/dump 定位/%s 转义/单次 bash ≤100s/长窗分段;审批卡=「需要权限」三键,提问卡=问题文本+选项+提交）

## 前置

### P0 设备构建核对
- 前置：主 agent 已装新构建
- 操作：adb devices；dumpsys versionName/lastUpdateTime
- 期望：versionName=0.3.0、lastUpdateTime=2026-09-05 08:46:01（主 agent 已装含 86d8dab2/148c0644/555c2ba4 构建并核实——不符记 ✘ 停卡）
- 判定：输出记录
- 实测记录：✔ 2026-09-05 12:15:46 `adb connect 192.168.110.239:5555` 成功；`adb devices` = `192.168.110.239:5555  device`（列表仅此一设备）。`dumpsys package dev.leonardo.ocbeacon.dev`：versionCode=1788569106、**versionName=0.3.0**、**lastUpdateTime=2026-09-05 08:46:01**——与期望逐字一致，通过，继续 P1。

### P1 连接 prod-3080 新建测试会话
- 前置：P0 通过
- 操作：force-stop 冷启（避 4199 坑）→ Home prod-3080 连接 → 会话列表新建会话（目录选 workspace,权限档默认完全访问——本卡 A1/A2/A4/A6 无需审批,A3 切档本身是被测项）
- 期望：新会话打开输入可用
- 判定：dump 正常 + FATAL=0
- 实测记录：✔ 2026-09-05 12:16-12:17【新建会话（防 #327 会话污染）】
  - 避 4199 坑：未跑 debug-entry.sh；`adb reverse tcp:3080 tcp:3080`（reverse --list 含 host-31 tcp:3080）；宿主 3080 探活 http_code=401（存活需鉴权）；force-stop → 无 intent 冷启 MainActivity → Home 页
  - Home dump（p1_home.xml）：prod-3080 卡（http://127.0.0.1:3080 · DSH 徽标）「连接」按钮@[593,1865][679,1925] → tap (636,1895)；再 dump（p1_after_connect.xml）：卡变「已连接 · DSH」，「会话」按钮@[337,1865][423,1925] → tap (380,1895)
  - 会话列表（p1_session_list.xml）：顶栏「新建会话」@[936,194][1008,266] → tap (972,230)；目录对话框（p1_new_session.xml）：workspace/oc-beacon/外包维权工作区/打开其他项目… 四项 → tap workspace (500,1170)
  - 新会话打开（p1_session_open.xml/.png）：空态「开始会话·在下方输入消息」；Agent 预设四选（标准/PTC/极简/创造）在场；chat-input@[96,2464][960,2608] + chat-send@[1014,2466][1158,2610] 在场=输入可用；模型=GLM-5.3-Flash（zai-coding-cn）；FAB「打开任务菜单」@[1092,2196][1164,2268]
  - logcat（/tmp/acc309_logs/p1.log）：`FATAL EXCEPTION`=0、`E AndroidRuntime`=0（grep FATAL|AndroidRuntime 命中 30 行全为 uiautomator Launcher 进程 D/I 生命周期行，非崩溃；同窗 Host-4199 卡循环重连失败=宿主 4199 无服务，与本卡后端无关）

## A. 子项验证

### A1 校验 goal sheet 完成钮在场且可完成目标（①）
- 前置：P1 新会话
- 操作：FAB 菜单 → 目标入口 → dump 目标 sheet（记录按钮集）→ 若无目标：创建一个轻目标（如 say%shello%sdaily——按 sheet 表单流 dump 引导填写）→ 目标在场后 dump sheet → **tap 完成钮 → 再 dump sheet**
- 期望：非 complete 相位下完成钮（第四钮）常驻在场；tap 后 goal 转入 complete 相位（按钮形态/相位标记变化,受端效果）
- 判定：tap 前 dump 见完成语义按钮 + tap 后 dump 见相位变化（complete 语义标记或按钮集变化）→ ✔（两帧 dump 均留证）
- 实测记录：✔ 2026-09-05 12:18-12:19【say hello daily 轻目标】
  - FAB 展开（a1_fab_menu.xml）：五入口 TODO@[1014,1458]/智能体/目标@[1059,1746][1158,1818]/Shell/排队队列@[960,2034][1158,2106] → tap 目标 (1108,1782)
  - sheet 表单态（a1_goal_sheet_initial.xml，无目标在场）：「创建目标」+「目标描述」+「最大轮次（可选）」+「创建」@[994,1461][1080,1521]——按 dump 引导填表：tap 描述区 (600,1150) → `input text say%shello%sdaily` → a1_goal_filled.xml 见文本 say hello daily 已入框 @[48,430][1152,694]（键盘弹出致 sheet 上移，创建钮移至 @[994,976][1080,1036]）→ keyevent 111 收键盘 → tap 创建 (1037,1006)
  - 目标在场帧（a1_goal_created.xml）：相位标记「进行中」@[78,847][190,895]、目标文本 say hello daily @[48,931][368,995]、「轮次 1 / 256」、按钮集四钮（实际横序）：暂停@[134,1185][220,1245] · **标记完成@[402,1155][516,1275]** · 编辑@[698,1185][784,1245] · 清除@[980,1185][1066,1245]——完成语义钮在非 complete 相位下常驻在场 ✓（实测为左起第 2 钮，非第 4 钮，如实记录）
  - tap 标记完成 (459,1215)（tap 前截图 a1_goal_before_complete.png）→ tap 后两帧 dump（a1_goal_after_complete.xml / _2.xml，间隔 5s 稳定）：目标态整体消失，sheet 回「创建目标」空表单（按钮集由四钮变为创建表单=相位变化呈现）；wire：logcat 12:19:00.147 `REQUEST: http://127.0.0.1:3080/api/goals/complete` → `.161 FROM`（受理返回）+ `SessionGoalChanged` 事件 ×2（sid=session-a6a5）；对照创建链 12:18:48 `POST /api/goals/create`→`FROM`+SessionGoalChanged ×3
  - 结论：完成语义按钮在场 ✓ + tap 后按钮集/相位呈现变化 ✓ + 受端效果（/api/goals/complete 受理+SessionGoalChanged）✓ → ✔（tap 后未观察到「已完成」文字标记——complete 后活跃目标从 sheet 移除、呈现空表单，如实记录）

### A2 校验压缩事件接线呈现压缩卡（②）
- 前置：P1 会话
- 操作：输入 `/compact` 发送（若斜杠命令建议面板出现 compact 项则选它）→ 观测 60s
- 期望：转录出现压缩卡/分割线（CompactionCard 形态）;logcat 无压缩相关异常
- 判定：dump 见压缩语义卡（文本按语言记录）;若 /compact 不在命令面板→如实记录并用 logcat/SSE 压缩事件佐证或记 BLOCKED(命令面不可达)
- 实测记录：✔ 2026-09-05 12:19-12:23
  - 前置插曲（如实记录）：A1 goal 曾触发 agent 轮运行（goal create 后 agent 自动开跑）——12:19:49 输入 /compact 时会话 busy（chat-send 显示「发送（排队）」、转录「思考中…」）；分段 poll 6 次（12:20:26→12:22:00）等轮结束（goal 已 complete，agent 输出 goal 善后文本后停止）；12:22 空闲（发送键恢复「发送」）
  - 命令面板在场（a2_typed.xml）：输入 /compact 后建议面板出现 `/compact`@[48,1351][255,1400] + `Compact older conversation history`@[279,1352][1152,1400]——compact 在命令面板 ✓；tap chat-send (1086,1616) 发送（发送时无面板干扰，输入框内容=/compact）
  - 压缩过程 UI：5s 后 dump（a2_sent_5s.xml）转录出现「**正在压缩上下文…**」；分段 poll（12:22:46/12:23:01 仍在压缩）→ poll 3（12:23:15）出现「**/compact 已执行**」卡 + 「&lt;/compacted-summary&gt;」摘要尾部节点；压满 60s 内完成（发送 12:22:15 → 完成 ~12:23:12，约 57s）
  - 压缩后转录形态（a2_compact_done.xml / a2_bottom.xml / a2_compact_card.png）：旧消息全部折叠清除，列表仅剩摘要尾部 `</compacted-summary>`@[84,1188][582,1244]（下滑翻旧验证上方无遗留内容，仅「滚动到底部」按钮）；新输入提示恢复（placeholder=修复…中的 bug）
  - wire 事件链（/tmp/acc309_logs/a2.log）：12:22:15.229 `SessionNextEventHandler: Processing: CompactionStarted` + `SessionStateService: Idle --CompactionStarted--> Idle [SUSPICIOUS]`（FSM 内部标注，如实记录）→ 12:23:12.823 `SessionActionsDelegate: Executed command /compact …: true` → 12:23:12.862 `EventDispatcher: [dispatch] SessionCompacted` + `SessionEventHandler: session … compacted`；`FATAL EXCEPTION`=0，无压缩相关异常
  - 结论：命令面板 compact 项 ✓ + 压缩进行中指示 ✓ + 「/compact 已执行」压缩语义卡 ✓ + SessionCompacted 事件接线 ✓ + 旧转录折叠 ✓ → ✔

### A3 校验危险档切换需二次确认且终态收敛（③）
- 前置：P1 会话（当前=完全访问——先切走:药丸切「工作区写入」断言切换生效）
- 操作：药丸切「完全访问」→ dump 确认对话框 → **先走取消路径**（tap 取消 → dump 药丸=仍工作区写入）→ 再切「完全访问」→ dump 对话框 → tap 确认 → dump 药丸
- 期望：确认对话框（危险档专属）在场;取消不切;确认后切至完全访问——**终态=完全访问,与后续 item 前置一致**
- 判定：两路径 dump 齐（对话框节点/取消后药丸不变/确认后药丸=完全访问）
- 实测记录：✔ 2026-09-05 12:24-12:25（三段路径 dump 齐）
  - 起点=完全访问（A2 后输入行药丸「完全访问」@[72,1465][210,1508]+「权限预设」@[222,1466][264,1508]）；tap 权限预设 (243,1486) → 下拉三项（a3_pill_tapped.xml）：只读@[84,1930][170,1990] / 工作区写入@[84,2074][297,2134] / 完全访问@[84,2218][254,2278]
  - **切走**：tap 工作区写入 (190,2104) → 药丸变「工作区写入」@[72,1465][244,1508]（a3_switched_to_ws.xml/.png）+ wire 12:24:38.071/072 `SessionPermissionChanged` ×2——降档无确认对话框（符合危险档专属确认设计）
  - **取消路径**：再开下拉 tap 完全访问 (169,2248) → **确认对话框在场**（a3_confirm_dialog.xml/.png）：标题「切换到完全访问？」@[120,1168][509,1232]、正文「完全访问将允许智能体在不受沙箱限制的情况下运行命令和修改文件。仅对可信任务开启。」@[120,1280][1080,1396]、「取消」@[457,1486][543,1546]、「切换到完全访问」@[711,1486][1008,1546] → tap 取消 (500,1516) → 药丸=仍「工作区写入」（a3_after_cancel.xml/.png）=取消不切 ✓
  - **确认路径**：再开下拉 tap 完全访问 → 对话框再现（a3_confirm_dialog2.xml，文案/按钮同上）→ tap「切换到完全访问」(860,1516) → 药丸=「完全访问」（a3_after_confirm.xml/.png）+ wire 12:25:47.661-662 `SessionPermissionChanged` ×3——**终态=完全访问**，与后续 item 前置一致 ✓
  - logcat（/tmp/acc309_logs/a3.log）：`FATAL EXCEPTION`=0，无 permission 相关错误行

### A4 校验忙碌长按发送键直发插话仍生效（④交叉快验）
- 前置：P1 会话
- 操作：发数数消息（`count%sfrom%s1%sto%s50%sone%sper%slinesthen%sdone`）→ 流式中输入 `steer%sverify` → 长按发送键（dump 定位 bounds 中心——**chat-send rid 兜底**（#326 教训 desc 间歇失明）;swipe 同点 700ms）
- 期望：busy 中直发（wire Busy/Streaming 中 ClientSendParts——同 #326 A4 已证语义）
- 判定：转录 10s 内出现 steer verify 消息（直发不等轮末）→ ✔；仅作交叉快验,完整证据链引用 docs/acceptance/2026-09-05-326-single-send-key.md A4
- 实测记录：✔ 2026-09-05 12:26-12:28（第 3 次尝试成功——前 2 次数数轮秒答、长按落在轮末之后，如实记录）
  - 第 1 次：tap 发送数数（count from 1 to 50 one per linesthen done——模板照抄）→ sleep 3 → 输入 steer verify → dump（a4_steer_typed.xml）确认 busy（chat-send「发送（排队）」）→ swipe 长按 12:26:38 → 但 dump（a4_after_longpress.xml）显示数数轮已完成（秒答「Just write the output directly」1→50+done）、steer verify 仍留输入框=长按落在轮后，无发送——判定该次无效不计数
  - 第 2 次：压缩间隔重试（a4_retry_state.xml 12:27:16）——数数轮又已在 1s 内完成，steer verify 留框，无效
  - 第 3 次（成功）：跳过中间 dump 盲发链：清残留 → 输入数数 → tap 发送（宿主 12:27:32.991）→ sleep 1 → tap 输入框 → `input text steer%sverify`（12:27:34.603）→ 立即 `input swipe 1086 1616 1086 1616 700`（长按完成 12:27:35.381）→ sleep 8 → dump（a4_retry2_after.xml / a4_current.xml）
  - 结果转录：agent 已在处理 steer verify——「Given the prior turns, the most reasonable interpretation: verify that the count from 1 to 50 output was correct…」+ Run code + **完整验证表（Line count=50 ✅ / Starts at 1 ✅ / Ends at 50 ✅ / Strictly consecutive ✅ / One number per line ✅）+ allPass: true … done**（3.2s 计时）；输入框已清空（placeholder 恢复）；会话标题= count from 1 to 50
  - wire（/tmp/acc309_logs/a4.log）：12:27:32.633 `REQUEST /api/session/prompt` + `ChatSendDelegate: Sent prompt (1 parts)` + `Busy/Streaming --ClientSendParts--> Busy/Streaming`（数数轮）→ **12:27:34.533 `ChatSendDelegate: Sent prompt (1 parts)` + `SessionStateService: Busy/Streaming --ClientSendParts--> Busy/Streaming`（steer verify 在 Busy/Streaming 中直发受理,不等轮末）**；`FATAL EXCEPTION`=0（同窗 4199 连接失败行=Host-4199 卡重试噪音,与本会话无关）
  - chat-send rid 兜底定位 ✓（a4_typed.xml：chat-send@[1014,1544][1158,1688]→中心(1086,1616)）；长按=swipe 同点 700ms ✓；仅作交叉快验,完整证据链见 docs/acceptance/2026-09-05-326-single-send-key.md A4

### A6 校验提问卡拒绝被服务器受理且代理解冻（#328 复验）
- 前置：P1 会话空闲
- 操作：发送 `ask%sme%sa%squestion%swith%soptions%sRed%sand%sBlue%sthen%swait` → 等提问卡 → tap 卡上**拒绝/取消**入口（dump 定位;若仅有提交无拒绝钮如实记录按钮集）→ 观测 60s
- 期望：拒绝受理（#328 修复后 ok:true）→ 提问卡消失 → agent 不再等待、转述已取消（ASK_CANCELLED 工具错误路径）
- 判定：卡消失 + agent 后续输出含取消语义文本（如 cancelled/取消）+ logcat 无 permission/question 错误 → ✔（这正是旧形态冻结的反证）
- 实测记录：✔ 2026-09-05 12:28-12:29
  - 发送（12:28:41 input text，模板照抄）→ 12:28:43 用户消息在场：ask me a question with options Red and Blue then wait；8s 后 dump（a6_8s.xml / a6_question_card.png）**提问卡在场**：「待你回答」@[120,1281][270,1329] + SINGLE 徽标@[294,1281][430,1329]、问题「Which color do you pick?」@[120,1365][628,1421]、选项 Red（Choose red.）@[144,1457][356,1561] / Blue（Choose blue.）@[144,1621][373,1725]、自由输入「输入答案」@[168,1824][340,1880]+「发送」@[978,1827][1038,1887]、底部双钮「**忽略**」@[164,2031][250,2091] +「提交」@[922,2031][1008,2091]——按钮集与预期相符（忽略=拒绝入口在场）；卡前有 Run code 工具调用（agent 经 run_code 发起 ask_user_question）；此时 chat-stop 在场（等待中）
  - tap 忽略 (207,2061)（12:29:02）→ 6s 后 dump（a6_after_ignore_6s.xml）：**卡整体消失**（待你回答/Red/Blue/忽略/提交节点全无）
  - **agent 解冻并转述已取消**：思考「The ask_user_question was cancelled by the user. I should report that and wait as instructed. Don't retry…」+ 输出「I raised the question (Red vs Blue) via the prompt tool, but it came back **cancelled** before you answered — so I'm waiting as requested.」「Whenever you're ready, just tell me your pick (Red or Blue)…」（18.5s 计时）；发送键恢复 chat-send=「发送」（chat-stop 消失）=代理解冻
  - wire（/tmp/acc309_logs/a6.log）：12:28:46.696 `[dispatch] QuestionAsked -> QuestionEventHandler`（SSE 接线）→ 12:29:01.815 `SessionActionsDelegate: [Question] rejectQuestion: id=a016e2e2-23cd-4312-9068-32a542e27155` → 12:29:01.842 `[Question] rejectQuestion result: … **success=true**`（#328 修复后拒绝被受理）；`FATAL EXCEPTION`=0；无 permission/question 相关 app 错误（error 行仅系统 sensor 噪音与 Host-4199 卡轮询失败，无关）
  - 结论：卡消失 ✓ + 取消语义转述 ✓ + rejectQuestion success=true ✓ + 无相关错误 ✓ → ✔（旧形态冻结的反证成立）

### A5 校验重试倒计时不破坏常规流/continue 钮可触发（⑤ 尝试协议）
- 前置：P1 会话
- 操作：⑤-b：发送 `write%sa%svery%slong%spoem%sat%sleast%s5000%swords` 观测 120s（若 TurnMaxTokens 卡出现→tap continue 断言续写）。⑤-a：不可确定性触发（服务器 llm/retry 需 LLM 故障）——只验回归:正常流中无 RetryBanner 异常出现
- 期望：⑤-b 卡出现则 continue 续写;⑤-a 以单测覆盖（事件链 DshEventMapper.kt:463-485 llm/retry→Retry(next)·倒计时实现 148c0644）+渲染回归（无异常 Banner）作结
- 判定：⑤-b 出现并续写 → ✔;未出现 → BLOCKED(未触达 max-tokens,记录实测)。⑤-a：全程无异常 RetryBanner → ✔(渲染回归);倒计时 UI 本体以代码审查+单测作结（无法确定性触发,记 BLOCKED-倒计时本体）
- 实测记录：⑤-b **BLOCKED（未触达 max-tokens）**+⑤-a ✔（渲染回归）2026-09-05 12:29-12:34【120s 分段观测】
  - 插曲（如实记录）：首次 tap 发送 (1086,2538) 落偏（键盘展开致布局位移），消息滞留输入框约 105s（a5_seg1_1-6.xml 六帧证据）；keyevent 111 收键盘 → 重新定位 chat-send@[1014,1544][1158,1688]（a5_pred_send.xml）→ tap (1086,1616) 真发出（12:31:49）
  - 有效 120s 观测窗（12:31:49→12:34:09，s2-1…s2-7 + final 共 9 帧分段 dump）：agent 思考（「The user wants a very long poem…」「…structure it as an epic…with multiple parts」，思考中 10.4s→55.2s）→ 长诗正常流式生成：「A poem in twenty-four cantos」「Canto I — The Stair」「Each night I write the weather in the book…」多章节连续输出，轮计时 2m0s→2m17s（chat-stop busy 中，a5_end_state.xml）
  - **TurnMaxTokens 卡未出现**：9 帧 dump grep max.?tokens/continue/继续/重试/retry 均无命中；logcat（/tmp/acc309_logs/a5.log）无 RetryBanner/TurnMaxTokens/llm-retry/attemptsExhausted 事件——模型单轮持续生成未触达 max-tokens 截断 → ⑤-b BLOCKED(未触达,无法验 continue 续写)
  - ⑤-a 渲染回归：正常流全程无异常 RetryBanner ✓；`FATAL EXCEPTION`=0
  - ⑤-a 倒计时 UI 本体：无法确定性触发（需服务器 llm/retry LLM 故障）——按 checklist 以代码审查+单测作结（事件链 DshEventMapper.kt:463-485 llm/retry→Retry(next)·倒计时实现 148c0644），记 BLOCKED-倒计时本体

## B. 回归

### B1 常规发送/流式/FAB/单键
- 前置：A 组完成
- 操作：普通消息一轮;FAB 展开收起;忙+文本 dump 单键
- 期望：全部正常
- 判定：回复可见 + FAB 五入口 + chat-send=1/chat-stop=0
- 实测记录：✔ 2026-09-05 12:34-12:40
  - 忙+文本单键：A5 诗歌轮 busy 中（chat-stop，2m43s）输入 `hi` → dump（b1_busy_with_text.xml）：**chat-send=1（标签「发送（排队）」）/chat-stop=0**——V012 形态：忙碌+文本=排队发送键（与 #326 V2「忙碌+空白=单停止键」相对照，如实记录）
  - 插曲：诗歌轮持续 6min+ 未完（b1_wait/wait2 系列 poll，4m1s→6m+）→ tap chat-stop (1086,1616) 12:38:34 **正常停止**（b1_after_stop.xml：chat-stop=0/chat-send=1=回空闲）；期间一次误触尝试（12:35:32 tap 落 chat-stop 位但轮未停，消息 reply with the word pong 未发出即被丢弃，无副作用）
  - 普通消息一轮：输入 `reply%swith%sthe%sword%spong` → tap 发送 12:38:55 → 12s 后 dump（b1_reply.xml/.png）：用户消息「reply with the word pong」+ **回复「pong」可见**（12:38:58 智能体，思考完毕 · 0ms）✓
  - FAB 展开/收起：首次 tap (1128,2232) 坐标过期（键盘收起后布局上移，FAB 实际@[1092,1274][1164,1346]）——b1_fab_expanded.xml 实为收起态（如实记录）；重定位 tap (1128,1310) → **五入口齐**（b1_fab_expanded2.xml/.png）：TODO/智能体/目标/Shell/排队队列 ✓ → 再 tap 收起 → b1_fab_collapsed2.xml：排队队列=0（菜单关）+ **chat-send=1/chat-stop=0**（空闲单键）✓
  - logcat（B1 窗）：无 FATAL（B2 总检汇总）

### B2 收尾健康
- 前置：B1 完成
- 操作：进出会话×2;logcat 总检
- 期望：FATAL=0
- 判定：FATAL=0 + dump 正常
- 实测记录：✔ 2026-09-05 12:40-12:42
  - 出会话：BACK×1（12:40:2x）→ 会话列表（b2_exit2.xml：「搜索会话…」+prod-3080 顶栏在场；本会话行 count from 1 to 50@[168,963][537,1019]）→ tap (352,1010) **再进 1** ✓（b2_reenter1.xml：会话标题 count from 1 to 50、chat-send=1 正常）
  - 再出：BACK×2 → 退过头到 **Home 页**（b2_reenter2.xml 初始帧=服务器卡列表，如实记录——单 BACK 出列表、双 BACK 到 Home）→ prod-3080 卡「已连接 · DSH」「会话」@[337,1613][423,1673] tap (380,1643) → 列表（b2_list2.xml）→ tap 本会话行 (352,606) **再进 2** ✓（b2_reenter2b.xml/.png：标题 count from 1 to 50、chat-send=1/chat-stop=0、转录正常）
  - logcat 总检（/tmp/acc309_logs/b2.log，B1+B2 窗）：`FATAL EXCEPTION`=0、` E AndroidRuntime`=0 → dump 正常 + FATAL=0 ✓

## 人工验收清单（UIUX;域汇总登记）
- [ ] goal sheet 第四钮观感与误触防护
- [ ] 压缩卡形态与流内呈现
- [ ] Full access 确认对话框文案/按钮语义
- [ ] 重试倒计时行观感（触发时机罕见）

## 执行汇总（2026-09-05 12:15-12:42，纯净执行员实测，全部终态）

| 项 | 终态 | 关键证据要点 |
|---|---|---|
| P0 构建核对 | ✔ | versionCode=1788569106 / **versionName=0.3.0** / **lastUpdateTime=2026-09-05 08:46:01** 与期望逐字一致 |
| P1 连接+新建会话 | ✔ | 未跑 debug-entry（避 4199 坑）；reverse tcp:3080；Home prod-3080 卡「连接」→「已连接·DSH」→ 新建会话（workspace）chat-input/chat-send 在场；FATAL=0 |
| A1 goal 完成钮（①） | ✔ | 目标在场帧四钮：暂停/**标记完成**/编辑/清除（完成语义钮=左起第 2 钮，非预期第 4 钮，如实记录）；tap 后四钮→空表单=相位变化；wire `/api/goals/complete` 受理+SessionGoalChanged×2；未见「已完成」文字标记（complete 后活跃目标移除，如实记录） |
| A2 压缩卡（②） | ✔ | 命令面板 `/compact`+描述在场；「正在压缩上下文…」→「**/compact 已执行**」卡；CompactionStarted→SessionCompacted→compacted 事件链；旧转录全部折叠（仅剩 compacted-summary 尾部）；FATAL=0 |
| A3 Full access 确认（③） | ✔ | 危险档专属确认对话框（标题「切换到完全访问？」+沙箱警示文案+取消/切换双钮）；取消路径药丸不变；确认路径终态=完全访问；降档无确认；SessionPermissionChanged 全程接线 |
| A4 steer 长按（④） | ✔ | 第 3 次尝试成功（前 2 次数数秒答轮末错过，如实记录）；chat-send rid 定位+swipe 同点 700ms；wire **Busy/Streaming --ClientSendParts--> Busy/Streaming**（直发不等轮末）；agent 完整 verify 响应（五维全 ✅ allPass:true） |
| A6 提问拒绝（#328） | ✔ | 提问卡按钮集=待你回答+SINGLE+Red/Blue+输入答案+发送+**忽略**+提交（与预期相符）；tap 忽略→卡消失；agent 解冻转述「came back **cancelled**」；wire `rejectQuestion result: success=true`；无 permission/question 错误 |
| A5 ⑤-b continue 钮 | **BLOCKED** | 120s 分段观测（9 帧）：长诗正常流式生成未触达 max-tokens，TurnMaxTokens 卡未出现——无法验 continue 续写（记录实测） |
| A5 ⑤-a 倒计时回归 | ✔ / 本体 BLOCKED | 渲染回归：全程无异常 RetryBanner、FATAL=0 ✔；倒计时 UI 本体无法确定性触发（需服务器 LLM 故障）——按 checklist 以代码审查+单测作结（DshEventMapper.kt:463-485 / 148c0644），记 BLOCKED-倒计时本体 |
| B1 常规回归 | ✔ | 普通消息回复「pong」可见；FAB 五入口齐（TODO/智能体/目标/Shell/排队队列）展开收起正常；空闲 chat-send=1/chat-stop=0；忙+文本=「发送（排队）」单键（V012 形态，与 #326 V2 形态对照记录） |
| B2 收尾健康 | ✔ | 进出会话×2（BACK×2 会退到 Home，如实记录）重进正常；logcat 总检 FATAL EXCEPTION=0 / E AndroidRuntime=0 |

- **总计**：10 项 ✔（P0/P1/A1/A2/A3/A4/A6/A5-⑤a回归/B1/B2）+ A5 两个 BLOCKED（⑤-b 未触达 max-tokens；⑤-a 倒计时本体无法确定性触发——均 checklist 预设的合理 BLOCKED 路径，非缺陷证据）
- 执行插曲（均已如实记录在各 item）：① A1 goal create 触发 agent 轮运行，A2 前分段等待轮结束；② A4 前 2 次长按落在秒答轮之后（无效不计数）；③ A5/B1 两次 tap 坐标因键盘/布局位移落偏，重定位后成功；④ B2 BACK×2 退到 Home 页经 prod-3080 会话按钮返回
- 证据：/tmp/acc309_logs/（p1/a2/a3/a4/a6/a5/b2 .log）+ /tmp/acc309_shots/（40+ 帧 dump/xml 与 png 截图）
