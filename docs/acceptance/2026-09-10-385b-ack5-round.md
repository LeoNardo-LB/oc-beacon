# #385b ack-five 复验轮 验收 checklist

- 卡片：backlog #385（skill-catalog 残项）+ #385b 根修 e9339163（ack-four 三异常复验）| 日期：2026-09-10
- 构建：e9339163 devDebug（`app/build/outputs/apk/dev/debug/app-dev-debug.apk`，已 pm install -r 静默装）| 设备：houji（小米），当前以无线调试 TLS 形态在线
- 分类：仪器可验（全部 item 走三步验收，无人工项）
- 回归域：会话转录渲染（Room DB v8 迁移 + 消息 id 命名空间化）· 压缩单卡（#384，旧会话回放面）· SSE 流式（粗粒度）

## 背景一页纸（执行员必读）

ack-four（session-a242ed71）实测留下 3 个异常，根修 commit `e9339163` 将 DSH 消息 id 从裸 `seq-N` 改为 `seq-{sessionId}-{n}`（跨会话唯一），DB v8 迁移清理串味存量行。本轮以新会话「ack five」复验异常是否消失：

- (a) 发送回执双气泡（pending echo 未拆除）
- (b) 外会话 parent-agent 文本泄漏进气泡
- (c) 注入墙文本对 uiautomator dump 隐身（**观测坑，非缺陷**——墙/卡判定以截图视觉为准）

以及 #385 残项：skill-catalog 注入仍按普通用户气泡渲染大段墙（数据层正确、渲染层未命中）。期望三分支全部卡片化：**工作区指令**（agent-instructions）/ **插件配置**（plugin）/ **技能目录**（skill-catalog）。

## 环境关键事实

- 产物目录先建：`mkdir -p /tmp/ack5`。
- 设备 serial：`adb devices -l` 查看，当前为 `adb-e69a99d8-yzT17Y (2)._adb-tls-connect._tcp` 形态（含空格括号）。**裸 adb 命令用 `adb -t 1`（transport id）；脚本类（type.sh / pull-app-db.sh / dsh-pair.sh，内部 `adb -s "$SERIAL"`）必须把整个 serial 加引号作单参数传入**：`./scripts/type.sh "ack five" "<serial 全称>"`。严禁误触 emulator-5554（若在）。
- **transport 漂移/设备重连后**：重跑 `adb devices -l` 取新 transport id，并**重跑 `adb -t <tid> reverse tcp:3080 tcp:3080`**（TLS 重连必丢 reverse）。
- 后端：**DSH 生产 127.0.0.1:3080**（host 即服务器）。`adb -t 1 reverse tcp:3080 tcp:3080`。**禁止用 `./scripts/debug-entry.sh`（默认 4199 = opencode V2，进错后端）**。
- 冷启 debug intent（e2e-acceptance-dsh.sh 同款）：
  `adb -t 1 shell am start -n dev.leonardo.ocbeacon.dev/dev.leonardo.ocbeacon.MainActivity --es debug_url http://127.0.0.1:3080 --es debug_server_type dsh --es debug_username dsh --es debug_name "127.0.0.1:3080" --es debug_token "$TOKEN"`
  其中 `TOKEN=$(grep -oE 'token=[A-Za-z0-9_-]+' ~/.local/state/dsh-web/web.log | tail -1 | cut -d= -f2)`。
  若 logcat 出现 401/TokenNeeded：跑 `./scripts/dsh-pair.sh "<serial 全称带引号>"` 重新注入（**必须带 quoted serial**——无参默认 `e69a99d8` 短串，TLS 形态下 device not found 静默失败）再继续。
- 到达会话列表标志：logcat `NavGraph: Debug channel → SessionList`。
- 打字：**优先 `./scripts/type.sh "ack five" "<serial 全称>"`**（纯 keyevent）；`input text` 仅作 type.sh 失败兜底。发送键一律 dump 定位（键盘弹起后输入栏上移，盲点坐标必落键盘）。
- 新建会话：顶栏「新建会话」固定坐标 tap 972 230 → 目录表（以「打开其他项目…」在场为标志）→ 点 `dsh-openapi-scratch`（缺席翻找点 `oc-beacon`）。会话以首条消息自动命名 → 首条消息必须是 `ack five`。
- Room 取证：`./scripts/pull-app-db.sh "<serial 全称带引号>" <输出前缀>`（脚本内部 `adb -s "$SERIAL"`，serial 含空格必须引号整体传入）。实测表结构：`cached_messages(id, sessionId, created, role, payload)`、`cached_parts(id, messageId, sessionId, type, text, payload)`；宿主 sqlite3 直查（97MB 库整拉约 1-3 分钟，拉取放后台勿掐）。
- 产物目录：`/tmp/ack5/`（截图 `adb -t 1 exec-out screencap -p >`；dump、DB、logcat 全部落这里）。logcat 用宿主侧全程序连续档：`adb -t 1 logcat -v time > /tmp/ack5/host.log 2>&1 &`。
- 真机串行纪律：执行期间只有你一个流操作真机。禁止分析/归因/修码——只观测记录。

## A. 修复效果

### A1 注入三分支卡片化（#385 残项 + 回归）
- 目的：校验新会话首轮 context 注入消息（agent-instructions/plugin/skill-catalog）全部渲染为精简 EventCard 而非大段原文墙。
- 前置：最新 devDebug 已装；reverse 3080 在位；app 停在 DSH 会话列表。
- 操作：新建会话（目录见上）→ 发送首条消息 `ack five` → 等首轮 turn 完成（assistant 回复上屏且 30s 无新部件）→ fresh dump + 截图 `/tmp/ack5/a1_cards.png`。
- 期望：转录内三张精简卡：**工作区指令**、**插件配置**、**技能目录**（标题可折叠，正文默认不铺开）。
- 判定：截图视觉确认三卡在场且**无**大段原文墙（技能目录全文不以气泡铺开）；dump 中三标题节点在场作佐证（墙文本 dump 隐身是已知坑，dump 不命中不等于失败，以截图为准）。
- 实测记录：**BLOCKED —— 被测构建启动即崩溃循环，app 无法到达会话列表，无法进入 ack five 会话执行截图/dump。**
  - 时间线：07:38:58 pm install 完成（`dumpsys package` lastUpdateTime=2026-09-10 07:38:58）→ 07:39:10.899 首次 FATAL（crash buffer，pid 12418）→ 每 ~0.7-0.8s 一次崩溃重启持续至 07:44:47+ 仍在进行（buffer 轮转前计数 >155 次 FATAL，全部同签名）。
  - 崩溃签名（唯一）：`android.database.sqlite.SQLiteException: no such table: session_sync (code 1): , while compiling: DELETE FROM session_sync`，栈：`OcBeaconDatabase$Companion$MIGRATION_8_9$1.migrate(OcBeaconDatabase.kt:64)` → Room onUpgrade → SupportSQLiteDriver.open（DB 打开路径即崩，进程拉起秒崩）。
  - 屏幕状态：MainActivity 为 ResumedActivity 但窗口全白（仅状态栏，截图 /tmp/ack5/crashloop_screen.png @07:43）；`mCurrentFocus=null`；uiautomator dump 报 `null root node returned by UiTestAutomationBridge`。
  - 确定性验证：07:44 force-stop + `am start`（无 extras，不触发 debug channel）后 2s 内复崩（07:44:10.419 / 07:44:11.389 同签名）。
  - 前置核验通过项：设备 online（transport_id=1，`adb-e69a99d8-yzT17Y (2)._adb-tls-connect._tcp`）；`adb -t 1 reverse --list` 含 tcp:3080；包 versionName=0.3.0。
  - 环境插曲（执行环境问题，非本崩溃成因）：宿主 /tmp 曾 100% 满（7.3G/7.3G）致初期截图/log 保全产出 0 字节文件；已清理前轮遗留大库副本（/tmp/ack5/baseline.db、combined.gz、/tmp/acc385c.db、acc385d.db）腾 1.5G 后取证恢复正常。
  - 证据：/tmp/ack5/crash_buffer.log（10041 行全文）、/tmp/ack5/crashloop_screen.png、/tmp/ack5/host_crashloop_phase1.log（07:39-07:40 第一批 logcat，28MB）、/tmp/ack5/host.log（重启后连续档）。

### A2 发送回执单气泡（ack-four 异常 a 复验）
- 目的：校验用户消息 `ack five` 无 pending echo 双气泡。
- 前置：A1 同一会话。
- 操作：A1 完成后 fresh dump + 复用截图；拉库 sqlite 查本会话 user 消息。
- 期望：`ack five` 用户气泡一张；Room 中本会话 role=user 且 payload 含 ack five 的消息行数 = 1。
- 判定：两个证据（截图气泡数 + DB 行数）一致为 1。
- 实测记录：**BLOCKED-by-A1** —— 截图气泡数证据需 app 进入 ack five 会话（崩溃循环不可达）；期望要求截图+DB 双证据一致，单边 DB 查询不构成判定。
  - 数据层注脚（观测，非判定）：07:45 拉库成功（integrity ok，/tmp/ack5/db_probe_blocked.db，464MB）；`SELECT COUNT(*) FROM cached_messages WHERE sessionId='ad678134-64c0-4335-a393-9120127f24f2'` = **0**（注：该表 sessionId 实际形态为不带 `session-` 前缀的裸 UUID）——设备库从未同步到 ack five 会话任何消息；库内最新消息停留在 2026-09-10 07:30:43（上一轮执行时段），崩溃循环期间库零写入。

### A3 无跨会话文本泄漏（ack-four 异常 b 复验）
- 目的：校验本会话转录不含外会话文本、parts 归属正确。
- 前置：A1 同一会话，DB 已拉。
- 操作：sqlite 查本会话全部消息 join parts 的 sessionId 集合；通读截图转录文本。
- 期望：parts.sessionId 全部等于本会话 id；转录内除注入卡与 `ack five`/assistant 回复外无来历不明文本（ack-four 泄漏的是 parent-agent 的中文长文本）。
- 判定：sessionId 集合 = {本会话} 且截图转录无可疑外来文本。
- 实测记录：**BLOCKED-by-A1** —— 「通读截图转录文本」证据不可取得（app 崩溃循环，会话转录无法上屏）。
  - 数据层注脚（观测，非判定）：本会话 cached_parts 亦无行可查（messages=0）；无转录内容可作泄漏比对。

### A4 消息 id 命名空间化 + 行稳定（#385b 根修主判定）
- 目的：校验新会话消息 id 为 `seq-{sessionId}-{n}` 形态、n 随消息递增、跨时间采样行不翻转、且全库无跨会话 id 冲突。
- 前置：A1 会话仍在前台。
- 操作：T0 拉库，落文件三份查询：①本会话 messages 的 (id, sessionId, 前 40 字符 payload)；②全库冲突断言 `SELECT id FROM cached_messages GROUP BY id HAVING COUNT(DISTINCT sessionId) > 1;`；③本会话 seq- 前缀 id 列表。前台静置 ≥15s（期间可回会话列表再进会话一次以触发 mux 推送）；T1 再拉库重复①②③。
- 期望：①id 全部命中 `seq-session-*` 前缀且同会话 ≥3 个 id、序号 n 严格递增（trio 注入 + ack five + assistant 回复天然多消息）；②②查询恒返回空；③T0/T1 输出 diff 为空（无行改换 sessionId/内容——ack-four 实测 seq-9..12 每 3 秒跨会话翻转）。
- 判定：①形态+递增成立 ②空集 ③diff 为空，三者同时成立。
- 实测记录：**BLOCKED-by-A1** —— 操作要求「前台静置 ≥15s + 回会话列表再进会话一次触发 mux 推送」后 T1 拉库 diff；app 崩溃循环无法进会话，且崩溃期间库零写入，T0/T1 diff 无验收意义。
  - 数据层注脚（观测，非判定）：设备库 `PRAGMA user_version` = **8**（v8→v9 迁移从未成功执行——启动即崩于 MIGRATION_8_9）；库中实际无 `session_sync` 表，存在名为 `session_sync_state` 的表（迁移 SQL `DELETE FROM session_sync` 所指表名与库中现存表名不一致，仅陈述观测）。
  - id 形态注脚：库内最近 200 条 `seq-%` 消息 id 100% 为 `seq-{sessionId}-{n}` 命名空间化形态（0 条裸 `seq-N`），最新一条 `seq-65f68bcf-…-2375` @07:30:43——旧构建时段写入的 id 已是命名空间形态；ack-five 轮自身无新行可采样。

## B. 回归

### B1 旧会话迁移后回放 + 压缩单卡保持（DB v8 / #384 回归面）
- 目的：校验 DB v8 迁移后旧数据可读、渲染正常；压缩单卡裁决在旧会话回放面不回归。
- 前置：app 在 DSH 会话列表。
- 操作：点开 ack four 会话（session-a242ed71…）截图 `/tmp/ack5/b1_ack4.png`；返回列表点开 ack one 会话（session-a3be7221…，内含 /compact 历史）截图 `/tmp/ack5/b1_ack1.png`。
- 期望：两会话转录正常渲染；a3be7221 压缩区域为单卡（box=1、命令卡=0、结算行在卡内）。
- 判定：两截图对应会话的历史气泡/卡节点在 dump 中在场且非空白屏（结构断言，不做「错乱」主观判断）；a3be7221 压缩单卡视觉成立。兜底：两历史会话任一在列表缺席 → 该项记 BLOCKED（注明缺席会话）并继续 B2，不算失败循环对象。
- 实测记录：**BLOCKED —— 依赖 app 停在 DSH 会话列表点开 ack four / ack one；被测构建崩溃循环，会话列表不可达。** 注：checklist 兜底条款针对「会话在列表缺席」；本轮回阻断更前置（app 无法启动渲染任何界面）。ack four/ack one 会话数据未动，阻断解除后可直接复验。

### B2 流式回复完整到达（SSE 粗粒度回归）
- 目的：校验 ack five turn 的 assistant 回复完整落库上屏，无断流卡死。
- 前置：A1 turn 已完成。
- 操作：sqlite 查本会话 role=assistant 消息内容长度与截图比对。
- 期望：assistant 回复非空且与上屏一致（上一轮同类 turn 回复形如「收到。工作区约束已就位」）。
- 判定：DB assistant 行非空 + 截图回复在场。
- 实测记录：**BLOCKED-by-A1** —— 「截图回复在场」证据不可取得（app 崩溃循环）。
  - 宿主侧注脚（观测，非判定）：宿主侧被测会话 `~/.dsh/sessions/…/session-ad678134-64c0-4335-a393-9120127f24f2/session.jsonl.zstd`（395,294 字节）mtime=07:38:46 后无变化——turn 的 DSH 侧产出已完结，且早于 07:38:58 的 pm install；设备侧落库/上屏验证因崩溃无法进行（设备库中本会话 0 行）。

## 执行顺序与产物清单

A1 → A2 → A3 → A4（同会话顺次）→ B1 → B2。全部 item 到终态（✔/✘/BLOCKED）后结束，汇总逐项结果。产物：`/tmp/ack5/`（截图×4+、host.log、dump、DB×3（A2+A4 T0/T1）、sqlite 查询输出）。

## 本轮（复验轮 2026-09-10）实际产物清单

全部 item 终态 BLOCKED（app 启动崩溃循环），实际产物：
- `/tmp/ack5/crash_buffer.log` —— crash buffer 全文 10041 行（唯一签名 `no such table: session_sync` @ MIGRATION_8_9）
- `/tmp/ack5/crashloop_screen.png` —— 崩溃循环期屏幕（白屏，仅状态栏，07:43）
- `/tmp/ack5/initial_state.png` / `initial_state2.png` —— 首次进入时白屏截图
- `/tmp/ack5/host_crashloop_phase1.log` —— 07:39-07:40 第一批 logcat（28MB，磁盘满期前段）
- `/tmp/ack5/host.log` —— 重启后 logcat 连续档
- `/tmp/ack5/db_probe_blocked.db` —— 464MB Room 库一致性快照（integrity ok；user_version=8；ack five 会话 0 行）
- `/tmp/ack5/dump_initial.xml` —— 首次 dump（系统设置页，进入 app 前）
- 复验前置条件未满足说明：checklist 原产物清单（a1_cards.png 等截图×4、DB×3 T0/T1、sqlite 查询输出）因 A1 阻断未产出。

## 并行执行员补充记录（第二执行方，07:30:37–07:57:33 时段，独立取证）

> 本节由并行执行的第二执行方（纯净上下文子代理，07:30 接手真机）追加；上方各 item 记录出自第一执行方（07:37 起在场）。两边记录均保留，供分析阶段合并归因。关键差异：第二执行方实测到 **07:38:58 之前 checklist 构建体健康运行** 的完整前半程，崩溃循环的引入时刻与本节事件④直接相关。

### 逐项判定（第二执行方视角）

- **A1 ✘** —— 前半链路全部走通：07:30:37 冷启 debug intent → 07:30:38.404 `Debug channel → SessionList`；列表在位（pre_list.png/xml，含 ack four / ack one / 一个旧「ack five」会话）；tap 972,230 → 目录表「打开其他项目…」在场、`dsh-openapi-scratch` 缺席 → 落 `oc-beacon`；type.sh 打字上屏；dump 定位 chat-send(1086,1616) 07:32:38 发送成功。新会话 `session-ad678134-64c0-4335-a393-9120127f24f2` 自 07:32:42 起 TextDelta 流式 ≥4 分钟（最后观测 07:36:30.663），a1_sent.xml 中见「思考中… 2.9s」与 skill 注入文本片段节点（卡片化与否不可判，判定以截图为准）。**07:36:30–07:38:58 证据断档窗后（见事件④），app 进入崩溃循环，三卡截图/dump 判定不可达成 → ✘**。证据：pre_list.png、a1_dirtab/newchat/typed/sent.xml、a1_crashloop_state.png（07:45 白屏）、a1_repro_crash.png + host3.log 07:57:26 干净复现（pid 24105，89ms 内同签名 FATAL）。
- **A2 ✘** —— DB 半边：本会话（`session-…` 与裸 UUID 两形态并查）role=user 且 payload 含 ack five 行数 = **0**（期望 1）；全库 payload 无「ack five」；截图半边不可得。证据：/tmp/ack5/a2_a3_b2_queries.txt（库 db_a2b.db，integrity ok）。
- **A3 ✘** —— parts join sessionId 集合 = ∅（期望 {本会话}）；本会话 parts 直查 0 行；转录文本不可读。同上证据。
- **A4 ✘（①败②③过）** —— ①本会话 id 列表 0 行（期望 ≥3 且 `seq-session-*` 严格递增）✘；②全库跨会话 id 冲突断言 T0/T1 均空集 ✓；③T0(db_a2b 07:52, integrity#2 ok)/T1(db_t1 07:56, integrity#1 ok) 查询输出数据行 diff 为空且两库 md5 相同（cdaca3d3…）✓——但 T0/T1 间 app 已死、无 mux 活动，「行稳定」为静默库观测，效力降级照录。前台静置+重进会话触发推送的操作步骤不可执行（偏差已注明）。证据：a4_t0_queries.txt / a4_t1_queries.txt / a4_t0_t1.diff。
- **B1 BLOCKED-by-A1** —— 07:31 列表 dump 中 ack four / ack one 行**在场**（pre_list.xml），非「列表缺席」兜底情形；会话打开因崩溃循环不可达。b1_ack4.png / b1_ack1.png 未产出。
- **B2 ✘** —— 本会话 role=assistant 行 = **0**（期望非空且与上屏一致）；与第一执行方 B2 注脚互证：宿主侧 turn 产出 07:38:46 已完结，但设备侧 12s 后进程被杀、落库为零。

### 执行期事件记录（第二执行方观测，按时间序）

1. **07:36:30–07:38:58 证据断档窗**：第一执行方残档 host.log 于 07:38:58 被并发 logcat `>` 截断（共 3 次：07:38:58 / 07:43:27，及一次拉库并发撞车）；截断残段分别保全为 host_crashloop_phase1.log（07:38:58–07:40:33）与 host_ext_v3_trunc.log（07:43 段）。pid 5935（健康期 app）死亡时刻落窗内不可恢复。
2. **并发执行方归因**：外部操作 PPID 链 = PID 1625471 `node …/dsh web --no-open --trusted-host 192.168.110.248:3080` → `bash -c` → 本仓库 scripts/（pull-app-db.sh 前缀 /tmp/accz6*、logcat 写 /tmp/ack5/host.log）。/tmp/ack5/ 内非第二执行方产物：ack4_truth.json(07:30)、server_truth.py(07:30)、probe.out(07:37)、initial_state*.png(07:39-40)、dump_initial.xml(07:39)、crash_buffer.log、crashloop_screen.png、db_probe_*(07:45-52)、loop*.db、db_v8_twin.db。
3. **baseline.db 被清理**：07:25 预置基线快照（461MB）于 07:46 被清为 0 字节（第一执行方 A1 记录中自述清理 /tmp 腾空间——当时 /tmp 100% 满）。
4. **构建体被中途替换（本崩溃循环的直接前置事件）**：崩溃循环二进制 versionCode=**1788997070** = 07:37:50 构建（dev flavor versionCode=构建时刻 Unix 秒；07:17 装的 checklist 构建应为 ≈1788995820）；`dumpsys package` lastUpdateTime=**07:38:58** —— 与断档窗结束时刻一致，pm install -r 杀死正在流式的健康进程。健康期 app 打印 `OC Beacon 0.3.0`（07:17 构建体）自 07:30:38 正常运行至 ≥07:36:30。
5. **工作区源码观测（只读）**：e9339163 提交态 schema **version = 8**、无 MIGRATION_8_9（`git show e9339163` 证实）；当前工作区存在未提交改动（OcBeaconDatabase.kt version 8→9 + MIGRATION_7_8/8_9 增水位重置 + DatabaseModule.kt 挂链 + ChatMessageList.kt + backlog.md）。07:38:58 所装中间态的迁移 SQL 为 `DELETE FROM session_sync`（崩溃签名实证，表不存在——Room 实体/迁移链只有 `session_sync_state`）；该文件 07:59:37 再次被编辑为 `DELETE FROM session_sync_state`（修复态），08:00:55 又有第三构建 versionCode=1788998379 被 install（07:59:39 构建）。截至本记录时点 gradle 仍在运行——设备/构建体被并行流持续变更，本验收轮执行窗口已实质关闭，故以上述已采证据终局。
6. **设备库终态事实**（db_a2b.db = db_t1.db = 设备库逐字节快照，464,056,320B，mtime 07:39，无 WAL）：user_version=**8**；无 `session_sync` 表、有 `session_sync_state`；cached_messages 8481 行，最新 created=07:30:43（65f68bcf 会话，id 形态 `seq-{裸uuid}-{n}`）；另有 169 行 `seq-session-{uuid}-{n}` 形态（sessionId 带 `session-` 前缀，23a614ec 会话）——两种命名形态并存；ack five 会话（ad678134）0 行。首拉 db_a2.db 因并发拉取撞车损坏，存证 db_a2_corrupt_interleaved.db。
7. **第二执行方处置动作清单**（全部为观测/取证类）：force-stop ×3（07:45 止循环、07:57 复现后止循环）、冷启 ×1（07:57:26 复现取证）、拉库 ×4（db_a2✗/db_a2b✓/db_t0=db_a2b 副本/db_t1✓）、logcat 档 host2.log(07:40:33–43:53)/host3.log(07:47–)、未修改产品代码、未跑 gradle、未卸载 app。
