# #311 批3·组织面 全子项验收 checklist

- 卡片：backlog #311（①归档 ①-d 多 workspace ②deliverables ③工具卡 ④待审批状态点）| 日期：2026-09-05
- 构建：含 4ced86f5/0498e775/0fd15da8/3ac2dd71/a69fd71a | 包：`dev.leonardo.ocbeacon.dev`
- 设备：一律 `adb -s 192.168.110.239:5555`；后端=DSH prod-3080；测试会话=新建（完全访问,便于写文件）+一个只读档会话（④ 触发审批）
- **分类：UIUX** → AI 三步验收后人工清单（域汇总）
- 回归域：会话列表（菜单/左滑/过滤/指示）· 新建对话框（三态数据源）· 转录渲染（产出行/工具卡/台账共存）· composer · 会话健康
- 证据目录：`/tmp/acc311_logs/`、`/tmp/acc311_shots/`

## 执行纪律（一次一项/逐项记录/禁止分析/dump 定位 tap/%s 转义/单次 bash ≤100s/长窗分段;MENU 长按=input swipe 同点 700 或 longpress）

## 前置
### P0 设备构建核对
- 前置：主 agent 已装新构建
- 操作：adb devices;dumpsys versionName/lastUpdateTime
- 期望：versionName=0.3.0、lastUpdateTime=2026-09-05 22:08:18（主 agent 已装 a69fd71a 构建并核实——不符 ✘ 停卡）
- 判定：输出记录
- 实测记录：✔ 2026-09-05 22:16:06。`adb devices`→`192.168.110.239:5555  device`（唯一在线设备）;`dumpsys package dev.leonardo.ocbeacon.dev`→`versionName=0.3.0`、`lastUpdateTime=2026-09-05 22:08:18`，与期望逐字相符。附:wm size=1200x2670。证据:/tmp/acc311_logs/ 已建。通过,继续 P1。

### P1 连接 prod-3080 准备两档会话
- 前置：P0 通过
- 操作：`adb -s 192.168.110.239:5555 reverse tcp:3080 tcp:3080`（每轮确认）;force-stop 冷启→prod-3080 连接→新建 workspace 会话 A（默认完全访问——发一条 hi 得回复建立 session）;确认只读档会话在场（g2-once 等;若无:另新建会话→输入区权限药丸切「只读」——tap 药丸→选只读）
- 期望：会话 A 可用;只读档会话在场
- 判定：dump 记录两档会话在场
- 实测记录：✔ 2026-09-05 22:22:52。事实链:①`reverse tcp:3080 tcp:3080`→`reverse --list` 见 `host-38 tcp:3080 tcp:3080`。②force-stop+`am start` 冷启→服务器列表页,`prod-3080`(`http://127.0.0.1:3080`)条目在场(dsh012-a5 此前为「已连接」状态)。③tap prod-3080 卡片「开始 连接」钮→状态转「已连接」+`DSH` 探测徽标。④tap「打开聊天 会话」→会话列表(g2-once 等在场,列表底部已存在「已归档（2）」折叠区——既有基线)。⑤新建会话对话框 workspace 条目:workspace(计 3)/外包维权工作区(计 2)/oc-beacon(计 2)/打开其他项目…。⑥选 workspace 条目→新建页权限药丸=「完全访问」(默认),input text `hi`+发送→得回复「Hi! 👋 I'm ready to help — I'm working in your workspace at /home/leo-tkp/workspace」,轮次 1 完结(0ms·1 步·0 工具)。⑦BACK×2 回列表,会话 A「hi」列于顶部(`9月 5, 22:20`)——注:22:21:32 首 dump 尚无 A,22:22:21 复 dump 在场(增量到达时延内)。⑧进入「用 bash 执行 echo g2-once」会话,输入区权限药丸=「只读」✓。两档会话(完全访问 A+只读 g2-once)dump 在案:p1_sessionlist/p1_list_afterA2/p1_list_top/p1_g2once.xml。顺带观测(仅记录):首次 BACK 出聊天页时未离开聊天,顶部出现一段 system-reminder 文本块(带「复制」钮),二次 BACK 才回列表。通过。

## A. 归档（①-a/b/c）
### A1 行菜单三态
- 前置：会话 A 在列表
- 操作：长按会话 A 行 → dump 菜单 → 逐项记录 → 收起
- 期望：菜单项=详情/重命名/归档（**无取消归档**——wire 单向,契约注释）
- 判定：dump 三项齐+无取消归档项
- 实测记录：✔ 2026-09-05 22:24:12。长按=`input swipe 600 640 600 640 700`（会话 A 行「hi」标题 bounds [168,578][204,634]）。dump(a1_longpress_menu.xml)菜单恰三项:`会话详情`[192,812][362,872]、`重命名会话`[192,956][405,1016]、`归档`[192,1100][278,1160]。grep `取消归档`=0 处——**无取消归档项**✓。BACK 收起→复 dump(a1_menu_closed.xml)`归档`菜单项消失、A 行仍在列表。logcat(a1_logcat.txt):FATAL EXCEPTION=0（24 行 AndroidRuntime 匹配均为 uiautomator 自身 RuntimeInit D/I 日志,非崩溃）。通过。
### A2 归档动作+follow 增量
- 前置：A1
- 操作：菜单 tap 归档 → 5s 内 dump 列表
- 期望：会话 A 从主列表消失（服务器回执→follow 增量→响应式,**不重启不刷新**）;底部出现「已归档(N≥1)」折叠区
- 判定：主列表无 A+已归档区在场
- 实测记录：✔ 2026-09-05 22:25:25。22:25:08 重长按 A 行(600,640,700ms)菜单再现(归档项 [192,1100][278,1160]),22:25:1x tap(235,1130)归档→**5s 内 dump**(a2_after_archive.xml):主列表 grep `>hi<`=0——A 从主列表消失✓,列表首项已为「根据这一分handoff继续」(18:43)。全程仅 tap 操作,**无重启无刷新**。滚至底部复 dump(a2_bottom.xml):`已归档（3）`[216,2282][454,2338] 在场——较 P1 基线「已归档（2）」+1（N=3≥1）✓。logcat(a2_logcat.txt):FATAL EXCEPTION=0。通过。
### A3 已归档折叠区
- 前置：A2
- 操作：展开已归档区 → dump → 长按已归档 A 行 dump 菜单
- 期望：区含 A;行菜单仅详情（单向）;区可收起
- 判定：dump 在案
- 实测记录：✔ 2026-09-05 22:27:42。①展开:tap 已归档（3）header（可点击容器 [48,2238][1152,2382],中心 600,2310）→下滑后 dump(a3_expanded3.xml):header 移至 [216,1697][454,1753],下方列 3 条:`hi`(`9月 5, 22:20`,即会话 A)/`根据这个handoff 继续吧`(9月 4, 11:11)/`安装这几个插件： https://github.`(9月 4, 11:04)——**区含 A**✓。②长按已归档 A 行(600,1837,700ms)dump(a3_archived_menu.xml):菜单**仅 `会话详情`**[192,2043][362,2103],无重命名/无归档/无取消归档——单向✓。③BACK 收菜单→tap header 折叠→dump(a3_collapsed.xml):`>hi<`不可见,header 回落 [216,2282][454,2338]——**区可收起**✓。观测:首次 tap header(335,2310)后 dump 区看似未变,实为展开内容位于折叠线下需下滑可见(第 3 次 dump 证实展开成功;点击容器中心即生效)。logcat(a3_logcat.txt):FATAL EXCEPTION=0。通过。
### A4 左滑归档
- 前置：另备一个可弃会话（或用既有 count 会话）
- 操作：该行向左滑（swipe 起 x+250 至 x-200 同 y,**时长 ~200ms 快滑**——慢滑会被判滚动）→ dump
- 期望：左滑触发归档（行消失入已归档区）
- 判定：行从主列表消失+归档区计数+1
- 实测记录：✔ 2026-09-05 22:28:26。可弃会话=既有 `count from 1 to 50`(9月 5, 12:38,/home/leo-tkp/workspace),行标题 bounds [168,714][537,770]。快滑 `input swipe 950 786 300 786 200`（水平左滑 650px,~200ms）。dump(a4_after_swipe.xml):`count from 1 to 50` 在屏 grep=0——**行从主列表消失**✓（原位由 `reply with shortsgreeting` 顶替）;底部 `已归档（4）`[216,2282][454,2338]——**计数 3→4 +1**✓。dump 无「撤销」snackbar 节点（仅记录,非断言项）。logcat(a4_logcat.txt):FATAL EXCEPTION=0。通过。

## B. 多 workspace（①-d）
### B1 新建对话框 workspace 建模
- 前置：P1
- 操作：tap 新建会话 → dump 对话框
- 期望：workspace 条目在场（title+计数角标;Workspaces 图标域）;stray/目录兜底条目按实际
- 判定：dump 记录条目形态（V012 快照三态按实际服务器注册表,如实记录）
- 实测记录：✔ 2026-09-05 22:30:54。tap 新建会话(972,230)→dump(b1_dialog.xml)对话框「新会话」含条目:①`workspace`+`/home/leo-tkp/workspace`+计数角标`3`[1047,1151][1068,1194];②`外包维权工作区`+`/home/leo-tkp/Documents/docs/外包维权工作区`+角标`2`;③`oc-beacon`+`/home/leo-tkp/Documents/code/mine/oc-beacon`+角标`1`;④兜底条目`打开其他项目…`[228,1677][528,1733]。条目形态=title+路径+右侧计数角标（dump 文本层三要素齐;图标域非 dump 可见层,不计判定）。无 stray 会话条目。与 P1 快照对比:workspace 3→3（P1 后新建 A 又归档,净值 0）,oc-beacon 2→1——按实际服务器注册表如实记录。logcat(b1_logcat.txt):FATAL EXCEPTION=0。通过。
### B2 经 workspace 新建入组
- 前置：B1
- 操作：选 workspace 条目新建 → 会话建立后重开对话框 dump 该 workspace 计数
- 期望：新建成功;upsert 增量到达后计数+1（不重启）
- 判定：新会话可用+计数较 B1 增 1（或按 blank 复用语义如实记录实际路径）
- 实测记录：✔ 2026-09-05 22:32:51。22:31:1x tap workspace 条目(600,1150)→新建页(权限药丸「完全访问」),input text `hi`→发送→得回复「Hi! I'm ready to help you with coding, debugging, research, file work, and more…」,轮次 1 完结(0ms·1 步·0 工具)——**会话 B 可用**(b2_created/b2_sent.xml)。BACK×2 回列表→tap 新建会话重开对话框→dump(b2_dialog_after.xml):`workspace`+`/home/leo-tkp/workspace`+角标 **`4`**[1047,1151][1068,1194]——较 B1 的 `3` **+1**✓,全程无重启无刷新(upsert 增量到达);外包=2、oc-beacon=1 不变。实际路径:经 workspace 条目新建独立会话,未见 blank 复用语义迹象(计数+1 与新行成立一致)。logcat(b2_logcat.txt):FATAL EXCEPTION=0。通过。

## C. deliverables（②）
### C1 写文件产出行
- 前置：**会话 B=新建完全访问会话**（A 已在 A2 归档且单向不可恢复——B 经 B2 新建或另新建,发一条 hi 建立后空闲）
- 操作：发送 `create%sa%sfile%snamed%smade_by_acc311.txt%swith%sone%sline%shello%sthen%sconfirm` → 轮询至轮次完结（≤120s 分段）→ dump 转录轮次尾
- 期望：轮次尾「产出文件」行在场,含 made_by_acc311.txt;宿主机 `ls /home/leo-tkp/workspace/made_by_acc311.txt` 存在（受端双证）
- 判定：dump 产出行+宿主文件存在
- 实测记录：✘ 2026-09-05 22:37:14（受端半证成立,产出行 dump 断言不成）。前置达成:会话 B=B2 新建完全访问会话(22:32 建立,轮次 1 完结后空闲;注:B 行在列表顶位呈现滞后约 3 分钟——22:32 创建 22:35:19 才见顶行,与 P1 时 A 同样时延,如实记录)。22:35:47 input text（%s 转义）原文完整入框,发送。**轮次完结**:首段 18s 内即达稳态,`轮次 2 · 1.8s · 3 步 · 1 个工具`(c1_poll1-3.xml)。**轮次尾 dump**(c1_tail_final.xml):reasoning 块「Then confirm — maybe verify by reading it back.」[120,562][1086,706]→工具行 `Run code`[96,809][999,857]（含复制钮）→间隔带 y857-965（无文本节点）→终消息「Confirmed. Created made_by_acc311.txt in the workspace with exactly one line: hello — verified by reading it back.」[84,965][1116,1153]→`1.8s`→`轮次 2` footer——**「产出文件」行不在场**(grep `产出`=0;全轮次区间含起始缝 c1_seam.xml 亦 0)。**像素探针**(c1_tail_final.png,间隔带 x[84,1116) y[860,962)):79.5% 背景 #dde4e8+17.5% 容器 #ebf2f6+2.9% #cfd6da——后者定位为 y≈918 全宽 1px 分隔线非 chips;对照区 y[758,806) 无此成分之外内容——间隔带视觉为空。**宿主机受端**:`ls /home/leo-tkp/workspace/made_by_acc311.txt`→存在(-rw------- 6字节,内容 `hello`,mtime 22:36)✓。可观测补充:本轮唯一工具卡为 `Run code`(agent 以 run_code 包裹内联 bash 建文件,转录层只见外层工具卡)。截图证据:c1_transcript_tail/c1_tail_bottom/c1_tail_final.png。logcat(c1_logcat.txt):FATAL EXCEPTION=0。判定:宿主文件存在✓+dump 产出行✗ → **✘**。
- **C1-retry（主 agent 裁定后追加,2026-09-05 22:47:00 终态:BLOCKED-模型行为）**:主 agent 裁定本次 ✘ 成因=模型以 run_code 内联 bash 建文件——run_code 不在产出行折叠域(web 同款判定:仅 write/edit/str_replace_editor),非产品缺陷;处置=追加 retry。**retry-1**(22:44:27 发送 `use the write tool to create made_by_acc311_r2.txt with content hello then confirm`):轮次 4 完结(1.4s·3 步·1 工具,c1r_poll1-3.xml);工具卡仍为 `Run code`[96,809]（终消息自称「via the write tool」但外层工具卡=Run code）;grep `产出`=0 无产出行;宿主 `made_by_acc311_r2.txt` 存在(6字节 hello,22:44)——模型仍用 run_code。**retry-2 edit 变体**(22:46:01 发送 `use the edit tool directly not run_code to create made_by_acc311_r3.txt containing one line hello`):轮次 5 完结(4.8s·3 步·1 工具,c1r3_poll3.xml);agent 终消息明言:「edit cannot create files… I can only invoke edit from inside run_code — direct calls to non-run_code tools aren't supported in this harness」「Fallback: created the file with write instead」——工具卡仍 `Run code`,grep `产出`=0;宿主 `made_by_acc311_r3.txt` 存在(6字节 hello,22:46)。两试均不配合(harness 仅暴露 run_code 外层工具,折叠域工具不可直达)→按裁定 **C1 终态 BLOCKED-模型行为**,产出行折叠域正确性由单测矩阵钉死。logcat(c1_retry_logcat.txt):FATAL EXCEPTION=0。
### C2 只读轮无产出行
- 前置：C1
- 操作：发送 `reply%swith%sjust%sthe%sword%sdone` → 完结后 dump 该轮
- 期望：纯文本轮无产出行（空不挂载）
- 判定：该轮尾无产出行
- 实测记录：✔ 2026-09-05 22:43:26。22:42:16 input text（%s 转义,入框文本带前导空格「 reply with just the word done」——可观测事实）→发送。首段 15s 内完结:`轮次 3 · 0ms · 1 步 · 0 个工具`(c2_poll1-3.xml)。轮次尾 dump(c2_poll3.xml):`22:42:34 智能体/思考完毕 · 0ms`→终消息 `done`[62,1053][206,1197]→`轮次 3` footer——**该轮尾无产出行**(grep `产出`=0)✓,纯文本轮空不挂载符合期望。附带观测(仅记录):turn 3 用户消息上方（turn 2 footer 与 turn 3 之间）出现两段 agent 内部叙述文本（「The get_goal binding requires an argument…」「The get_goal binding needs an explicit (possibly empty) argument object. Retrying:」带复制钮,与 P1 首次 BACK 后出现的 system-reminder 文本块同族现象）。logcat(c2_logcat.txt):FATAL EXCEPTION=0。通过。

## D. 工具卡（③）
### D1 skill 卡
- 前置：会话 B（完全访问）
- 操作：发送 `load%sthe%scode-review%sskill%sand%stell%sme%sits%sname%sthen%sstop` → 完结后 dump 转录
- 期望：skill 工具卡在场（名称+状态+指令折叠,默认收起）;展开 dump 一次
- 判定：dump 卡在场+可展开（若 agent 未调 skill 工具→BLOCKED-模型行为,如实记录回复）
- 实测记录：**BLOCKED-模型行为** 2026-09-05 22:48:53。22:47:40 input text 原文完整入框,发送。轮次 6 完结(3.6s·3 步·1 工具,d1_poll1-3.xml)。轮次尾 dump:工具行仅 `Run code`[96,429][999,477]（1 工具=run_code）;终消息如实回复:「Loaded the skill. Its name is **code-review** (provider: filesystem). Stopping here as requested — no review performed.」+reasoning「The skill loaded successfully…just report the name and stop」——**skill 确已加载**但经由 run_code 内联调用（与 C1-retry 同一 harness 限制:非 run_code 工具不可直达）,转录层**无 skill 工具卡节点**(grep `skill` 卡形态/`指令`折叠节点=0;code-review 字样仅现于终消息文本)。判定出口:agent 未以独立 skill 工具调用→按项写明处置记 **BLOCKED-模型行为**;「展开 dump」步骤无从执行（无卡可展）。skill 卡折叠形态正确性由单测矩阵钉死（主 agent C1 裁定同族路径）。logcat(d1_logcat.txt):FATAL EXCEPTION=0。
### D2 ask_user_question 工具行
- 前置：会话 B
- 操作：发送 `use%sask_user_question%sto%sask%sme%smy%sfavorite%scolor%swith%soptions%sRed%sGreen%sthen%swait` → 提问卡出现 → 点选 Red 提交 → 完结后 dump 转录
- 期望：转录呈现 question 工具行（Asked 形态,双名分派 DSH 恒名）
- 判定：dump 工具行在场（复述 Red 佐证链路）。**顺带子断言**:提交前（提问卡挂起时）BACK 回列表 dump B 行——question 族指示在场（与 Asking 合流形态,④ 三态补全）;提交后回列表指示消
- 实测记录：✘（主断言）+顺带子断言✔✔ 2026-09-05 22:53:24。22:49:34 input text 原文完整入框→发送。**提问卡出现**(d2_wait5-7.xml,~40s 内稳定):`待你回答`+`SINGLE`[120,1281]→`What is your favorite color?`[120,1365]→选项 `Red`[144,1457]+描述/Pick this if red…、`Green`[144,1621]+描述→`输入答案`+`发送`→`忽略`[164,2031]/`提交`[922,2031][1008,2091]→计时 `1m 3s`。**顺带子断言1✔**:卡挂起时 BACK 回列表(d2_list_pending.xml),B 行呈现 question 族指示 `待回答`[464,680][567,723]（时间戳行内,Asking 合流形态,④ 三态补全）。**点选提交**:重进 B(卡仍在)tap Red(183,1485)→tap 提交(965,2061)→轮次 7 完结 `1m 51s · 4 步 · 1 个工具`(d2_submitted.xml),终消息「Your favorite color is **Red** (question id: favorite-color). Waiting for your next instruction.」——**复述 Red 佐证链路**✓。**顺带子断言2✔**:提交后回列表(d2_list_after2.xml,B 行时间戳更新 22:49)`待回答` grep=0——指示消✓。**主断言✘**:转录轮次 7 区间（用户消息→reasoning→`Run code` 卡[96,1483][999,1531]→思考完毕→终消息→footer）**无 question 工具行节点**(grep `question/提问/Asked` 卡形态=0;仅 reasoning 与终消息文本含字样);像素探针(d2_after_submit.png)Run code 卡与思考完毕间隔带 y[1535,1640) 仅气泡背景 #e7ebee+分隔线 #cfd6da,#ebf2f6 定位为卡片容器 y[1462,1550] 不入隔带——无独立 Asked 行视觉。与 C1/D1 同族现象:agent 明言「I must call it inside run_code via tools.ask_user_question」（reasoning 原文）,交互事件面（提问卡/行指示/答案链路）全通,唯转录工具行形态不成（仅记录,非分析）。logcat(d2_logcat.txt):FATAL EXCEPTION=0。

## E. 待审批状态点（④）
### E1 审批挂起列表行琥珀点
- 前置：只读档会话
- 操作：该会话发送 `run%sbash%stouch%s/tmp/acc311_pending` → 审批卡出现（不应答）→ BACK 回会话列表 → dump 该会话行
- 期望：行呈现琥珀待审批指示（与 Asking 族形态,approval 独立点）
- 判定：dump 行指示在场;回会话应答仅一次收尾（探针存在）
- 实测记录：✔ 2026-09-05 22:57:25。22:54:32 进入只读档会话 g2-once（权限药丸「只读」复核）,input text 原文入框→发送。**审批卡出现**(e1_wait1-4.xml,~20s 内):`需要权限`[72,1656]+`tool`[72,1740]+按钮 `拒绝`[557,1854]/`仅一次`[536,2010][664,2070]/`始终允许`[515,2166];转录上下文:首次 Shell `$ touch /tmp/acc311_pending` 完成·[stderr]（只读沙箱拦截）→「正常执行被只读沙箱拦截，按流程提权重试一次：」→权限请求挂起。**不应答 BACK 回列表**:dump(e1_row_full.xml)g2-once 行时间戳行呈**双指示**:`处理中`[402,1984][505,2027]+`待批准`[595,1984][698,2027]——待审批独立点在场（与 Asking 族处理中共存）✓。**像素探针**(e1_row_pending2.png):`待批准`徽章区含 **#f5c344 琥珀**(16.5%)——对照`处理中`徽章为灰紫 #5d5b7a(15.0%)非琥珀——琥珀色证实✓。**回会话应答仅一次收尾**:重进(卡仍在)tap `仅一次`(600,2040)→轮次 89 完结 `2m 16s · 9 步 · 4 个工具`(e1_approve_poll1.xml):提权 Shell `$ touch /tmp/acc311_pending` 完成·(no output)→agent 自验证 `$ ls -la /tmp/acc311_pending`→`-rw-rw-r-- 1 leo-tkp leo-tkp 0 9月 5日 22:57`——**宿主探针 `ls /tmp/acc311_pending` 存在**(0字节,22:57,与设备侧回执逐字一致)✓。logcat(e1_logcat.txt):FATAL EXCEPTION=0。通过。
### E2 应答后点消
- 前置：E1 应答完成
- 操作：BACK 回列表 dump 该行
- 期望：琥珀点消失
- 判定：dump 无指示
- 实测记录：✔ 2026-09-05 22:58:01。E1 应答收尾后 BACK 回列表 dump(e2_list.xml):g2-once 行升至列表顶部（时间戳更新 `9月 5, 22:54`,最近活动排序）,行内 grep `待批准`=0、`处理中`=0——**双指示皆消**（琥珀待审批点消失,处理中随轮次完结同消）✓。通过。

## F. 回归
### F0 冷启 baseline 已归档消费
- 前置：A2 完成（A 在已归档集合）
- 操作：force-stop → 冷启重进 prod-3080 列表 → dump
- 期望：主列表无 A;已归档折叠区仍在含 A（workspace follow **baseline** 路径消费——A2 只证增量路）
- 判定：dump 两断言
- 实测记录：✔ 2026-09-05 22:30:20。22:29:0x `am force-stop`+`am start` 冷启(reverse 先行确认 host-38 tcp:3080)。冷启首屏=服务器列表页,prod-3080 呈「开始 连接」未连态(不自动重连——如实记录),tap 连接(600,1895)→已连接+DSH 徽标→tap 打开聊天(344,1895)入列表。**断言1**(f0_list.xml):主列表顶部=「根据这一分handoff继续」(18:43),grep `>hi<`=0——主列表无 A✓。**断言2**(f0_bottom.xml/f0_archived_expanded.xml):底部 `已归档（4）`[216,2282][454,2338] 在场;tap header 展开后区内含 `hi`(9月 5, 22:20 即 A)✓,另含 `count from 1 to 50`/`根据这个handoff 继续吧`/`安装这几个插件…`——与 A2/A4 归档产物一致,baseline 路径消费证实。logcat(f0_logcat.txt):FATAL EXCEPTION=0。通过。
### F1 常规健康
- 前置：A-E 完成
- 操作：普通收发一轮;进出会话×2;logcat 总检
- 期望：FATAL=0 无断连
- 判定：FATAL=0
- 实测记录：✔ 2026-09-05 22:59:32。①普通收发一轮:会话 B 发 `ok`→回复「Sounds good! Just let me know what you'd like to do next…」,轮次 8 完结(0ms·1 步·0 工具,f1_poll1.xml)。②进出会话×2:BACK→重进(BACK→再进,两次均落聊天页,f1_inout1/2.xml,返回 marker 在场)。③logcat 总检(f1_logcat.txt):`FATAL EXCEPTION`=0、`E AndroidRuntime`=0、app 层 disconnect/断开/connection lost/closed 模式=0、屏面无「正在连接…」横幅——**无崩溃无断连**。通过。

## 人工验收清单（UIUX;域汇总登记）
- [ ] 行菜单形态/锚点;左滑手感与回弹;已归档折叠区动画
- [ ] 新建对话框 workspace 条目形态/计数角标
- [ ] 产出行 chips 观感;skill 折叠卡;question 工具行
- [ ] 琥珀点视觉与 Asking 族协调

## 主 agent 终裁（2026-09-05）
- **D2 主断言归并 BLOCKED-harness routing**（同 C1/D1 族）：该 DSH prod harness 的 write/edit/skill/ask_user_question 全部经 run_code 内联直发不可达——产出行折叠域/工具卡 name 分派与 web 契约同形（web 在同场景同样只显 Run code）;交互面/指示三态/宿主受端证据全通=特性本体已证;折叠域与双名分派正确性由单测矩阵钉死（TurnDeliverablesTest 17/SpecialToolCardsTest 9）。工具直连 preset 激活时自然呈现。
- 顺带观测 4 条移交：新会话行入列表 ~3min 时延（对话框计数即时）登记 #331;其余三条（首次 BACK 内部文本块/冷启不自动重连/列表非严格时间排序）journal 记录观察。

## 执行汇总

- 执行者:真机验收执行员(纯净上下文 subagent);设备 `192.168.110.239:5555`(1200x2670);包 `dev.leonardo.ocbeacon.dev`(versionName=0.3.0,lastUpdateTime=2026-09-05 22:08:18);后端 DSH prod-3080(经 `adb reverse tcp:3080 tcp:3080`,未用 debug-entry.sh 以避 4199 重指坑);执行窗口 22:16–22:59(2026-09-05);证据 `/tmp/acc311_logs/`(分项 logcat 12 份,FATAL 全 0)+`/tmp/acc311_shots/`(dump 40+ 份/截图 5 份/像素探针脚本)。
- **终态 16 项**:✔ 12 项(P0/P1/A1/A2/A3/A4/F0/B1/B2/C2/E1/E2/F1 中计 13 项通过,含 F1);BLOCKED 2 项(C1 模型行为、D1 模型行为);✘ 1 项(D2 主断言,顺带子断言两段✔)。
- **通过要点**:①归档链全通——行菜单三态无取消归档、归档动作 follow 增量不重启(已归档 2→3)、折叠区含 A 且行菜单仅详情(单向)、左滑 200ms 快滑归档(3→4)、冷启 baseline 消费(已归档(4) 含 A)。②多 workspace——对话框条目 title+路径+计数角标三要素,经 workspace 新建后计数 3→4 不重启。③④状态点——只读档审批挂起时列表行双指示(处理中+待批准),待批准徽章像素证实琥珀 #f5c344(对照处理中灰紫 #5d5b7a);应答(仅一次)后双指示消,宿主探针 /tmp/acc311_pending 存在与设备侧回执逐字一致;question 族「待回答」指示在场→提交后消(④ 三态补全)。
- **不成项(同族根因:DSH prod harness 的文件/skill/ask 类工具均由 agent 经 run_code 内联调用,非 run_code 工具不可直达——agent 轮次5 原文「I can only invoke edit from inside run_code — direct calls to non-run_code tools aren't supported in this harness」)**:C1 写文件产出行——宿主受端三试全成立(made_by_acc311{,_r2,_r3}.txt 均在场),但转录层唯一工具卡恒为 Run code(不在产出行折叠域 write/edit/str_replace_editor),轮次尾无「产出文件」行(dump+像素双证);按主 agent 裁定两 retry 后记 **BLOCKED-模型行为**,折叠域正确性由单测矩阵钉死。D1 skill 卡——skill 确已加载(agent 回复 code-review/filesystem)但经 run_code 内联,转录无 skill 工具卡节点,无卡可展,按项处置记 **BLOCKED-模型行为**。D2 question 工具行——交互面全通(提问卡挂起/Red 提交/复述 Red/列表行指示两段),唯转录层无 Asked 形态工具行(同 run_code 包裹族),主断言记 ✘(项未写 BLOCKED 出口;仅记录现象供裁定)。
- **顺带观测(仅记录)**:①新会话行入列表顶位有约 3 分钟时延(P1 的 A 与 B2 的 B 均如此),而新建对话框 workspace 计数即时+1;②两次聊天页首次 BACK 未离屏且顶部出现 system-reminder/get_goal 类内部文本块(带复制钮);③冷启后停在服务器列表页且不自动重连上次服务器,需手动点连接;④会话列表非严格时间排序(顶部 18:43 先于 21:12)。
- 人工验收清单 4 组 UIUX 项留人工(C1/D1/D2 的卡形态未在真机呈现,人工项相应无法目验)。
