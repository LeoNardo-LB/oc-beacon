# 2026-09-10 · 批次验收清单：#378 命令/压缩卡族 + #379/#368/#371/#373/#369/#370/#355

> 批次范围：goal 驱动的可做项全集（14 提交，a0de724d…4fe938d2）。
> 分类依据：ai-acceptance-workflow §1——仪器可验项入本清单；真手指体感/IME 注入受限项入 §9 人工清单（类别标注+仪器不可行理由）。
> 环境：小米 houji（WiFi adb mDNS serial 见下）；DSH-prod3080（adb reverse tcp:3080）；Host-4199（V2，reverse tcp:4199）；V1-4198（cred-probe.sh v1）。

## A. #378 命令/压缩卡族（伞卡，含 #375/#376/#377 子证）

### A1 重进重建（#376 主证）
- 目的：校验重进含压缩历史的 DSH 会话后命令卡/压缩实体在场（历史 transcriptEvents dispatch 生效）。
- 前置：DSH-prod3080 已连接；设备已装本批 APK；/calculator 会话（session-134b999b，3 次压缩）在列表。
- 操作：进会话 → `adb logcat -d | grep Transcript378`。
- 期望：`plan cmds=4 compactions=3 extras=1 trailing=1` 字样在场。
- 判定：logcat 行存在且计数=4/3/1/1。
- 实测记录：

### A2 顶部卡渲染（#378 视觉面）
- 目的：校验转录最顶部 trailing 命令卡可见。
- 前置：A1 会话内。
- 操作：连续快速上滑（`input swipe 540 600 540 2300 90` ×40）到顶 → dump。
- 期望：dump 含 `/compact` 与 `已完成` 文本节点。
- 判定：两文本在场。
- 实测记录：

### A3 遮蔽折叠（#375 surfaceOp）
- 目的：校验压缩前旧消息不渲染（转录顶=后压缩内容+卡，无 13 条旧消息堆叠）。
- 前置：A2 视口。
- 操作：观察顶部区域 dump/截图。
- 期望：最旧可见内容为技能注入消息/卡，非压缩前旧对话。
- 判定：顶部无「昨天的 echo 播种时代」旧消息串（主观项移交人工时以结构为准：列表项数量显著少于未折叠全量）。
- 实测记录：

### A4 无双轨（Phase C）
- 目的：校验压缩呈现无分割线/横幅与 box 并存。
- 前置：A1 会话任意位置。
- 操作：dump 查找压缩分割线特征（「压缩中」横幅文本）。
- 期望：无压缩进行中分割线（本会话压缩均终态；transcript 实体在场时让位）。
- 判定：dump 无进行中分割线节点。
- 实测记录：

### A5 V1/V2 回归（卡族不出现）
- 目的：校验非 DSH 会话无命令卡/压缩 box（transcriptEvents 恒空）。
- 前置：Host-4199（V2）任一会话。
- 操作：进会话 → logcat `grep Transcript378`。
- 期望：无 plan 行（或 cmds=0 compactions=0）。
- 判定：无卡数据。
- 实测记录：

## B. #379 抽屉手势纪律

### B1 内容 fling 不收起
- 目的：校验 sheet 内容区强向下 fling 不致收起。
- 前置：任一会话 → 点模型选择器开 sheet。
- 操作：`input swipe 600 1400 600 2200 120` ×6 于列表区 → dump。
- 期望：sheet 标题（如「选择模型」）仍在 dump。
- 判定：SHEET-OPEN。
- 实测记录：

### B2 手柄拖拽收起保留
- 目的：校验手柄区拖拽仍可收起。
- 操作：`input swipe 600 700 600 2300 400` → dump。
- 期望：sheet 关闭。
- 判定：DISMISSED。
- 实测记录：

## C. #371-D7 服务器徽标统一

### C1 三卡同容器色
- 目的：校验 home 三服务器卡版本徽标同为中性容器。
- 前置：home 服务器列表可见。
- 操作：截图 → 像素采样三卡徽标区（或 vision 通道判色）。
- 期望：DSH/API v2/API v1 徽标底色一致（surfaceVariant 族），无 tertiary/primary 异色。
- 判定：三区色值一致（±阈值）。
- 实测记录：

## D. #368 V2 服务器钟域

### D1 消息时间戳=服务器钟
- 目的：校验 V2 新轮消息 created/completed 来自信封服务器时刻而非设备钟。
- 前置：Host-4199；宿主可 curl V2（service.json 凭据）。
- 操作：宿主创建会话+prompt（curl /api/session + /prompt，抓 SSE step.started 帧 created 值）→ 设备进该会话 → logcat `[msg] MessageUpdated` 或 Room 直查 created。
- 期望：消息 created ≈ 帧 created（±2s），非设备本地时刻漂移特征。
- 判定：差值 < 2000ms。
- 实测记录：**✔（2026-09-10 主会话执行）** 服务器侧驱动 prompt（ses_f7b7f6318）：SSE 帧 step.started created=1788929418746；设备进会话后 Room 直查（cached_messages）assistant 行 created=1788929418746——**毫秒级全等**（修复前此处为设备钟，偏差=设备-服务器钟差）。user 行 1788929416547 同序 sane。测试会话已删。

## E. #369 回归：ShellSheet V2 可用

### E1 Shell 面板打开
- 目的：校验死代码删除后 V2 ShellSheet 正常。
- 操作：V2 会话 FAB → SHELL 入口 → 打开面板 → dump。
- 期望：面板标题在场，无崩溃（logcat 无 FATAL）。
- 判定：dump 有面板节点。
- 实测记录：

## F. #355 搜索结构

### F1 筛选菜单在场（条件项）
- 目的：校验内容命中区用标准筛选菜单（非 tag chips）。
- 前置：已同步会话的 DSH 服务器；输入注入若失败标 BLOCKED-IME。
- 操作：搜索框输入关键词（type.sh）→ dump。
- 期望：`筛选` 文本按钮在场；无 FilterChip 行。
- 判定：菜单节点在场。
- 实测记录：

## G. #373 V1 空会话面板

### G1 scratch 面板 tap 建会话（条件项）
- 目的：校验空会话斜杠面板 tap 不再静默清空。
- 操作：V1-4198 新会话 → 输入 /todo 面板 tap（type.sh 依赖 → 可能 BLOCKED-IME）。
- 期望：会话被创建（logcat 无 /session//todo 400）命令执行。
- 判定：logcat 无空 sid 请求。
- 实测记录：

## H. #370 V2 队列轮末刷新（观察项）

### H1 轮末重拉
- 目的：校验 V2 排队后轮末 QueueSheet 数据自动刷新。
- 操作：V2 忙时排队一条 → 打开 QueueSheet → 轮末等待 → dump 队列区。
- 期望：队列项消失（派发后面板新鲜）。
- 判定：dump 无已派发项。（时序长，可 BLOCKED-TIMEBOX）
- 实测记录：

## 9. 人工清单（V6，类别标注）

- #378 进行中 box 体感+展开收起交互（①真手指连续手势——IME/注入不可达）；
- #379 fling 体感终验（①）；
- #355 检索区视觉与归档不出现（①输入注入受限，日常搜索即验）；
- #368 时长呈现主观确认（④观感）。