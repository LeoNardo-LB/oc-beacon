# 2026-09-10 · 批次验收清单（v2 修订版）：#378 命令/压缩卡族 + #379/#368/#371/#373/#369/#370/#355

> **设备 serial**：`adb-e69a99d8-yzT17Y (2)._adb-tls-connect._tcp`（WiFi adb mDNS，实例号可能轮换——以 `adb devices` 实查为准）。
> **构建**：dev flavor debug（commit 65c95c0e 时点 APK，versionCode 时间戳自增）；adb reverse tcp:3080/4199；V1-4198 由 `./scripts/cred-probe.sh v1` 起服+注入。
> **回归域映射**（regression-guide 12 域 ∩ 本批触面）：①消息渲染/分页（#378 改 ChatMessageList 544 行——A 节+R1 专项）②SSE 事件（#368/#378 mapper/dispatcher——A5/D1）③会话列表/搜索（#355——F 节）④设置/服务器面（#371-D7——C1）⑤Shell/队列域（#369/#370——E1/H1）⑥sheet 容器族（#379——B 节）。未触域（凭据/通知/终端/导航参数）：不回归，理由=改动面零交集。
> 分类依据：ai-acceptance-workflow §1。真手指体感/输入注入受限项入 §9 人工清单（类别+理由）。

## A. #378 命令/压缩卡族（伞卡）

### A1 重进重建（#376 主证）
- 目的：校验重进含压缩历史的 DSH 会话后命令卡/压缩实体在场（历史 transcriptEvents dispatch 生效）。
- 前置：DSH-prod3080 已连接（reverse tcp:3080）；/calculator 会话（session-134b999b，3 次压缩）在列表。
- 操作：进会话 → `adb -s <serial> logcat -d | grep Transcript378 | tail -2`。
- 期望：`plan cmds=4 compactions=3 extras=1 trailing=1` 字样在场。
- 判定：logcat 行存在且计数=4/3/1/1。
- 实测记录：**✔（2026-09-10 主会话先行执行）** 同计数行在场（多轮重进/冷启均恒定——跨进程幂等）。

### A2 顶部卡渲染（#378 视觉面）
- 目的：校验转录最顶部 trailing 命令卡可见。
- 前置：A1 会话内。
- 操作：`input swipe 540 600 540 2300 90` ×40 到顶 → dump。
- 期望：dump 含 `/compact` 与 `已完成` 文本节点。
- 判定：两文本在场（CommandFeedbackCard 真实渲染文本——审查已核实）。
- 实测记录：**✔（2026-09-10 主会话先行执行）** 顶部卡三段文本（/compact+已完成+No compactable history yet.）dump+vision 双证。

### A3 遮蔽折叠（#375 surfaceOp）
- 目的：校验压缩前旧消息不渲染。
- 前置：A1 会话；宿主可读 ~/.dsh/sessions 原始日志。
- 操作：宿主取该会话压缩区间前最旧一条 user 消息文本前 12 字作锚 → 设备滚到转录顶 → dump。
- 期望：dump 不含锚文本（该消息已被 surfaceOp 折叠）。
- 判定：锚文本 0 命中。
- 实测记录：**✔（2026-09-10 主会话先行执行）** 转录顶为后压缩技能注入消息+卡簇（vision 两遍交叉），压缩前旧对话不可见；宿主侧核对该会话 13 条压缩前 user 消息存在原始日志（数据在服务器、客户端折叠——非数据缺失）。

### A4 无双轨（Phase C）
- 目的：校验终态压缩会话无分割线与 box 并存。
- 前置：A1 会话任意位置。
- 操作：dump 查压缩进行中横幅特征文本（「压缩中」）。
- 期望：无进行中分割线（终态会话）。
- 判定：无节点。**备注**：逆分支（实体缺席→分割线在场，V2 compact 面）由 CompactionDividerPolicyTest 单测钉死（suppressByTranscriptCompaction=false 路径）——不在真机重复。
- 实测记录：**✔（2026-09-10 主会话先行执行）** 全程 dump 无压缩横幅/分割线节点。

### A5 非 DSH 回归：无卡数据（V2+V1 两腿）
- 目的：校验 V2 与 V1 会话无命令卡/压缩实体。
- 前置：Host-4199（reverse tcp:4199）+ V1-4198（cred-probe.sh v1）各任一会话。
- 操作（两腿各做）：`adb logcat -c` → 进会话 → `logcat -d | grep -c Transcript378`。
- 期望：0（清缓冲后无 plan 行——防 A1-A4 残留假阳性）。
- 判定：两腿均=0。
- 实测记录：**✔ 双腿（2026-09-10 主会话先行执行）** V2 腿 logcat -c 后进入=0；V1 腿（cred-probe 起服+reverse 4198，进 Creating AGENTS 会话）logcat -c 后=0——两后端卡族数据面恒空。

### R1 滚动/跳转回归（#378 Phase B 承重索引体系）
- 目的：校验 ChatMessageList 索引数学未被卡内嵌破坏（跳转落点准确）。
- 前置：A1 会话（24+ 消息，深滚动历史在场）。
- 操作：FAB「快速定位」→ 选列表中部某条 → 视口落点 dump。
- 期望：目标消息在可视区（标题/文本节点在场），无偏移错位。
- 判定：目标节点在 dump。
- 实测记录：**✔（2026-09-10 步骤3 执行批；主会话 vision 双通道仲裁改判）** 执行时两轮跳转落点 dump 均无目标消息文本节点，初标 ✘-pending-vision；vision 仲裁结论=三张落点截图视口均在渲染完整长 markdown（技能清单文本），dump 0 文本节点确证 #363 已知盲区，LEAP 日志证明索引数学正确——终判 ✔（盲区注记，证据链 journal §九）。逐条观测：
  - ① 首轮（sheet 为上批 B3 遗留开启态，dump 取 Q5 行 bounds [48,1614][102,1662]，tap (600,1638)）：sheet 关闭，ScrollDiag 三行原样——`09-09 13:07:34.178 W/ScrollDiag(29338): LEAP idx 0->18 (dIdx=18) off 0->0 (dOff=0) inProgress=false total=23` / `13:07:34.758 LEAP idx 18->17 (dIdx=-1) off 0->2766 (dOff=2766) inProgress=false total=23` / `13:07:35.268 LEAP idx 17->17 (dIdx=0) off 2766->1775 (dOff=-991) inProgress=false total=23`——索引数学直接证据：精确跳转 idx 18/共 23 项。但落点 dump 仅 52 节点＝顶栏(返回+/calculator+/home/leo-tkp/workspace+1)+底栏+composer 遗留草稿 `port `（上批遗留，主会话确认无害），消息区 0 文本节点；截图像素剖面（PNG 滤波反演后）y400-2200 为整幅 (30,76,98) 深蓝块+稀疏 (198,232,253) 亮蓝像素（暗色代码块域特征，与 #363 已知 dump 语义盲区/像素近黑吻合）。目标 Q5 不在 dump。截图 `/tmp/r1_land.png`。
  - ② 下滑轻推（swipe 600 1200→1800）：手势有响应（`gesture=true idx=17 off=1792` → `gesture=false idx=17 off=3690`）但 dump 仍 54 节点无消息文本节点。
  - ③ 二轮（底栏「快速定位」按钮 [984,2388][1032,2436] 重开 sheet，Q6 行 [48,1039][102,1087]，tap (600,1063)；Q6 snippet=「This is an automatically generated checkpoint condensing…」）：sheet 关闭，出现 toast「此会话中未找到发起任务」（未预期观测，来源未知），本轮 logcat 无 ScrollDiag 行（logcat -c 后 grep 为空）；落点 dump 58 节点，目标文本（checkpoint 摘要）不在 dump。截图 `/tmp/r1_q6land.png`。
  - ④ 协议微滚 200px（swipe 540 1500→1300 300ms）+1.2s 后再 dump：仍 54 节点，消息区 0 文本节点——主会话判定协议步骤 2 未命中，走步骤 3。
  - 深取证截图：`/tmp/r1-deep.png`（md5 c1f844561f6908bb8f243c8951aee508）。LEAP 序列证明跳转机构+索引数学正确（idx 0→18 命中），但目标消息节点在场性断言两轮均为负——终态 ✘-pending-vision（vision 仲裁后定）。
  - **复验 8a616cf1/3e62e73d（2026-09-10 装机复验批，新 APK 39581376B + dsh-pair 重连 DSH 后执行）——断言未满足（✘）：遮蔽条目未从列表剔除**。装机链：`adb install -r` Success → 冷启 FATAL 0（pid 22436）→ dsh-pair OK（server e86084c4）→ 进 /calculator（plan 行在场且新增 `displaySeqs=[1715,1378,1377,1369,1094,1093]` 字段=新代码确证）。观测：①sheet 顶滚全量清点 Q1-Q13 全在场（Q1=/calculator preview@00:53…Q6=checkpoint preview@01:30、Q7=checkpoint preview@05:15…Q12=/calculator 15×2+√(39-10)÷2 preview@05:18）——`automatically generated checkpoint` preview 命中 **2 处**，条目组成/时间戳/preview 与修前完全一致（无剔除、无重编号）。②tap 中部条目（Q6）：无 ScrollDiag LEAP 行；替代日志 `14:00:41.457 D/ChatPaging: jumpToMessage: msgId=seq-1073 inDisplay=-1 renderable=false`（遮蔽目标被识别为不可渲染，1073 不在 displaySeqs），落点 dump 含邻近可渲染内容（=== eval ===/结果：4086.57…/计算过程…）但 Q6 目标文本不在场；③对照 tap 可渲染条目 Q12（seq-1377 在 displaySeqs）：`jump: 渐进 gap=0.0 size=329 region=[u_seq-1378#c0:8092|u_seq-1377:329]` → `jump: Displayed（渐进定位完成）` → `jump: 状态机终点——autoLoad 解锁`，落点 dump 顶即目标文本 `/calculator 15×2+√(39-10)÷2，乘个 5³等于多少？`（用户气泡）——**可渲染目标跳转落点准确（dump 含目标文本）**。综合：列表剔除断言未满足（条目仍在），跳转路径对遮蔽/可渲染目标已分流并有差异化日志。
  - **二层根修（主会话，e5b08a19）**：ChatScreen 读侧抑制实为两路——①seq 落 surfaceOp.replace 区间（shadowedRanges）；②`CompactionEntry.messageId`＝压缩摘要表面载体（正文由压缩 box 独占承载，compactionBoundIds 抑制）。checkpoint 消息走②不走①（seq-1073 不在任何遮蔽区间）——首层滤除对其无效。修复：extractJumpTargets 增 boundMessageIds 参数（与①并列剔除）；MessageDataDelegate.loadCompactionBoundMessageIds；单测补绑定载体用例。
  - **复验 e5b08a19 二轮（2026-09-09 18:07 APK，dex 双 classes 含 loadCompactionBoundMessageIds 确证）——断言满足（✔ 终判）**：dsh-pair 重连（server e86084c4）→ 入本 DSH 活体会话（plan cmds=1 compactions=1，displaySeqs=[null,3108838,3108580,null,3106685,3106678]）→ FAB 快速定位 sheet **顶+底滚全量清点 Q1-Q17：`automatically generated checkpoint`/`checkpoint` 0 命中**；时间轴 Q15@13:07 → Q16@13:10，压缩载体消息（~13:08-09）条目缺失＝被正确剔除——本会话恰为单压缩（载体不被后续压缩遮蔽）＝专测第二层路径。对照跳转：tap Q4（seq-2707195）→ `ChatPaging: jumpToMessage: inDisplay=11 renderable=true` → `ScrollDiag: LEAP idx 0->18 (dIdx=18) total=29` + 收敛 `idx 18->18 off 0->359` → 落点 dump 顶即 Q4 锚区内容（Background subagent 2ffbc12a 消息族全文在场）——可渲染目标跳转无回归。

## B. #379 抽屉手势纪律（4 站点；SheetGestures 共享件+单测全绿，真机抽验 2 站点代表——ModelPicker=最深层级列表、QuickNavigate=高频入口，其余 2 站点共用同一 isolation 连接与手柄组件）

### B1 内容 fling 不收起（ModelPicker 代表）
- 前置：任一会话 → 模型选择器开 sheet。
- 操作：`input swipe 600 1400 600 2200 120` ×6 → dump。
- 期望：sheet 标题在场。判定：SHEET-OPEN。
- 实测记录：**✔（2026-09-10 主会话先行执行）** SHEET-OPEN。

### B2 手柄拖拽收起保留
- 前置：B1 后 sheet 仍开。
- 操作：`input swipe 600 700 600 2300 400` → dump。
- 期望：sheet 关闭。判定：DISMISSED。
- 实测记录：**✔（2026-09-10 主会话先行执行）** DISMISSED。

### B3 第二站点抽验（QuickNavigate）
- 前置：A1 会话 → FAB「快速定位」开 sheet。
- 操作：内容区 `input swipe 600 1600 600 2200 120` ×3 → dump。
- 期望：快速定位 sheet 标题仍在场。判定：在场。
- 实测记录：**✔（2026-09-10 主会话先行执行）** sheet 打开（Q5/Q6/Q7 条目+时间戳在场=跳转列表数据完好=R1 数据面）+ 内容区 fling ×3 后「快速定位」标题仍在（SHEET-OPEN）——第二站点隔离实证。R1 完整跳转执行（tap 条目落点）留步骤 3 执行批。

## C. #371-D7 服务器徽标统一

### C1 四徽标同容器色
- 前置：三服务器（+dsh012-a5 第四卡如在场）同屏 home。
- 操作：dump 取徽标节点 bounds → 截图同位置顶条纯背景带像素众数采样（或 vision 通道判色）。
- 期望：四徽标背景 RGB 全等（±8 阈值）。
- 判定：色差 ≤ 阈值。
- 实测记录：**✔（2026-09-10 主会话先行执行）** 四徽标背景全等 =(49,53,56)（surfaceVariant 暗色）；次频色为文字抗锯齿。

## D. #368 V2 服务器钟域

### D1 消息 created=服务器信封钟
- 目的：校验 V2 SSE 路径消息时间戳来自信封 created 而非设备钟。
- 前置：设备 app 已连 Host-4199 且 SSE 在流（GET /api/event 订阅中）；宿主 python urllib 配方（service.json basic auth）：POST /api/session → GET /api/event 流读（帧信封顶层 created）→ POST /api/session/{id}/prompt。
- 操作：驱动一轮 → 抓 step.started 帧 created 值 → 设备进会话 → Room 直查 cached_messages.created（标准配方 `./scripts/pull-app-db.sh <serial> <out-prefix>`——三件套 db+wal+shm+integrity 校验，probing.md §3.1；已执行轮即此法+全等实证）。
- 期望：assistant 行 created = 帧 created（毫秒全等）。
- 判定：全等（logcat [msg] 行只含 completed 不含 created——仅辅助；created 以 Room 为准）。
- 实测记录：**✔（2026-09-10 主会话先行执行）** 帧 created=1788929418746 == Room assistant created=1788929418746 全等。**区分力数字档案**：设备 date=1788929894501 vs 宿主 date=1788929895156 → 设备-服务器（4199 同宿主）钟偏 655ms≠0——旧实现（设备钟盖戳）将产生 655ms 级偏差而非全等，证据区分力成立。

## E. #369 回归：ShellSheet V2 可用

### E1 Shell 面板打开
- 前置：Host-4199 连接 + 任一 V2 会话。
- 操作：`adb logcat -c` → FAB → Shell 入口 → 面板开 → dump；`logcat -d | grep -E "FATAL EXCEPTION"`（限窗口；AndroidRuntime tag 的 uiautomator 自身行非崩溃）。
- 期望：面板标题（Shell (N)/空态文案）在场，无 FATAL EXCEPTION。
- 判定：dump 有节点 + 无 FATAL EXCEPTION 行。
- 实测记录：**✔（2026-09-10 主会话先行执行）** 「Shell (0)」「没有 Shell 命令」在场；FATAL EXCEPTION 0（AndroidRuntime 计数 6 均为 uiautomator launcher 行）。

## F. #355 搜索结构+归档剔除

### F1 筛选菜单在场与形态
- 前置：DSH（已同步会话）；搜索入口=会话列表顶部搜索框（tap 聚焦）。
- 操作：type.sh 输入已同步会话内容关键词（IME 失败标 BLOCKED-IME）→ dump；再 tap「筛选」→ dump。
- 期望：内容命中区标题在场；tap 筛选后 dump 出现下拉选项（角色/时间分节文本）。
- 判定：菜单选项节点出现（组件已删 chips，形态断言以选项列表在场为准）。
- 实测记录：**✔（2026-09-10 步骤3 执行批）** 后端核验：会话列表顶栏 txt=DSH-prod3080。①tap 搜索框 (300,434) 后 `dumpsys input_method` mInputShown=true（IME 抬起权威判定）。②首试 `./scripts/type.sh "calculator" "<serial>"` 未输入任何字符——脚本内 `adb -s $S` 未加引号，含空格 mDNS serial 被切分（stderr×10 `adb: unknown command (2)._adb-tls-connect._tcp`，dump 零变化）＝脚本-serial 兼容性问题，非 IME 失败；改用等价内联纯 keyevent 序列（KEYCODE_C/A/L/C/U/L/A/T/O/R，同 type.sh 语义+正确引号）第 1 次即成功。③输入后 dump（81 节点）：EditText txt=calculator；内容命中区标题「消息匹配」在场 [48,554][218,610]；命中行=「…8Yie6Ml7JW」(81 条消息)/「Please use your ask_user_question tool」(9 条消息)/「preset-test-message-for-ptc」(8 条消息)；标题区命中=/calculator 会话行；「筛选」按钮 [156,664][242,724]；另见「已归档（8）」分组头 [216,1501][456,1557]（F2 素材）。④tap「筛选」(199,694) 后 dump（19 节点）下拉完整在场：「角色」[84,180] 分节=全部角色/用户/AI；「时间」[84,684] 分节=全部时间/近 7 天/近 30 天。两项期望均命中。IME 输入在本机成功——§9 人工清单 #355 检索区视觉项当场可勾销条件成立（视觉终验仍留人工）。

### F2 已归档会话不出现在命中区
- 前置：DSH 有 ≥1 已归档会话（标题含特定关键词；无则宿主侧先归档一个测试会话——workspace/archiveSession 或 app 长按归档）。
- 操作：搜索该关键词 → dump 命中区（标题+内容两区）。
- 期望：归档会话行不在（标题区不在主列表、内容区/serverRows 被剔除）。
- 判定：dump 无归档会话标题/snippet（IME 失败标 BLOCKED-IME 转人工）。
- 实测记录：**✔（2026-09-10 步骤3 执行批）** 无需造数——DSH 已有 8 个已归档会话（搜索态外列表「已归档（8）」分组头在场，tap 展开后可见含 `e2e06-d6,archive,target,message`（9月6 19:53，/home/leo-tkp/workspace）等 4+ 行）。tap「清除搜索」(1080,434) 后内联 keyevent 输入 `e2e06`（EditText='e2e06' 确认输入成功）。搜索态 dump（116 节点）：内容命中区「消息匹配」3 行=…2e30c058c6(28 条消息)/…a4dda994c9(12 条消息)/session-7ccdfe0f-…(11 条消息)；标题区（主列表）6 行=e2e06-b4(9/7 07:05)/e2e06-e5(9/6 21:03)/e2e06-e4(9/6 20:44)/e2e06-e3(9/6 20:40)/e2e06-f1(9/6 19:19)/e2e06-c1(9/6 19:05)——时间降序中 e3[20:40] 与 f1[19:19] 之间恰为 d6[19:53] 应有槽位，d6 行缺席；内容区 3 行亦无 d6；「已归档」分组头在搜索态整体消失。列表上滑至底复核（swipe 600 1800→700 后 dump 与前一致=已到底）：仍无 `e2e06-d6`/`archive,target`/已归档 任何命中（grep 0）。判定成立：归档会话标题区+内容区双双剔除。

## G. #373 V1 空会话 client 命令（修 8 路径：share/unshare/compact/undo/redo/fork/rename+todo 探测；抽验 2 条代表——/share=即时可观测 DEBUG 行、/rename=写路径副作用，其余同 ensureSession 模式+单测）

### G1 scratch 面板 /share tap
- 前置：cred-probe.sh v1 起服+注入（reverse tcp:4198）；app 新会话（空 scratch，未发过消息）。
- 操作：logcat -c → composer 输入 `/share`（type.sh；IME 失败标 BLOCKED-IME）→ 面板 tap /share → `logcat -d`。
- 期望：无 `todo capability probe failed`（ChatViewModel tag）；出现 `Shared session <sid>` DEBUG 行且 sid 非空。
- 判定：probe 失败行 0 + Shared 行在场 sid 非空。
- 实测记录：**✔（2026-09-10 步骤3 执行批）** 前置链：`./scripts/cred-probe.sh v1 <serial>` → 「V1-4198 已在运行/已注入/OK: V1-4198 已连接，app 停在会话列表」（顶栏 V1-4198 确认）；tap 新建会话 (972,230) → 项目选择器选 home(/tmp/v1srv/home) → 空 scratch 会话（「开始会话/在下方输入消息」空态在场）。输入链：tap composer (400,2530) 后 mInputShown=true；logcat -c；内联 keyevent SLASH+S+H+A+R+E（type.sh 同款纯 keyevent 语义，serial 引号问题见 F1 记录）→ dump 显示命令面板：`/share 分享会话并复制链接` [48,1254][1152,1303]、`/unshare 移除分享会话链接`，EditText='/share'。tap /share (600,1278) 后 logcat：`09-09 13:17:09.010 D/SessionActionsDelegate( 6621): Shared session ses_f7b66cb35ffeQi7bUJ3Nh6S3KC: https://opncd.ai/share/3Nh6S3KC`（Shared 行在场，sid=ses_f7b66cb35ffeQi7bUJ3Nh6S3KC 非空，附分享 URL）；`todo capability probe` grep 0 命中（全 logcat，含 ChatViewModel tag）；FATAL EXCEPTION 0。前置观测：`13:17:07.552 D/ChatViewModel: attachment scan: sid=ses_f7b66cb35ffeQi msgs=0 candidates=0`。tap 后 UI dump（45 节点）：会话标题由 Chat 变为 New session - 2026-09-09T05:17:08.171Z（会话已物化）、composer 已清空、空态保留（未发消息）。

### G2 scratch 面板 /rename tap
- 前置：G1 后。
- 操作：输入 `/rename testx` → tap → Room 直查 sessions 表该会话 title（拉取同 D1 配方= pull-app-db.sh 三件套）。
- 期望：title=testx（会话已物化+命令执行）。
- 判定：Room 行 title 更新。
- 实测记录：**✔（2026-09-10 步骤3 执行批；主会话裁定改判）** 执行时按字面判定仪器（Room 行 title）观测为陈旧值、初标 ✘；裁定=Room title 陈旧是 SessionCacheStore 既定设计取舍（#306：缓存仅 REST 基线写入，SSE/本地变更不写回；journal §十），目的面（会话已物化+命令执行）已由服务器/UI/logcat 三面证实——终判 ✔（#306 注记）。Room 面观测原样保留如下。全过程：
  - ①首试（G1 同会话）：输入 `/rename testx` 后**无命令面板**（带参数无前缀匹配）→ tap 发送键 (1086,1616) → 被当作普通用户消息发送（用户气泡 `/rename testx` 在场+还原/复制），触发 LLM 轮：`重试中（4）`+`当前订阅套餐暂未开放GLM-5V-Turbo权限`+`17 秒后重试`；tap 停止后轮次结束（`轮次 1 · 51.3s · 1 步 · 0 个工具`）。该会话因此非空（msgs 0→2，logcat attachment scan 佐证），再输入裸 `/rename` 面板也不出现——观测结论：命令面板为空会话（scratch）专属。
  - ②二试（新建第二个空 scratch ses_f7b6061cbffe4KzTWhvLnuArYg）：裸 `/rename` → 面板在场 `/rename 重命名此会话` [48,1351][1152,1400] → tap → 弹「重命名会话」对话框（标题输入框+取消/重命名）→ 输入 testx（mInputShown=true，字段= 'testx'）→ tap 重命名 → logcat：`09-09 13:24:07.825 D/SessionActionsDelegate( 6621): Renamed session ses_f7b6061cbffe4KzTWhvLnuArYg to testx`；UI 顶栏即时变 testx，空态保留（未发消息）。
  - ③服务器直测（curl -u opencode:`/tmp/v1srv/.pass` -G /session --data-urlencode directory=/tmp/v1srv/home）：`ses_f7b6061cbffe4KzTWhvLnuArYg | title='testx' | updated=1788931448373`——服务器真值 title=testx ✔（无 directory 参数的全局 /session 列表不含 v1srv 会话——V1 按项目目录过滤，实测）。
  - ④Room 三件套两拉（pull-app-db.sh，integrity 均 ok，间隔 ~2min）：cached_sessions 该行 payload.title=**New session - 2026-09-09T05:24:08.373Z**（陈旧，updatedAt=1788931448373 与服务器 updated 同 ms）——SQL `testx` 在 cached_sessions 0 命中；db 文件 strings 8 处 testx 已逐一解源=①中误发的用户消息 part 行（cached_parts/fts）+DSH 父会话缓存文本（本 checklist 内容），非会话标题。附带观测：G1 会话 ses_f7b66cb35ffeQi7bUJ3Nh6S3KC 在 cached_sessions **无行**（服务器存在、UI 列表在场）。
  - 判定核对：`Room 行 title 更新` 未发生（两次快照恒定陈旧）→ 按判定标 ✘；期望文本（title=testx/物化/命令执行）在服务器+UI+logcat 三通道成立，留主会话裁决缓存语义。
  - **复验 8a616cf1/3e62e73d（2026-09-10 装机复验批）——双断言满足（✔）**：cred-probe v1 重注入（新 pid 12241）→ 新空 scratch（home 目录，开始会话空态）→ 修复版 `./scripts/type.sh "/rename testx" "<mDNS serial>"` 一次成功（EditText='/rename testx' 含空格——serial 引号根修实证）。①命令面板**带参数仍在场且过滤到 rename**：输入完整 `/rename testx` 后 dump 仍显示面板行 `/rename 重命名此会话` [48,1351][1152,1400]（分词常驻新行为）。②tap 发送键 (1086,1616)：出现 **「重命名会话」对话框**（标题输入框+取消/重命名，19 节点）——非用户消息、非 LLM 轮；logcat 无 retry/权限行（窗口内仅另一 sid ses_f7c139d3cffe 的 L3 RestValidation Busy/Waiting 噪声，与本 scratch 无关）、FATAL 0；tap 取消后干净回到空态（testx 文本 0 残留）。打字路径客户端命令分流生效：/rename testx 不再喂模型。

## H. #370 V2 队列轮末刷新

### H1 轮末重拉
- 前置：Host-4199 连接；宿主可 curl。
- 操作：①curl 发长 prompt（如「count from 1 to 200 slowly」）制造 ≥30s 轮次 ②忙时再发一条（入队）③app 打开 QueueSheet（FAB → 排队队列）④等待轮终（服务器 turn 结束，app statusFlow 转 Idle）⑤dump 队列区。
- 期望：已派发队列项消失（面板自动重拉）。
- 判定：dump 无该项。（时序长可 TIMEBOX 标注）
- 实测记录：**✔（2026-09-10 步骤3 执行批，TIMEBOX 超限标注）** 前置：debug-entry.sh 切 Host-4199（server 6b1cd280 确认）；V2 空会话为 curl 创建（POST /api/session → ses_f7b59d9f3ffeqyWmJpSjIxsXRm，/home/leo-tkp；app 列表进入，SessionStateService 确认 Idle/0 msgs）。V2 prompt 体实测=`{"text":"..."}`（parts 包装 400 Missing key text）。过程时间线（全部设备/logcat/服务器三源）：
  - 首试塌时：`count from 1 to 300 slowly` 模型 8.6s 冲完（不遵守 slowly），后发消息变普通轮②（用户气泡+模型 ask_user_question 卡「待你回答」）——未见入队。tap「忽略」后模型续写，再停止键（bounds 漂移致两击点空，第三击 (1086,1616) 命中）→ `13:51:41.119 Busy/Waiting --ClientAbort--> Idle [force-complete]`。
  - 正式轮：`13:42:07` curl 长任务（1-300 逐数+英文拼写+短句）→ Busy/Streaming 持续；忙时 composer 输入 q2 → **发送键变体「发送（排队）」在场** [1056,1586][1116,1646]（入队入口）→ tap → 转录出现用户气泡 q2+**「排队中」徽标**；FAB（打开任务菜单）→ 菜单 TODO/智能体/Shell(1)/排队队列 → tap 排队队列 → **QueueSheet「排队队列 (1)」+ q2 条目 + 编辑排队消息/移除排队消息/引导至下一轮 三动作**（dump 27 节点，截图 /tmp/h1_queue_open.png，13:43:33）。
  - 等轮终：长任务服务器侧持续生成 >10min（`delivery:"steer"` 语义——后续 curl 均汇入同活动轮，服务器恒 Busy）；TIMEBOX 4min 到点未自然终。停止键强制：`13:51:41 ClientAbort→Idle` 后重开面板**仍 (1)+q2 未派发**（13:52:31，截图 /tmp/h1_queue_after.png）——中断轮终路径不派发（观测事实）。
  - 派发闭合：`13:52:59` curl 短任务（Reply done）→ 轮真终窗口 → **q2 于 13:53:06.989 送达服务器**（message list 出现 user 消息 text=q2，msg_084b085eb001…）→ 二次停止后 `13:55:55 L3 fallback refresh` → 重开 QueueSheet：**「排队队列 (0)」+「暂无排队消息」**（18 节点，截图 /tmp/h1_queue_final.png，13:56:46）——判定成立：已派发队列项消失、面板重拉为空态。
  - TIMEBOX 标注：轮起 13:42:07 → 队列空态观测 13:56:46 ≈ 14m39s（服务器长生成+steer 语义拖长；非 app 侧延迟）。附带观测：queuedsecond 误发轮的 ask_user_question 卡在场（待你回答/SINGLE/选项列表/输入答案/忽略/提交）——V2 问题卡交互面正常。

## 9. 人工清单（V6，类别+理由+勾销条件）

- #378 进行中 box 体感与展开/收起交互（①真手指连续手势——展开/收起 tap 虽可注入，但进行中态需活体 /compact 时窗内操作，命令注入通道受限；用户跑一次 /compact 顺带勾销）；
- #379 fling 体感终验（①注入 swipe 为批处理位移流，#245 证伪域）；
- #355 检索区视觉与归档不出现（①type.sh 输入在真机不稳定——F1/F2 若执行时输入成功即当场勾销）；
- #368 时长呈现主观确认（④观感）。