# #387 v2 批次(#410-#417)验收 checklist(修订版 r2)

- 卡片：backlog #410 #411 #412 #413 #414 #415 #416 #417 #387 | 日期：2026-09-18(r2 按纯净审查意见修订)
- 构建：随 #417 修复 commit 924023c2 devDebug（执行时以 adb dumpsys versionCode=1789744508 核对） | 设备：emulator-5554（Pixel6_Android36，1080x2400，zh-CN）
- 分类：仪器可验（截图判读由多模态执行代理读图；流式时序走 screenrecord+抽帧，属 §6 仪器证据；仅主观观感入人工清单）
- 回归域：chat-ui 卡片层（#410/#413/#415/#416）、DSH 数据面（#411）、测试基建（#412/#414/#417）、消息层 v2 全量+流式滚动稳定性+跳转高亮（#387 spec 回归重点）
- 环境前置：
  - V2 服务器：opencode2 端口 4199 已监听；`./scripts/debug-entry.sh emulator-5554 dev.leonardo.ocbeacon.dev`（reverse tcp:4199/4198 幂等）
  - DSH：127.0.0.1:3080 已监听，已配对（cookie 365d）；`./scripts/dsh-pair.sh emulator-5554` 兜底
  - 已装 devDebug（versionCode 1789744508）；一切设备操作走 adb -s emulator-5554
  - 证据目录：docs/acceptance/2026-09-18-387-v2-evidence/（截图/录屏拉回至此，文件名按 item 号）
  - 通用降级规则：标注「条件项」的 item 若 5 分钟内无法构造场景 → 记 N/A + 引用既列单测/既有证据路径后条件通过，不得卡死

## A. #410 尾部单点 AssistantTurnTail
### A1 V2 会话末条助手消息尾部信息簇完整在场（主路径）
- 前置：V2 面打开一个含 ≥2 轮回复的历史会话（任选既有会话）
- 操作：`adb shell uiautomator dump` 拉回解析末条助手消息区域
- 期望：尾部一行含 耗时（如 2.4s）、步数·工具摘要（N 步·M 个工具）、复制按钮、ⓘ 详情入口（contentDescription 消息详情/Message details）
- 判定：dump 文本命中上述四类元素
- 实测记录：
  - 2026-09-18 23:38 ✔。冷启 `./scripts/debug-entry.sh emulator-5554 dev.leonardo.ocbeacon.dev` EXIT=0（logcat 23:37:46 `Debug channel activated` + `NavGraph: Debug channel → SessionList`，server fa178d82=127.0.0.1:4199 V2）；版本核对 `dumpsys package` versionCode=1789744508 与 checklist 一致，versionName=0.3.0。会话列表 tap「DSHWeb Token问题咨询」（会话行 bounds [148,1371][551,1423]）。
  - dump 末条助手消息尾部信息簇（V2 面）：耗时 `11m 39s` bounds [416,1798][527,1828]；步数·工具摘要 `22 步 · 30 个工具` bounds [548,1795][760,1832]；复制按钮 content-desc=`复制` bounds [949,1796][964,1833]；ⓘ 详情入口 content-desc=`消息详情` bounds [964,1750][1080,1876]。四类元素全部命中，同行单点（Build 标签 [53,1796][122,1830] + 模型名 deepseek-flash [198,1798][395,1828] 同行）。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/a1-tail.png（23:38:54，读图确认尾部一行四元素在场）。dump 存 /tmp/ui_a1.xml。
  - 判定：✔（dump + 截图双证）。

### A1b 长回复（多 part）尾部簇同样渲染（分片/分段路径共用点）
- 前置：V2 面当前会话，输入并发送「列出当前目录的文件，然后用中文逐一说明每个文件的作用」（触发 工具调用+多段文本 → 多 part 长回复）
- 操作：等回复完成（尾部停止更新）；uiautomator dump 末条
- 期望：长回复末条尾部簇与 A1 同构（耗时/摘要/复制/ⓘ 全在），且同一消息尾部簇只出现一次（单点后无双份）
- 判定：dump 命中；同一 message 区域 ⓘ 计数 = 1；顺带记录该消息文本块/工具卡数量（不低于 2 佐证确已多 part）
- 实测记录：
  - 2026-09-18 23:49 ✔（有过程插曲，见下）。23:39 经 AdbIME 注入并发送「列出当前目录的文件,然后用中文逐一说明每个文件的作用」（用户气泡 bounds [176,1454][1006,1559]）；该轮 23:40:20 终态但 **outcome=failed**（V2 API `GET /api/session` 佐证：ses_f74f6626fffexKT0VMNw1uOEF3 outcome=failed updated=1789746020312），无助手回复渲染——首试因 provider 失败未构造出长回复。
  - 23:45 补发探针「PING」，约 3 分钟后终态；实际渲染出的新长回复 = 上轮失败任务的完整重答（模型此前已被切为 zhipuai-coding-plan / GLM-5.3-Flash，非 deepseek）。该长回复多段 Markdown 文本：视口内可见文本块 ≥6（运行时/包管理、Deepin/UOS 桌面、安全/系统、⚠️ 可疑项提醒等段落），0 个工具卡 → 多 part 佐证成立（文本块 ≥2）。
  - 末条尾部簇（/tmp/ui_ping2.xml，与 A1 同构四类全在）：耗时 `1m 25s` bounds [393,1798][488,1828]；步数·工具摘要 `1 步 · 0 个工具` bounds [509,1795][689,1832]；复制 desc=`复制` bounds [949,1796][964,1833]；ⓘ desc=`消息详情` bounds [964,1750][1080,1876]；同一消息区域 ⓘ 计数 = 1（grep 计数=1；上方旧消息尾部簇在视口外）。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/a1b-tail.png（23:49:58，读图确认尾部一行四元素+多段正文）。单点后无双份：dump 全树无第二个相邻尾部簇。
  - 判定：✔（尾部簇同构 + ⓘ 单点 + 多 part 佐证均满足；首试 provider 失败为环境因素，已用同会话成功轮覆盖主路径）。

### A2 ⓘ「消息详情」居中弹窗内容（V2 面）
- 前置：A1 会话
- 操作：tap 末条 ⓘ；uiautomator dump
- 期望：弹窗含标题（消息详情）+ 详情行（时间/provider·模型/耗时/步数/工具数，空值整行不渲染）+ 动作区（复制 Markdown 源码、从此轮分支、删除消息——V2 有 deleteMessage 端点正向在场；无点赞点踩——DSH 专属）
- 判定：dump 命中「删除消息」与「复制 Markdown 源码」动作、无 点赞/点踩 文本；弹窗 bounds 中心 x 距屏幕中心 x(540px) 偏差 ≤ 120px（居中）
- 实测记录：
  - 2026-09-18 23:50 ✔。tap A1b 末条 ⓘ（中心 1022,1813）→ 弹窗 dump（/tmp/ui_a2.xml）。
  - 标题 `消息详情` [183,630][435,722]；详情行全在：时间=23:47:14、智能体=build、模型=zhipuai-coding-plan · glm-5.3-flash、时长=1m 25s、步数=1、工具=0、令牌=3.8k、成本=$0.0000（本条无空值行，空值整行不渲染逻辑未触发观察）。
  - 动作区：`复制 Markdown 源文` [262,1329][603,1381]（注：app 实际文案为「源文」非 checklist 所写「源码」，同动作语义命中）、`从此轮分支` [262,1445][446,1497]（末轮在场）、`删除消息` [262,1561][409,1613]（V2 deleteMessage 正向在场）；关闭按钮 [784,1745][858,1798]。全 dump 无「点赞」「点踩」文本 ✔（DSH 专属评分未泄入 V2 面）。
  - 居中：弹窗容器 FrameLayout bounds [120,567][960,1897]，中心 x=(120+960)/2=540，与屏幕中心 540 偏差 0px ≤ 120px ✔。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/a2-dialog.png（23:50:58，读图确认标题/详情行/三动作/红色删除态/居中形态）。
  - 判定：✔。

### A3 末轮 vs 历史轮：分支入口差异 + 历史轮尾部点击展开（US#34/#14）
- 前置：A1 会话（≥2 轮）
- 操作：①滚到上一轮助手消息，打开其 ⓘ 弹窗 dump；②关闭弹窗，tap 历史轮尾部摘要行 dump
- 期望：①历史轮弹窗无「从此轮分支」；②点击后该轮尾部展开完整信息簇（或产出行，若该轮有产出）
- 判定：①两 dump 对比分支动作仅末轮在；②第二次 dump 该轮信息元素多于第一次（展开生效）
- 实测记录：
  - 2026-09-18 23:53 ✘（两项期望均不成立）。会话内消息序列：…11m 39s（旧轮）→ 50.4s（PING 轮第一响应，23:46:21，1 步·1 工具）→ 1m 25s（末条 23:47:14）。
  - ① 历史轮弹窗：tap 50.4s 轮 ⓘ（中心 1022,1159）→ 弹窗 dump（/tmp/ui_a3_hist.xml）：标题「消息详情」+详情行（时间 23:46:21/智能体 build/模型 zhipuai-coding-plan · glm-5.3-flash/时长 50.4s/步数 1/工具 1/令牌 5.4k/成本 $0.0000）+ **`从此轮分支` [262,1387][446,1439] 在场** + `删除消息` [262,1503][409,1555] 在场。与末轮弹窗（A2 记录，同动作在）对比：**分支动作末轮/历史轮均在场，无差异** → 期望①「历史轮无从此轮分支」不成立。截图：docs/acceptance/2026-09-18-387-v2-evidence/a3-hist-dialog.png（23:53:00，读图确认）。
  - ② 历史轮尾部点击展开：关闭弹窗后该 50.4s 历史轮尾部簇（Build/glm-5.3-flash/`50.4s`[393,1144][465,1174]/`1 步 · 1 个工具`[486,1141][666,1178]/复制/消息详情）已完整在场（与末轮同构，无收起摘要态）。tap 摘要行（570,1160）与耗时文本（429,1159）各一次，前后 dump（/tmp/ui_a3_exp.xml vs /tmp/ui_a3_exp2.xml）唯一差异为 composer 随机占位词（「为…添加测试」→「提问…」），该轮信息元素计数不变（未增多）→ 「展开生效」判据不成立。
  - 判定：✘（①分支入口无末轮/历史轮差异；②历史轮尾部簇恒完整在场，点击展开交互不存在或无状态变化）。

## B. #411 DSH 逐轮 TTFT / 解码速度
### B1 逐轮展开行 timing 两行（双分支确定性判定）
- 前置：DSH 面；打开或新建会话，发送「你好，请介绍一下你自己」（若 provider 余额不足该轮即错误轮——正是分支 B）
- 操作：等轮次终态；打开顶部统计弹窗 → 逐轮列表 → 展开最新一轮；uiautomator dump
- 期望（分支判定，二者必居其一）：
  - 分支 A（回复成功）：展开体含 TTFT 行匹配 `\d+ ms` 与解码速度行匹配 `\d+(\.\d+)? tokens/s`（或 tokens·s）
  - 分支 B（错误轮，如 Insufficient Balance）：展开体**无** TTFT 行与解码速度行（整项隐藏，宁缺勿谎）
- 判定：dump 按14正则匹配分支 A 两行，或确认分支 B 两行均不存在；错误/成功由会话消息内容佐证（错误文本在场/正常回复在场）
- 兜底：若两分支都无法确认 → 引用单测 DshStepTimingTest（4 例）+ ContextDetailDelegateTest timing 用例条件通过
- 实测记录：
  - 2026-09-19 00:05-00:10 条件通过（逐轮展开体两分支均被 **app 崩溃** 阻断，崩溃缺陷实录在案）。流程：`./scripts/dsh-pair.sh emulator-5554` 切 DSH 面（23:56:16 token 交换 ok）。
  - 场景①（分支 B 路径）：「用一句话介绍你自己」会话发送「你好,请介绍一下你自己」（23:57 发出）→ 终态为 **Insufficient Balance 错误轮**（文本 [64,1669][358,1820] 在场）；该会话累计 0 条助手消息（统计 sheet「消息 9（用户 9 · 助手 0）」）。统计 sheet（顶栏令牌圆环 tap）dump（/tmp/ui_b1_stats.xml）：会话统计行「轮次 2 · 2 步 · 0ms LLM · 0ms 个工具」，**无 TTFT 行、无解码速度行**；逐轮明细区整区不渲染（轮内无 assistant 消息合法跳过）→ 分支 B 判据「两行均不存在 + 错误文本在场佐证」成立。
  - 场景②（分支 A 路径）：「我怎么在opencode的模型列表中」会话（DSH，3 成功轮，末条尾部簇 2m 15s / 7 步 · 3 个工具在场）。统计 sheet 首次打开成功（00:05，/tmp/ui_b1_sheet2.xml）：会话级「平均 TTFT：1.7s」「83.2 令牌/秒」在场，逐轮「各轮次详情」标题在 y=2270（列表项在视口外未及组合）。
  - **崩溃缺陷**：00:08:15 重新打开该统计 sheet 时 app FATAL（logcat -b crash）：`java.lang.IllegalArgumentException: Key "turn-SERVER-1" was already used. If you are using LazyColumn/Row please make sure you provide a unique key for each item.`（ContextDetailDialog 逐轮 LazyColumn key 冲突，serverTurn 重复时必现路径）→ 逐轮展开体两分支均无法现场完成 dump 判定。系统弹「”OC Beacon Dev“屡次停止运行」。
  - 判定：逐轮部分按 checklist 兜底条款条件通过（引用单测 DshStepTimingTest 4 例 + ContextDetailDelegateTest timing 用例）；**崩溃缺陷登记**：DSH 统计 sheet 逐轮列表 serverTurn 重复 → key 冲突崩溃（仪器可验，✘ 级缺陷，随执行汇总上报）。

### B2 会话级 TTFT 均值 / 解码速度在场（sessionStats 区，DSH）
- 前置：同 B1（至少一轮已终态）
- 操作：统计弹窗会话级区 uiautomator dump
- 期望：TTFT 均值与解码速度两行在场（有数据时）；无数据时整行不渲染
- 判定：dump 命中两行或确认双缺（与 B1 分支一致）
- 实测记录：
  - 2026-09-19 00:05 ✔（双分支双会话证据）。
  - 有数据形态：「我怎么在opencode的模型列表中」（DSH，3 成功轮）统计 sheet 会话级区 dump（/tmp/ui_b1_sheet2.xml）：「轮次 3 · 34 步 · 7m 44s LLM · 2m 20s 个工具」[42,2092][687,2137] 下方 **「平均 TTFT：1.7s」[42,2148][288,2193] 与「83.2 令牌/秒」[42,2204][225,2249] 两行均在场**（有数据时渲染 ✔）。截屏证据：docs/acceptance/2026-09-18-387-v2-evidence/b1-stats-sheet.png 为错误轮会话 sheet（无 TTFT 行形态）。
  - 无数据形态：「用一句话介绍你自己」（全错误轮，0ms LLM）sheet dump（/tmp/ui_b1_stats.xml）：会话统计区仅有轮次摘要行，**TTFT 均值行与解码速度行均不渲染**（无数据整行不渲染 ✔）。
  - 判定：✔（两形态均符合「有数据两行在场 / 无数据双缺」）。

### B3 DSH 面详情弹窗评分正向（US#12）
- 前置：DSH 面任一含助手回复的既有会话
- 操作：tap 末条 ⓘ 打开详情弹窗；uiautomator dump
- 期望：动作区含 点赞/点踩（DSH 专属正向）
- 判定：dump 命中评分动作文本
- 实测记录：
  - 2026-09-19 00:10 ✔。DSH 面「我怎么在opencode的模型列表中」会话，tap 末条 ⓘ（中心 1022,1820）→ 详情弹窗 dump（/tmp/ui_b3_dialog.xml）：标题「消息详情」+详情行（时间 2026-09-17 17:59:29/模型 zai-coding-cn · glm-5.3/步数 7/工具 3）+动作区 **`评价为有帮助`[262,1207][483,1259]（点赞）+`评价为没帮助`[262,1323][483,1375]（点踩）**（DSH 专属评分正向在场）+`复制 Markdown 源文`[262,1439][603,1491]；关闭按钮 [784,1623][858,1676]。旁证：DSH 面弹窗无「从此轮分支」「删除消息」（与 V2 面动作集差异符合预期）。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/b3-rating-dialog.png（00:10:31）。
  - 判定：✔。

## C. #413 FAB 与末条消息尾部动作分离
### C1 贴底末条 ⓘ 不被任务 FAB 遮挡
- 前置：任一服务器；消息多到列表可滚至底（用 A1b 会话即可）
- 操作：滚动到底；uiautomator dump 取任务 FAB（contentDescription「Open tasks menu」或本地化文案；zh-CN 锚空时按右下角约 (954,1936)-(1080,2062) 一带浮动圆钮定位）与末条 ⓘ 的 bounds
- 期望：FAB.top ≥ 末条尾部动作 bottom（垂直分离）
- 判定：bounds 数值比较成立（记录两值）
- 实测记录：
  - 2026-09-19 00:11 ✔。V2 面 A1b 会话（DSHWeb Token问题咨询）打开即贴底（最新轮末条在场）。dump（/tmp/ui_c1_chat.xml）：任务 FAB content-desc=`打开任务菜单` bounds [986,1968][1049,2031]（FAB.top=**1968**）；末条尾部动作 ⓘ content-desc=`消息详情` bounds [964,1750][1080,1876]（ⓘ bottom=**1876**，尾部簇动作区最下缘）。
  - 比较：FAB.top(1968) ≥ 尾部动作 bottom(1876)，差值 92px，垂直分离成立，无遮挡。zh-CN 下 FAB content-desc 为中文锚「打开任务菜单」直接命中（未落到坐标兜底）。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/c1-fab-tail.png（00:11:38，读图确认 FAB 圆钮位于尾部簇下方独立悬浮）。
  - 判定：✔。

## D. #415/#416 卡片形态与图标
### D1 DSH 注入卡淡形态 + DataObject 图标（构造路径：新会话首条消息）
- 前置：DSH 面**新建会话**（会话列表新建/发首条消息）——DSH 会话启动会注入「插件配置」类上下文（既有证据 docs/acceptance/2026-09-17-387-v2/25-dsh-injection-card-live.png 即此路径）
- 操作：发送任意首条消息；注入卡出现后截屏 /sdcard/d1.png 拉回 evidence/d1-injection.png；uiautomator dump
- 期望：注入卡 = 左侧竖色条 + 弱底色、**无描边**；图标为 {} 形（DataObject）；折叠单行（标签+时间+chevron）
- 判定：截图读图（色条在场/描边缺席/{} 图标形态）+ dump 标签文本（上下文注入/插件配置类标签在场）
- 条件项：若新会话未出现注入卡 → N/A + 引用 25-dsh-injection-card-live.png 与 InjectionLabelKind rowmodel 单测
- 实测记录：
  - 2026-09-19 00:14-00:20 ✘（标签在场、形态不符）。DSH 面新建会话（tap 顶栏「新建会话」→ 选项目 workspace → 弹 Agent 预设选择器 → 选「标准模式」）→ 发送首条消息「报告当前时间」→ 轮次终态（Insufficient Balance 错误，不影响注入卡观察）。
  - **注入卡在场**：dump 标签文本 `插件配置` 命中（DSHWeb「用一句话介绍你自己」会话同款 4 张：[98,1320][207,1357] 等，/tmp/ui_d1_chat5.xml）。
  - **形态不符**：放大读图（/tmp/d1_card_zoom.png，3x 裁剪）与截图 docs/acceptance/2026-09-18-387-v2-evidence/d1-injection-folded.png（00:16）：卡容器 = **灰色描边圆角完整卡**（1dp 描边在场 ✘ 期望无描边）、**无左侧竖色条** ✘、**无弱底色**（卡底=页面底色）✘、图标 = **ⓘ（Info 圈）** ✘ 期望 {}（DataObject）；折叠单行结构（标签+时间+chevron）✔ 在场。新会话「报告当前时间」内另一注入语料渲染为 assistant 消息气泡（淡紫底，非注入卡）：docs/acceptance/2026-09-18-387-v2-evidence/d1-injection.png（00:14:59）。
  - 与 9-17 基线一致：25-dsh-injection-card-live.png 中「插件配置」卡同为描边+ⓘ 形态（非回归，#415 淡形态未落地于该渲染路径）。工作区源码观测（仅定位）：InjectionCard.kt 存在 #415/#416 淡形态实现（DataObject 13dp/色条 2.5dp/surfaceContainer 弱底/无 border），但 ChatMessageList.kt 中 0 调用点，L1694/L1761「插件配置」标签实传 **EventCard**（透明底+1dp 描边）。
  - 判定：✘（标签在场 ✔，但色条/无描边/弱底/DataObject 四项形态期望均不成立）。

### D2 V2 通知卡双分支（铃铛 + 描边完整卡）
- 前置：V2 面找一个含 role=system 消息的既有会话（会话列表逐个打开，dump 搜「工具目录已变更」或「通知」标签；工具目录变更常出现在较早会话）
- 操作：截屏 evidence/d2-notify.png；uiautomator dump
- 期望：通知卡 1dp 描边（完整卡语言）；图标铃铛；分支 1 标签 =「工具目录已变更」；分支 2 标签 =「通知」且折叠态带描述行（原文首行 ≤200 字符）
- 判定：截图读图（描边在/铃铛形态）；dump 命中确切标签文本之一（两个都会更好）
- 条件项：两类都找不到 → N/A + 引用 systemNoticeKindFor/injectionLabelKindFor rowmodel 单测
- 实测记录：
  - 2026-09-19 00:22 ✘（部分命中：描边/标签 ✔，铃铛 ✘；分支 2 未遇）。V2 面「DSHWeb Token问题咨询」会话，上滑 5 屏后在消息流找到 role=system 通知卡。
  - dump 命中分支 1 标签 `工具目录已变更` [98,648][289,685] + 时间 `2026-09-18 23:40:20` [403,652][676,682] + chevron（/tmp/ui_d2_s5.xml）。
  - 截图 docs/acceptance/2026-09-18-387-v2-evidence/d2-notify.png（00:22:18）读图：**1dp 灰描边完整卡 ✔**（描边圆角矩形清晰）；3x 放大（/tmp/d2_card_zoom.png）核实图标为 **ⓘ（Info 圈）而非铃铛** ✘。折叠态带描述行未观察（分支 1 卡头单行，正文展开未见）。
  - 分支 2（「通知」标签）：本会话及翻阅路径未出现，未强求（判定允许命中其一）。
  - 2026-09-19 00:33 补充：**分支 2「通知」卡在 E2 同屏取证中命中**（e2-flat.png + /tmp/ui_e2.xml）：`通知` 标签 [98,689][153,726] + 时间 `00:29:27` + **折叠态带描述行** `Today's date is now: Sat Sep 19 2026` [74,752][1006,786]（原文首行 ≤200 字符 ✔），描边完整卡形态同分支 1。两标签文本均已 dump 命中；铃铛图标期望两分支均不成立（实测皆 ⓘ）。
  - 判定：✘（描边与标签双分支命中，铃铛图标期望不成立——实测 Info 图标家族）。

### D2b V2 <system-reminder> 嗅探注入卡形态（条件项）
- 前置：V2 会话消息流中存在 <system-reminder> 嗅探注入（日期注入等）
- 操作：dump 搜注入卡；截屏 evidence/d2b.png
- 期望：走 InjectionCard 淡形态（同 D1：色条+弱底+无描边+DataObject）
- 判定：截图+dump（在场则判形态）
- 条件项：不在线上会话出现 → N/A + 引用 mapper 嗅探单测
- 实测记录：
  - 2026-09-19 00:25 **N-A（条件项条件通过）**。V2 面线上会话（含翻阅的「DSHWeb Token问题咨询」消息流）未出现 <system-reminder> 嗅探注入卡；Room 直查佐证：`cached_messages` payload 含 "system-reminder" 计数 = **0**（run-as sqlite3，ocbeacon.db；/tmp/q.sql）。
  - 引用单测：MessageRowModelTest.kt L351 `dsh system sentinel and v2 sniffed context fall back to context injection`（injectionLabelKindFor 嗅探 seam，V2 嗅探值 context 回落「上下文注入」路径已由单测覆盖）。
  - 判定：N-A 条件通过。

### D3 DSH 注入卡展开/收起（交互）
- 前置：D1 的注入卡在场
- 操作：tap 注入卡本体 → dump；再 tap → dump
- 期望：展开后正文文本可见（dump 命中正文多行）；收起后正文消失、折叠单行
- 判定：两次 dump 正文文本存在性翻转
- 实测记录：
  - 2026-09-19 00:26-00:27 ✔。DSH 面「用一句话介绍你自己」会话（4 张折叠「插件配置」卡）。注意首 tap（540,928 打在视口上部卡）被解释为滚动而非展开（dump 变化仅为视口位移，卡未展开）；对可见卡（y=1320 → tap (540,1340)）重试后展开生效。
  - 翻转证据：tap 展开 → dump（/tmp/ui_d3_t2.xml）出现正文 `[model changed: assistant turns above this poi…` [74,1236][1006,1357]（原本无此节点）；再 tap 同卡收起 → dump（/tmp/ui_d3_t3.xml）正文消失、仅剩折叠单行 `插件配置` [98,1320][207,1357] + 时间 + chevron。正文存在性翻转 ✔。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/d3-toggle.png（00:27:30，收起态）。
  - 判定：✔（交互本身工作；附注：卡片头部热区偏小/首 tap 易被滚动吞掉，交互判定不受影响）。

### D4 思考卡边框双态（非流式截图 + 流式录屏抽帧）
- 前置：V2/DSH 任一面含思考卡会话（消息含「已思考/思考中」卡）
- 操作：①非流式态：截屏 evidence/d4-reasoning-static.png；②流式态：发送新消息触发思考，`adb shell screenrecord --time-limit 25 /sdcard/d4-stream.mp4` 录制全程，拉回 evidence/ 后用 ffmpeg 每 2s 抽帧（evidence/d4-frames/）取 2-3 帧读图
- 期望：思考卡 1dp 标准描边 + smallMedium 圆角（非流式与流式抽帧一致）
- 判定：静态截图 + 流式抽帧读图均命中描边/圆角
- 条件项：若流式期无法取得思考卡 → 流式态 N/A + 静态帧判定 + 引用 ReasoningBlock 样式代码（#13 修订）
- 实测记录：
  - 2026-09-19 00:28-00:31 ✔（双态均命中）。V2 面「DSHWeb Token问题咨询」会话。
  - ①非流式态：上滑至历史轮「思考完毕 · 50.3s」「思考完毕 · 1m 25s」卡，截屏 docs/acceptance/2026-09-18-387-v2-evidence/d4-reasoning-static.png（00:28:50）；3x 放大（/tmp/d4_static_zoom.png）：**1dp 灰描边 + smallMedium 圆角 + 淡灰底 + 圆点前缀**，命中。
  - ②流式态：发送「写一首60行的长诗」，`screenrecord --time-limit 25` 全程录制（d4-stream.mp4，1.4MB），ffmpeg 每 2s 抽帧 12 张（evidence/d4-frames/）。frame_02（4s）：用户气泡已发出、停止键（红）在场；**frame_06（12s）：流式思考卡 `思考中… 2.7s` 在场**，同帧还见「正在流式输出」徽标 + 尾部簇（1.7s / 1 步 · 0 个工具）。3x 放大（/tmp/d4_stream_zoom.png）：描边+圆角与静态态一致 ✔。
  - 判定：✔（非流式与流式抽帧描边/圆角均命中，双态同构）。

## E. #387 v2 消息层形态（V2 面）
### E1 用户气泡量化 + 用户尾部三动作
- 前置：V2 会话含用户消息（A1b 会话）
- 操作：uiautomator dump 取用户气泡 bounds 与其尾部动作区
- 期望：宽 ≤ 972px（0.9×1080）；右缘边距 < 左缘边距（右对齐）；无「用户」「智能体」头部标签文本；尾部含 撤销、复制、详情（ⓘ）三动作（V2 有 revert 域 → 撤销在场）
- 判定：bounds 数值（宽/双边距）+ dump 命中三动作；「用户」/「智能体」作为独立标签节点不存在
- 实测记录：
  - 2026-09-19 00:32 ✔。V2 面「DSHWeb Token问题咨询」会话，用户气泡「写一首60行的长诗」（/tmp/ui_e1_s1.xml）。
  - 气泡容器（clickable View）bounds **[134,1059][1048,1274]**：宽 = 914px ≤ 972px（0.9×1080）✔；右缘边距 = 1080−1048 = **32px** < 左缘边距 = **134px** ✔（右对齐）。文本节点 [176,1096][475,1148]（w=299）。
  - 尾部三动作全在：`还原` [804,1154][862,1280]（V2 revert 域正向在场）、`复制` [862,1154][988,1280]、`消息详情`(ⓘ) [922,1153][1048,1279] ✔。
  - 头部标签：dump 全树无独立 `用户` / `智能体` 标签文本节点（grep 0 命中）✔。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/e1-user-bubble.png（00:32:17）。
  - 判定：✔。

### E2 助手正文扁平（容器语言对比判定）
- 前置：同 E1（同屏用户气泡与助手正文对比）
- 操作：截屏 evidence/e2-flat.png（同屏含用户气泡+助手正文）；uiautomator dump
- 期望：助手正文区无气泡容器（无底色块/无描边/无圆角——与用户气泡形成形态对比）；无头部标签/时间戳；尾部统计栏在场（在场性由 A1 判定，此处不重复）
- 判定：截图读图（助手正文区背景与页面底色一致、无描边圆角）+ dump 无头部文本
- 实测记录：
  - 2026-09-19 00:32 ✔。同屏截图 docs/acceptance/2026-09-18-387-v2-evidence/e2-flat.png（00:32:57）：同屏含用户气泡（淡紫底右对齐）+「通知」卡+思考卡+助手正文（《长夜书》诗歌）。
  - 助手正文区读图：正文直接排版于页面底色上，**无底色块、无描边、无圆角容器**（与上方用户气泡淡紫底形成鲜明形态对比）；无头部标签、无时间戳（dump 正文文本节点全宽 [32,..][1048,..] 直接挂列表，无气泡容器节点包裹）✔。尾部统计栏在场性由 A1 判定，不重复。
  - 判定：✔。

### E3 统计底部面板逐轮倒序 + 编号
- 前置：V2 多轮会话（A1b）
- 操作：打开顶部统计入口（Context）；uiautomator dump 逐轮列表
- 期望：最新轮在最上；「第 N 轮」编号自上而下递减（客户端统计编号连续）
- 判定：dump 顺序断言（首行编号 ≥ 次行编号）
- 实测记录：
  - 2026-09-19 00:35 ✔。V2 面「DSHWeb Token问题咨询」（多轮），顶栏令牌圆环 tap 打开「上下文使用详情」sheet（V2 面正常打开，**未复现 DSH 面的 key 崩溃**——V2 逐轮无 serverTurn，走客户端序号），滚动至逐轮列表 dump（/tmp/ui_e3_3.xml）。
  - 逐轮行顺序（自上而下）：`轮次 7 · 1m 18s · 128.7k` → `轮次 6` → `轮次 5` → `轮次 4` → `轮次 3` → `轮次 2` → `轮次 1`，每行右侧带「展开轮次详情」按钮 [991,1483][1038,1530] 等。
  - 顺序断言：首行编号 7 ≥ 次行 6，7→1 严格递减，最新轮在最上 ✔。
  - 截图：docs/acceptance/2026-09-18-387-v2-evidence/e3-perturn-order.png（00:35:02）。
  - 判定：✔。

## F. 测试基建门禁（#412/#414/#417）
### F1 单元测试全量
- 操作：`./gradlew :app:testDevDebugUnitTest --rerun`
- 期望：0 failure
- 判定：gradle 输出（BUILD SUCCESSFUL）
- 实测记录：
  - 2026-09-19 00:37 ✔。`BUILD SUCCESSFUL in 2m 9s`，EXIT=0。结果 XML 统计（app/build/test-results/testDevDebugUnitTest/）：**tests=3352 failures=0 errors=0 skipped=0**。
  - 判定：✔（0 failure）。

### F2 androidTest 全量（#412/#417 核心）
- 操作：`./gradlew :app:connectedDevDebugAndroidTest`
- 期望：0 failure（记录实际用例总数，应不低于 113）
- 判定：gradle 输出 / 结果 XML failed=0
- 实测记录：
  - 2026-09-19 00:41 ✔。`BUILD SUCCESSFUL in 3m 48s`，EXIT=0。结果 XML（connected 设备 emulator-5554）：**tests=113 failures=0 errors=0 skipped=1**（总数 113 达门槛「不低于 113」；1 例 skipped 为条件跳过类，非失败）。
  - 判定：✔（0 failure，总数达 113）。

### F3 lint + assemble + i18n
- 操作：`./gradlew :app:lintDevDebug :app:assembleDevDebug`；`./scripts/i18n-check.sh`
- 期望：lint 0 error；assemble SUCCESS；i18n PASSED
- 判定：命令输出
- 实测记录：
  - 2026-09-19 00:47 ✔。`./gradlew :app:lintDevDebug :app:assembleDevDebug` → `BUILD SUCCESSFUL in 5m 7s`，EXIT=0（lint abortOnError=true 下 BUILD SUCCESSFUL 即 0 error；assemble 产物正常生成）。
  - `./scripts/i18n-check.sh` → `i18n check PASSED: 915 keys x 14 languages, all consistent.`，EXIT=0。
  - 判定：✔（三项全过）。

### F4 T11 稳定性（#414）
- 操作：`./gradlew :app:testDevDebugUnitTest --rerun --tests '*RenderSupplyCoordinatorTest*'` 连续 2 次
- 期望：2/2 全绿
- 判定：gradle 输出
- 实测记录：
  - 2026-09-19 00:52 ✔。第 1 次：`BUILD SUCCESSFUL in 52s`，XML tests=17 failures=0 errors=0；第 2 次：`BUILD SUCCESSFUL in 46s`，EXIT=0。
  - 判定：✔（2/2 全绿，17 用例两次零失败）。
  - **F2 副作用记录**：connectedDevDebugAndroidTest 结束时卸载了主包（debug-entry 报 Activity does not exist）→ 已用 F3 刚构建的 `app-dev-debug.apk`（同工作区代码，00:43 产物）`adb install -r` 重装，versionCode 由 1789744508 变为 **1789749746**（dev flavor 时间戳自动递增，代码同源；versionName 0.3.0 不变）。此版本号变化自 F2 完成后生效，G/H 系及后续项在新包上执行。

## G. 流式回归（#387 spec 回归重点）
### G1 流式滚动稳定性（录屏抽帧）
- 前置：V2 面 A1b 类会话
- 操作：发送长回复请求（如「写一首 60 行的长诗」）；`adb shell screenrecord --time-limit 40 /sdcard/g1.mp4` 全程录制；拉回 evidence/g1-stream.mp4；每 3s 抽帧（evidence/g1-frames/）
- 期望：流式输出期间视口稳定跟随（无整屏跳底往返、无闪烁黑块、无布局塌跳）；文本增量平滑追加
- 判定：抽帧序列读图——相邻帧视口内容连续（顶部锚点漂移 ≤ 1 屏高度的 1/3）；无肉眼闪烁带
- 主观流畅感（自然度）不入判定 → 人工清单
- 实测记录：
  - 2026-09-19 00:53-00:56 ✔。V2 面「DSHWeb Token问题咨询」，发送「写一首60行的长诗」，`screenrecord --time-limit 40` 全程录制（g1-stream.mp4，1.18MB），ffmpeg 每 3s 抽帧 13 张（evidence/g1-frames/）。
  - 帧采样读图：g_03（9s）/g_06（18s）/g_10（30s）三帧视口内容**完全一致**（视口贴用户气泡处等待回复，停止键红色在场），顶部锚点漂移 = 0 ≤ 1/3 屏高；无整屏跳底往返、无闪烁黑块、无布局塌跳。终态后（00:56）dump 视口合理贴住末条消息（用户气泡 y=1507 + 末条尾部簇在场）。
  - 附注：本段录制内 GLM 回复可见增量少（慢启动），视口稳定性以「零漂移+无闪烁」判定；终态贴底正常。
  - 判定：✔（主观流畅感→人工清单）。

### G2 跳转高亮（快速导航跳转后）
- 前置：V2 长会话（消息足够多）
- 操作：打开快速导航 sheet（顶栏入口）→ 选一条历史消息跳转；截屏 evidence/g2-jump.png + dump
- 期望：跳转目标消息高亮（背景强调）
- 判定：截图读图目标行高亮在场
- 实测记录：
  - 2026-09-19 00:56 ✔。V2 面「DSHWeb Token问题咨询」（长会话），顶栏「快速定位」[891,2131][933,2173] tap 打开导航 sheet（问题列表 Q2-Q6…），tap Q3 历史消息（「不要轮换token…」，中心 500,1047）跳转。
  - 跳转后 dump（/tmp/ui_g2_jump.xml）视口定位到目标消息区域（其后续思考卡 2.7s / Shell 工具卡相继可见）；截图 docs/acceptance/2026-09-18-387-v2-evidence/g2-jump.png（00:56:05）读图：**跳转目标消息（用户气泡）外层灰色强调高亮背景块在场**（y≈290-650 灰色矩形包裹目标气泡，与普通气泡仅淡紫底形成对比）。
  - 判定：✔（高亮在场）。

### G3 事件身份键去重（弱断言）
- 前置：V2 含工具调用的会话（A1b）
- 操作：dump 统计同 callId/同命令的工具卡文本出现次数
- 期望：同源工具事件不重复成卡（相邻重复文本块计数 ≤ 1 处成卡形态）
- 判定：dump 相邻重复块不呈两张独立卡（弱断言；深层由 dedupeByEventIdentity 单测覆盖——引用条件）
- 实测记录：
  - 2026-09-19 00:58 ✔。V2 面「DSHWeb Token问题咨询」（A1b 会话，PING 轮含多工具调用），上滑至工具卡区 dump（/tmp/ui_g3_s6.xml）。
  - 视口内工具卡清单：①`$ ls -la /home/leo-tkp`（完成 · 总计 488）②（完成 · === 沙漠与星空.md 头部 ===）③（完成 · === .sys1og.conf（名字可疑，看下内容）===）——三卡文本各不相同；另 g2-jump.png 同会话其他段：`readlink -f`、`grep -rl "launch-token…"`、两张 `P=/home/linuxbrew/…` 卡（输出摘要分别为 === 文件列表 === 与 === browser-auth.d.ts ===，属不同 callId 的两次调用非重复）。
  - 相邻重复块不呈两张独立卡（无同文本卡相邻出现）✔ 弱断言成立。
  - 深层引用：MessageRowModelTest.kt L245 `dedupe by event identity keeps first position and newest value` / L253 `dedupe by event identity keeps null key items untouched`（dedupeByEventIdentity 单测覆盖）。
  - 判定：✔。

## H. 状态徽标（US#5/#6/#37）
### H1 流式中徽标在场 + 完成态徽标隐
- 前置：V2 面；网络正常
- 操作：发送长回复请求（「写一首 60 行的长诗」，与 G1 同款——避免短回复在 dump 前终态造成假失败）；流式期间（5s 内）dump；回复完成后再次 dump
- 期望：流式 dump 命中流式态徽标（如「生成中/Thinking」类文本或计时器在走）；完成后 dump 无该徽标（完成态零噪音）
- 判定：两次 dump 徽标存在性翻转
- 实测记录：
  - 2026-09-19 01:02 ✔（徽标在场性以录屏抽帧仪器证据为准）。两次发送（「写一首60行的长诗」「再写一首四行的短诗」）+ 密集轮询（3s 间隔 ×11 次 uiautomator dump）：**dump 全程未命中「正在流式输出」文本**——该徽标属 a11y 树失明区（probing.md §五已知失明；Compose 自绘节点）。
  - **流式态徽标在场（仪器证据）**：D4 流式录屏抽帧 d4-frames/frame_06.png（12s 处）明确可见徽标行 `正在流式输出`（药丸形强调底）+ `Build + glm-5.3-flash + 1.7s + 1 步 · 0 个工具 + ⓘ`，同帧另有思考计时器卡 `思考中… 2.7s`（计时器在走）——流式态徽标在场 ✔（§6 仪器证据，checklist 头部允许流式时序走 screenrecord+抽帧）。
  - **完成态徽标隐**：终态 dump（/tmp/ui_h1_q11.xml）无「正在流式输出」文本，末条尾部簇恢复正常形态（耗时/步数·工具/复制/ⓘ），截图 docs/acceptance/2026-09-18-387-v2-evidence/h1-done.png（01:02:25）读图确认完成态零噪音 ✔。
  - 判定：✔（流式徽标在场由录屏帧证明 + 完成态徽标隐由 dump/截图证明，存在性翻转成立）。

### H2 中断标记
- 前置：V2 面
- 操作：发送长回复请求；流式中 tap 停止按钮（chat-stop，contentDescription 停止/Stop）；dump
- 期望：被中断消息尾部出现「已中断」类标记
- 判定：dump 命中中断标记文本；出错态徽标由 statusBadgeFor 纯函数单测覆盖（引用），不现场构造
- 实测记录：
  - 2026-09-19 01:02-01:06 ✘（两次尝试均未出现中断标记；停止操作本身生效）。V2 面同会话两次发送长回复请求（「写一首80行的长诗」「写一首90行的长诗」），流式中（停止键 desc=`停止` bounds [954,2123][1007,2176] 在场确认）tap 停止键。
  - 两次 tap 后 dump（/tmp/ui_h2_post.xml、/tmp/ui_h2_post2.xml）与截图 h2-interrupt.png（01:04:08）、h2-interrupt2.png（01:05:47）：发送键回归（流式确已终止）✔，但列表末条仅为用户气泡，**其下无任何 assistant 消息残留，也无「已中断」类标记文本**（grep 中断/已停/interrupt 均 0 命中）。
  - 现场限制：GLM-5.3-Flash 该场景首 token 延迟 >45s，tap 停止时 assistant 消息尚未创建，中断后直接无消息残留 → 「被中断消息尾部标记」无从在现 UI 流程中构造（未在 5 分钟内构造出「已开始输出的消息被中断」场景）。
  - 引用单测（statusBadgeFor 纯函数，MessageRowModelTest.kt）：L87 `streaming interrupted and error badges are derived`、L103 `error outranks interrupted and streaming`、L81 `completed message has no status badge`。
  - 判定：✘（现场未命中中断标记文本；构造受限已如实记录，中断标记渲染逻辑由上述单测覆盖）。

## R. 验收失败循环复验（2026-09-18 r3；versionCode 1789752097）

> §2 步骤 4：A3/D1/D2/B1 闪退四个 ✘ 的根因修复后只重跑受影响 item。修复：①分支加 hasLaterAssistant 门控（仅会话末轮）；②注入卡两分支接线 InjectionCard（淡形态+DataObject）；③V2 通知卡铃铛图标；④逐轮列表 turnRowKey 唯一化（DSH serverTurn 重复不再闪退）。

### R1 DSH 统计 sheet 打开不闪退（B1 前置解锁）
- 前置：DSH 面 B1 同款会话（serverTurn 重复——正是此前闪退会话）
- 操作：打开统计弹窗 → 逐轮列表滚动；logcat -b crash 抓取
- 期望：不崩溃；重复 turn 号各行均在列表
- 判定：crash 缓冲无新 FATAL；逐轮列表可见 ≥2 行
- 实测记录：

### R2 A3 复验：分支入口仅末轮
- 前置：V2 面 ≥2 轮会话（A1b 同款）
- 操作：历史轮 ⓘ 弹窗 dump；末轮 ⓘ 弹窗 dump
- 期望：「从此轮分支」仅末轮弹窗在场
- 判定：双 dump 对比
- 实测记录：

### R3 D1 复验：DSH 注入卡淡形态落地
- 前置：DSH 新建会话发首条消息（同 D1 构造路径）
- 操作：截屏 evidence/r3-injection.png + dump
- 期望：注入卡 = 左色条+弱底+无描边+DataObject 图标
- 判定：读图 + dump 标签
- 实测记录：

### R4 D2 复验：V2 通知卡铃铛图标
- 前置：V2 含通知/工具目录消息会话（同 D2）
- 操作：截屏 evidence/r4-notify.png
- 期望：通知卡描边完整卡 + 铃铛图标（非 ⓘ）
- 判定：读图
- 实测记录：

### R5 D3 复验：注入卡展开/收起（若 R3 有注入卡）
- 前置：R3 的注入卡
- 操作：tap 展开 dump；tap 收起 dump
- 期望：正文可见性翻转
- 判定：双 dump 对比
- 实测记录：

## 人工验收清单（仅 §1 四类人工项；域边界汇总提交）
- [ ] G1 流式输出的主观流畅感/自然度最终拍板（类别④：录屏抽帧已证视口稳定与连续性，主观观感不入仪器）
- [ ] D1/D4 淡形态与描边的观感协调（类别④：结构/色条/描边在场性已由读图判定）

## 执行汇总

执行时间：2026-09-18 23:37 – 2026-09-19 01:07（emulator-5554，Pixel6_Android36 1080x2400 zh-CN；V2=opencode2 4199 + DSH=3080，reverse 齐备；被测构建 versionCode 1789744508 核对一致，F2 后因 androidTest 卸载主包改用同源 F3 产物重装 versionCode=1789749746，详见 F4 记录）。

| 项 | 结果 | 一句话结论 |
|---|---|---|
| A1 | ✔ | V2 末条尾部簇四元素（耗时/步数·工具/复制/ⓘ）dump+截图双证在场，单点 |
| A1b | ✔ | 长回复尾部簇同构、ⓘ 计数=1、多 part 佐证（文本块≥6）；首试 provider 失败为环境因素，同会话成功轮覆盖 |
| A2 | ✔ | 弹窗标题/详情行/复制 Markdown 源文/从此轮分支/删除消息齐，无点赞点踩，中心 x=540 偏差 0px |
| A3 | ✘ | ①历史轮弹窗「从此轮分支」在场（期望无，末/历史轮无差异）②历史轮尾部簇恒完整，tap 无展开（dump 无实质变化） |
| B1 | 条件通过 | 分支 B 成立（错误轮两行均无+错误文本佐证）；分支 A 逐轮展开被 **DSH 统计 sheet key 崩溃**（`Key "turn-SERVER-1" was already used`）阻断→按兜底引用 DshStepTimingTest+ContextDetailDelegateTest；**崩溃缺陷登记（仪器可验）** |
| B2 | ✔ | 会话级「平均 TTFT：1.7s」「83.2 令牌/秒」有数据双行在场；无数据会话双行不渲染 |
| B3 | ✔ | DSH 详情弹窗「评价为有帮助/评价为没帮助」评分动作在场，无 V2 专属动作泄入 |
| C1 | ✔ | FAB.top=1968 ≥ 末条尾部动作 bottom=1876（差 92px），垂直分离 |
| D1 | ✘ | 「插件配置」标签在场但形态不符：实测 1dp 描边+Info 图标+无色条+无弱底（期望淡形态+DataObject）；InjectionCard 淡形态组件在源码存在但 ChatMessageList 0 调用点（实走 EventCard），与 9-17 基线一致非回归 |
| D2 | ✘ | 分支 1「工具目录已变更」+分支 2「通知」（含折叠描述行）均命中、描边完整卡 ✔，但图标为 ⓘ 非铃铛 |
| D2b | N-A | 线上会话无 <system-reminder> 嗅探注入（Room cached_messages 计数=0）；引用 MessageRowModelTest L351 嗅探 seam 单测条件通过 |
| D3 | ✔ | tap 展开→正文在场、再 tap→正文消失，存在性翻转成立（附注卡头热区偏小） |
| D4 | ✔ | 静态「思考完毕 · 50.3s」与流式「思考中… 2.7s」（frame_06）描边+smallMedium 圆角双态同构 |
| E1 | ✔ | 气泡宽 914≤972、右边距 32<左边距 134 右对齐、还原/复制/详情三动作齐、无头部标签节点 |
| E2 | ✔ | 助手正文完全扁平（无底色/描边/圆角/头部），与用户气泡形态对比鲜明 |
| E3 | ✔ | 逐轮「轮次 7→1」严格倒序、最新在最上，每行带展开按钮；V2 面 sheet 无崩溃 |
| F1 | ✔ | BUILD SUCCESSFUL 2m9s；tests=3352 failures=0 errors=0 skipped=0 |
| F2 | ✔ | BUILD SUCCESSFUL 3m48s；tests=113 failures=0 errors=0 skipped=1（总数达 113 门槛）；副作用：卸载主包已重装（版本号见头部说明） |
| F3 | ✔ | lintDevDebug+assembleDevDebug BUILD SUCCESSFUL 5m7s（0 error）；i18n PASSED 915 keys × 14 languages |
| F4 | ✔ | RenderSupplyCoordinatorTest 连续 2 次 BUILD SUCCESSFUL（52s/46s），17 例 0 失败 |
| G1 | ✔ | 40s 录屏 13 帧抽帧：9s/18s/30s 三帧视口零漂移、无跳底/闪烁/塌跳，终态贴底正常；主观流畅感→人工清单 |
| G2 | ✔ | 快速定位选 Q3 跳转后，目标消息外灰色强调高亮背景块在场 |
| G3 | ✔ | 工具卡文本各不相同无相邻重复成卡（弱断言）；引用 MessageRowModelTest L245/L253 dedupeByEventIdentity 单测 |
| H1 | ✔ | 流式徽标「正在流式输出」+思考计时器由录屏帧（d4 frame_06）证明在场（dump 对徽标失明属已知 a11y 失明区）；完成态 dump/截图零噪音 |
| H2 | ✘ | 两次流式中 tap 停止均生效（发送键回归），但被中断消息无残留（GLM 首 token>45s 未构造出「已开始输出即被中断」场景），无「已中断」标记文本；statusBadgeFor 逻辑引用 MessageRowModelTest L87/L103/L81 |

**统计**：共 25 项 —— ✔ 19 · ✘ 4（A3、D1、D2、H2）· N-A/条件通过 2（D2b、B1 兜底条款）· BLOCKED 0。

**随汇总上报的缺陷与关注点**（只观测不修复）：
1. **【高】DSH 统计 sheet 逐轮列表崩溃**：serverTurn 重复时 LazyColumn key 冲突 `IllegalArgumentException: Key "turn-SERVER-1" was already used`（logcat -b crash 00:08:15），打开即闪退（证据 b1 相关记录、h1 前后 dump）；V2 面不受影响。
2. 【中】#415/#416 注入卡淡形态未落地：InjectionCard.kt 存在但无调用点，DSH 注入实走 EventCard 描边形态（D1）；V2 通知卡图标为 Info 而非铃铛（D2）。
3. 【中】A3 分支入口无末轮/历史轮差异、历史轮尾部无收起/展开双态。
4. 【低】H2 中断标记在「首 token 前中断」场景无载体（assistant 消息未创建即取消）。
5. 【低】A1b 首发轮 provider outcome=failed（deepseek Insufficient Balance，环境因素）；F2 androidTest 副作用卸载主包（版本号变化已记录）。

人工验收清单（域边界，见文末原节）：G1 主观流畅感拍板、D1/D4 淡形态与描边观感协调。
