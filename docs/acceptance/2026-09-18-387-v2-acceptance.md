# #387 v2 批次(#410-#417)验收 checklist

- 卡片：backlog #410 #411 #412 #413 #414 #415 #416 #417 #387 | 日期：2026-09-18
- 构建：本 checklist 随 #417 修复 commit 一体交付（执行时以 adb dumpsys versionCode/versionName 实际核对） | 设备：emulator-5554（Pixel6_Android36）
- 分类：仪器可验（全部 item 走三步验收；D1/D2/D4 截图判读由多模态 subagent 读图，仍属仪器证据）
- 回归域：chat-ui 卡片层（#410/#413/#415/#416）、DSH 数据面（#411）、测试基建（#412/#414/#417）、消息层 v2 全量（#387）
- 环境前置：
  - V2 服务器：`./scripts/debug-entry.sh emulator-5554 dev.leonardo.ocbeacon.dev`（reverse tcp:4199/4198）
  - DSH：`./scripts/dsh-pair.sh emulator-5554`（已配对，cookie 365d；服务器 127.0.0.1:3080）
  - 装最新 devDebug 后执行；一切设备操作走 adb -s emulator-5554

## A. #410 尾部单点 AssistantTurnTail（V2 面）
### A1 V2 会话末条助手消息尾部信息簇完整在场
- 前置：V2 服务器连通；打开一个含 ≥2 轮回复的历史会话
- 操作：`adb shell uiautomator dump` 后解析末条助手消息区域
- 期望：尾部一行含 耗时（x.xs）、步数·工具摘要（N 步·M 个工具）、复制按钮、ⓘ 详情入口
- 判定：dump 文本命中上述元素；无 ChunkStatsBar 旧双份渲染（同簇只出现一次）
- 实测记录：

### A2 ⓘ 打开「消息详情」居中弹窗
- 前置：A1 会话
- 操作：tap 末条 ⓘ；dump
- 期望：居中 AlertDialog：详情区（时间/provider·模型/耗时/步数/工具数/tokens 有则显示、空值整行不渲染）+ 动作区（点赞点踩仅 DSH；复制 Markdown 源码；从此轮分支仅末轮；删除能力位就绪才显示）
- 判定：dump 命中「消息详情」标题与 ≥3 详情行；V2 面无点赞点踩
- 实测记录：

### A3 历史轮 vs 末轮的分支入口差异
- 前置：A1 会话（≥2 轮）
- 操作：滚到上一轮，打开其 ⓘ 弹窗，dump；对比 A2
- 期望：历史轮弹窗无「从此轮分支」，末轮弹窗有
- 判定：两 dump 对比，分支动作仅出现在末轮
- 实测记录：

## B. #411 DSH 逐轮 TTFT / 解码速度（DSH 面）
### B1 逐轮展开行 timing 两行（数值路径）
- 前置：DSH 服务器配对可用；打开有成功回复的会话；统计弹窗可达
- 操作：打开顶部统计弹窗 → 逐轮列表 → 展开一轮成功回复
- 期望：展开体含 TTFT（xx ms）与解码速度（xx tokens/s）两行数值
- 判定：dump 命中 TTFT 与 tokens/s 文本且为数值；若该轮为错误轮（如余额不足）则两行整项隐藏（宁缺勿谎）
- 实测记录：（若 DSH 服务器当时仍余额不足：记录错误轮隐藏路径实测 + 数值路径以单测 DshStepTimingTest 为准，本项标条件通过）

### B2 会话级 TTFT 均值 / 解码速度在场（sessionStats 区）
- 前置：同 B1
- 操作：统计弹窗会话级区 dump
- 期望：TTFT 均值与解码速度行在场（有数据）
- 判定：dump 命中两行
- 实测记录：

## C. #413 FAB 与末条消息尾部动作分离
### C1 贴底末条 ⓘ 不被任务 FAB 遮挡
- 前置：任一服务器；消息多到列表可滚至底
- 操作：滚动到底；`uiautomator dump` 取 FAB 与末条 ⓘ 的 bounds
- 期望：FAB.top >= lastTailAction.bottom（垂直分离）
- 判定：bounds 数值比较成立
- 实测记录：

## D. #415/#416 卡片形态与图标
### D1 DSH 注入卡淡形态 + DataObject 图标
- 前置：DSH 面；打开含「插件配置/上下文注入」事件的会话（如 dsh web 插件配置注入）
- 操作：截图 /sdcard/d1-injection.png 拉回；dump
- 期望：注入卡 = 左侧色条 + 弱底、无描边；图标为 {}（DataObject）；标签「上下文注入」类；本体点击展开
- 判定：截图多模态判读（色条在/无描边/图标形态）+ dump 文本（标签/时间/正文）
- 实测记录：

### D2 V2 通知卡铃铛图标 + 完整描边卡
- 前置：V2 面；含 role=system 通知（如工具目录变更）的会话
- 操作：截图 /sdcard/d2-notify.png；dump
- 期望：通知卡 1dp 描边完整卡；图标铃铛（Notifications）；非注入类标签
- 判定：截图判读（描边在/铃铛形态）+ dump 标签
- 实测记录：

### D3 DSH 注入卡展开/收起交互
- 前置：D1 会话
- 操作：tap 注入卡本体 → dump；再 tap → dump
- 期望：展开后正文可见（≤240dp 内滚动）；再 tap 收起
- 判定：两次 dump 正文可见性翻转
- 实测记录：

### D4 思考卡边框（流式与非流式）
- 前置：任一面；含思考卡会话（流式期最佳）
- 操作：截图 /sdcard/d4-reasoning.png
- 期望：ReasoningBlock 有 1dp 标准描边 + smallMedium 圆角
- 判定：截图判读（描边/圆角在场）
- 实测记录：

## E. #387 v2 消息层冒烟（V2 面）
### E1 用户气泡 90% 右对齐、无头部
- 前置：V2 会话含用户消息
- 操作：dump 取用户气泡 bounds 与屏幕宽
- 期望：右对齐；宽 ≤ 90% 屏宽；无「用户/智能体」头部标签文字
- 判定：bounds 计算 + dump 无角色标签文本
- 实测记录：

### E2 助手扁平 + 尾部锚点
- 前置：同 E1
- 操作：dump 助手消息区域
- 期望：无头部标签/时间；尾部统计栏在场（耗时/摘要/复制/ⓘ）
- 判定：dump 断言
- 实测记录：

### E3 统计底部面板逐轮倒序
- 前置：V2 多轮会话
- 操作：打开统计弹窗；dump 逐轮列表首行
- 期望：最新轮在最上（第 N 轮编号最大者在首行）
- 判定：dump 顺序断言
- 实测记录：

## F. 测试基建门禁（#412/#414/#417）
### F1 单元测试全量
- 操作：`./gradlew :app:testDevDebugUnitTest --rerun`
- 期望：0 failure
- 判定：gradle 输出
- 实测记录：

### F2 androidTest 全量（#412/#417 核心）
- 操作：`./gradlew :app:connectedDevDebugAndroidTest`
- 期望：0 failure（114+ 例全绿；4 个历史失败已修）
- 判定：gradle 输出 / XML
- 实测记录：

### F3 lint + assemble + i18n
- 操作：`./gradlew :app:lintDevDebug :app:assembleDevDebug`；`./scripts/i18n-check.sh`
- 期望：lint 0 error；assemble SUCCESS；i18n PASSED
- 判定：命令输出
- 实测记录：

### F4 T11 稳定性（#414）
- 操作：`./gradlew :app:testDevDebugUnitTest --rerun --tests '*RenderSupplyCoordinatorTest*'` 连续 2 次
- 期望：2/2 全绿
- 判定：gradle 输出
- 实测记录：

## 人工验收清单（仅 §1 四类人工项）
- [ ] D1/D4 的观感自然度（淡形态是否「不过分显眼」、描边观感）最终拍板（类别④：主观观感；仪器已证结构/色条/描边在场性）
