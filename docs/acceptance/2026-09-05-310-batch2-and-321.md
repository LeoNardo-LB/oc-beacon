# #310 批2 五子项 + #321 @文件引用 验收 checklist

- 卡片：backlog #310（①子智能体续聊 ②消息反馈 ③Plan 模式 ④轮次台账 ⑤@会话源）+ #321（@文件引用）| 日期：2026-09-05
- 构建：含 905f25fd..c7e530da/753ee6fc 全链 | 包：`dev.leonardo.ocbeacon.dev`
- 设备：一律 `adb -s 192.168.110.239:5555`；后端=DSH prod-3080；**新建会话**（防上下文污染）
- **分类：UIUX**（五子项均用户可见）→ AI 三步验收后人工清单（域汇总）
- 回归域：composer（@补全/PlanChip/单键）· 转录渲染（台账行/反馈钮）· 子会话导航 · 会话健康
- 证据目录：`/tmp/acc310_logs/`、`/tmp/acc310_shots/`

## 执行纪律（一次一项/逐项记录/禁止分析/dump 定位 tap/%s 转义/单次 bash ≤100s/长窗分段;新会话每大项可复用同一会话但避免数数类污染——本卡用真实功能消息制造状态）

## 前置
### P0 设备构建核对
- 前置：主 agent 已装新构建
- 操作：adb devices;dumpsys versionName/lastUpdateTime
- 期望：versionName=0.3.0、lastUpdateTime=2026-09-05 15:24:46（主 agent 已装 905f25fd..753ee6fc 全链构建并核实——不符 ✘ 停卡）
- 判定：输出记录
- 实测记录：✔（2026-09-05 15:29:55）【adb devices：192.168.110.239:5555 device 在线（另见 adb-e69a99d8-yzT17Y._adb-tls-connect._tcp device——同机 mDNS 别名，本卡一律用 192.168.110.239:5555）】
  - `dumpsys package dev.leonardo.ocbeacon.dev`：versionCode=1788593037 minSdk=26 targetSdk=36；**versionName=0.3.0 ✔**；**lastUpdateTime=2026-09-05 15:24:46 ✔**——与期望逐字一致，P0 通过不停卡
  - 轮2 2026-09-05 17:14:03（P0' 重跑指纹核对，两簇根因修复构建已装）判定：✔
    - `adb devices`：192.168.110.239:5555 device 在线（另见 adb-e69a99d8-yzT17Y._adb-tls-connect._tcp 同机 mDNS 别名，本卡一律用 192.168.110.239:5555）；`dumpsys package dev.leonardo.ocbeacon.dev`：versionCode=1788599536 minSdk=26 targetSdk=36；**versionName=0.3.0 ✔**；**lastUpdateTime=2026-09-05 17:12:54 ✔**——与轮2 期望逐字一致，不停卡（/tmp/acc310_logs/p0r2.log）
  - 轮3 2026-09-05 18:02（P0'' 重跑指纹核对，A3 根因修复 4b463eec + A8 双根因修复 098e89a2 构建已装）判定：✔
    - `adb devices`：192.168.110.239:5555 device 在线（另见 adb-e69a99d8-yzT17Y._adb-tls-connect._tcp 同机 mDNS 别名，本卡一律用 192.168.110.239:5555）；`dumpsys package dev.leonardo.ocbeacon.dev`：versionCode=1788602465 minSdk=26 targetSdk=36；**versionName=0.3.0 ✔**；**lastUpdateTime=2026-09-05 18:01:39 ✔**——与轮3 期望逐字一致，不停卡（/tmp/acc310_logs/p0r3.log）

### P1 连接 prod-3080 新建会话+候选存在性准备
- 前置：P0 通过
- 操作：force-stop 冷启（避 4199 坑）→ prod-3080 连接 → 新建 workspace 会话（默认完全访问档）;**候选准备**:宿主机直查/touch `/home/leo-tkp/workspace/acc310-mention-probe.md`（保证 @a 有 a* 匹配文件,#321 正向证据前提）;会话列表确认 ≥1 其他会话在场（g2-once 等既有会话,⑤ 会话候选前提）
- 期望：会话可用;准备文件存在;其他会话在场
- 判定：dump 正常+FATAL=0+准备动作输出记录
- 实测记录：✔（2026-09-05 15:31:45）【全程未跑 debug-entry.sh（避 4199 重指坑）】
  - 准备（15:30）：/tmp/acc310_logs、/tmp/acc310_shots 建好;logcat -c 已清;宿主 `touch /home/leo-tkp/workspace/acc310-mention-probe.md` 成功（-rw-rw-r-- 0B 15:30）;同目录另有 a* 文件：AI视频理解帧采样算法…md、ASR语音识别模型选型…md → `@a` 匹配文件 ≥1 前提成立
  - reverse：`adb reverse tcp:3080 tcp:3080` 成功,`reverse --list` 含 `host-31 tcp:3080 tcp:3080`
  - 冷启：15:30:20 force-stop → 无 intent `am start -n dev.leonardo.ocbeacon.dev/dev.leonardo.ocbeacon.MainActivity` → 落 Home;Home dump（p1_home.xml 34KB）:Host-4199「正在连接…」、192.168.110.248:248「已连接」、**prod-3080 卡（http://127.0.0.1:3080 · DSH 徽标）未连接**
  - 连接：tap prod-3080 卡「连接」(636,1895) 15:30:49 → 15:30:53 dump 卡变「已连接 · DSH」（p1_connected.xml）
  - 会话列表：tap 卡上「会话」(380,1895) → 15:31:09 dump（p1_sessionlist.xml）：**≥1 其他会话在场**——「用 bash 执行 echo g2-once」（/home/leo-tkp/workspace,9月5 08:22）、「count from 1 to 50」、「run bash touch /tmp/acc308_probe/a8a」、「我有个问题，现在启动subagent」等多行 ✔
  - 新建：tap「新建会话」(972,230) 15:31:29 → 新会话对话框（workspace 7 会话/外包维权工作区 3/oc-beacon 1/打开其他项目…）→ tap workspace 行 (336,1148) → **15:31:45 新会话建立**：空态「开始会话·在下方输入消息」+ Agent 预设列表（标准/PTC/极简/创造四档）+ composer 在场（hint「提问…」+「发送」钮,resource 可见）+ 模型 zai-coding-cn GLM-5.3-Flash（p1_newsession.xml）
  - 权限档观测：新会话无可观测只读药丸/权限横幅——按 #308 记录 DSH 新会话默认=完全访问（服务器侧,不发 PermissionAsked）,如实记录为服务器默认档
  - logcat：p1_after.log 中 AndroidRuntime 30 行全为 uiautomator Launcher D 级启动噪音（uid 2000 shell,`com.android.commands.uiautomator.Launcher`）;**`FATAL EXCEPTION|E AndroidRuntime` 精确计数=0** ✔

## A. 五子项验证

### A1 ⑤+#321 @ 补全（文件+会话候选）【轮2 重跑:前置修订=会话须已发≥1条消息(懒创建 session 已建立;簇1修复 6290102b 后空会话按设计静默无候选)——先发 hi%sand%swait 得回复再敲 @a】
- 前置：P1 会话空闲
- 操作：输入框敲 `@a`（不发送）→ 等 1s → dump 建议弹窗 → 分别记录候选行类型（文件行/会话行）→ 清空
- 期望：**文件候选非空**（#321 核心——此前 DSH 恒空）;若存在其他会话则**会话行在场**（会话图标+label+可能同工作区徽标）
- 判定：dump 见文件路径候选行 ≥1（#321 ✔ 前提:服务器有匹配文件,无则如实记录）;会话行按实际会话存在性记录
- 实测记录：✘（2026-09-05 15:34:20）【P1 新会话（workspace,空闲,未发首条消息）】
  - 输入：tap 输入框 (170,2530) → `input text '@a'` **成功**（@ 符号经 adb 正常输入,15:32:31 dump 输入框 text="@a"）→ 等 2s → dump（a1_at_popup.xml,19160B/24 节点）;30s 后复 dump（a1_at_popup2.xml）仍 24 节点——**无任何弹层/候选行节点**（文件行 0,会话行 0）
  - logcat（a1_after.log,23522 行）关键证据：`15:32:28.472 W DraftInputDelegate: Mention candidates failed for '': resume failed for session "": SessionQueryError: failed to observe session "": cannot encode an empty path segment` + `15:32:28.655 W DraftInputDelegate: Mention candidates failed for 'a': …同错`——@ 触发与候选查询管线已启动（对 query '' 与 'a' 各发一次）,但**整体失败于 SessionQueryError（session id 为空串,URL path 编码拒绝）**,故无候选返回、弹窗未呈现
  - 像素通道交叉验证（a1_popup.png 1200x2670,y1900-2560@x600 竖条采样）：该带为 IME 键面浅色区（RGB 224-255 连续）,composer 上方无独立弹层表面——与 dump 结论一致（双通道互证,非 dump 失明误判）
  - 服务器匹配文件前提已满足（P1 已建 /home/leo-tkp/workspace/acc310-mention-probe.md + 2 个既有 a* .md）——排除「服务器无匹配文件」解释;失败成因=客户端查询错误（empty session id）,非候选为空
  - 截图证据：/tmp/acc310_shots/a1_popup.png;输入框已清空复厂（dump 回 hint「提问…」,15:34:20）
  - 附注（观测事实,非分析结论）：本会话为新建未发首条,SessionQueryError 中 session id=""——与「会话未落库前无 id」形态一致;A2/A3 在此状态下的可验性见各项记录
- 轮2 实测记录：✔（轮2 2026-09-05 17:16:21）【轮2 重跑:前置修订执行——新会话先发消息得回复再敲 @a;新构建 lastUpdateTime=17:12:54】
  - 前置修订执行：冷启→连 prod-3080（17:14:38 已连接·DSH）→新建 workspace 会话（17:15:11）→发送 `hi and wait`（input text 'hi%sand%swait',%s 转义,输入框逐字核对）→17:15:37 回复到达「Hi! I'm ready and waiting. What would you like me to help with?」+思考行+轮次 1 台账——会话已落库,轮1 空 session id 前提消除
  - `@a` 输入（tap 输入框→input text '@a',dump 输入框 text="@a" 确认）→3s 后 dump（r2_a1_at_popup.xml）：**建议弹窗在场**（scrollable [0,380][1200,1424],弹层覆盖原转录区）,**两型候选行均在场**
  - 文件行 ×3（图标 desc=「文件」+路径文本）：`dsh-keepalive/ai-dev-guide`、`dsh-keepalive/docs/archive`、`ASR语音识别模型选型调研报告.md`（均 a* 前缀匹配）——**文件候选非空 ✔（#321 核心,轮1 恒空已破）**
  - 会话行 ×5（图标 desc=「会话 label」+label 文本+「同工作区」徽标+cwd 行）：`Count slowly from 1 to`、`reply with shortsgreeting`、`count from 1 to 50`、`我们安装了idlefix 插件了吗？`、`run bash touch /tmp/acc308_probe/a8a`——**会话行在场 ✔**
  - logcat（a1r2.log）：轮1 形态 `Mention candidates failed … SessionQueryError` **不复现**;FATAL=0
  - 证据：/tmp/acc310_shots/a1r2_popup.png、r2_a1_at_popup.xml；/tmp/acc310_logs/a1r2.log

### A2 ⑤ 会话行点选→mention 串插入
- 前置：A1 有会话候选
- 操作：tap 一个会话候选行 → dump 输入框文本
- 期望：草稿出现 `@[label](dsh-session:id)` 规范串（@ 开头,含 dsh-session:）+ 尾随空格,trigger 词被替换
- 判定：输入框文本含 dsh-session: 串 → ✔（若 A1 无会话候选,本项 BLOCKED 记原因）
- 实测记录：BLOCKED（2026-09-05 15:35:00）【前置不成立:A1 无会话候选】
  - A1 实测：@a 触发后候选查询整体失败（`Mention candidates failed for 'a': SessionQueryError: failed to observe session "": cannot encode an empty path segment`）,弹窗未呈现,无可 tap 的会话候选行——「tap 一个会话候选行」操作对象不存在
  - 处置：按本项判定规则记 BLOCKED;未做替代性操作（不在他 会话试 tap——超 checklist 操作面）;mention 串插入链路（@[label](dsh-session:id) 规范串生成）本卡未获验证
- 轮2 实测记录：✔（轮2 2026-09-05 17:17:22）【A1 轮2 会话候选在场,前置成立】
  - tap 会话候选行 `count from 1 to 50`（行区 (310,736)）→弹窗关闭→dump 输入框（resource-id=chat-input）：text=`@[count from 1 to 50](dsh-session:InNlc3Npb24tYTZhNTViYzEtYjFmYi00M2NhLWJhMDgtMDllMjQxZTJiNTcwIg) `（尾部空格在场）
  - 断言逐项：@ 开头 ✔;含 `dsh-session:` 规范串 ✔;尾随空格 ✔;trigger 词 `@a` 被替换 ✔（输入框无 @a 残留）
  - 证据：/tmp/acc310_shots/a2r2_inserted.png、r2_a2_inserted.xml；/tmp/acc310_logs/a2r2.log

### A3 ⑤ @" 引号形态只文件
- 前置：A2 完成
- 操作：清空后输入 `@"a` → dump 弹窗
- 期望：候选**只有文件行**,无会话行（quoted 判定）
- 判定：dump 无会话图标行 → ✔
- 实测记录：✘（2026-09-05 15:37:11）【同一 P1 新会话;引号输入法事实已记录】
  - 引号输入替代法（如实记录）：`input text '@"a'` 直发在 adb 参数拼接层断裂（device shell 报 `no closing quote`,首次尝试 input 未执行,15:34:56 dump 53 节点=键盘建议条节点增集,输入框仍空）;**成功通路**＝把 `input text '@"a'` 写成 device 侧脚本 push 到 /data/local/tmp/a3.sh 后 `sh` 执行（15:36:11）——绕开 adb 引号拼接层
  - 触发确认：logcat `15:36:11.358 W DraftInputDelegate: Mention candidates failed for '"a': resume failed for session "": SessionQueryError: failed to observe session "": cannot encode an empty path segment`——quoted 形态（查询串含引号）触发链路已走到,与 A1 同一 SessionQueryError（session id 空串）失败
  - 弹窗：dump（a3_quoted_popup2.xml,52 节点=24 基础+28 IME 建议条）无候选行——**文件行 0**（quoted 形态核心期望「只文件」无从呈现,非「只有文件」）;会话行亦 0（无会话图标行,该子断言空真但不构成 ✔ 依据）
  - 截图：/tmp/acc310_shots/a3_quoted.png、a3_quoted2.png;日志：/tmp/acc310_logs/a3_after.log（29966 行）;输入框已清空+IME 收起（keyevent 67×3+111,15:37:11）
- 轮2 实测记录：✘（轮2 2026-09-05 17:18:27–17:24:18）【同一轮2 会话（有历史）;引号输入沿用轮1 device 侧脚本法】
  - 引号输入：`input text '@"a'` 写脚本 push /data/local/tmp/a3r2.sh 后 `sh` 执行——输入框 text='@\"a' 逐字确认（**解析注记**：uiautomator 对含 `"` 的属性值改用单引号序列化,双引号 regex 会失明,需引号无关解析）
  - `@\"a` 后 3s/8s 两帧 dump（r2_a3_quoted_popup3/4.xml）：**无弹窗**——会话图标行 0/文件图标行 0,转录区直接可见（无覆盖弹层）;加字符重触发（`@\"as`）与回退（`@\"a`）复测仍无弹窗
  - 像素互证（y326-1420 弹窗带 top-colors）：A1 帧（@a,有弹窗）=86.4% 均一 (224,224,224) 弹窗表面 vs A3 帧（@\"a）=混色转录内容（38.9%(208,224,224)+32.3%(224,224,224)+11.5%+11.2% 多色分布）——**无弹窗表面**（dump+像素双通道一致,非 dump 失明误判）
  - 控制组（17:21:59/17:23:49）：同会话同态清空后重新输入 `@a` →弹窗完整复现（会话行 5+文件行 3,行集与 A1 帧一致）——**差异锁定在引号形态本身**（同会话同态非引号形态可用）
  - logcat（a3r2.log）：无 `Mention candidates failed` 行（轮1 quoted 查询 SessionQueryError 不复现,亦无可观测候选查询日志）;FATAL=0
  - 插曲（如实记录）：观测中一次 composer 区 tap 后弹出权限预设选择面板（只读/工作区写入/完全访问三选项）,BACK 关闭,输入框 '@\"a' 文本未受影响,后续观测不受干扰
  - 判定 ✘：quoted 形态核心期望「只有文件行（文件行≥1+会话行 0）」无从呈现——**文件行 0（弹窗未呈现）**;「无会话图标行」子断言空真,不构成 ✔ 依据（与轮1 同口径）
  - 证据：/tmp/acc310_shots/a3r2_quoted.png、a3r2_quoted3.png、a3r2_control2.png；r2_a3_quoted_popup*.xml、r2_a3_control2.xml；/tmp/acc310_logs/a3r2.log
- 轮3 实测记录：✔（轮3 2026-09-05 18:09–18:12）【轮3 重跑:4b463eec quoted query 去引号修复构建（lastUpdateTime=18:01:39）;新会话+有历史（发 hi 得回复）;引号输入沿用 device 侧脚本法】
  - 前置：冷启→连 prod-3080（18:0x 已连接·DSH）→新建 workspace 会话→发送 `hi and wait`（%s 转义,输入框逐字核对;发送键 IME 开态坐标 (1086,1616)）→18:09:10 `POST /api/session/prompt` 受理（session-0ec36d9b）→回复到达「Hi! 👋 I'm here and standing by — nothing running…」+轮次 1 台账——会话已落库
  - `@\"a` 输入：device 侧脚本（printf 写 `input text '@\"a'` → push /data/local/tmp/a3r3.sh → sh 执行）——输入框 text='@\"a' 逐字确认（引号无关解析,沿用轮2 注记）→3s 后 dump（r3_a3_quoted_popup1.xml,78 节点）：**建议弹窗在场,文件行 ×10,会话图标行=0**——文件行文本：dsh-keepalive/ai-dev-guide、dsh-keepalive/docs/archive、ASR语音识别模型选型调研报告.md、AI视频理解帧采样算法与业界实践调研报告.md、acc310-mention-probe.md、dsh-keepalive/AGENTS.md、临时保存/质检报告/A-核心谈话-跟 TL 谈话..md、dsh-config-backup-20260904/AGENTS.md、dsh-keepalive/docs/ability-domains.md、dsh-keepalive/docs/architecture-debt.md（图标 desc=「文件」×10,全 dump「会话」desc=0）
  - 控制组（18:1x）：清空（DEL×2+ESC）后重新输入 `@a` →dump（r3_a3_control_popup.xml）：**弹窗两型并存复现**——会话行 ×5（You are researching known bugs/You are auditing installed code/Your task: count slowly from/please start a background subagent/hi and wait,均带「同工作区」徽标+cwd 行）+文件行 ×3（dsh-keepalive/ai-dev-guide、dsh-keepalive/docs/archive、ASR语音识别模型选型调研报告.md）——差异锁定在引号形态（quoted=只文件,非引号=两型）,quoted 语义正确
  - logcat（a3r3.log）：`Mention candidates failed` 计数=0（轮1 SessionQueryError 形态不复现,轮2 「无候选查询失败日志」状态下轮3 亦无失败）;FATAL=0;输入框清空复厂（chat-input text='""'）
  - 判定 ✔：quoted 形态核心期望「弹窗出现+文件行 ≥1+无会话行」三项全中（轮1/轮2 ✘→✔,4b463eec 修复实测成立）;控制组两型并存复现佐证非引号链路未回归
  - 证据：/tmp/acc310_shots/r3_a3_quoted.png、r3_a3_control.png、r3_a3_quoted_popup1.xml、r3_a3_control_popup.xml；/tmp/acc310_logs/a3r3.log、a3r3.sh（device 侧脚本留档）

### A4 ② 消息反馈 👍/👎
- 前置：P1 会话发一条 `reply%swith%sshortsgreeting` 得到回复
- 操作：dump assistant 回复卡脚部 → 记录 👍/👎 钮在场 → tap 👍 → dump（高亮态）→ 再 tap 👍（撤销）→ dump
- 期望：DSH 下已完结 assistant 消息脚部有反馈钮;tap 评分→高亮;同向再 tap→撤销回常态（**👎 负向分支豁免 E2E**:同向撤销已覆盖 delete 路径+换向 put 路径,负向 rating 闭集由单测钉死——wire 层 rating:'negative' 载荷用例）
- 判定：三帧 dump 状态变化链（无钮→高亮→常态）→ ✔;若钮不可见记录实际脚部形态
- 实测记录：✔（2026-09-05 15:46:00）【同一 P1 会话;首个轮次后反馈钮/台账行均在场】
  - 发送：15:37:31 输入 `reply with shortsgreeting`（%s 转义成功,输入框 text 逐字核对）→ tap 发送 (1086,1616);8s 后回复到达：assistant「Hi there! 👋 How can I help you today?」+思考行「思考完毕 · 0ms」;**脚部反馈钮在场**：content-desc「评价为有帮助」(👍,父可点区 [942,1207][996,1261])与「评价为没帮助」(👎,[1002,1213][1023,1255])——帧1（a4_poll1.xml,常态）
  - wire 前置：回复完结后 app 自动 `GET /api/messageFeedback/list`（15:37:40.909 REQUEST+FROM,拉既有评分态）
  - tap 👍 (969,1234)：**`PUT /api/messageFeedback/put` 15:44:40.614 REQUEST+FROM 受理**（15:44:44 段时间差=工具调用间隔,会话空闲无影响）——帧2（a4_thumbup.xml+a4_frame2_up.png）
  - 再 tap 👍（同向撤销）：**`DELETE /api/messageFeedback/delete` 15:45:20.052 REQUEST+FROM 受理**——帧3（a4_frame3.xml+a4_frame3_undo.png）
  - 高亮视觉证据（像素直方图,👍 图标区 x940-1000/y1205-1265）：帧2 主色 **(47,100,127) 深蓝 718px=primary 高亮态** → 帧3 回 **(123,128,131) 灰 738px=常态**;背景 (221,228,232) 两帧一致——高亮↔常态颜色链与 put/delete wire 链一一对应（dump 的 selected 属性无变化,Compose 高亮为纯色彩态,以像素通道钉死）
  - 附带观测：本回复下已见「轮次 1 · 0ms · 1 步 · 0 个工具」台账行（A9 项详测）;权限药丸「完全访问」在场;后台 Host-4199 卡持续重连失败噪音（ConnectException,非本卡域,4199 服务器未开属预期）
  - 证据：截图×3（a4_poll1 帧未截屏,以 dump 代;frame2/frame3 png 在 /tmp/acc310_shots/）;日志 a4_after.log（83558 行,含完整 put/delete 链）

### A5 ③ Plan 模式
- 前置：空闲
- 操作：输入 `/plan` 发送（走命令链）→ 等 5s dump composer 选择器行 → 记录 PlanChip 形态 → 给 agent 一个会触发计划的任务消息（`first%smake%sa%sshort%splan%sto%screate%sa%sreadme%sthen%swait%sfor%smy%sapproval`）→ 等待 plan-review 卡或 dump → 记录卡形态（计划正文+Approve 强调）→ 点 Approve → 观测
- 期望：/plan 后 chip 出现（激活态）;plan-review 到达时审阅卡呈现计划正文;Approve 后卡消,agent 继续
- 判定：chip 在场 dump + 审阅卡 dump + Approve 后卡消失（agent 行为如实记录）→ ✔;若 agent 不走 plan-review（模型行为）→ 记录实际,chip 断言独立成立即 ✔,审阅卡部分 BLOCKED-模型行为。**Keep 分支豁免 E2E**:同提交链路(Approve 已覆盖),Keep 仅选项差异——模型行为分支,选项提交形状由单测覆盖
- 实测记录：✔（2026-09-05 15:49:25）【同一 P1 会话,A4 后空闲态起】
  - /plan 命令链：15:46:24 输入 `/plan` → dump 出现**命令建议行**（[0,1304][1200,1448] 全宽行,composer 上方）→ tap 建议行 (600,1376) → 插入 `/plan `（尾随空格）→ tap 发送 15:46:51
  - **PlanChip 出现**：5s 后 dump（a5_chip.xml）composer 选择器行 text=「Plan」@ [336,1465][409,1508]（权限药丸「完全访问」与模型「GLM-5.3-Flash」之间）——chip 在场 ✔（截图 a5_chip.png）;composer hint 同步变「重构…」
  - 任务消息：15:47:25 发送 `first make a short plan to create a readme then wait for my approval`（%s 转义逐字核对）
  - **plan-review 卡到达**（~27s,15:47:52 dump a5_poll1.xml）：形态=待答卡「待你回答」+「SINGLE」徽标 + 问题 **「Approve this plan and leave plan mode?」** + **计划正文**（markdown「# Plan: Create Workspace README ## Goal Create `/home/leo-tkp/workspace/README.md` …」）+「忽略/提交」钮;卡内滚动后见**选项组**：「Approve（Leave plan mode; the plan is carried out from the next step.）」+「Keep planning（Stay in plan mode; feedback goes back to the model.）」+「输入答案」自定义行（a5_scrolled.xml）——审阅卡呈现计划正文 ✔
  - Approve：tap Approve 选项行 (600,1385) →（选中态 dump selected=0 不可见,Compose 色彩态,功能以下步为准）→ tap 提交 (964,2060) 15:48:49
  - **Approve 后观测**（15:49:07 dump a5_after_submit.xml）：待答卡**消失**（无 待你回答/忽略/提交 节点）;agent **继续**：新 assistant 消息「Approved — here's the plan I'll follow:」+ 完整计划正文（步骤 1/2+要点列表+Assumptions）+「Shall I proceed?」;轮次 2 台账行「轮次 2 · 1m 34s · 5 步 · 2 个工具」在场;**PlanChip 已随批准自动离场**（composer 区无 Plan 文本）——批准=leave plan mode 语义 ✓
  - wire：run code 步骤 + ask 链正常;FATAL EXCEPTION 计数=0（a5_after.log）
  - 证据：a5_chip.png、a5_before_submit.png;a5_* dump×6;a5_after.log

### A6 ③ PlanChip 退出
- 前置：A5 chip 在场
- 操作：tap PlanChip → 等 3s → dump chip 区
- 期望：chip 消失（/plan off 生效）
- 判定：dump 无 chip → ✔
- 实测记录：✔（2026-09-05 15:50:24）【前置事实+复建后双路径验证】
  - 前置偏差（如实记录）：A5 Approve 提交后 PlanChip **已随批准自动离场**（a5_after_submit.xml 无 Plan 文本——批准=leave plan mode 语义）,A6 起点无 chip 可 tap;为执行本项操作,按 A5 同命令链复建：/plan → 命令建议行 tap (600,1376) → 发送 15:50:06 → chip 回位 [336,1465][409,1508]（a6_chip_back.xml,截图 a6_chip_present.png）
  - **tap chip (372,1486)** → 等 3s → dump（a6_after_tap.xml）：`text="Plan"` 计数=**0**——chip 消失,/plan off 生效 ✔;composer 完整性 sanity：发送钮/权限药丸/模型行均在场
  - FATAL EXCEPTION 计数=0（a6_after.log）;截图 a6_after_tap.png（对比 a6_chip_present.png 可见 chip 区空缺）
  - 附注：本会话共历 /plan on→Approve 自动离场→/plan on→chip-tap 离场,两路退出均实测成立

### A7 ① 子智能体续聊（数据链）
- 前置：空闲
- 操作：发送 `please%sstart%sa%sbackground%ssubagent%sto%scount%sslowly%sto%s10%sthen%sreport` → 轮询 60s 等子代理出现在会话（AgentSheet/FAB 子代理入口 dump）→ 打开子会话（子代理条目 tap → ChatRoute）→ dump 输入区
- 期望：子会话打开;若 continuable → composer 可见可输入;若 one-shot → 只读提示行
- 判定：两种形态 dump 任一成立（mode 由服务器目录决定,如实记录 mode）→ ✔;**60s 子代理未出现 → BLOCKED-模型行为**（服务器未起子代理,数据链不可验,如实记录 agent 回复实际内容）
- 实测记录：✔（2026-09-05 15:55:17）【mode=continuable 形态（composer 在场）;数据链全程实测成立】
  - 发送：15:50:50 `please start a background subagent to count slowly to 10 then report`（%s 转义核对）→ poll1 15:51:22：agent 忙「Run code」走时 14.3s（subagent 工具执行中）→ poll2 15:52:50：轮次 5 完成「Is there anything else you'd like — more work for that subagent, or a new task?」+台账「轮次 5 · 0ms · 1 步 · 0 个工具」——agent 汇报子代理已跑（在 60s 窗口内出现）
  - FAB 定位事实：IME 开态 FAB 随布局上移至 desc「打开任务菜单」@[1092,1210][1164,1282]（#326 记录的 y2196 为 IME 收起态坐标,首 tap (1128,2232) 落键盘区未中——如实记录）;重 tap 中心 (1128,1246) 15:54:30
  - FAB 菜单展开（像素 diff 硬证据 y488→1365 连续变化带 839 行 + dump 五药丸）：**TODO/智能体/目标/Shell/排队队列**（药丸文本这次 dump 可见,与 #326 a11y 失明记录不同——如实记录本帧可见）
  - **AgentSheet**：tap「智能体」药丸 (1084,704) 15:54:58 → sheet「智能体 (1)」+ 子代理条目 **「Slowly count to 10」**@[234,859][663,931]——子代理在会话在场 ✔
  - **子会话打开**：tap 条目 (448,895) 15:55:13 → ChatRoute：标题「Count slowly from 1 to」（截断）+cwd /home/leo-tkp/workspace+右上徽标「1」;**输入区 dump**：composer 在场（hint「提问…」@[96,2498]+权限药丸「完全访问」+模型行）——continuable 形态 ✔（可输入性 A8 实证）;转录区为空态「开始会话/在下方输入消息」（子代理计数过程不渲染为转录,如实记录）
  - FATAL EXCEPTION 计数=0（a7_after.log）;证据：a7_menu_before/after.png（diff 对）、a7_agentsheet.xml、a7_subsession.xml/png

### A8 ① 子会话发送+停止（若 A7 composer 可用）【轮2 重跑:8faf940c 双根因修复(子会话 durable 地址开流+history;added 帧父址键)——期望转录渲染+二次发送仍走 subagents/prompt+停止键可观测】
- 前置：A7=continuable
- 操作：子会话输入 `status%scheck` → tap 发送 → 观测 60s 回复 → 制造忙碌（发送 `write%sa%svery%slong%sessay%sabout%sthe%soceanssin`）→ 流式中 tap 停止键;logcat 可佐证 interruptByParent 受理（次要）
- 期望：发送受理（subagents/prompt）;回复到达;停止键中断（interruptByParent）回闲
- 判定：子会话内回复可见 + 停止后回发送键 → ✔;A7=one-shot 则本项 BLOCKED-形态
- 实测记录：✘（2026-09-05 15:58:49）【A7=continuable,本项可执行;三段观测事实如下】
  - **status check 发送受理 ✔（wire）**：15:55:56.040 `POST /api/subagents/prompt` REQUEST+FROM 受理 + `ChatSendDelegate: Sent continuable prompt to subagent 4ca8416a-82a9-4602-aafe-a92feb5341ce (parent session-fd054cc9-0cfb-4feb-9952-e407ae2b6f73, 1 parts)`——subagents/prompt 链路成立,continuable 确认
  - **回复不可见 ✘**：15:55:56→15:58:49（~3min,超 60s 观测窗）子会话转录恒空态（「开始会话/在下方输入消息/Agent 预设卡」三帧 dump 一致）,用户消息「status check」亦未渲染;composer 全程「发送」键（无忙碌态）;SSE 仅见 `SessionSubagentTimingChanged → sid=d6d0f00f`（父侧计时事件）,无子会话消息部件事件
  - **忙碌制造失败 ✘（服务器拒绝,产品侧路由不一致观测）**：15:57:37 发送 `write a very long essay about the oceanssin` → 走 `POST /api/session/selectModel` + **`/api/session/prompt`（常规端点,非 subagents/prompt）** → 服务器拒绝：`E ChatSendDelegate: DshApiError(code=DshRpcErrorCode(wire=session/agent-busy), message=session "4ca8416a-…" is owned by subagent routing, details={"reason":"use subagent delivery for this child session"}, httpStatus=200)` → app 弹「发送失败」对话框（tap 确定 15:58:40 收场,草稿回填输入框）——同一 composer 两次发送路由不一致（首 发 subagents/prompt ✔ / 次 发 session/prompt ✘）为直接可观测事实
  - **停止分支不可达**：全程无停止键出现（会话从未进入忙碌渲染态）,interruptByParent 未获观测（logcat 无 interrupt 行）
  - FATAL EXCEPTION=0（a8_after.log;ChatSendDelegate E 级为业务错误非崩溃,app 存活对话框可收）;证据：a8_poll1-3.xml、a8_state2.xml（失败框）、a8_final.xml、a8_after.log
  - **附记（16:01 A9 轮次取证时新增观测,改写部分结论）**：父转录轮次 3 区见合成行「Agent 4ca8416a-82a9-4602-aafe-a92feb5341ce sent a message:」+ 正文「Count from 1 to 10 completed, one number per line: 1…10 Done: counted to 10」——此为 **A7 计数任务的报告**（非 status check 回复）,即子代理产出走父转录渲染通道;status check 的回复截至 16:01 在子会话（空态）与父转录尾部（末条=15:52:15「Is there anything else…」）均未见——「回复可见」结论维持 ✘,但『受理无渲染』的归属比初记更完整
- 轮2 实测记录：✘（轮2 2026-09-05 17:24:37–17:32:50）【轮2 重跑:8faf940c 双根因修复构建;按修订流程=新会话+新子代理全链】
  - 新会话+子代理：新 workspace 会话（17:24:59）发送 `please start a background subagent to count slowly to 10 then report`（17:25:11 `POST /api/session/prompt`=父会话首条,预期通道）→轮次 1 · 5.1s · 3 步 · 1 个工具;FAB 徽标=1;SessionSubagentTimingChanged 事件流 sid=6caf346b-…
  - AgentSheet：FAB 菜单五药丸（TODO/智能体/目标/Shell/排队队列）→「智能体 (1)」条目「Count slowly to 10」+徽标「已完成」→tap 进入子会话 ChatRoute（标题 Your task: count slowly from,17:27:07）
  - 子会话转录**非空**（轮1 恒空态 → 改善）：转录区见「轮次 1 · 0ms · 1 步 · 0 个工具」台账行（消息气泡区该帧为空）
  - **status check 发送（第一次）✔（wire）**：17:27:45.105 `POST /api/subagents/prompt` REQUEST+FROM 受理 + `ChatSendDelegate: Sent continuable prompt to subagent 6caf346b-…（parent session-9bf7abd5-…, 1 parts）`
  - 发送后 12s 帧（17:27:57）：**子会话活动期渲染在场**——「思考中… 2.1s」+子代理步骤正文（「The job_list binding requires arguments to be lossless JSON…」「The call needs an explicit (empty) arguments object — retrying:」）+计时行 3.7s;**composer=「停止」键**（轮1 不可达 → 在场）
  - **status check 完结回复未渲染 ✘**：~17:28:18 轮次完结后,17:28:42 帧子会话路由**自发回落空态 Chat**（标题 Chat/「开始会话/在下方输入消息/选择 Agent 预设」列表/hint 提问…/发送键,无用户输入介入）;完结正文在子会话未见——其内容经**父转录**呈现（父轮次 4：「That's the subagent's final wrap-up — everything is closed out cleanly: Counting task: Complete — 1 through 10…Subagent 6caf346b…is now idle and available for follow-ups」）
  - 重入子会话（17:29:59）：标题恢复+右上徽标变 2;转录仅剩轮次 1 台账行——status check 交换（用户消息+回复）**不持久渲染**
  - **essay 发送（第二次）✔（wire）**：17:30:24.549 `POST /api/subagents/prompt` REQUEST+FROM 受理+同型 continuable prompt 日志——**两次子会话发送均走 subagents/prompt ✔**（a8r2.log 内唯一 /api/session/prompt=17:25:11 父会话首条,归属已核对;轮1「二次发送走 session/prompt 被服务器拒+发送失败框」不复现）;**用户消息气泡渲染在场**（「用户 · 17:30:25 · write a very long essay about the oceans」,轮1 用户消息不渲染 → 改善）
  - **停止键行为 ✔**：流式中（17:30:54 帧）composer=「停止」→tap 17:31:00 →`POST /api/subagents/interruptByParent` REQUEST+FROM 受理 + `SessionActionsDelegate: Interrupted subagent session 6caf346b-… via parent session-9bf7abd5-…` + `SessionStateService: [6caf346b…] Idle --ClientAbort--> Idle [force-complete]` + `ChatViewModel: Aborted session 6caf346b…` →4s 后帧 composer=「发送」（回闲）
  - 中止后同样回落空态 Chat（17:31:08 帧）;AgentSheet 复查（17:32:50）：「智能体 (0) · 没有运行中的子智能体」——停止后子代理条目从列表消失
  - FATAL=0（a8r2.log）;计数核对：subagents/prompt REQUEST=2、interruptByParent 行=2（REQUEST+FROM）
  - **判定 ✘**（按「子会话内回复可见+停止后回发送键」口径:后半 ✔,前半 ✘）——status check 完结回复未在子会话渲染,且完结/中止后路由自发回落空态、重入不持久;轮1 三缺陷中「路由不一致」「停止不可达」实测修复,「转录渲染」为活动期正文/用户消息气泡/台账行级改善,**完结回复渲染未成立**
  - 证据：/tmp/acc310_shots/a8r2_*.png（fabmenu/agentsheet/subsession/status_poll2/subsession2/essay_busy/after_stop）、r2_a8_*.xml；/tmp/acc310_logs/a8r2.log
- 轮3 实测记录：✔（轮3 2026-09-05 18:12–18:35）【轮3 重跑:098e89a2 双根因修复构建（host/session-removed 对 durable 子会话降级 Idle 不再删转录;history fold 收编两 log-only 词汇）;按修订流程=新会话+新子代理全链】
  - 新会话+子代理：新 workspace 会话（18:12）发送 `please start a background subagent to count slowly to 10 then report`（18:13:41 ChatSendDelegate: Sent prompt to session session-c6de9470=父会话首条,预期通道）→轮次 1 · 3m54s · 3 步 · 1 个工具完结（「Started a background subagent…Subagent id: bd5a33c9-cd05-4d73-baff-2319c036681e」）;FAB 徽标=1（插曲如实记录:观测中 adb 一度掉线,重连+恢复 reverse 后 SSE 自动回填补齐,事件无损——SseConnManager 3080 disconnect 计数=0）
  - AgentSheet：FAB（转录在场态实际位于 y1274-1346,#326 的 y2196 坐标需按帧重定位——沿用轮1 IME 坐标偏移事实）→五药丸（TODO/智能体/目标/Shell/排队队列）→「智能体 (1)」条目「Count slowly to 10, report」→tap 进入子会话 ChatRoute（标题 Your task: count slowly from,18:17:52）
  - **子会话转录打开即渲染正文**（轮2 仅轮次 1 台账行 → 改善）：思考行+任务推理正文（「Let me work through this task…I'll run the slow count with sleep 1 between each number…」）+「Run code」步骤+轮次 1 · 8m 52s 台账;composer 在场（发送键,hint 提问…）——continuable 形态
  - **status check 发送 ✔（wire,经一次瞬态重试）**：首次 18:28:16 `POST /api/subagents/prompt` REQUEST 受理但响应 120s 超时（HttpRequestTimeoutException,app 弹「发送失败」框,tap 确定 18:32 收场,草稿回填——服务器侧实际已受理处理,子会话 msgs=6 持续增长）;重发 18:31:17.737 REQUEST+FROM 44ms 受理 + `ChatSendDelegate: Sent continuable prompt to subagent bd5a33c9-…（parent session-c6de9470-…, 1 parts）`
  - **status check 用户消息+回复均渲染 ✔**：用户气泡「用户 · 18:31:18 · status check」在场（滚动定位,引号无关解析）;活动期子代理步骤正文渲染（「The previous run_code call was interrupted…Let me run the count again…」+思考行）;~18:31:5x 轮次完结：**轮次 2 · 20.9s · 5 步 · 2 个工具台账在场,完结正文渲染**（「Report (each number counted with sleep 1 before it…)」「1: counted … Done — counted slowly to 10.」「Result also delivered to parent agent session-c6de9470…」）——**完结后子会话路由不回落空态:标题保持 Your task: count slowly from（无 Chat 回落帧）,composer 回「发送」**
  - **重出重入子会话转录持久 ✔**：返回出至父会话（父转录见子代理完结通知 wrap-up「Confirmed — the subagent has fully settled…Outcome: success — all 10 numbers counted, total measured time 10.02 s」）→FAB→「智能体 (1)」（条目保留）→重入子会话（18:33）：标题恢复+轮次 2 台账（2m 15s · 7 步 · 4 个工具）+完结正文（Done — counted slowly to 10./Result also delivered to parent agent…）均在场;上滚定位**用户气泡「用户 · 18:31:18 · status check」仍在**——status check 交换（用户消息+完结回复）重入后持久（轮2 重入仅剩轮次 1 台账 → 已修复）
  - **essay 第二次发送 ✔（wire）**：输入 `write a very long essay about the oceans`（%s 转义逐字核对）→18:34:56 `POST /api/subagents/prompt` REQUEST+FROM 受理+同型 continuable prompt 日志——**两次子会话发送均走 subagents/prompt**（a8r3.log 内 /api/session/prompt REQUEST=0——父会话首条 18:13:41 已被 logcat 滚动覆盖,归属此前已核;轮1 路由不一致/轮2 已修形态维持）;发送后 8s 帧 composer=「停止」+子代理思考正文在场
  - **停止键链路 ✔**：流式中 tap 停止 (1086,1616) →18:35:20.725/737 `POST /api/subagents/interruptByParent` REQUEST+FROM 受理 + `SessionActionsDelegate: Interrupted subagent session bd5a33c9-… via parent session-c6de9470-…` + `ChatViewModel: Aborted session bd5a33c9-…` + `SessionStateService: …Idle [force-complete]` →6s 后帧 composer=「发送」（回闲）,标题未回落,轮次 3 · 0ms · 1 步 · 0 个工具台账在场（中止轮成台账,essay 用户消息保留）
  - 计数核对（a8r3.log）：subagents/prompt REQUEST=4（首 status check+重发+essay+活动补发——含首次超时的 REQUEST）、interruptByParent REQUEST+FROM=2、FATAL=0
  - 判定 ✔：轮3 修订判定点全中——①完结后子会话转录保留 status check 用户消息+完结回复,不回落空态,标题不回 Chat;②重出重入转录持久（status check 交换仍在）;③第二次发送仍走 subagents/prompt;④停止键链路完整（interruptByParent 受理+回发送键）。轮2 遗留「完结回复不渲染+回落空态+重入不持久」实测修复（098e89a2）
  - 证据：/tmp/acc310_shots/r3_a8_*.png（fabmenu2/agentsheet2/subsession1/status_poll1-3/status_done/scrolled/reenter/essay_sent/after_stop/faildlg）、r3_a8_*.xml；/tmp/acc310_logs/a8r3.log

### A9 ④ 轮次台账行
- 前置：A4-A8 已产生多轮
- 操作：dump 转录轮次边界 → 找台账行（轮次 N·时长·步骤·工具）→ tap 展开 → dump
- 期望：已完结轮次气泡下方台账行在场;默认折叠一行;展开=工具 chips+明细;流式中轮次无台账
- 判定：折叠+展开两帧 dump → ✔（**流式中轮次无台账行**一并纳入判定:若 A5/A8 仍有流式窗口,dump 该轮无台账行;无窗口则该子断言记为未观测附注）
- 实测记录：✔（2026-09-05 16:01:19）【父会话,轮次 1-6 全链】
  - 台账行在场（默认折叠一行式）：本轮滚动窗见「轮次 6 · 0ms · 1 步 · 0 个工具」「轮次 5 · 0ms · 1 步 · 0 个工具」「轮次 4 · 0ms · 1 步 · 0 个工具」「轮次 3 · 26.5s · 5 步 · 2 个工具」（下滑逐帧 dump a9_scroll1-3.xml）——N·时长·步骤·工具 四元格式 ✔
  - **展开帧一（0 工具轮）**：tap 轮次 6 行 (570,2206) → dump 节点 58→90：行下新增明细「工具调用: 0」+「令牌: 466」（a9_expanded.xml;截图 a9_folded→a9_expanded.png）
  - **展开帧二（2 工具轮）**：滚至轮次 3 行 (579,1470) tap → dump 节点 77→91：**工具 chip「run_code」在场**@ (108,1388) + 明细「工具调用: 2」（a9_t3_expanded.xml;a9_t3_folded→a9_t3_expanded.png）——chips+明细 ✔
  - 流式中无台账子断言：**✔（B1 忙碌窗实测回填,16:04）**——流式两帧（busy_empty 16:02:18/busy_text 16:02:31）`轮次` 文本计数均=0,完结帧（16:03:22）=1（轮次 7 出现）——流式中轮次无台账行成立
  - 顺带观测（A8 域附记已回写）：轮次 3 展开区下方有合成行「Agent 4ca8416a-82a9-4602-aafe-a92feb5341ce sent a message:」+ 正文「Count from 1 to 10 completed…Done: counted to 10」——A7 子代理报告在父转录渲染
  - FATAL=0（a9_after.log）

## B. 回归
### B1 单键/composer 常规
- 前置：A 组完成（存在可复用忙碌窗口——如 A8 忙碌窗口已过,发送 `write%sa%slong%spoem%sabout%srivers` 制造）
- 操作：忙+文本 dump（单键保持）;普通消息收发一轮
- 期望：忙+文本=单发送键（#326 交叉）;常规收发正常
- 判定：chat-send=1/chat-stop=0 + 回复可见
- 实测记录：✔（2026-09-05 16:04:13）【父会话】
  - 制窗：16:02:08 发送 `write a long poem about rivers` → 10s 后 busy 帧一（b1_busy_empty.xml）：**composer=「停止」×1**（忙+空稿=停止键）
  - **忙+文本断言**：busy 中输入 `hi` → 帧二（b1_busy_text.xml 16:02:31,输入框 text=hi 逐字核对）：**composer=「发送（排队）」×1,停止键=0**——chat-send=1/chat-stop=0 ✔（与 #326 V012 busy 排队语义交叉一致;按钮 desc 变体「发送（排队）」为本次新观测形态）
  - 长诗完结（~16:03:22,b1_poll2.xml）：转录尾部诗正文在场（「I. I was born where the snow forgets its name…— finis —」）+轮次 7 台账行;`hi` 草稿未被误发（仍在输入框,未清先 DEL×2）
  - 常规收发一轮：16:03:47 发送 `reply with one short sentence` → 16:04:05 回复可见「Rivers teach us that letting go is just another way of flowing onward.」+思考行+轮次 8 台账;composer 回「发送」✔
  - FATAL EXCEPTION=0（b1_after.log）;证据：b1_busy_empty/text.xml、b1_busy_text.png、b1_poll2.xml、b1_round.xml
### B2 收尾健康
- 前置：B1 完成
- 操作：进出会话×2;logcat 总检（排除 uiautomator 噪音归属）
- 期望：无崩溃无断连
- 判定：FATAL=0
- 实测记录：✔（2026-09-05 16:06:06）
  - 进出会话×2：轮 1 = 会话列表顶部本会话行「reply with shortsgreeting · /home/leo-tkp/workspace · 9月 5, 16:03」tap (536,620) 进入（16:05:38,转录台账在场+composer「发送」）→ 返回钮 (84,230) 出至列表（搜索会话在场）16:05:46;轮 2 = 同行再进（16:05:52）→ 再出（16:06:00）——两轮均无异常 UI/无崩溃
  - logcat 总检（b2_after.log,自 16:04:14 logcat -c 起）：**`FATAL EXCEPTION|E AndroidRuntime` 计数=0**;`SseConnManager.*3080.*disconnect` 计数=0——被测连接（prod-3080）无断连;（背景噪音如实记录:Host-4199 卡自动重连 ConnectException 持续在场,属 4199 服务器未开的既有配置卡,非本卡被测对象,与 #326 记录同形态）
  - uiautomator 噪音归属排除说明：全程 AndroidRuntime D 级行均为 uid 2000 `com.android.commands.uiautomator.Launcher` 启动噪音,不计入
  - 轮2 2026-09-05 17:34:03（B2' 收尾健康重检）判定：✔
    - 进出会话×2：轮1 = 关 AgentSheet+BACK 落回 A8 父会话（转录轮次 5=子代理停止通知 wrap-up「stopped before it finished」+台账在场,17:33:14）→BACK 出至列表（17:33:39）;轮2 = tap「hi and wait」会话行进入（17:33:56,标题/转录在场）→BACK 出（17:34:01）——两轮均无异常 UI/无崩溃
    - logcat 总检（/tmp/acc310_logs/b2r2.log,自 17:14 logcat -c 起 234304 行）：**`FATAL EXCEPTION|E AndroidRuntime` 计数=0**;`SseConnManager.*3080.*disconnect` 计数=0——prod-3080 被测连接无断连
  - 轮3 2026-09-05 18:36–18:39（B2'' 收尾健康重检）判定：✔
    - 进出会话×2：轮1 = 停止后子会话 BACK×2 出至列表 → tap A8 父会话行「please start a background subagent」（18:35:4x 进入,转录轮次 4 wrap-up「subagent bd5a33c9 marked as stopped before finishing…Happy to start another subagent」+composer「发送」在场）→返回钮 (84,230) 出至列表;轮2 = tap「我们安装了idlefix 插件了吗？」行进入（18:38,转录在场+composer「发送」）→返回出至列表——两轮均无异常 UI/无崩溃
    - 插曲（如实记录,执行侧导航失误非 app 异常）：进出过程中一次误触导航至服务器页（Host-4199 卡列表）+一次 BACK 退到系统翻译 App,经 am start 拉回+prod-3080 卡「会话」钮回列表后流程继续,每帧 dump 均无崩溃
    - logcat 总检（/tmp/acc310_logs/b2r3.log,239749 行）：**`FATAL EXCEPTION|E AndroidRuntime` 计数=0**;`SseConnManager.*3080.*disconnect` 计数=0——prod-3080 被测连接无断连（含 18:1x adb 掉线重连场景,SSE 自动回补无断连记录）

## 人工验收清单（UIUX;域汇总登记）
- [ ] @ 候选弹窗两型行观感/会话徽标;mention 串确认态高亮(后续迭代项)
- [ ] 反馈钮轻量度/高亮反馈/撤销 snackbar
- [ ] PlanChip 形态/plan-review 计划正文呈现
- [ ] 台账行弱化度/展开 chips/轮次号平移感知
- [ ] 子会话 composer 解禁/one-shot 提示行/续聊+父址中断体验

## 执行汇总（2026-09-05 15:29–16:06,执行员=纯净验收子代理）

**终态：✔×9（P0/P1/A4/A5/A6/A7/A9/B1/B2）· ✘×3（A1/A3/A8）· BLOCKED×1（A2）**;全程单设备 `adb -s 192.168.110.239:5555`,被测包 versionName=0.3.0/lastUpdateTime=2026-09-05 15:24:46（P0 核对一致）,后端 DSH prod-3080（全程 reverse tcp:3080 在位,未跑 debug-entry.sh 避 4199 重指坑）,证据存 /tmp/acc310_logs/ 与 /tmp/acc310_shots/。

- **P0 ✔**（15:29:55）versionName=0.3.0、lastUpdateTime=2026-09-05 15:24:46 逐字符合。
- **P1 ✔**（15:31:45）prod-3080 卡连接（已连接·DSH）;会话列表 ≥1 其他会话（g2-once 等）;新建 workspace 会话成功（完全访问档,无只读药丸）;宿主 touch acc310-mention-probe.md 成功+同目录 2 个既有 a* 文件;FATAL=0（30 行 AndroidRuntime 全为 uiautomator uid2000 噪音）。
- **A1 ✘**（15:34:20）`@a` 输入成功（input text '@a' 可用）,但候选弹窗未呈现（dump 24 节点无弹层×2 次+像素通道互证）;logcat 铁证 `DraftInputDelegate: Mention candidates failed for 'a': resume failed for session "": SessionQueryError: failed to observe session "": cannot encode an empty path segment`——候选管线已触发但整体失败于空 session id;服务器匹配文件前提已满足,排除「无匹配文件」解释。
- **A2 BLOCKED**（15:35:00）前置不成立（A1 无会话候选行可 tap）,按本项规则记 BLOCKED;mention 串插入链路未获验证。
- **A3 ✘**（15:37:11）`@"a` 引号输入需 device 侧脚本 push 执行（adb 直发引号在 shell 拼接层断裂,替代法已记录）;quoted 查询串 '"a' 已到 DraftInputDelegate,同一 SessionQueryError 失败;文件行 0——「只文件」期望无从呈现。
- **A4 ✔**（15:46:00）反馈钮在场（评价为有帮助/没帮助）;tap 👍 → `PUT /api/messageFeedback/put` 受理+图标像素高亮 (47,100,127) 深蓝;同向再 tap → `DELETE /api/messageFeedback/delete` 受理+回灰 (123,128,131)——wire+像素双链闭环;回复后自动 list 拉既有评分态。
- **A5 ✔**（15:49:25）/plan 命令链（命令建议行→插入→发送）→ PlanChip 出场;任务消息后 ~27s plan-review 卡到达（待你回答/SINGLE/问题「Approve this plan and leave plan mode?」/计划正文 # Plan: Create Workspace README/选项 Approve+Keep planning+输入答案/忽略·提交）;提交 Approve 后卡消失、agent 继续（Approved — here's the plan… Shall I proceed?）、轮次 2 台账在场、PlanChip 随批准自动离场。
- **A6 ✔**（15:50:24）chip 随批准自动离场后按同命令链复建,tap chip → 3s dump `text="Plan"`=0——/plan off 生效;两路退出（批准自动/chip-tap）均实测成立。
- **A7 ✔**（15:55:17）60s 窗口内子代理出现（FAB 菜单五药丸 TODO/智能体/目标/Shell/排队队列 → AgentSheet「智能体 (1)」条目「Slowly count to 10」）;子会话 ChatRoute 打开（标题 Count slowly from 1 to,cwd workspace）,**composer 在场=continuable 形态**;FAB 定位含 IME 开态坐标偏移事实。
- **A8 ✘**（15:58:49）①`status check` 发送受理 ✔（`POST /api/subagents/prompt`+ChatSendDelegate continuable prompt to subagent 4ca8416a）;②回复 3min 不可见（子会话转录恒空态,composer 恒「发送」）;③忙碌制造失败——第二次发送路由走 `/api/session/prompt`（非 subagents/prompt）被服务器拒（wire=session/agent-busy,"owned by subagent routing, use subagent delivery"）→「发送失败」对话框;④停止分支不可达（无忙碌渲染态,interruptByParent 未获观测）;附记：A7 计数报告以「Agent 4ca8416a…sent a message」渲染在父转录,status check 回复两侧均未见。
- **A9 ✔**（16:01:19,流式子断言 16:04 回填）台账行折叠式在场（轮次 3-6:N·时长·步骤·工具）;展开两形态——0 工具轮（工具调用: 0+令牌: 466）与 2 工具轮（**工具 chip run_code**+工具调用: 2）;流式两帧 轮次=0、完结帧=1（流式中无台账成立）。
- **B1 ✔**（16:04:13）忙+空稿=「停止」×1;**忙+文本=「发送（排队）」×1/停止=0**（#326 交叉一致,desc 变体「发送（排队）」为新观测形态）;长诗完结正常;常规收发一轮 ✔（回复+轮次 8 台账）。
- **B2 ✔**（16:06:06）进出会话×2 无异常;`FATAL EXCEPTION|E AndroidRuntime`=0;prod-3080 SSE 无断连（4199 背景重连噪音为既有配置卡,非被测对象）。

**失败聚类（供 parent 裁决,执行员不做根因分析）**：①A1/A3/A2 同源——新建未发首条消息的会话上 @ 候选查询带空 session id 失败（SessionQueryError: cannot encode an empty path segment）,文件/会话候选均无; checklist 将 @ 项排在首条消息前为既定状态,该状态下功能不可用为实测事实。②A8——同一子会话 composer 两次发送端点路由不一致（首 subagents/prompt ✔/次 session/prompt ✘ 服务器拒）,且子代理回复不渲染于子会话 ChatRoute（A7 报告走父转录合成行）。

**降级路径使用情况**：A2 按项内规则 BLOCKED 记原因;A5 审阅卡未触发降级（plan-review 到达,✔ 成立）;A7 子代理出现（未走 BLOCKED-模型行为）;A8 为 continuable 形态（未走 one-shot BLOCKED-形态),其停止分支因无忙碌窗口未达。

## 轮2 执行汇总（2026-09-05 17:14–17:34,执行员=纯净验收子代理 轮2 重跑）

**终态：P0' ✔ · A1 ✔ · A2 ✔ · A3 ✘ · A8 ✘（三判定点中两点成立）· B2' ✔**;全程单设备 `adb -s 192.168.110.239:5555`,被测包 versionName=0.3.0/versionCode=1788599536/lastUpdateTime=2026-09-05 17:12:54（P0' 核对一致,较轮1 versionCode=1788593037 为新构建）,后端 DSH prod-3080（reverse tcp:3080 全程在位,未跑 debug-entry.sh 避 4199 重指坑）,证据存 /tmp/acc310_logs/*r2.log 与 /tmp/acc310_shots/*r2*。

- **P0' ✔**（17:14:03）versionName=0.3.0、lastUpdateTime=2026-09-05 17:12:54 逐字符合,不停卡。
- **A1 ✔**（17:16:21,轮1 ✘→✔）前置修订生效：有历史会话（发 `hi and wait` 得回复+轮次 1 台账）上敲 `@a` →弹窗呈现,文件行 3（dsh-keepalive/ai-dev-guide、dsh-keepalive/docs/archive、ASR语音识别模型选型调研报告.md）+会话行 5（含同工作区徽标）;轮1 SessionQueryError（空 session id）不复现——6290102b 簇1 修复实测成立。
- **A2 ✔**（17:17:22,轮1 BLOCKED→✔）tap 会话候选行 `count from 1 to 50` →输入框插入 `@[count from 1 to 50](dsh-session:…) ` 规范串（@ 开头/dsh-session:/尾随空格/trigger 替换四断言全中）——mention 串插入链路首次实测成立。
- **A3 ✘**（17:18:27–17:24:18,轮1 ✘→✘）`@\"a`（device 侧脚本法）后无弹窗（两帧 dump+加字符重触发+像素带互证三重一致）;控制组同会话同态 `@a` 弹窗完整复现（会话 5+文件 3）——差异锁定在引号形态;文件行 0,「只文件」期望无从呈现（与轮1 同口径记 ✘）;logcat 无 Mention candidates failed 行（轮1 quoted 查询报错亦不复现）。
- **A8 ✘**（17:24:37–17:32:50,轮1 ✘→✘ 但三缺陷中两项实测修复+一项部分改善）新会话新子代理全链：①**两次子会话发送均走 `POST /api/subagents/prompt`**（17:27:45 status check+17:30:24 essay,wire+ChatSendDelegate 双证;logcat 内唯一 session/prompt=父会话首条）——轮1「二次发送走 session/prompt 被服务器拒+发送失败框」实测修复;②**停止键行为完整成立**（流式停止键在场→tap→`POST /api/subagents/interruptByParent` 受理→Interrupted via parent→force-complete→composer 回发送键;停止后 AgentSheet 清空为「智能体 (0)」）——轮1「停止分支不可达」实测修复;③子会话渲染部分改善：转录非空（轮次 1 台账）、活动期正文（思考行+步骤正文+计时行）、用户消息气泡（essay 帧）均在场——但 **status check 完结回复未在子会话渲染**（内容经父转录轮次 4 呈现）,且完结/中止后子会话路由**自发回落空态 Chat**、重入后仅剩轮次 1 台账不持久——按「子会话内回复可见」口径未成立,综合记 ✘。
- **B2' ✔**（17:34:03）进出会话×2（A8 父会话+hi and wait 会话）无异常;自 17:14 起 234304 行 logcat `FATAL EXCEPTION|E AndroidRuntime`=0;prod-3080 SSE 无断连。

**轮2 失败遗留（供 parent 裁决,执行员不做根因分析）**：①A3——quoted `@\"` 形态触发后无弹窗（同会话同态非引号形态正常,文件行 0,无候选查询失败日志可观测）;②A8——子会话完结回复不渲染（内容走父转录）+轮次完结（完结与中止两路均复现）后子会话路由自发回落空态 Chat+重入后 status check 交换不持久（仅轮次 1 台账在场）。

**降级路径使用情况**：轮2 无 BLOCKED 项;A2 前提成立未走 BLOCKED;A8 停止分支经 essay 忙碌窗实测到达（轮1 未达）。

## 轮3 执行汇总（2026-09-05 18:02–18:39,执行员=纯净验收子代理 轮3 重跑）

**终态：P0'' ✔ · A3 ✔ · A8 ✔ · B2'' ✔**;全程单设备 `adb -s 192.168.110.239:5555`,被测包 versionName=0.3.0/versionCode=1788602465/lastUpdateTime=2026-09-05 18:01:39（P0'' 核对一致,较轮2 versionCode=1788599536 为新构建,含 4b463eec+098e89a2 两修复）,后端 DSH prod-3080（reverse tcp:3080 在位——中途 adb 掉线一次,重连后恢复 reverse,SSE 自动回补无事件损失）,证据存 /tmp/acc310_logs/*r3.log 与 /tmp/acc310_shots/*r3*（83 文件）。

- **P0'' ✔**（18:02）versionName=0.3.0、lastUpdateTime=2026-09-05 18:01:39 逐字符合,不停卡。
- **A3 ✔**（18:09–18:12,轮1 ✘→轮2 ✘→轮3 ✔）有历史会话（`hi and wait` 得回复+轮次 1 台账,18:09:10 session/prompt 受理）上 `@\"a`（device 侧脚本法）→**弹窗出现且只有文件行**：文件行 ×10（a* 前缀匹配,图标 desc=「文件」）+会话图标行=0;控制组 `@a` 两型并存复现（会话 5+文件 3,含同工作区徽标）——差异锁定引号形态,quoted=只文件语义正确;`Mention candidates failed`=0,FATAL=0——4b463eec（quoted query 去引号）修复实测成立。
- **A8 ✔**（18:12–18:35,轮1 ✘→轮2 ✘→轮3 ✔）新会话新子代理（bd5a33c9,parent session-c6de9470）全链：①子会话打开即渲染转录正文（思考行+任务推理+Run code+轮次 1 台账,轮2 仅台账行→改善）;②status check 发送走 `subagents/prompt`（18:31:17 REQUEST+FROM+continuable prompt 日志;首次 18:28:16 REQUEST 后 120s 响应超时弹「发送失败」框,重发即成——瞬态,服务器侧实际已受理,如实记录）;③**完结后转录保留 status check 用户消息（用户 · 18:31:18 气泡）+完结回复**（Done — counted slowly to 10./Result also delivered to parent agent…+轮次 2 台账）,**不回落空态、标题不回 Chat**;④重出重入转录**持久**（用户气泡+完结正文+台账均在,轮2 重入不持久→已修复）;⑤essay 第二次发送仍走 `subagents/prompt`（18:34:56 REQUEST+FROM）;⑥停止键链路完整（流式停止键在场→tap→`interruptByParent` REQUEST+FROM+Interrupted via parent+Aborted+force-complete→composer 回发送键,标题未回落）——098e89a2 双根因（host/session-removed 降级 Idle 不删转录+history fold 收编）修复实测成立;FATAL=0。
- **B2'' ✔**（18:36–18:39）进出会话×2（A8 父会话轮次 4 wrap-up 在场+idlefix 会话）无异常;239749 行 logcat `FATAL EXCEPTION|E AndroidRuntime`=0;`SseConnManager.*3080.*disconnect`=0——prod-3080 无断连（含 adb 掉线重连场景）。

**轮3 执行侧插曲（如实记录,均未影响判定）**：①发送键坐标依赖 IME 开合态（IME 开= (1086,1616)/收= (1086,2538)）——A3 首发因 IME 开态坐标错位 tap 落键盘区未发出（chat-input 残留证实）,重定位后发出;②观测中 adb 掉线一次（device not found）,disconnect/connect 重连+恢复 reverse 后 app SSE 自动回补,子代理任务与事件流无损;③A8 status check 首发遇 120s HttpRequestTimeoutException（subagents/prompt）,「发送失败」框确定后草稿回填,重发即成（服务器侧 msgs 持续增长证实首次已被受理——首发的轮内正文计入轮次 2）;④B2 进出中一次误触导航至服务器页、一次 BACK 退到系统翻译 App,am start 拉回后续行,每帧 dump 无崩溃。

**轮3 无失败遗留**：A3/A8 两项根因修复构建上按轮3 修订判定点全部实测成立;降级路径使用情况=无 BLOCKED 项、无替代性操作。
