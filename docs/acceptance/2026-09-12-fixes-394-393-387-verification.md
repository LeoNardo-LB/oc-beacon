# 修复复验：#394 / #393 / #387（模拟器 emulator-5554）

- 卡片：#394（跳转 5s 高亮 assistant 目标）、#393（搜索命中角色交错）、#387（无 source.kind 注入折叠）
- 日期：2026-09-11（会话时间；文件名按任务给定 2026-09-12）
- 设备：emulator-5554（sdk_gphone64_x86_64，Android 16/SDK36，1080x2400@420dpi，宿主无 DISPLAY）
- 构建：dev flavor（app/build/outputs/apk/dev/debug/app-dev-debug.apk，构建时间 09-11 22:06）
  - 安装：adb -s emulator-5554 install -r … → Success（未卸载，dev versionCode 时间戳覆盖）
  - 已装包 md5 = db9592b834c63403768578c6a5075d88（与任务给定值一致，pm path + md5sum 实测）
  - 代码版本：HEAD 0a87e8d4；修复提交 99f6430f + 3a663e8e（未运行 Gradle，未改任何产品代码）
- 服务：opencode V2 @4199（debug_server_type opencode，user=opencode，debug_name E2E-OC）
  - adb reverse → tcp:4199 tcp:4199、tcp:3080 tcp:3080
  - App locale：cmd locale get-app-locales dev.leonardo.ocbeacon.dev --user 0 = [zh-CN]
- 观测通道：uiautomator dump（文本 + bounds）+ Room DB 直查（宿主 sqlite3）+ logcat
- 证据目录：/tmp/acc-fix/（dump / logcat / DB 快照）

---

## 前置校验

| 项 | 结果 | 证据 |
|---|---|---|
| APK 安装（-r，不卸载） | PASS | Success |
| 已装包 md5 == db9592b834c63403768578c6a5075d88 | PASS | db9592b834c63403768578c6a5075d88  base.apk |
| adb reverse 4199/3080 | PASS | host-21 tcp:3080 tcp:3080 / host-21 tcp:4199 tcp:4199 |
| App 语言 zh-CN | PASS | [zh-CN] |
| debug intent 起步到会话列表 | PASS | /tmp/acc-fix/00-initial.xml |

---

## 1) #393 搜索命中角色交错 — PASS

### 操作
- 拉库：adb exec-out run-as dev.leonardo.ocbeacon.dev cat databases/ocbeacon.db > /tmp/acc-fix/ocbeacon2.db（连同 -wal/-shm）。
- 选词：在 FTS 表 message_fts 用 bm25 复算各候选词命中分组，要求「同一会话同时命中 user 与 assistant」且首屏两角色可同现。选定 test。
- 进搜索 UI：点搜索框 → input text test（ASCII，规避中文 IME）→ 角色过滤保持默认「全部」→ dump。
- 被父代理建议的 the / 的 不采用：其两角色命中集中在 session-94365bc9…（DSH 会话），该会话已被 archive_buckets 记录、被 SessionListScreen.archivedIds 从内容命中区过滤（实测 DSH 搜索首组并非该会话），首屏不会出现两角色。详见「附注」。

### 观测
- 命中词 test 的首组 = ses_fea2d6a60ffezM5isa95li4j4A（标题「验收测试会话AB」）。
  - DB（去重前 100 行、排除归档）：该会话 message_fts MATCH '"test"' 去重后 role 计数 = assistant 8 / user 11 / system 1（另一快照为 u8/a4，随 live 会话增长而变）。
  - 组内 rank 序（修复前顺序）首 4 = assistant, user, user, user；交错序（修复后）首 4 = user, assistant, user, assistant。
- 首屏 dump（/tmp/acc-fix/393-final.xml，消息匹配区 heightIn(max=240.dp)，区域 y∈[496,1126]）：
  - 命中行文本顺序：用户 · 智能体 · 用户 · 智能体 · 用户（交替，即 interleaveSearchHits 输出）。
  - 首屏行数：用户 3 行（y=756/913/1070）、智能体 2 行（y=835/992）。
  - 断言：text="用户" 与 text="智能体" 同时出现 → PASS。
- 组头「12 条消息」与选中会话计数一致；dump 中可见「消息匹配」「筛选」及组标题。

### 判定
PASS。首屏同时含两角色；且 UI 呈现的 u,a,u,a… 交替序与 DB 直算的 interleaveSearchHits 输出一致，直接证明交错逻辑生效。

### 证据路径
- /tmp/acc-fix/393-final.xml（终态首屏 dump）
- /tmp/acc-fix/393-search-test.xml（滚动/点击前快照）
- /tmp/acc-fix/393-search-DSH.xml（DSH 词对照：首组「DSHWeb Token问题咨询」，用户+智能体同现）
- /tmp/acc-fix/ocbeacon2.db(+wal/shm)（FTS role 计数）
- 复算 SQL（宿主）：
  SELECT f.sessionId, f.messageId, f.role, bm25(message_fts) AS score
  FROM message_fts f JOIN cached_messages mm ON mm.id=f.messageId
  WHERE message_fts MATCH '"test"' ORDER BY score LIMIT 100;

---

## 2) #394 跳转 5s 高亮（assistant 目标） — FAIL

### 操作
- 在 #393 的 test 搜索结果中，点「智能体」命中行（角色标签 智能体，内容「收到消息：test3 / test / test」，命中 messageId = msg_02ac03bff001xNnNxA40s2okwL，assistant）。
- 点击前 adb logcat -c；点击后轮询 logcat ≤15s，断言 Highlight target msgId= → Highlight set key=t_… → ~5s 后 Highlight clear key=。
- 对照组：同一结果集中点「用户」命中行（messageId = msg_02ac03be9001MY7GvHczuHpuvj）。

### 观测
- 用户目标（对照组）完全符合预期：
  D/Highlight: target msgId=msg_02ac03be9001MY7GvHczuHpuvj
  D/Highlight: set   key=u_msg_02ac03be9001MY7GvHczuHpuvj
  D/Highlight: clear key=u_msg_02ac03be9001MY7GvHczuHpuvj   （set→clear 间隔 5.001s）
- 智能体目标：全程无任何 Highlight 日志（target / set / clear 均缺失，grep -c Highlight = 0）。logcat：
  D/ChatPaging: jump: Preparing 开始 msg=msg_02ac03bf
  D/ChatPaging: jump: 进入测量 msg=msg_02ac03bf idx=1
  D/ChatPaging: jump: 底部定位 idx=1
  D/ChatPaging: jump: 底部定位后可见=[[0:u_msg_03635d7ed…, 1:t_msg_02ac03bff001xNnNxA40s2okwL, 2:u_msg_02ac03be9…, …]]
  D/ChatPaging: jump: 重定位 idx=1（null=2/3） ×14
  D/ChatPaging: jump: 布局稳定 超时      ← TimedOut(measuring) → Failed，永不 Displayed
  D/ChatPaging: jump: 状态机终点——autoLoad 解锁
- 目标项确实在 displayItems 且已渲染（可见列表 index=1，key = t_msg_02ac03bff001xNnNxA40s2okwL，与目标 msgId 同锚点）；失败原因是跳转查找键前缀不匹配：
  - ChatMessageList 的 initial-jump 路径（pendingJumpTarget effect，ChatMessageList.kt:1049-1067）对所有命中统一调用 jumpController.jumpTo(target, index)；
  - JumpNavigationController.jumpTo 固定 targetKeyPrefix = "u"（:252-255），于是查找键 = u_<assistantMsgId>；
  - 而渲染侧 Lazy item key = chatEntryKey(...)，assistant 目标 = t_<turn 组首条消息 id>（ChatMessageList.kt:2285-2291）。二者前缀不一致 → findJumpTargetItem 永远 null → 5s 超时。
- 因此「高亮键同源」修复（#394 的 chatEntryKey 统一）本身正确（若跳到 Displayed，highlightedTurnKey 会算出 t_msg_02ac03bff… 并命中），但搜索命中→assistant 的跳转在到达 Displayed 之前就失败，高亮观测点无法触发。

### 父代理要求的三项诊断证据
1. 点的是哪一行：是「消息匹配」区的内容命中行（角色标签 智能体），不是会话级行。
   - 该行 bounds = [42,792][1038,870]（点击坐标 (500,830)）；同区「用户」行为 [42,713][1038,792]。
   - 点击走 onNavigateToChat(sid,false,hit.messageId)，messageId 非空 = msg_02ac03bff001xNnNxA40s2okwL（DB 实测 assistant 消息，正文匹配「收到消息：test3 / test / test」）。
   - 会话级行在命中区下方（y≥1146，HorizontalScrollView），本次未点。
   - 证据：/tmp/acc-fix/394-search-test-rows.xml、394-assistant-click-ui.xml。
2. 是否出现 chat_locate_message_not_found（「未找到该消息」）Snackbar：未出现。点击后 dump（T+3s）显示会话已打开且目标消息可见（「收到消息：test3…」位于转录区），无 Snackbar 文本。即失败不是「消息不在 displayItems」，而是跳转键前缀不匹配。
   - 证据：/tmp/acc-fix/394-assistant-click-ui.xml。
3. logcat jumpToMessage: 与 ChatPaging 行：见上（完整文件 /tmp/acc-fix/394-assistant-highlight.txt）。
   - 说明：搜索命中经 initialJumpTarget/pendingJumpTarget 通道，直接调 jumpController.jumpTo，不打印 jumpToMessage:（该行仅快速定位 tap 路径产生）；用户对照组同样无 jumpToMessage:，但有 Highlight。

### 判定
FAIL（按任务判据：点「智能体」命中行后未出现 Highlight target msgId=，更无 set key=t_ / clear key=；grep -c Highlight = 0）。根因是 initial-jump 对 assistant 目标使用 u_ 前缀，未复用 chatEntryKey 的 t_<turn 锚点> 公式——建议修复点：ChatMessageList 的 pendingJumpTarget effect / jumpToMessage 按角色选择 jumpTo(u) 或 jumpToTask(t)，或在 jumpTo 内推导 turn 锚点键。

### 证据路径
- /tmp/acc-fix/394-assistant-highlight.txt（assistant 目标完整 logcat，0 条 Highlight）
- /tmp/acc-fix/394-user-highlight.txt（user 目标 target/set/clear 三连）
- /tmp/acc-fix/394-search-test-rows.xml、/tmp/acc-fix/394-assistant-click-ui.xml
- /tmp/acc-fix/ocbeacon2.db（命中 messageId 为 assistant）

---

## 3) #387 无 source.kind 注入折叠 — NOT-REPRODUCIBLE

### 操作
- DB 查询（.schema 实测字段）：cached_parts.text / type + cached_messages.role / payload；判定文本以 <system-reminder> 开头且整条闭合块。
- 另附加验证：定位到一个含无 source.kind 纯注入块的可打开会话「验收测试会话AB」并上滑至顶部，dump + logcat 观察 上下文注入 卡。

### 观测（DB 计数，快照 /tmp/acc-fix/ocbeacon2.db）
| 查询 | 结果 |
|---|---|
| role='user' AND text LIKE '<system-reminder>%' | 10 |
| 其中 payload LIKE '%injectionKind%'（即带 source.kind，走 #385 路径） | 10 |
| 其中 payload NOT LIKE '%injectionKind%'（#387 目标：无 source.kind） | 0 |
| 非 user 且无 injectionKind 的纯 <system-reminder> 块 | synthetic 5 |
- 10 条 user 纯注入块的归属会话全部是 DSH session-… 形（session-94365bc9… 等 5 个），当前 opencode 连接下不在会话列表；且均带 kind（labels：agent-instructions / skill-catalog / plugin / agent-message / goal / …）。
- 补充验证：ses_fea2d6a60ffezM5isa95li4j4A（「验收测试会话AB」）内含 5 条 synthetic 纯 <system-reminder> 块且无 source.kind（理论可触发嗅探分支）。打开该会话、连续上滑 30 次至顶部后：dump 中 无 text="上下文注入"（也无 工作区指令/技能目录/插件配置），logcat InjCard 全为 kind=null，grep -c kind=context = 0。说明这些 synthetic 注入未进入渲染（服务端历史不含 / 本地非 live）。
- 与修复记录一致：docs/journal/2026-09-11-post-refactor-backlog-triage.md §#387 已载「扫描本机 4199 全部 50 会话 REST 消息 0 条 system-reminder」，live 复现待注入源。

### 判定
NOT-REPRODUCIBLE。当前 DB 中无任何「user 消息 / 无 source.kind」的纯 <system-reminder> 缓存样本（0 条），#387 新增嗅探分支无 live 载体；10 条同类 user 文本均带 injectionKind，由既有 #385 source.kind 路径处理。仅单测（SystemInjectionTest）覆盖，符合任务「无 live 样本则记 NOT-REPRODUCIBLE」的预期。

### 证据路径
- /tmp/acc-fix/ocbeacon2.db（计数）
- /tmp/acc-fix/evidence-summary.txt
- /tmp/acc-fix/387-scrolltop.xml、/tmp/acc-fix/387-scrolltop2.xml（上滑至顶部 dump，无「上下文注入」）

---

## 4) 回归：crash buffer — PASS

### 操作
adb -s emulator-5554 logcat -d -b crash | grep -i ocbeacon

### 观测
- 输出为空；logcat -d -b crash | wc -l = 0。
- 全程 App 进程存活（pid 23555 贯穿各步，无重启/无 FATAL）。

### 判定
PASS（无崩溃记录）。

### 证据路径
- 命令输出（(no crash lines)；crash buffer 0 行）

---

## 汇总

| item | 判定 | 证据 | 一句话 |
|---|---|---|---|
| #393 搜索角色交错 | PASS | /tmp/acc-fix/393-final.xml · ocbeacon2.db | test 首组首屏同时出现「用户 3 行 + 智能体 2 行」且呈 u,a,u,a,u 交替，交错修复生效。 |
| #394 跳转高亮（assistant） | FAIL | /tmp/acc-fix/394-assistant-highlight.txt · 394-user-highlight.txt | 点「智能体」命中行无任何 Highlight（grep -c=0）；initial-jump 用 u_ 前缀查 assistant，Lazy key 为 t_<turn 锚点> → 5s 超时；用户目标对照组 target/set/clear 三连正常。 |
| #387 无 source.kind 注入折叠 | NOT-REPRODUCIBLE | ocbeacon2.db · evidence-summary.txt · 387-scrolltop*.xml | user 纯 <system-reminder> 缓存 10 条但 10/10 带 source.kind，#387 目标（无 kind）0 条，仅单测覆盖。 |
| 回归 crash | PASS | crash buffer 0 行 | logcat -b crash grep ocbeacon 无输出，进程存活。 |

## 附注：为何不使用父代理建议的 the / 的
the/的 的两角色命中主要落在 session-94365bc9-728e-4684-9128-02db5b589745（归档会话）。SessionListScreen.kt:386-389 会用 content.archivedSessions 过滤内容命中；实测搜索 DSH 首组为 ses_f74f6626…（「DSHWeb Token问题咨询」）而非 session-94365bc9，证明归档过滤生效。按 bm25 复算，the/的 在可展示的 top 组内只剩 assistant 行，首屏不满足「两角色同现」，故改用 test（同会话 u/a 双命中）。

## 纪律声明
- 未修改任何产品代码；未运行 Gradle。
- 设备串行独占；所有断言均留 XML dump / logcat / DB 快照。
- #394 判定为 FAIL 属据实记录，未臆造成通过。
