# #405 标题带下滑收起修复——复验（**已被内容根方案取代，见 [2026-09-12-405-content-root.md](2026-09-12-405-content-root.md)**）

> 状态说明（2026-09-12）：本文件记录的是把手势块挂在**标题 Row** 的中间版本（commit 730d5016）。该版本覆盖了标题带，但手柄下方 ~45px 空白带（y≈490–535）仍会下滑收起；后续改为挂**内容根 Column**（commit ae52aeba，函数 `sheetNonScrollableDragBlock`），复用同一证据目录。下方「补充观察」即为该残留带的发现过程。

- 日期：2026-09-12
- 执行：clean-context 复验员（模拟器 emulator-5554 独占；只读 + 设备操作，未改产品代码、未跑 Gradle）
- 关联：#405（修复）/ #392 / #379（抽屉手势纪律）
- 修复提交：`730d5016` fix(ui): #405 快速定位抽屉标题带下滑不再收起——指针层隔离非列表头部
- 证据目录：[docs/acceptance/2026-09-12-405/](2026-09-12-405/)

## 结论

| 项 | 结果 |
|---|---|
| 标题带 y=600/640/670/700 向下快拖 | **全部 VISIBLE（PASS）**——修复前基线（#392，旧 APK）此四点均 CLOSED |
| 手柄 y=477 向下拖 | **CLOSED（PASS）**——收起能力保留 |
| 点外 scrim | **CLOSED（PASS）** |
| 列表内容 fling 1100→1950 | **VISIBLE（PASS）**——无回归 |
| Close 按钮 | **CLOSED（PASS）**——点击未被误伤 |
| V2 抽查（标题带 600/670/700 + 手柄 477） | **与 V1 一致（PASS）** |
| 回归：列表 Q 编号 / crash buffer | **正常** |
| **总判定** | **PASS**（另见「补充观察」一处非既定点窄带残留，不属本次判定点） |

## 环境与指纹

| 项 | 值 |
|---|---|
| 设备 | emulator-5554，1080×2400，density 420（1dp≈2.625px），locale zh-CN |
| APK | dev.leonardo.ocbeacon.dev，本地与已装 pulled base.apk md5 均为 `c80a614d57fe0110efcf600baa95224c`（install -r 覆盖，未卸载） |
| 已装时间 | lastUpdateTime=2026-09-12 04:19:12，versionName=0.3.0 |
| 二进制佐证 | 已装 APK `classes7.dex` 含符号 `sheetHeaderGestureBlock`（修复确在装机二进制内） |
| 工作区 | HEAD=`730d5016`（#405 修复提交），git status 干净 |
| V1 服务器 | http://127.0.0.1:4198，opencode 1.18.27；logcat `ApiVersionDetector: Detected V1 API` → wire=v1 |
| V1 会话 | title `v1-sheet-test`，id `ses_f6dfd4cf4ffetUi8aVNKYzXCrs`；/message = 16 条（13 user）→ jumpTargets=13 |
| V2 服务器 | http://127.0.0.1:4199，0.0.0-beta-19086；logcat `Detected V2 API` → wire=v2 |
| V2 对照会话 | title `数字1至300逐行输出`，jumpTargets 非空（可见 Q11–Q14…） |
| reverse | tcp:4198、tcp:4199 均已在位 |
| 启动方式 | `am start ... --es debug_url ... --es debug_username opencode --es debug_password <pwd> --es debug_server_type opencode` |

抽屉几何（V1/V2 dump 一致）：scrim `关闭工作表 [0,0][1080,459]` → sheet 顶边 y=459；dragHandle 459–490（12dp）；标题「快速定位」文本框 `[42,592][912,643]`；Close `关闭 [944,591][1007,643]`；列表首项文本顶 y≈706。修复源码：`QuickNavigateSheet.kt:150` 标题 Row 挂 `Modifier.sheetHeaderGestureBlock()`（`SheetGestures.kt:79`，pointerInput Main 趟仅消费 dy>0）。

## 判定口径

`check.py` 解析 uiautomator dump：scrim content-desc 含「关闭工作表」→ **SHEET_VISIBLE**，否则 **SHEET_CLOSED**（题述口径）。每个用例：确保关闭 → 点 FAB 打开 → `*_before.xml`（确认 VISIBLE）→ 手势 → +1.6s → `*_after.xml` 判定。

## 逐点判定表（V1 为主）

| # | 操作 | 期望 | 实测 | 判定 | 证据（docs/acceptance/2026-09-12-405/） |
|---|---|---|---|---|---|
| 1a | swipe 540 600→1400 dur100 | VISIBLE | VISIBLE | PASS | t1_y600_before.xml / t1_y600_after.xml |
| 1b | swipe 540 640→1440 dur100 | VISIBLE | VISIBLE | PASS | t1_y640_before.xml / t1_y640_after.xml |
| 1c | swipe 540 670→1470 dur100 | VISIBLE | VISIBLE | PASS | t1_y670_before.xml / t1_y670_after.xml |
| 1d | swipe 540 700→1500 dur100 | VISIBLE | VISIBLE | PASS | t1_y700_before.xml / t1_y700_after.xml；+4s 复判仍 VISIBLE：v1_t1_y700_late4s.xml |
| 2 | 手柄 swipe 540 477→1277 dur100 | CLOSED | CLOSED | PASS | t2_handle477_before.xml / t2_handle477_after.xml |
| 3 | 点外 tap (540,200)（scrim） | CLOSED | CLOSED | PASS | t3_outside_before.xml / t3_outside_after.xml |
| 4 | 列表内容 swipe 540 1100→1950 dur100 | VISIBLE | VISIBLE | PASS | t4_fling_before.xml / t4_fling_after.xml |
| 5 | Close tap (975,617) | CLOSED | CLOSED | PASS | t5_close_before.xml / t5_close_after.xml |

- 所有 `*_before.xml` 判定均为 SHEET_VISIBLE，即每点手势起点确实落在已打开抽屉上。
- 阳性对照（2/3/5）均成功收起，证明手势与关闭通路本身有效，1/4 的 VISIBLE 非「手势未生效」造成的假阴性。
- 修复前基线：`docs/acceptance/2026-09-12-392-v1-sheet-fling.md`（旧 APK md5 `1a315b12...`）记录 bisect 640/670/700 = CLOSED、T5 y=520 / T6 y=600 = CLOSED；本次同坐标同手势全部 VISIBLE，构成跨构建 A/B。

## V2 抽查（同流程、同坐标）

| # | 操作 | 期望 | 实测 | 判定 | 证据 |
|---|---|---|---|---|---|
| 6a | swipe 540 600→1400 dur100 | VISIBLE | VISIBLE | PASS | v2_t1_y600_before.xml / v2_t1_y600_after.xml |
| 6b | swipe 540 670→1470 dur100 | VISIBLE | VISIBLE | PASS | v2_t1_y670_before.xml / v2_t1_y670_after.xml |
| 6c | swipe 540 700→1500 dur100 | VISIBLE | VISIBLE | PASS | v2_t1_y700_before.xml / v2_t1_y700_after.xml |
| 6d | 手柄 swipe 540 477→1277 dur100 | CLOSED | CLOSED | PASS | v2_t2_handle477_before.xml / v2_t2_handle477_after.xml |

V2 抽屉几何与 V1 相同（scrim 顶边 459、标题 [42,592][912,653]、Q 列表在场）。V1/V2 行为一致，无 server-type 分叉。

## 回归

| 项 | 结果 | 证据 |
|---|---|---|
| 抽屉列表条目正常（Q 编号在场） | PASS | v1_02_sheet.xml 含 Q5–Q13；t4_fling_after.xml 含 Q1–Q10；v2_02_sheet.xml 含 Q11–Q14 |
| crash buffer 无 ocbeacon | PASS | `adb logcat -d -b crash` 输出 0 行；`grep -i ocbeacon` 无匹配 |
| 抽屉仍可关闭（手柄/点外/Close） | PASS | 见判定表 #2/#3/#5 |

## 补充观察（非本次判定点，供裁决）

在既定点之外做了一次 V1 边界细扫（固定终点 +800、dur100，逐点先开后拖），用于刻画修复覆盖面的下边界：

| 起点 y | 结果 | 证据 |
|---|---|---|
| 477（手柄） | CLOSED | t2_handle477_after.xml |
| 490 | CLOSED | v1_extra_y490_after.xml |
| 520 | CLOSED | v1_extra_y520_before.xml / v1_extra_y520_after.xml |
| 525 | CLOSED | v1_extra_y525_after.xml |
| 530 | CLOSED | v1_extra_y530_after.xml |
| 540 | VISIBLE | v1_extra_y540_after.xml |
| 545 | VISIBLE | v1_extra_y545_after.xml |
| 550 | VISIBLE | v1_extra_y550_after.xml |
| 600 | VISIBLE | v1_extra_y600b_after.xml |

- 标题 Row（pointerInput 挂载处）实际顶边约在 y≈535（530 CLOSED / 540 VISIBLE），因此 **490–535 这一段（约 45px≈17dp，紧邻 12dp 手柄下方）仍可下滑收起**；该段位于标题 Row 之上，属手柄槽/顶部内边距，不在题述既定点（600/640/670/700）内。
- 与 #392 定义的「非列表带 490–706」相比，可收起残留由约 216px 缩至约 45px。是否要把这 17dp 一并纳入「仅手柄/点外/返回收起」的严格语义，请裁决（本次按既定点判定为 PASS，未判 FAIL）。
- 该证据对应用例为 swipe 模式，均带 before/after 两份 dump。

## 未做 / 限制

- 未做真手指体感复测：模拟器 `input swipe` 不能 100% 等价真实 touch 采样；本判定为仪器可断言的 UI 结构/流程面。
- 未在真机 houji 复测；未采集 perfetto 触摸事件。
- 未重装旧 APK 复现 CLOSED 基线（无旧包存档、且禁止跑 Gradle）；基线引用 #392 已归档判定与 dump。

## 证据清单（docs/acceptance/2026-09-12-405/）

- 判定/辅助脚本：`check.py`（scrim→SHEET_VISIBLE/CLOSED）、`bounds.py`（节点 bounds）、`run_case.sh`（关→开→before dump→手势→after dump）
- V1 入口：`v1_00_sessionlist.xml`、`v1_01_chat.xml`（FAB 快速定位 [891,2131][933,2173]）、`v1_02_sheet.xml`（抽屉几何）
- V1 判定：`t1_y600_*`、`t1_y640_*`、`t1_y670_*`、`t1_y700_*`、`v1_t1_y700_late4s.xml`、`t2_handle477_*`、`t3_outside_*`、`t4_fling_*`、`t5_close_*`
- V1 补充边界：`v1_extra_y490/520/525/530/540/545/550/600b_*`、`v1_sheet_after_y700.png`
- V2：`v2_00_sessionlist.xml`、`v2_01_chat.xml`、`v2_02_sheet.xml`、`v2_t1_y600/670/700_*`、`v2_t2_handle477_*`
