# #391 / #398 / #399 / #400 真机验收 checklist（DSH 面）

- 卡片：backlog #391（架构）/ #398（V3 渲染+按代词汇表）/ #399（UIUX 统一）/ #400（Testing seam 4 真机·真实服务器）
- 日期：2026-09-12
- 构建：devDebug（源码 HEAD `cd07e34a`，其后提交仅 docs；APK `app/build/outputs/apk/dev/debug/app-dev-debug.apk`，已装于真机）
- 设备：PLK110 / Android 16（SDK 36）/ 1272x2772 —— 无线 adb serial `adb-3B165D00SX600000-ZLHh0v._adb-tls-connect._tcp`
- 服务器（经 adb reverse，设备侧恒 `127.0.0.1`）：
  - DSH 0.1.5-rc.1 `http://127.0.0.1:3080`，token 读 `~/.dsh/token`；**A 节令牌门禁另开新 authority `3081→3080`**（token cookie 按 authority 加密持久化于 DataStore，旧 authority 会命中缓存 cookie，必须换端口才能复现 TokenNeeded）
  - OpenCode V2 `http://127.0.0.1:4199`，user `opencode`，password 取 `/persistent/home/leo-tkp/.config/opencode/service.json` 的 `password`
- 分类：仪器可验（全部 item 走三步验收）；无人工项
- **#399 范围声明**：本批 #399 的可见交付面 = 令牌门禁横幅/对话框 + 条目动作注册表（FAB 能力门控）+ 区域插槽两级门禁；设计令牌/间距/色值的存量迁移已由 `:lint-checks` 四条静态规则 + 单测覆盖（无新增可见面），故不另设视觉专项。
- 回归域（regression-guide ∩ 本卡触面）：① DSH 连接/鉴权 ② 会话列表 ③ 聊天转录/SSE 流式 ④ 服务器能力门控与界面插槽 ⑤ OpenCode(M1) 面 —— 本批改动映射层/词汇表/渲染投影/插槽，核心回归落 ①③④⑤，② 由 A3/E1 附带覆盖（列表出现即非空，专项排序/分页不在本批触面）。
- 纪律：真机独占；**串行执行、允许复用依赖链（A1→A2→A3、B2→E1、D1→D2）状态，禁止并发**；失败照记不改产品代码。
- **全局 BLOCKED 判据**：任一前置 5 分钟内无法满足（服务器不可达 / token 失效 / adb 掉线 / 目标 UI 元素不出现）→ 记 ✘/BLOCKED + 现场证据（dump/logcat/DB/截图），继续后续独立项；依赖项标 `BLOCKED-by-<id>`；**不得跳过或臆造通过**。

## 执行状态（2026-09-12，用户叫停）

- **已执行并通过（6 项）**：A1、A2、A3、B1、B2、B3（实测记录见下，均有 dump/证据路径）。
- **C1 阻塞（环境）**：DSH 轮次返回 `Insufficient Balance`——该新会话模型为 `GLM-5.3-Flash`（provider `zai-coding-cn`），轮次未执行、无 present 调用、无 deliverables 事件，故无 chip。执行员独立复现并留截图/logcat/DB。
- **未执行（叫停）**：C2、C3、D1、D2、E1、E2。其中 **D1/D2 与 C1 同受模型余额阻塞**（均需可用的 DSH 轮次）。
- 复跑指引：待 provider 余额恢复（或改用可用模型路由）后，从 C1 起重跑 C/D/E 各节即可；方案本身已通过纯净审查。

## 执行状态（模拟器复跑，2026-09-11 完成）

- **模拟器代替真机**（用户裁决 2026-09-11）：emulator-5554 / Android 16 / 无头，devDebug APK `c90ba98a…`；全程未改产品代码、未跑 Gradle。
- 全量证据：**docs/acceptance/2026-09-12-391-400-emulator-evidence.md**（164 行，逐项操作/观测/证据路径）。
- 结果：**A1/A2/A3/B1/B2/B3/C3/D1/D2/E1/E2 全 PASS，无 BLOCKED**。
- **C1**：产物 chip 功能 PASS（替代 DSH 会话 session-a84edbf7 命中 `text="产物"` + `content-desc="打开 <path>"`）；指定会话 session-94365bc9 因 V3 4 次压缩边界未呈现（其 2 条 deliverables 全在压缩边界前）——若以该会话为唯一判据记 FAIL。
- **C2**：注入类精简卡机制 PASS（`上下文注入`/`插件配置` 卡可渲染可展开）；字面判据「You are an AI agent powered by DeepSeek Harness」未作为卡正文出现在已加载转录（该片段在归档 seq=9、339 条 system/message 中，但均在压缩边界之外）——若以字面为准记 FAIL。
- **D1/D2 模型坑**：新会话默认路由 `deepseek-official/DeepSeek-V41-Flash` 首次发送 `Insufficient Balance`；切 `opencode-go/DeepSeek V4.1 Flash` 后正常（TextDelta 443/3471，转录 164→5672 递增）。

## 通用前置

```bash
SER=adb-3B165D00SX600000-ZLHh0v._adb-tls-connect._tcp
adb -s "$SER" reverse tcp:3080 tcp:3080
adb -s "$SER" reverse tcp:3081 tcp:3080     # A 节令牌门禁新 authority（无持久化 cookie）
adb -s "$SER" reverse tcp:4199 tcp:4199
adb -s "$SER" reverse --list                 # 确认 3080/3081/4199 三项均在
adb -s "$SER" shell input keyevent KEYCODE_WAKEUP
adb -s "$SER" shell wm dismiss-keyguard
```

- 包名：dev.leonardo.ocbeacon.dev；Activity：dev.leonardo.ocbeacon.MainActivity
- dump 断言：`adb -s "$SER" shell uiautomator dump /sdcard/a.xml && adb -s "$SER" exec-out cat /sdcard/a.xml`
- 点击元素通用手法：dump 内按 `text`/`content-desc` 找 `bounds="[x1,y1][x2,y2]"` → `adb -s "$SER" shell input tap $(( (x1+x2)/2 )) $(( (y1+y2)/2 ))`
- composer 输入（**空格必须转义为 %s**）：dump 找 composer 的 `EditText` bounds → tap 聚焦 → `adb -s "$SER" shell input text "Write%sa%s150-word%sstory"`
- 截图：`adb -s "$SER" exec-out screencap -p > /tmp/acc-<id>.png`
- DB 直查：`adb -s "$SER" exec-out run-as dev.leonardo.ocbeacon.dev cat databases/ocbeacon.db > /tmp/db`（同时取 `-wal`/`-shm`），宿主机 `sqlite3`；标题反查 `select json_extract(payload,'$.title') from cached_sessions where id='<sid>'`

## A. 连接 / 鉴权（#391 token 门禁，L1）

### A1 新 authority 未注入 token → 令牌横幅
- 前置：`am force-stop dev.leonardo.ocbeacon.dev`；reverse 3081 已建（该 authority 从未交换 cookie → 必走 TokenNeeded）
- 操作：`am start -n dev.leonardo.ocbeacon.dev/dev.leonardo.ocbeacon.MainActivity --es debug_url http://127.0.0.1:3081 --es debug_name E2E-DSH --es debug_server_type dsh`（**不带** `debug_token`），等待 ≤20s
- 期望：会话列表顶部出现 errorContainer 细条幅「此服务器需要访问令牌」+ 按钮「输入令牌」
- 判定：dump 同含 `text="此服务器需要访问令牌"` 与 `text="输入令牌"`
- 实测记录：✔ 3081 新 authority 无 token 冷启动 ≤20s 后 dump 同时含 `text="此服务器需要访问令牌"` 与 `text="输入令牌"`（errorContainer 横幅出现）；证据 /tmp/acc-A1.png、/sdcard/a1.xml（dump 快照）

### A2 无效 token → 拒绝提示（边界）
- 前置：A1 状态
- 操作：点「输入令牌」→ dump 确认对话框标题「需要访问令牌」→ tap 输入框聚焦 → `input text "invalid-token-xyz"` → 点「连接」→ 等待 ≤20s
- 期望：对话框不关闭，显示拒绝文案
- 判定：dump 含 `text="服务器拒绝了令牌。请检查后重试。"`
- 实测记录：✔ 点「输入令牌」→ 对话框标题「需要访问令牌」出现 → 输入 `invalid-token-xyz` → 点「连接」等待 ~18s 后 dump 含 `text="服务器拒绝了令牌。请检查后重试。"` 且对话框未关闭；证据 /tmp/acc-A2.png、/sdcard/a2-step4.xml

### A3 有效 token → 横幅消失、会话列表上线（正向）
- 前置：A2 后
- 操作：对话框输入框全选替换为空 → `input text "$(cat ~/.dsh/token)"` → 点「连接」→ 等待 ≤25s
- 期望：横幅消失；出现工作区会话项
- 判定：dump **不含** `此服务器需要访问令牌` 且含任一已知会话标题（如 `text="多服务器适配重构"`）
- 实测记录：✔ 清空输入框（MOVE_END+DEL）后注入 43 字符有效 token（与 `~/.dsh/token` 比对一致）→ 点「连接」等待 ~25s → dump 不含 `此服务器需要访问令牌`，含 `text="多服务器适配重构"`；证据 /tmp/acc-A3.png、/sdcard/a3-step2.xml

## B. 能力门控与界面插槽（#391 能力位 / #399 插槽，L3）

### B1 DSH 面 FAB 任务菜单按能力位过滤
- 前置：A3 通过并进入任一会话
- 操作：dump 找 `content-desc="打开任务菜单"` → tap → 再 dump
- 期望：菜单项 = TODO / 智能体 / 目标 / 排队队列；**无** 终端/Shell
- 判定：菜单 dump 含 `TODO`+`智能体`+`目标`+`排队队列`，且不含 `终端`/`Shell`
- 实测记录：✔ 进入「多服务器适配重构」→ tap `content-desc="打开任务菜单"` → 菜单 dump 含 TODO/智能体/目标/排队队列 4 项（bounds 均在菜单列 x≈1027-1229），不含「终端」；dump 中唯一 "Shell" 字样为菜单背后聊天转录的历史用户消息文本，非菜单项；证据 /tmp/acc-B1.png、/sdcard/b1-step2.xml

### B2 OpenCode 面不泄漏 DSH 私有区块（两级插槽门禁·逆向）
- 前置：`am force-stop`；reverse 4199
- 操作：`am start ... --es debug_url http://127.0.0.1:4199 --es debug_username opencode --es debug_password <pwd> --es debug_name E2E-OC --es debug_server_type opencode` → 会话列表 dump → 进入设置页 dump
- 期望：会话列表无令牌横幅；设置页无 DSH 私有区块
- 判定：两处 dump 均不含 `text="此服务器需要访问令牌"`；设置页 dump 不含 `text="DSH provider 目录"` 且不含 `text="服务器插件"`
- 实测记录：✔ debug intent 启 OpenCode 面（4199/opencode）后列表 dump 无令牌横幅；设置页首屏+上滑 4 屏逐屏 dump 均不含 `此服务器需要访问令牌`/`DSH provider 目录`/`服务器插件`；证据 /tmp/acc-B2-list.png、/tmp/acc-B2-settings.png、/tmp/acc-B2-settings-bottom.png、/sdcard/b2-list.xml、/sdcard/b2-settings*.xml

### B3 OpenCode 面 FAB 能力位过滤（正向对照）
- 前置：B2 的 OpenCode 会话（OpenCode V2 无 GOALS 端口）
- 操作：进入任一会话 → 打开任务菜单 → dump
- 期望：菜单含 TODO/智能体；**不含 目标**
- 判定：菜单 dump 含 `text="TODO"` 且不含 `text="目标"`
- 备注：终端/Shell 在 zh 文案下未确认，不做断言
- 实测记录：✔ 经服务器页「会话」进入 E2E-OC（OpenCode V2）会话「DSHWeb Token问题咨询」→ FAB 菜单 dump 含 TODO/智能体/排队队列（bounds x≈1027-1229 菜单列），全文不含「目标」；证据 /tmp/acc-B3.png、/sdcard/b3-menu.xml

## C. V3 事件族渲染（#398，L2）

### C1 deliverables/presented → turn 尾「产物」chip 可见
- 前置：A3 已连；oc-beacon 工作区；新建会话（`content-desc="New Session"` → 选 `oc-beacon` 工作区）
- 操作：composer 输入 `Use%sthe%swrite%stool%sto%screate%s/tmp/e2e-real.txt%swith%scontent%shello,%sthen%scall%sthe%spresent%stool%son%sit.` → 发送 → 轮询 ≤15min（每 30s dump）
- 期望：该轮完结后气泡尾部出现「产物」标签 + 文件名 chip `e2e-real.txt`
- 判定：dump 含 `text="产物"` 且 `content-desc="打开 /tmp/e2e-real.txt"`
- BLOCKED 处置：15min 内无 deliverables 事件 → 记 ✘/BLOCKED + DB（`select count(*) from cached_parts where type='deliverables'`）+ logcat；**不得臆造通过**
- 实测记录：✘ **BLOCKED（环境：模型余额）**——composer 已按 `%s` 转义发送 C1 提示词，DSH 轮次返回 `Insufficient Balance`（会话模型 `GLM-5.3-Flash` / `zai-coding-cn`），轮次未执行 → 无 `present` 调用、无 deliverables 事件、无 chip。证据：/tmp/c1.xml（dump 含 `text="Insufficient Balance"`）；执行员独立复现并留截图/logcat/DB。

### C2 system/message（非空 content）→ 注入类精简卡
- 前置：A3；本机工作区归档 `c1d0f2f1`/`72ba010c` 含非空 system/message
- 操作：DB 查含注入消息的会话 —— `select messageId from cached_messages where messageId like 'dsh-sys-%'` 得主机消息；对应 `cached_parts.sessionId` 或 `cached_messages.sessionId` 即会话 id → `select json_extract(payload,'$.title') from cached_sessions where id='<sid>'` 得标题 → UI 列表按标题打开该会话 → dump（必要时上滑）
- 期望：转录出现注入类消息（含 system prompt 片段），非普通用户气泡
- 判定：dump 含文本片段 `You are an AI agent powered by DeepSeek Harness`
- BLOCKED 处置：UI 定位不到对应会话 → BLOCKED + DB 证据
- 实测记录：未执行——用户叫停真机验证（2026-09-12）。

### C3 已知忽略/未知词汇不拒绝重建（按代词汇表容错）
- 前置：A3；打开任一 V3 大会话（如「多服务器适配重构」）
- 操作：等待加载 → dump 转录 → 拉 DB → `adb -s "$SER" logcat -d | grep -c STRUCTURAL_VIOLATION`
- 期望：转录非空、无空白页/无拒绝重建
- 判定：`select count(*) from cached_messages where sessionId like '%<sid>%'` > 0（`<sid>` = `select id from cached_sessions where payload like '%多服务器适配重构%' limit 1`）；STRUCTURAL_VIOLATION 计数 = 0
- 实测记录：未执行——用户叫停真机验证（2026-09-12）。

## D. SSE 实时流式（#400，L2/L3）

### D1 发送后 assistant-stream 增量渲染
- 前置：A3；进入某会话 composer
- 操作：**先清日志** `adb -s "$SER" logcat -c` → 发送长输出提示 `Write%sa%s150-word%sdescription%sof%sa%rainy%scity%street.` → 轮询 dump + logcat ≤10min
- 期望：turn 完成前 assistant 文本逐段增长；logcat 出现 `Busy/Streaming --TextDelta-->`
- 判定：logcat 含 ≥1 条 `TextDelta`（已 `-c`，必为本次）；间隔 ≥5s 的两次 dump 中 assistant 文本长度后一次 > 前一次
- 实测记录：未执行——用户叫停；且与 C1 同受模型余额阻塞（DSH 轮次无法执行）。

### D2 轮完结 → 台账行出现（allStepsCompleted 路径）
- 前置：D1 同轮完成
- 操作：dump
- 期望：轮尾台账行含时长
- 判定：dump 文本匹配正则 `[0-9]+m [0-9]+s`
- 实测记录：未执行——用户叫停；依赖 D1 同轮（受模型余额阻塞）。

## E. 回归冒烟

### E1 OpenCode V2 面连接 + 会话列表 + 转录
- 前置：B2 的 OpenCode 服务器
- 操作：打开任一 OpenCode 会话，等待转录
- 期望：列表与转录正常渲染；无崩溃
- 判定：dump 出现消息文本节点；crash buffer 空
- 实测记录：未执行——用户叫停真机验证（2026-09-12）。

### E2 全程无崩溃
- 操作：`adb -s "$SER" logcat -d -b crash | grep -i ocbeacon`
- 判定：无输出
- 实测记录：未执行——用户叫停；已执行阶段（A/B）期间 crash buffer 为空（见执行员记录）。

## F. 不可真机验证项（附理由，非执行项）

- **surfaceOp 越界 → 拒绝重建**：真实服务器面无法在不污染 DSH 会话存储的前提下注入畸形 V3 归档；由 `DshV3AdaptationTest`（surfaceOp double-read）+ `DshEventMapperCompaction378Test`（STRUCTURAL_VIOLATION 断言）覆盖。**已知替代**：`debug_url` 可指向本地 mock DSH 服务器构造畸形归档（半真机），因偏离 real-server 范围而豁免。
- **assistant/attempt 静默**：设计为不产事件（瞬态尝试，681/692 后随 llm/retry）；dump 断言"不存在"可靠性差；由 `DshV3AdaptationTest` 覆盖。
- **subagent/catalog 不消费 / feedback/message-* log-only**：双源裁定无转录语义（目录走 `subagents/list` RPC、反馈走 `MessageFeedbackDelegate` RPC），无 UI 面；由 `DshEventVocabularyTest` / `DshV3AdaptationTest` 覆盖。

## 人工验收清单

（空——本批全部为仪器可验项，无 §1 四类人工项。）
