# #392 V1 靶机复测（最新构建）——抽屉手势隔离一次性复核（2026-09-12）

- 关联：#392（本卡，用户要求 V1 复测通过即关闭）/ #379 / #405
- 设备：emulator-5554（1080x2400 @420dpi zh-CN），APK md5 `d05e712f8510ef4e27c1530e76810fd8`（含 #387/#408 改动）
- 服务器：V1 `127.0.0.1:4198`（opencode 1.18.27 隔离实例，adb reverse 在位）
- 证据目录：`docs/acceptance/2026-09-12-392-v1-recheck/`（每用例 before/after dump + `check.py`）

## 结论：全部 PASS

| # | 操作（x=540） | 期望 | 实测 before → after | 判定 |
|---|---|---|---|---|
| c1 | 列表区快速下滑 fling 1100→1950 dur60 | VISIBLE（不收起） | VISIBLE → VISIBLE | PASS |
| c2 | 标题带 600→1400 dur100 | VISIBLE | VISIBLE → VISIBLE | PASS |
| c3 | 标题带 670→1470 dur100 | VISIBLE | VISIBLE → VISIBLE | PASS |
| c4 | 标题带 700→1500 dur100 | VISIBLE | VISIBLE → VISIBLE | PASS |
| c5 | 列表区快速上滑 fling 1900→500 dur60 | VISIBLE | VISIBLE → VISIBLE | PASS |
| c6 | 内容区最上沿 545→1345 dur100 | VISIBLE | VISIBLE → VISIBLE | PASS |
| c7 | 手柄槽中线 522→1322 dur100 | CLOSED（#379 设计内） | VISIBLE → CLOSED | PASS |
| c8 | 点外 scrim tap(540,200) | CLOSED | VISIBLE → CLOSED | PASS |

- 每条 before 均为 SHEET_VISIBLE（手势起点确实落在已打开抽屉上）；阳性对照 c7/c8 成功收起，证明手势与关闭通路有效。
- 几何与 #405 复验一致：手柄槽 [506,538]、内容 Column 顶边 538、标题文本 592 起——**V1 与 V2 无 server-type 分叉**（#392 的原始疑虑不成立）。

## 判定口径

`check.py`：uiautomator dump 中 scrim content-desc 含「关闭工作表」→ SHEET_VISIBLE，否则 SHEET_CLOSED。脚本流程：确保关闭 → tap FAB(912,2152) → before dump 必须 VISIBLE → 手势 → sleep 1.6 → after dump。

## 未做与限制

- 注入 swipe 非真手指采样（#245 已知限制）；
- 仅竖屏 zh-CN；未复测 V2（同日 #405 复验已覆盖 V2 同坐标且行为一致）。