VERDICT: NEEDS-REVISION

# 2026-09-06 全功能 E2E 回归 checklist 纯净审查(第 1 轮)

- 被审对象:`docs/acceptance/2026-09-06-full-e2e-regression-checklist.md`(构建 dc299e15)
- 审查依据:`docs/ai-acceptance-workflow.md` §2 步骤1 质量标准;覆盖对照 `docs/journal/2026-09-04-fix-308-always-326-327.md` §十一~§二十一 + `backlog.md`;回归域依据 `docs/regression-guide.md`(12 能力域)
- 审查维度:A 覆盖完整性 / B item 正交性 / C 可执行性 / D 判定可观测性 / E 前置充分性 / F 数据安全红线
- 总体:结构、全局纪律节(§0)、证据与 BLOCKED 预案意识显著优于模板基线;覆盖矩阵与 item 双向核对一致(矩阵所列 item 全部存在,无游离 item)。但存在 1 处 item 前提自相矛盾(必致误判)、1 处红线判定依据悬空、以及 4 处子项覆盖缺口无豁免注记,未达「可直接上生产」标准。

---

## A. 覆盖完整性(对照 journal §十一~§二十一 + backlog 全部卡)

- **A1|#309⑤(重试 continue / ⑤-a ⑤-b max-tokens 倒计时)无 item 亦无豁免注记**。journal §十四:A5 两个 BLOCKED=预设合理降级(单测作结)。本 checklist 标题宣称「全量」,矩阵却只映射 B4 B5 B6 C4(①~④);⑤ 应显式写豁免行(引 2026-09-05-309 验收报告的 BLOCKED 依据),否则「全量」不实。
- **A2|#310① 子代理续聊通道(prompt / interruptByParent / history / composer 门控)无对应 item**。A4 只验「树挂载关系」;journal §十五轮3 的 A3/A8(子会话转录持久、二次路由、停止链)未在本 checklist 复现。至少应有一项「进入 subagent 子会话发送一条消息并得回复」(受端效果证据,§1 证据标准),或注记豁免理由。
- **A3|#325① adb 注入通道(dsh-pair.sh)无 item 无豁免**。矩阵自称「四通道」却只映射 E1(QR 深链)E2(sameBackend);③SSH 已由 2026-09-06 用户裁决否决(backlog 有据)可豁免但未注记;①完全没有交代。若因红线「服务器条目恒=2」不宜真跑,应写明该冲突并以既有验收轮 E1-r2 证据豁免。
- **A4|#320「三清除径撤通知」未逐径枚举**。E3 只写「再测三清除径之应答撤除(配合 E4)」——哪三径(应答 / 审批处置 / 轮末)未列出;目前实际只覆盖应答径(E4)与「处理后撤除」泛称一次,审批处置径与轮末径无显式断言,与 journal §十八「补三清除径同点撤通知」的交付面不齐。
- **A5|回归域选择未按 §2 步骤1「逐域列明为何回归/不回归」**。矩阵仅一行「回归域 A8 F1-F3 G1-G5 H1 H3」。对照 regression-guide 12 域,以下 ★必测点既未覆盖也无豁免说明:3.2★2 断开/重连(自动重连是 SSE/会话域承重能力,本批大量改 DshRemoteMuxEngine 增量帧解析——恰是最该回归的点之一)、3.3★4 未读红点、3.8★1 上滑加载早期历史(A3 只向下滚)、3.9★ Room/DataStore 直查(H2 只 ls 文件名)、3.4★6 Markdown 表格渲染。全功能回归应逐域给出一行去留理由。
- **A6(轻)|#331 原始症状(会话行时间戳 ~3min 时延)未被直接断言**。A2 的「升顶且停留」是代理指标;建议判定补一行「dump 行时间=当前时刻(±60s)」,直接锚定 updatedAt 透传修复。
- **A7(轻)|B2 审批卡正向路径(受限档)仅 BLOCKED 指针**,见 E1/E2 意见——受限档构造方法缺失使该预案大概率被触发而无人知道能否避免。

## B. item 正交性

- **B1|A4 与 A5 排序矛盾**:A4 操作要求「对比 fork 派生会话(A5 产物)」,但 A4 排在 A5 之前且纪律是「一项一项按序做」——执行到 A4 时产物不存在。调序(A5 提前)或注明「此对比在 A5 完成后回填」。
- **B2|A1 自身操作与判定矛盾**:A1 的会话由「宿主侧(本 wave agent 自身)发起短任务」产生,标题由任务内容派生,不会带 E2E06 前缀;判定却要求「列表 dump 首行=E2E06 新会话」。二者必有一错——判定应改为可识别的宿主任务特征,或操作补「派生后设备端重命名为 E2E06-…」。
- **B3|B9① 与 B5 重叠**:B9① 用「/help 或 /compact」验反馈卡,B5 已全链验 /compact;B9① 固定用 /help 即可消除同命令双验模糊。
- **B4(轻)|C4① 与 C5 同构操作**:两者都是「busy 中发消息看轮末」,建议 C5 注明「时序证据复用 C4①,本项只断言数据完整性」划清边界。C1⑤「blocked(若无则跳过)」与 E4 前置(必然造出提问卡)矛盾——blocked 态在本 checklist 内可构造(复用 B1 未应答卡),应指明构造来源而非留「若无则跳过」。

## C. 可执行性

- **C1|E3 触发手段不明**:「宿主侧(或另一途径)使测试会话获得回复」——app 已退后台,宿主如何向设备端创建的 session 注入一轮回复(哪条命令/哪个 API/哪个工具)完全未给。E5 用「设备端先提示再 HOME」实现,E3 应改成同一时序(设备端发送→立即 HOME→等通知),否则无上下文执行者无法照做。
- **C2|E1 token 来源未给**:深链 `ocbeacon://pair?endpoint=…&token=…` 的取值途径(宿主 dsh-url 工具输出?)未写,只给了 & 转义注意事项;补获取命令。
- **C3|E2E06 命名机制整体缺失(红线悬空)**:红线#3、D1 前置、H3 终检全依赖「会话名含 E2E06 前缀」,但全文没有任何 item 说明如何让会话得到该名字(首条消息带前缀?用重命名功能?操作步骤在哪)。B1/B4/B8 给的提示语本身不含 E2E06,若标题由内容派生则前缀不会出现——数据安全红线的判定依据无法成立。
- **C4|C3「入口齐全」不可判**:未列出五入口清单(TODO/AGENT/GOAL/SHELL/队列);QueueSheet 三动作已枚举,FAB 入口应同样枚举,否则执行员无从核对「齐全」。
- **C5|A1 时序不可行**:`screenrecord --time-limit 45` 装不下「连续 3 次」子代理派生(单次派生常 >15s,还有 BACK 往返);且「≥1 次 fork-of-fork」无 BLOCKED 预案(模型/harness 不派子子代理时无从达标)。改为分段录屏或 --time-limit 180,并给 fork-of-fork 降级预案。
- **C6|H1 无从汇总**:判定要求「grep 各 wave 日志」但 §0-7 只规定了截图/录屏路径(/tmp/e2e-full/),logcat 抓取方式(按 PID/tag 过滤——regression-guide §3.8 铁律 4)与落盘文件名无约定。§0 应增补「每 wave logcat 持续抓取至 /tmp/e2e-full/W<n>.log」。
- **C7(轻)|模板字段缺失与零散模糊**:D2-D5、E2、G1-G5、H1 缺「前置」字段(§8.1 必填);A3 无「不存在 ≥24h 旧会话」的 BLOCKED 预案;A7「切到文件候选(或输入文件名前缀)」二选一未定;G3「如应用内可切」条件含糊;G5 检索 tag 用「如 queue/notification 相关」举例而非确定清单。

## D. 判定可观测性

- **D1|D7 期望是内部实现概念**:「三态数据源」「upsert 计数正确」在 UI 上的可观测现象(对话框里看到什么、计数显示在哪)未描述,判定仅「截图」无法核对成败。
- **D2(轻)|三处判定偏主观**:A6「回复文本语义相关」应补锚点(如「回复含被引会话首条消息的关键词」);B10「截图对照系统时间」应补 `adb shell date` 输出记录;F1「logcat 无 flush 异常」未给 tag/关键字。

## E. 前置充分性

- **E1(最重要)|B6 前提与已知环境事实自相矛盾,必致误判**:journal §十一环境事实③「新会话默认权限档=完全访问」,B2 据此判定「无审批卡=正确」;B6(危险档确认)前置仅「任会话」,若同为完全访问档,rm -rf 类操作大概率直接执行、根本不出现确认对话框——按 B6 现行判定(确认→执行/取消→不执行两路径)将必然 ✘ 或误报「取消后仍执行=✘(严重)」假阳性。B6 必须与 B2 同构:写明档位前提 + 受限档不可得时的 BLOCKED 预案(引 2026-09-04-308 验收报告),否则执行员会制造出严重级假缺陷。
- **E2|受限档会话构造方法缺失(波及 B2 正向 / B6 / D10)**:journal §十一 A8-revised 曾用宿主侧 preset 规则实现只读档,但本 checklist 既没给步骤、也没说禁止;执行员跑到一半才发现环境不可得。三处共用,应统一在 §0 或首个用到处交代。
- **E3|E4 前置子步骤未写全**:「测试会话已发起 ask_user_question(卡在场)」需要先给出发提示语(可引 B1 原文)与等待判据(卡在场的 dump/截图证据),否则「卡在场=前台抑制期」的确认动作无标准。

## F. 数据安全红线完备性(设备曾发生 UTP 卸载致配置清空事故)

- **F1|红线未禁止宿主侧 harness 配置/权限规则/凭据修改**:现有红线(禁 gradle/UTP/pm uninstall/pm clear/改设备应用数据)直击 2026-09-06 事故根因,好;但 B2/B6/D10 需要受限档会话,执行员最自然的路径就是改 prod-3080 宿主侧权限规则/配置——这是真实 harness 环境,误改即姊妹事故。应显式加入红线,或给出免修改的替代构造法。
- **F2|A1 宿主侧派生会话不在任何管辖内**:红线#3 只管设备端 E2E06 会话;A1 让 wave agent 在宿主 spawn 子代理(含 fork-of-fork)产生的真实 harness 会话,结束时如何清点、留存还是删除、H3「非测试会话未被动」如何归类,均未约定——终检时这批会话的处置悬空。
- **F3(轻)|两条补强**:①D8「若服务器有其他 workspace 可列」应加显式禁令「不改动/不排序非自建 workspace」(现文只约束 remove e2e06ws,order 操作误触他 ws 的风险未封);②结束清点建议加「设备侧 /sdcard 录屏残留 pull 后清理」(可选)。

---

## 结论

修订优先级:E1(B6 前提矛盾)> C3(E2E06 命名机制/红线悬空)+ B2(A1 判定自相矛盾)> A1-A4(四处子项覆盖缺口补豁免或补 item)> E2/E3/C1/C2/C5(前置与触发手段)> 其余轻量项。修订后建议复审一次再进步骤 3。
