VERDICT: PASS

# 2026-09-06 全功能 E2E 回归 checklist 纯净审查(第 2 轮,v2 复审)

- 被审对象:`docs/acceptance/2026-09-06-full-e2e-regression-checklist.md`(v2,2026-09-06 11:14 构建)
- 审查依据:同第 1 轮(ai-acceptance-workflow §2 步骤1 质量标准;journal §十一~§二十一;backlog;regression-guide 12 域)
- 方法:逐条核对第 1 轮 22 条意见的落实;抽查新增内容的事实正确性(脚本/文档/组件名实证核验)。

## 第 1 轮 22 条意见逐条核验(全部落实)

| # | 意见 | 落实情况 |
|---|---|---|
| A1 #309⑤ | ✔ 矩阵显式豁免行+prior 报告指针 |
| A2 #310①续聊 | ✔ A4②「进入子会话发送 E2E06-A4 并等回复」受端证据,矩阵注明「含子代理续聊受端」 |
| A3 #325①/③ | ✔ 两条豁免行(红线冲突理由+prior E1-r2;用户裁决否决) |
| A4 #320 三清除径 | ✔ E3 逐径展开(发布直达/a 应答并 E4/b 轮末含 BLOCKED/c 断开+重连),审批径由注¹统摄(见余留 3) |
| A5 回归域去留 | ✔ 12 域全表,每域去留+理由+item;断开重连→E3c、未读红点→G6、上滑→A3、Room 直查→H2、表格→B15 均补齐;豁免项均给理由 |
| A6 #331 时延锚定 | ✔ A2 判定补「行时间戳=当前时刻(±60s)」+`date +%s` 记录 |
| A7 B2 正向 | ✔ BLOCKED-environment 注¹ |
| B1 A4/A5 排序 | ✔ 域首明示执行顺序 A1→A2→A3→A5→A4…;§0-13 允许依赖微调+注记 |
| B2 A1 自相矛盾 | ✔ probe-a1 特征前缀替代 E2E06 断言,操作与判定一致 |
| B3 B9①/B5 重叠 | ✔ B9① 固定 /help |
| B4 C4①/C5 边界、C1⑤ | ✔ C5 注明复用 C4① 时序;C1⑤ 给 blocked 构造来源(E2E06-C1 提问会话) |
| C1 E3 触发 | ✔ 改为设备端发送→立即 HOME 时序 |
| C2 E1 token | ✔ 假 token 方案(只测预填+取消),完整 am start 命令含转义处理 |
| C3 E2E06 命名 | ✔ 红线④:首条消息以 E2E06-<item-id> 开头,标题派生;各 item 提示语已嵌前缀;D1 前置「必然满足」成立 |
| C4 C3 入口清单 | ✔ 对照 prior 327 报告清单逐个核对并列出 |
| C5 A1 录屏/fork-of-fork | ✔ 180s+分段续录;fork-of-fork 亚项 BLOCKED 预案,主判定 3/3 普通派生 |
| C6 H1 logcat | ✔ §0-10 全约定(PID 过滤/每 wave 落盘/crash 缓冲/PID 变更),H1 grep 目标明确+dropbox |
| C7 前置缺失等 | ✔ D2-D5/E2/G1-G5/H1 均补前置;A3 无旧会话预案;A7 改探查记录式;G3 双路+脚本;G5 固定关键字 |
| D1 D7 内部概念 | ✔ 改为可观测三要素(列表/输入/确认)+取消无残留 |
| D2 主观判定 | ✔ A6 特征词 grep;B10 date 输出;F1 改帧间比对判据 |
| E1/E2 B6 档位矛盾 | ✔ B6 重写为档位语义(完全访问=直接执行无确认);注¹统一受限档 BLOCKED-environment+prior 指针;红线⑥封死改宿主配置的构造路径 |
| E3 E4 子步骤 | ✔ B1 同型提示+卡在场 dump/截图基线 |
| F1-F3 红线 | ✔ 红线⑥(宿主 harness 保护);红线⑤(宿主派生会话留存+notes 清单+H3 归类);D8 禁触非自建 ws;H3 可选清 /sdcard |

## 事实核验(新增引用全部实证)

- `scripts/i18n-check.sh`、`scripts/pull-app-db.sh`、`scripts/debug-entry.sh`、`scripts/dsh-pair.sh` 均存在 ✔
- 引用的 prior 验收文档(2026-09-05-327 / 2026-09-05-309 / 2026-09-04-308)均在 docs/acceptance/ 在场 ✔
- `am start -n dev.leonardo.ocbeacon.dev/dev.leonardo.ocbeacon.MainActivity` 组件名正确(namespace=dev.leonardo.ocbeacon + dev flavor applicationIdSuffix=.dev,manifest activity=.MainActivity)✔
- 覆盖矩阵与 item 双向仍一致;新增 B15/G6/H2② 均有回归域表锚点 ✔

## 余留事项(轻微,不阻塞执行;可选吸收)

1. **红线⑦与 H2② 的豁免措粒度**:H2② 用 pull-app-db.sh 拉库属「读应用数据目录内容」,而红线⑦豁免只写了「run-as ls 只读列目录」。意图明显是只读观测,但严格执行者可能视为冲突——建议豁免句补「及 H2② 只读拉库」。
2. **两处示例提示语未带 E2E06 前缀**:C1③/C2 的忙碌制造提示「写一篇 800 字文章」(及 F1 的 600 字短文)未按红线④格式嵌入前缀;全局规则已覆盖,但执行者照抄示例会漏——建议示例补前缀(如「E2E06-C2:请写一篇 800 字文章」)。
3. **E3 审批处置径未逐条列名**:三清除径枚举中无「审批处置」一行;由注¹统摄可推出 BLOCKED-environment,但为枚举完整性建议补一行显式注记。
4. **G6 首选触发手段仍略泛**(「宿主侧向 Q 注入一轮回复」机制未给),但已给可构造的替代路径与记录要求,可执行性成立。

## 结论

v2 对 22 条意见全部落实且无新引入错误;新增内容(回归域去留表、logcat 约定、红线④⑤⑥⑦、B15/G6/H2②)事实核验通过。4 条余留均为措辞级微调,不影响无上下文执行者照做、不影响判定可观测性、不削弱数据安全红线。**通过,可进步骤 3(纯净执行)。**
