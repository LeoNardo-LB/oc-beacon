# 走查反馈批2——交互重设计与崩溃修复（2026-09-08）

## §一 #357/#358 双 P0 修复(2026-09-08 10:2x-11:1x,用户指令「先记录后逐项修」)

- **#357 崩溃根因链**:crash buffer 取证=StringIndexOutOfBounds(begin 0,end 53,length 0)@getTextInNode→buildClickableMarkdown(**StreamingMarkdownSuccess** 路径)——流式 state 过渡期 content 清空而 AST 残留失配对;间歇性(真机 3+5 轮进出未复现)。修复=渲染接缝单点防护(buildClickableMarkdown 全仓唯一 annotator 调用):越界→纯文本降级一帧,remember(content,node) 键变自愈;JVM 回归测试钉死崩溃形态。
- **#358 @file 根因**:服务器 PromptContentPart 契约(types.d.ts 实证)=text|image(base64 data **必填**)——旧 promptContentPart 把 file:// 引用包 {type:image,url:…}→gateway boundary validation 整单拒收。修复:非 data-url 文件→@path 文本保真降级。真机:read the file @dsh-keepalive/backlog.md 发送成功(2 parts)。
- **#358 斜杠根因(三层剥洋葱)**:①界面 false 但 HTTP 200→非网关错;②RPC 直探双证 args 语义(payload.args.{agentId,line,images} 正确,双层 args→arguments-invalid)——旧包裹本来就对;③真凶=DshRpcClient.call 的「value 必为对象」前置:受理-异步形态回 {ok:true} **无 value**(CommandExecution|undefined)→误判失败。修复:callOptional+三分派(缺席=true/{}=false/kind 判定)。真机:/calculator 12x34→**Executed:true**。
- **环境注记**:会话模型=deepseek-official 时技能轮 Insufficient Balance(欠费)——用户会话失败观感的放大器;已切 GLM-5.3 验证通过。空会话素材已建(session-fe57e926,workspace,standard)。
- 测试:ClickableMarkdownResultTest+1(越界降级)/DshApiClientTest+1(缺席受理);全量 3186 绿(2 旧断言随三分派语义修正保真)。

> 状态：进行中
> 关联：（spec 路径，若有）·（issue 编号，若有）
> 来源：用户反馈 / grilling / E2E / 顺带发现

<!-- 过程中的取证/验证证据直接写本文件；backlog.md 只留 ≤3 行卡片。 -->
