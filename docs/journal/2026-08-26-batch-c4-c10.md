# 2026-08-26 批次 C4+C10：CompactionDividerPolicy + AutoLoadPolicy（架构走查候选 4/10）

目标文件：ChatMessageList.kt（全库最热，批前 1864 行 → 批后 1669 行，净减 195）。

## 交付物

- components/CompactionDividerPolicy.kt（271 行，纯 Kotlin 无 Compose）：压缩分割线
  认领/判定/去重唯一真相源——6 碎片（尾部去重/交接桥/banner 判据/尾部 item/
  V1 摘要认领/触发消息判定）纯逻辑 + #217/#219/#221/#226/#227 历史语义注释存档。
- components/CompactionDividerSlot.kt（86 行）：三认领点共用渲染槽（撤销对话框+
  长按+a11y+#227 展开表接线+CompactionCard 分发；CompactionCard 本体零改动）。
- components/AutoLoadPolicy.kt（160 行，纯函数）：older/newer 触发决策
  （nearTop ≤8 reverseLayout 方向/不足一屏/退避/fire-time 跳转互斥）；
  2026-08-10/08-12/08-21×2 四次历史修复语义逐条存档。
- ChatMessageList：effect 桥瘦身（snapshotFlow → policy → delegate.load）；
  bannerCount/revealBannerCount 压缩项改由 bannerTerms 派生（其余 6 项保持手算）；
  16 个未用 import 清理。

## 铁律遵守

- 5 个 deferredRevealCompensation 挂载点原位不动（COMP-MSG/TOOL/CMP(msg)/(v1)/(tail)
  全部在原认领点构造 growModifier 传入 slot）；isStreamingMsg 判定沿旧标识符名。
- 未触碰 data 层（scheduleFlush/批处理零 diff）；CompactionCard.kt / ChatScreen.kt 零 diff。

## 验证

- 每阶段 compileDevDebugKotlin 绿后 commit（07→7 commits，见 git log）。
- 全量单测 testDevDebugUnitTest --rerun：2007 绿（基线 1975 + CompactionDividerPolicyTest
  21 + AutoLoadPolicyTest 11），0 失败。
- 新测试为历史 bug 语义哨兵：#226 空安全陷阱（普通用户消息不被吞）/#219 骨架期
  两边互补不消失/#227 交接桥（含收起态）/08-12 reverseLayout 方向矩阵
  （滑到顶差 1 项回归哨兵）/08-21 fire-time 互斥。

## 偏离记录

- 完工标准「CML 净减 ≥250 行」实际 195（78%）：差值 55 行源于
  (a) 铁律要求 3 个 COMP-CMP 挂载点+modifier 构造原位保留（无法收入 slot/policy）；
  (b) Phase C 明确「其余 6 项 banner 仍手算则保持」；
  (c) 撤销对话框/长按等 Compose 脚手架只能经 slot 文件去重（净 -77 已实现），
      不能进纯 Kotlin policy；(d) #232 system 通知/转后台分割线/ScrollDiag 诊断/
  jump 定位 effects 不在 C4/C10 批次边界内未动。
