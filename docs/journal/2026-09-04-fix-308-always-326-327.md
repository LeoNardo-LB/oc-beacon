# fix-308-always + #326/#327 批次(2026-09-04)

> 状态:#308 修复+真机终验 GREEN,待用户再验收;#326/#327 待做
> 来源:用户 #308 验收发现三问题(2026-09-04);/diagnosing-bugs 全相位推进

## 一、#308 G2C 根因诊断(全相位)

### 反馈回路
- 回路 A(仪器):PermissionAutoApprover 加 [DEBUG-g2c] 探针(解析目录/规则数/匹配结果)+ DshRpcClient eventsResult 探针(clientId/eventId/回包)→ 真机 logcat。
- 回路 B(对照):用户验收遗留的真机会话(session-d25241a4,只读 preset)持续可复现——同一会话内 auto vs manual 应答、字节级比对。

### 证据链(时间序)
1. 用户验收轮(21:55-21:58,旧进程):once/reject/always×3 全部 success=true,但 always 后同类 bash 再发仍弹卡;[auto-approve] 日志零条 → 规则静默不匹配。
2. DataStore 直查:3 条规则 toolName=tool(引擎合成帧无 toolName → mapper 回退默认)、directoryPattern=具体目录(非 *)。
3. 重启后活体复现(22:22,新进程):approver 匹配成功并自动应答(resolvedDir 来自重启预载 session.list)——但卡不消、代理自述「no approval channel is available — rejected without a prompt」。
4. 字节级对照(22:39):auto(+24ms)与 manual(+34s)的 eventsResult 请求 clientId/eventId/载荷完全相同,服务器均回 ok——排除 app 侧 id 错位。
5. 服务器源码定音(dsh-api-gateway/lib/index.js + dsh-user-approval/lib/index.js):
   - 网关 dispatchRpc 对 $events/result 恒回 ok:true(L572-575)——即使 receiveRemoteEventResult L690 因 pending 缺失/client 不在 deliveries 静默丢弃;
   - dsh-user-approval decide() L179:waterfall 解析值以 OUTCOMES.includes(outcome) 归一化——必须是裸字符串 allowed-once/rejected;对象形态恒判 unavailable(fail-closed)——与代理自述逐字吻合。

### 根因(三层)
- 主因(wire 形状):DshApiClient V012 审批应答 value={outcome:...}(对象)→ 服务器判 unavailable → 从未真正放行;app 侧 success=true 是网关恒 ok 掩盖的假阳性。09-03 双门禁 PASS 是 V011 容器(/api/respond 三键形态,真实有效),V012 形态此前零真实验证、零测试覆盖。
- Layer1(always 规则静默不命中):运行期新建会话 cwd 回填与 session-added 帧竞态(服务器 manager 源码注释明言)→ handler 目录可空 → approver 解析空目录 → 具体目录 pattern 不命中(旧进程零日志实况)。
- Layer2(自动应答后卡滞留):手动路径靠本地 removePermission 收卡(应答方不收 cancel/resolved 帧),自动路径成功后不做——即使应答生效卡也不消。

### 修复(三层)
1. DshApiClient.replyToPermission V012:value = 裸字符串(JsonPrimitive(outcome))。
2. AutoApproveRule.matches:会话锚定规则(sessionId 非空命中)免目录检查;savePermissionRule 恒带 sessionId=event.sessionId。
3. PermissionAutoApprover:应答受理(ok)→ repo.removePermission(event.id) 本地收卡(与手动对齐)。

### 回归测试(红→绿)
- v012 replyToPermission eventsResult value is bare outcome string(新,红→绿:断言 value 为 JsonPrimitive)。
- session-anchored rule skips directory check even with blank resolved directory(新)。
- 全量单测 BUILD SUCCESSFUL;[DEBUG-g2c] 探针零残留(grep=0)。

### 真机终验(23:23,生产 :3080 V012,只读 preset 会话)
- probe4(run bash to create /tmp/g2c_probe4.txt)→ 审批卡自动消失(零手动);
- /tmp/g2c_probe4.txt 真实落盘(内容 g2c-probe4)——提权 bash 首次真正执行;
- 代理转述「这次成功了!✅」。

## 二、复盘(什么能预防此 bug)
- 契约形状必须钉死在服务端源码:journal 自己的踩坑记录(回执形状必须按服务端源码定音)在 V012 审批 outcome 上重演——按 {kind:result,value:{outcome}} 形态的假设注释是出发点却从未校准。规则:每个 V012 waterfall outcome 形状必须对 dsh-user-approval(审批)/dsh-web 客户端(提问)源码定音 + wire 测试锁死(提问形状由 G1 生产 E2E 证明,审批形状本次源码+真执行 E2E 双证)。
- 恒 ok 网关下的假阳性:eventsResult 的 ok 不代表落账——DSH 侧「成功」断言必须有代理侧效果证据(执行/恢复),不能只看回包。
- 无正确 seam 的补课:V012 eventsResult 此前零测试——测试缺口正是错误形状存活通道。

## 三、待办(本批余项)
- #327 FAB 队列徽标不显示——**根因已钉死(2026-09-05 源码取证,待动工)**:
  - 服务器契约(dsh-api-session-controller/lib/types/control.js SessionControlController):session/control 流 = 一次 baseline `{type:'baseline',value:{queues,jobs,projections}}` + 增量帧 `{type:'queue',sessionId,items}`(agent/inbox/spliced 触发)/`{type:'jobs',sessionId,jobs}`/`{type:'projection',sessionId,key,value,seq}`。
  - app 断裂点:DshRemoteMuxEngine.onControlValue(L502)把 ctl 流**所有** value 按 baseline 形状解析(`value["value"]?.{queues,jobs,projections}`),增量帧无 `value` 键 → `?: return` 整包静默丢弃 → 入队只产生增量帧 → DshQueueStore 永不更新 → 角标恒 0(与症状逐字吻合)。jobs/projection 增量同病。
  - 修复方向:onControlValue 分型处理三种增量帧,镜像为与 baseline 相同的合成帧(session/queue、session/jobs、session/projection);红测试用上述真实帧形。词表已核对(placement "queued"==PLACEMENT_QUEUED,DshEventMapper L625 是历史行防御忽略非 bug)。
- #326 busy 单键统一——**设计已定稿(2026-09-05 web 源码取证)**:dsh web 主按钮(mod20.js)`primaryStops = running && !subagent && (empty||blocked)` 否则 SEND(忙+文本=排队提交)。移动端映射:idle→SEND / busy+空→STOP(转圈) / busy+文本→SEND(排队,长按=steer #309④) / inputEnabled=false(blocked)→STOP。改造点:sendStopAreaState 纯函数(+inputBlocked 参数,忙碌+文本单键)、SendStopButton 渲染、ChatInputBar showStop 计算、ChatScreenBottomBar 传参;红测试=SendStopAreaStateTest 两用例翻转+新增 blocked 组合。ChatScreen 协议文件串行编辑。

## 十四、#309 批1 收尾验收 + #328 关卡(2026-09-05)
- **#328 关卡**(非 UIUX wire 修复,AI 证据确凿):修复(86d8dab2 error{name,code} 正字法)真机复验成立——A6:提问卡 tap「忽略」→ 卡消失 + wire rejectQuestion success=true + agent 解冻转述 cancelled(旧形态冻结的反证)。卡片迁出。
- **#309 批1 AI 验收 10✔+2BLOCKED**(checklist+报告:docs/acceptance/2026-09-05-309-batch1-and-328.md):A1 完成钮在场+受端效果(tap 后目标移除/goals/complete 受理;实测左起第 2 钮非第 4,如实记录)·A2 /compact 全链(面板→压缩卡→CompactionStarted→SessionCompacted→旧转录折叠)·A3 危险档确认双路径+终态收敛·A4 steer 三尝试 wire 直发证·A6 即 #328·A5 两个 BLOCKED=预设合理降级(⑤-b max-tokens 未触达/⑤-a 需 LLM 故障,以单测 DshEventMapper.kt:463-485+148c0644 作结)·B 组常规/FAB/单键/0 FATAL。UIUX 卡转 [~] 待人工(与 #326/#313 同域)。
- 执行经验:数数窗口竞态(前 2 次轮末错过,第 3 次成)——后续 checklist 的忙碌制造可考虑「写长文」类更慢任务;goal create 会触发 agent 轮运行(表单提交即开跑,如实记录)。

## 二十三、E2E 新立卡三连修:#338/#339/#340(2026-09-06 深夜,用户指令「将新立卡的问题都修了」)

- **#338 时间腿跨钟域(7c9fddc7)**:根因三层实证钉死——①W2 B4「-207ms」非台账腿,是 StreamingElapsedText 无下限钳制(startMs=DSH 服务器信封时刻,设备钟慢 207ms 的窗口内差值为负;ReasoningBlock 早已 coerceAtLeast(0),该组件漏同款);②工具宿主 completed 由 markSessionIdle 本地钟回填,W6-db 直查实证 call_17 completed=created+**3.5h**(21:38 resync 期回填),且 mergeAssistantMeta 的 incoming?:existing 使污染**永久残留**(重开会话轮时长会显示 3h29m)——修法=EventDispatcher 分发点采集域内基准(MessageUpdated.created+SessionIdle.time,先于 FSM forward),markSessionIdle 域一致回填+逐消息 max 钳制,part end 同口径;③零/负跨度=时长未知→null(台账回落「-」,思考行新增无时长变体 i18n×15)。附:B4-goal-ui2.xml bounds 级定位(-207ms 紧贴 Run code 卡下方=消息页脚 ticker),W2 notes 的 RenderableTurn 归因经取证**翻案**。
- **#339 resync 通知族(b2a84aea)**:①径②清除延后复核(2s settle+届时仍 Idle 才清)——W5-logcat 21:06:59.723 实证伪边沿 .725 即回 Busy/Waiting,且**通知层对同一事件已判 6min 陈旧**(Skip stale idle)而 pending 域无判,单边沿双判据不一致是设计裂缝;②SessionError+time(映射器 turn/end 透传)+5min 陈旧过滤;③重放用户消息不再重置 streak(created 陈旧门);④重发布前有界等待注册表水化(250ms×12)+question/error 文本剥离 <system-reminder> 注入语料。断开撤除/TTL 仍留产品裁量未实现(卡内注明)。
- **#340 持久化背压(07069ad0)**:旧管线 BUFFERED(64)+trySend 满即丢(N=1150→1500 实证,丢的是**最新**写含终态修复)——重构两路:全量 upsert 按 (sessionId,messageId) 最新快照合并(latest-wins 幂等)+128 条/250ms 阈值批量刷洗(每会话单次调用=单事务);增量 delta 走 UNLIMITED 保序队(流式速率有界 48ms 批)。
- **验证**:单测 +22(全绿):MessageEventHandlerTest×5(域回填)/RenderableTurnTest×3(零跨度)/PendingInteractionStoreTest×2(伪边沿)/SessionNotificationCoordinatorTest×6(陈旧门/水化/消毒)/DshEventMapperTest(时刻契约)/CoalescingPersistTest×4(5000 条洪峰零丢失);全量套件与真机 resync 场景验收见下续。
- **方法论**:取证优先于归因——三卡全部靠既有证据(W6-db 直查/logcat 时间窗/dump bounds 定位)翻案或钉死,无一凭 notes 表面结论动手;「执行员观测≠结论」定律再+1(W2 的 RenderableTurn 归因错误)。

### 二十三附：真机验收记录(2026-09-07 06:46-07:25,设备回网后补做)

- **环境**：设备无线调试随机端口 45963(mDNS 发现——5555 重启后失效);构建 06:37 装机→07:25 终版(含陈旧错误日志插值修正)复装;POST_NOTIFICATIONS 重授;reverse tcp:3080。全程 FATAL=0。
- **#340 ✔**：四轮 resync 全程 `persist queue full`=0(旧版同场景 9 条 WARN/N=1150→1500);`coalesced flush` 17/14/4 批×195-300 msgs(sessions=2-3/批);库 integrity=ok 12,981 条。
- **#339 ✔ 活体链**：新会话(zai GLM-5.3-Flash)构造 red/blue 挂起问题→服务器侧 Busy→HOME 补发通知(opencode_questions id=428455022)→force-stop 断开→重连 resync→**QuestionAsked 重放(07:20:47)→通知重发布(id 回场)+零误撤(四轮 Revoked=0)**;旧错误轮零重发(stale error 13 skips)/旧 idle 124-129 skips;终版复装后同链复验通过;red/blue 留服务器侧自然超时(tea/coffee 先例)。
- **#338 ✔**：resync 后 B4 全部 9 个工具宿主 completed 域内修复(call_17:+3.5h→轮末信封 306495),轮1 台账数学回归 3.7s(=W2 未污染基线);UI 轮4 3.2s 正常、「思考完毕」无 0ms 后缀(全 dump 0 例);存量 2544 条 dsh-call 集中未回放旧会话(打开自愈);轮1 UI 文本因台区 dump 盲区未能直采(值由 DB+单测+基线三重钉死)。
- **顺带发现 #341**:发送确认弹窗 adb 注入免疫+双发僵尸(立卡待人工手指);另证标题双机制——新会话即得语义标题「Ask User Tool Red or Blue」。
- **操作层经验**:adb reverse --remove 不杀存量隧道(断连测试用 force-stop);MIUI `cmd connectivity airplane-mode` 未生效;B4 变体=deepseek-official(Insufficient Balance 实锤),设备端会话模型走 zai-coding-cn 组。
## 二十三B、#342 会话列表整列红底——三卡验收后的用户报障(2026-09-07 07:3x-08:0x)

- **现象**:用户发现会话列表整列红底。像素取证:行区主色 (128,39,31) 暗/(245,223,221) 亮=errorContainer。
- **根因(M3 1.4.0 源码 jsdelivr+本地 AAR 字节码双证)**:SwipeToDismissBox 的 backgroundContent 是**无条件全尺寸常驻**的 Row(无 alpha/zIndex/offset 门——字节码 grep 仅 matchParentSize 一处),而 SessionRow 前景 Row 无底色(透明)→ errorContainer 恒透出。
- **溯源**:自 #311 Task2 左滑归档上线(0498e775,09-05 22:2x)起恒在——该验收与 09-06 全量 E2E 列表截图(A0/A1/A2/B10/B14 等 12+ 张全红)均 XML dump 主导(色盲),且「左滑手感」人工项(311 验收 L125)未做,双通道皆漏。
- **修复(1c68f2fa)**:SwipeToArchiveBackground 接 dismissState——rest/复位/正向位移不绘制(透出列表 surface),仅负向位移(EndToStart 锚=-width)绘制;shouldRevealArchiveBackground 纯函数(-1f 亚像素容错)+2 单测。
- **真机复验 ✔**:装机(07:54)后列表行区全带 (247,250,253) surface,红底清零;左滑揭示手感终验留人工(原 311 人工项)。
- **方法论**:①「验收证据模态要覆盖断言维度」——结构性 dump 验收对颜色类缺陷天然盲,视觉类必须有像素/截图采样位;②用户肉眼是最强回归器——12+ 张红截图在证据库躺了两天无人看。
## 二十二、全功能端到端真机回归(2026-09-06 11:14-22:30,用户指令「完整/全面/没有遗漏/所有」)

- **三步法全量执行**:checklist v2(55 item+12 域去留表,纯净审查 2 轮:NEEDS-REVISION 22 条全落实→PASS)→六波纯净执行真机串行(W1 会话树/W2 消息卡/W3 队列流式/W4 设置工作区/W5 配对通知/W6 通用清点)。
- **总账**:39✔+4 修复闭环+3 改判非缺陷+5 预案内 BLOCKED+2 服务端环境✘+2 待人工;FATAL=0/ANR=0(~189 万行 logcat),服务器条目恒=2,数据零丢失(H2 integrity=ok,12,764 消息在库)。报告:`docs/acceptance/2026-09-06-full-e2e-regression.md`(总报告)+checklist(55 item 全终态)+`e2e-2026-09-06/W1~W6-notes.md`。
- **修复闭环 3+1**:A2 行升顶滞后(**60a44edf**——web mod04 handleSessionActivity 单调合并 vs app 丢时间载荷;6s 复验✔)/mkdir DSH 双缺陷(**fed82377** 原生 directoryPicker/createDirectory+泄漏可观测)/Add 死代码接线(**9bf0bf9c**;真机 tap 表单全字段✔)。
- **改判 3**:B8(应答已送达工具已解析,模型自行收尾)、E3c(断开撤除=超契约发明,#320 三径原文核对)、C3 跳转(web locale 定音=笔误)。
- **新卡**: #338 负时长族/#339 resync 通知族(伪 Idle 误撤+错误重发+水化竞态,改动面大记录)/#340 Room 背压丢写。
- **方法论高光与教训**:instrument 先行(A2 三源定音:服务器事件面+web 源码+app 代码);宿主向量选错代价(A1 BLOCKED→session.list 直查破案:宿主派生=origin subagent 被滤系设计,subagent_fork 同;正确向量=API 直建顶层会话);执行员观测≠结论(B8/C3/E3c 均靠主 agent 裁决翻案——三步法「执行期禁分析」的价值反证);重装触发通知权限弹窗(权限重授);dump 失明族再+4 例;MIUI 组通知 adb tap 不可达(待人工)。

## 二十一、续篇后裁决归档(2026-09-06)
- **#329 用户裁决:保持 web 平价**——乐观本地 pending 呈现选项否决(服务器 inbox 语义:流式期 hold→轮末 drain,web 队列镜像同源同形同样不显示;无数据丢失 #326 A3 已证 44s)。卡片以记录项完结迁出 backlog;将来 web 端若先行乐观呈现再跟进对齐。
- **#325 SSH 子项用户裁决:暂不引入**——sshj(~1.5MB 新依赖,红线)否决;配对维持 adb 注入+QR 深链双通道(局域网场景全覆盖),远程 SSH 需求出现再议。#325 主卡保持 [~] 待人工(其余三子项已落地验收)。
- **goal goal-76f442b9 关账(rev4 complete)**:续篇全清——9 卡关卡(#308/#321/#327/#328/#330/#331/#333/#335/#337)+12 卡 [~] 待人工(#336 含)+#334 非缺陷+#329 平价记录(本轮裁决);P4/外部记录项存续 #332/#288/#146。

## 二十、续篇级联清理收官(2026-09-06)
- **六卡全修+验收**:report:docs/acceptance/2026-09-06-cascade-fixes.md——#330(42ceff63 remove/order 全链)/#335(0bbe999f corruption 留档+重置)/#336(279b7639 退后台补发+已通知槽去重;终验 A4 三段链✔)/#333(c939355c 旧会话 follow snapshot 自愈——服务器源码实证冷会话 asOfSeq 合法缺席;44.5h 会话重进转录全渲染✔)/#331(734fa702 三腿:added 帧 updatedAt 透传+origin 判别+echo 时钟;A1 新建顶位✔)/#337(b95a2bc9 revoker 父槽镜像+三侧共享冒泡函数)。
- **A2 三轮终局(方法论高光)**:r1/r2 同形态(dispatch 在案列表不加行)→插桩回路四 seam 定罪——**数据层全绿,断点=LazyColumn 滚动锚定竞态**(离开期间前插行,返回 scrollToItem(0) 先于重订阅新态执行,锚点拉回旧头视口冻结;冷载新建 listState 起步 0 故可见——完整解释冷热差异与静态测试盲区)。修=前插揭示(head 变化+组合期 firstVisibleItemIndex==0→scrollToItem(0);第一版 key 判据被真机复核击穿后改组合期读数)——010ef55a,真机 3 连复验 fork 行即时顶位(含 fork-of-fork)。
- **关卡**:#330/#331/#333/#335/#337 迁出(非 UIUX/行为恢复,仪器证据);#336 转[~](新用户可见行为入人工域);#329 终裁=平价记录项(web 同源同形,无数据丢失;乐观呈现增强选项留用户)。
- 环境注记:MIUI POST_NOTIFICATIONS 默认拒(appops ignore)——通知类验收先 grant;设备 deepseek-official 通道欠费(测试切 zai flash 完成)。

## 十九、级联未修卡清理(2026-09-06 续篇)
- 用户纠偏正确:按既定门槛(仅>10轻微/大改动面/硬阻碍可只记录)级联卡应修非仅登记。本段清理:
- **#330 修**(42ceff63):workspace remove/order 增量全链(engine 合成帧→mapper→bind(红阶段当场抓漏 bind 坑)→store applyRemove/applyOrder)。
- **#335 修**(0bbe999f):DataStore corruptionHandler=损坏日志+corrupt-<ts> 留档+仍重置防崩溃循环;纯函数提取直测 3 用例。
- **#336 修**(279b7639):退后台补发(fg→bg 转换沿扫描 pending,已通知槽去重,防 SSE 重放重复;14+5+7 用例)。
- **#334 改判非缺陷迁出**:三源证据(代码审计无复活路径+仓库两源真机互证 DEL 聚焦态有效+2026-08-22 先例已裁测试手法问题;跨冷启持久=#54 特性);方法论注记「自动化清稿=全选替换法,禁 keyevent 67 连发计数」入 ai-acceptance-workflow。
- **#337 立卡**:#334 诊断线顺带发现 revoker 子会话撤通知 id 槽错配(#320 遗留)。
- 进行中:#331/#333 诊断修;#329 考据定夺(初判 web 同形平价)。

## 十八、终轮合并验收(#320/#322/#323/#325/#324)+批次收官(2026-09-06)
- **实现**:四卡落地——#320(4e25b716 审计发现发布链既有全覆盖,补三清除径撤通知+标题回退)/#322(c7170723 searchSessions+命中区)/#323(ea52a106 命令反馈卡 commandId 原位刷新)/#325(9ad9bb03 四通道裁决:adb 实现/QR 深链降级/SSH 暂缓[sshj 依赖红线]/sameBackend 修)/#324(四域 5 commits:provider CRUD/preset/插件清单+动态表单/skills 触发组)。
- **终轮合并验收 12/12**(报告:docs/acceptance/2026-09-06-final-combined.md):首轮 10✔+2✘→修复循环:F1=#324 提供方页嵌套 LazyColumn 100% 崩溃(3cb324a8 Column+forEach,唯一同款嵌套)/E1=深链不导航(根因=**adb 客户端不转义 &**→token 参数丢失→app 静默拒绝;修 7a31a788 详细拒绝原因+日志+脚本转义命令)→r2 双翻案(F1 页开不崩/E1 预填对话框首现+exchange ok)。D1 到达时语义→#336。
- **⚠️ 设备数据事故(透明披露)**:F1 线 connectedDevDebugAndroidTest 的 Gradle UTP 通道**卸载已装 dev flavor 再装→服务器配置五条目清空**(05:13:29);恢复:Host-4199(debug-entry.sh 一条命令)+prod-3080(dsh-pair USB);**不可恢复三条**:192.168.110.248:248/V1-4198/dsh012-a5(凭据不在宿主,需用户手输重配)。根因链:我的调度失误(双 fix agent 并行共用真机,违反自家串行纪律)+UTP 卸载行为未知。衍生:#335(DataStore 无 corruptionHandler 静默清空,含跷跷板三连新实证);教训刻录:**androidTest(UTP)严禁生产用户设备;设备级 agent 必须全串行(含修复 agent)**。
- **批次总账**:13 原卡全处置(4 关卡:#308/#327/#328/#321;9 转[~]待人工:#326/#313/#309/#310/#311/#312/#320/#322/#323/#325/#324——实际 11 卡含 #312);级联立卡 #329-#336(P3/P4 为主,#333/#335 P2 待后续);全量单测 3050+ 绿;i18n 867 键×14。

## 十七、#312 S 池实现+验收(2026-09-06)
- 实现:①相对时间戳 timeAgo(4 显示点)/②数学块降级方案 C(transformMathFallback→normalizeMarkdown 单点,20 单测;流式取舍=只接完结管点)/④SlashCommandGate 命令带图拦截(wire 级不派发)/⑤fork atSeq 全链(seqOf 反解→wire→台账行「从此轮分支」→导航入子会话)——f6e288b7+4b5f1618;③spill 三路 grep 无事件→#332 P4。
- 验收 6✔+A2 终裁✔(报告:docs/acceptance/2026-09-05-312-s-pool.md):A2 执行员判 ✘→主 agent 改判——用户卡纯 Text=既有设计(TUI 对齐,MessageCardUser L360 注释),实现者「用户卡全覆盖」断言失真(单测直调函数未证卡片接线);markdown 面交叉事实已证围栏可渲染;下轮补助手面定向确认。fork wire 112ms 受理+VM/HistorySync 即时绑定。
- 级联登记:#333(P2 旧会话转录空白)/#334(P3 草稿清理);#331 扩(含 fork 派生会话)。
- 卡转 [~] 待人工。

## 十六、#311 批3 全子项实现+验收(2026-09-05)
- **实现**(契约 docs/research/2026-09-05-311-wire-contracts.md):Task1 workspace 数据层(4ced86f5——**契约勘误:V012 无 workspace.list,权威源=follow baseline**;archiveSession 集合替换式)→Task2 归档 UI(0498e775 行菜单/左滑/折叠区;**四重取证:归档单向无取消动词**)→Task3 多 workspace(0fd15da8 新建对话框三态数据源+web connectWorkspace 四条件复用语义;upsert 增量接线)→Task4+5(3ac2dd71 deliverables 纯 fold 写类工具域+skill 折叠卡+ask_user_question 双名分派)→Task6 待审批点(a69fd71a 本地 pending 域三清除径,**FSM 零动**)。
- **验收** 13✔+3 BLOCKED(报告:docs/acceptance/2026-09-05-311-batch3.md):归档链全绿(菜单三态/增量不重启/折叠区/左滑 200ms/冷启 F0 baseline 路双证)·多 workspace(对话框建模/入组计数 upsert)·待审批(琥珀点像素证 #f5c344/应答消/question 族 D2 顺带三态补全)·0 FATAL。
- **BLOCKED 族=DSH prod harness 工具恒经 run_code 内联**(agent 原文直证):write/edit/skill/ask_user_question 直发不可达→产出行/skill 卡/question 行在该 harness 休眠——**与 web 契约同构**(web 同场景只显 Run code);交互面/宿主受端证据全通,折叠域与分派由单测矩阵钉死(TurnDeliverablesTest 17/SpecialToolCardsTest 9)。
- 级联登记:#330(workspace remove/order 未消费)/#331(会话行 3min 时延);其余观测(首次 BACK 内部文本块/冷启不自动重连/列表非严格时间序)journal 记录。
- 卡转 [~] 待人工(域汇总)。

## 二十八、#349 真机腿→DSH 面根因深修:code-dispatch 升格+装配器同卡合并(2026-09-07 20:xx-21:3x,用户指令「开始后续任务」)

- **素材生成**:fb650391 发送「Please dispatch one subagent and tell me when done」→GLM-5.3 经 run_code 派 tools.subagent(前台)→子会话 219905a7「This is a connectivity test」(14.6s,轮次 8·3 步·1 工具)。
- **首验受挫→根因链**:卡体点击无效(顶栏不变)→排除坐标污染(误中「更多选项」菜单)→RPC 直查服务器 session/page 定位 wire 真相:**DSH 派发被 run_code 包裹,tool/call 名恒=run_code;childSessionId 仅存两处**——code-dispatch 回执(bg="started subagent <uuid>")与根 tool/result 信封(fg={kind:foreground,runId,output});DshEventMapper 从不写 metadata→navTarget=null。
- **三层修复**(全部根因向):①mapper 升格 subagent/subagent_fork 族 code-dispatch 为子代理卡(键={root}:subagent,无状态跨事件汇合;bg 派发即得 id,fg 由根信封补写);②**服务器双份信封形态**(fb650391 seq12556 实证 959 字符=2×480,"{…}\n{…}",整串 parse 恒失败)→firstJsonObjectOf 花括号深度扫描取首份;③**顺带根因**:DshMessageAssembler 同 id part 按到达序 append→历史页每卡携带全部中间态副本(×N 角标+陈旧首份胜出;DSH 每张工具卡 Pending+Completed 双份=潜伏 bug)→改 mergePart 原位合并(与 dispatcher 同语义)。
- **调试方法论沉淀**:JVM 管线测试(EventDispatcher 全装配+fold+assemble 双路径)复现设备行为→真 page.json(225 行)直喂定位双份信封;测试载荷转义坑:raw string 里 `\\\"` 双重转义产出带回杠文本→parse 失败假阳性(以 $envelope 模板+replace 嵌入为准)。
- **真机终验**:重进会话→子代理卡箭头在场(metadata 落位)+×2 角标消失(装配器修复);卡体点击→直达子会话「This is a connectivity test」;BACK→回父会话「Please use your ask_user_question tool」;Room 直查 metadata={sessionId:219905a7…}双写。附带活体实证:同服务器 23a614ec 会话 bg 派发卡 running 态即带 id。
- **测试**:DshEventMapperTest +6(start/非 subagent 忽略/bg id/fg 无 id/fg 信封关联/双份信封/克制触发);DshSubagentCard349PipelineTest 新建(dispatcher 管线 fg+bg、fold+assemble 真实四连发);全量单测绿。

## 二十七、统一审计批 1+2 落地:FAB 门控/#347/#349 + #348 气泡菜单与堆积链重建(2026-09-07 14:xx-20:xx,用户指令「开工」)

- **批 1(36734c23)**:FAB 入口能力位门控(ChatFabMenu entries 参数化+几何随动;GOAL=goalSupported/SHELL=terminalSupported;真机双面:V2=TODO/智能体/Shell/队列 无目标,DSH=TODO/智能体/目标/队列 无 Shell);#347 左划归档下线(SwipeToDismissBox/#342 背景/测试全链移除,长按菜单唯一入口,真机左划无反应);#349 ToolCardScaffold onCardClick 槽+TaskToolCard 本体点击直达(chevron 承接展开)。
- **批 2 = #348 主体**:StackedMessageStore 精益重建——DataStore JSON 持久化(替代旧 Room 表,无迁移面)+三触发(T1 心跳 5s 补偿/T2 入队即查/T3 statusFlow Idle 转移含首见)+at-least-once(send 成功才删)+护栏(非 Idle/hasPendingUserInput/无归属跳过);语义复刻 ce8cbc1e+#176/#177(agent 空串/model 不携带);UI=气泡菜单(Popup 锚 composer 右上)+chips 条(编辑/移除/立即发送/draining 锁);#326 修订=busy+文本点发送改弹菜单(空闲路径零改动,长按 steer 保留);EventDispatcher SessionDeleted 级联;i18n 9 键 x15(877 keys 过检)。
- **#348 真机六腿验证**:忙时点发送弹菜单(两轮复现,文案正确)/空闲直发无误弹/堆积清输入+chip 在场/忙时 chip 滞留/轮末自动 drain(消息入转录+轮次台账+chip 消失)/轮已结束时堆积 T2 即时发出(意外验证);附带实证 #343 零跨度台账真机在场(轮次 6 · - · 1 步 · 0 个工具)。
- **过程坑**:测试 JVM OOM 根因=T1 无限 delay 心跳 x 虚拟时钟 advanceUntilIdle(时钟无限推进任务队列无限膨胀)——加 disableCompensationHeartbeat 测试门(JVM instrument 断言为其先兆);/tmp 被系统清理(取证脚本重建);真机 FAB 定位:composer 在场时上箭头钮浮于 (1056,2160) 区非底栏——扫描 clickable 节点定位;SSE 中途断连(重试中9)为环境噪声不阻验证(服务器 running=False+转录收据已足证)。
- **验证**:全量单测绿(含 StackedMessageStoreTest x9);i18n 877x14 PASSED;dev 包装机两轮。
## 二十六、走查反馈批:#346 即修 + #347-#349 裁决立卡 + 确认关闭(2026-09-07 11:4x-12:0x)

- **用户走查九站反馈**:⑤FAB 形态 ok/⑧设置 ok（关卡）;④渲染/⑦反馈委托 AI 验证（既有报告证据引用）;①问题通知不可见→诊断+即修;②左划归档否决→#347;③忙碌直入队否决→#348;⑥subagent 卡点击→#349;⑨断开通知滞留重新解释待裁决;「发送消息前不用确认」→设备开关已关(默认本 false,系前期测试开启)。
- **#346 根因与修复**:用户「没看到问题通知」而 dumpsys 在场(id=724696787,importance=4,文案正确)——三类需关注通知(问题/权限/错误)setGroup(server_x) 挂在 **LOW 重要度 tasks_silent 组汇总**下,MIUI 整组折叠成一行静默项子卡不可见(同 E4②)。修复=三构建器退组独立成卡(轮完成静默流保留分组);真机验证:resync 重放 QuestionAsked→到达发布→groupKey=自身独立键、静默组汇总消失、cancel 后撤销链保持。附带定论:问题通知持久性=挂起恒在/轮终即撤(超时死链撤除正确);E4② 组卡 tap 不可达随组卡消失而消解。
- **诊断插曲**:素材会话 fb650391 在问题超时后模型自主继续 run_code(失控跑)——session/cancel API 止损;debug-entry 冷启只连 4199 致 prod-3080 事件流缺席(runbook 已有⚠️,本次再证:验证前先连全服务器)。
- **发送前确认关闭**:设置→聊天行为→发送前确认 开关已关(默认 false,前期测试遗留开启);用户三感叹号强调,记录为永久偏好——后续 E2E 遇确认弹窗应视为配置异常而非流程步骤。

## 二十五、新立卡三连修:#343/#344/#345(2026-09-07 09:5x-10:3x,用户指令「修复新卡,要求根因修复」)

- **#343 台账吞行(语义解耦修复)**:根因=MaybeTurnLedgerRow/MaybeProducedFilesRow 用 durationMs==null 兼当「未完结」判定——而 durationMs 语义是「跨度可测与否」(#338 零跨度=null);DSH 单消息轮(整装事件 created==completed 同信封,活态流式宿主又被整装 MessageRemoved 拆除,轮组只剩零跨度单行)恰好落进两语义交集被整行吞掉。修法=RenderableTurn 新增 allStepsCompleted(≥1 assistant 且全带 completed)独立承载完结信号,两门控改判它——零跨度完结轮照常渲染「-」,流式中缺席铁律不变(assistantsForMeta 任一 completed 缺席即 false);统计栏等 durationMs 消费面纯显示零改动。**真机双径验证**:活体新轮轮末即现「轮次 3 · - · 1 步 · 0 个工具」(旧代码无行);重载后历史 fold 双行(dsh-t3s1+seq-1246)凑出真实 15.4s 照常呈现。RenderableTurnTest +4。
- **#344 通知注入语料(两层根因修复)**:根因链钉死——#336 退后台补发故意传空串→AppNotificationManager 回退「最新用户消息」→DSH 把 skill catalog/workspace 指引按 user/message 入库(Room 实证 seq-10..13 同刻四行,注入行晚真 prompt 1ms 恰为最新)→注入全文成为正文;服务器侧问题文本本身干净(seq-207 arguments 直证),#339 消毒器只挂在到达路径,回退路径从未消毒。修法两层:①PendingInteractionStore 条目化(kind+text;record 携带问题/权限原文,同 kind 空值不抹/非空覆盖,EventDispatcher 两记录点接线)→补发携带真实载荷+消毒发布;②回退侧 isNotificationPreviewText 选段谓词(消毒后非空且不以标记开头:整块注入/未闭合前缀拒收,嵌块+真文本放行)+预览管线统一过 sanitizeNotificationText。**真机验证**:同场景(挂起→HOME→dumpsys)正文=「Verify six forty four.」非 reminder;作答后撤销 ✓。单测 +11(store 载荷 4/补发载荷 2/预览过滤 3/断言适配)。
- **#345 定性(非 app 缺陷,不修 app)**:二查翻案第二案——Doubang 浅色键盘与 app surface 同色族 (247,250,253),像素分析误判「无 IME」→tap 实打键盘;7 节点 dump=输入法安全窗致盲(平台正常)。第一案仍疑 MIUI 注入丢弃家族(E4② 先例);两案键事件/焦点全程有效(mServedView 在场),app 侧无缺陷证据。定向复现(IME 抬起+乱序 tap 轰击)未再现。缓解纪律沉淀 real-device-testing.md(IME 权威判定 dumpsys input_method/tap 失活二分/冷启恢复);卡保持 [ ] 观察,真手指复现才升级。
- **验证**:全量单测 3162 BUILD SUCCESSFUL(含修配 6 处 VM 测试桩条目化+311 测试断言 ?.kind);dev 包装机复验(10:08);三卡真机证据见 backlog 卡内。
- **方法论**:「一个字段承载两个语义」是 #343 的病灶(完结 vs 可测),解耦而非在门控补特判;#344 的根因在数据流设计(补发空串+回退裸取),修源头(条目带载荷)而非在回退再叠一层过滤了事——过滤仅作纵深防御层。

## 二十四、可仪器化验收批:9 项真机执行(2026-09-07 08:1x-09:4x,用户指令「开始可仪器化的跑掉吧」)

- **背景**:ea63f9a1 文档重构后,人工清单中 9 项仪器可验条目转 AI 真机执行(分类新规首次批量落地)。设备 192.168.110.239:45963(mDNS),prod-3080(reverse)+Host-4199 双服务器,全程 logcat 取证(/tmp/e2e-instr/,240 文件)。9✔/0✘,另 2 项素材生产受阻如实降级。
- **I2 ✔ 新建目录**:向导→打开其他项目…→/home/leo-tkp/workspace→新建目录 i2-mkdir-test→宿主 ls 落盘(08:24)✔;服务器会话集 168→168 零新增、mkdir 标题行仅存历史泄漏 e9c61e1a(修复前残留)✔;对话框正常关闭无错误弹窗(dump)。宿主侧测后清理。
- **D4 ✔ fork-of-fork**:Red-or-Blue 会话→菜单分叉会话→fork#1=d1730150(服务器 TOP1)→fork²=27a667f6(TOP2=d1730150),UI 列表返回即见两行 08:29 顶位双 fork(截图 d4-list-top.png);fork 成功即入新会话(onNavigateToSession)与 #331 主链一致。
- **D5 ✔(question 族)待回答指示**:b7b3cdc1 挂起问题(下述)回列表→行内「待回答」标签(dump 结构腿)+图标/文本像素 (47,100,127)=primary 一致、区别背景 (247,250,253)(d5-row.png 像素腿)。**琥珀 APPROVAL 腿素材不可生产**:prod-3080 审批禁用(approval/policy never);4199 复活后尝试 touch 命令触发权限——agent 未发起工具调用(0 工具),该腿维持服务器侧前置(见 I5 注)。
- **B3 ✔ 轮次台账**:在场(「轮次 1 · 16m 38s · 3 步 · 1 个工具」dump)·折叠弱化(像素:台账墨 (183,188,191) vs 同屏标签 (128,131,134),反推 alpha≈0.35-0.52 区间=FAINT 族,b3-collapsed.png)·展开(ask_user_question chip+工具调用:1+从此轮分支,dump)·流式中缺席(600 词长文流式 seq 812→1203 攀升期 ledger=0 双 poll)。
- **I1 ✔ contentIntent 契约腿**:HOME 退后台→问题通知(opencode_questions,id=-35536795,importance=4)→dumpsys 提取 contentIntent=PendingIntent{startActivity}✔→按 AppNotificationManager.createSessionPendingIntent 构造克隆(OPEN_SESSION+server_id/sessionId/session_path)am start 直发→直达 b7b3cdc1 聊天页且「待你回答」卡在场(dump)✔。加餐:卡内选 A→提交→服务器 seq 207→210 受理;问题通知届时已撤(dumpsys 无该 id)——G 域「应答后撤销」仪器腿顺带闭环。
- **D6 ✔ 搜索双区并存**:query=e2e06→「消息匹配」区(角色/时间过滤 chips+snippet 行+「29 条消息」计数)与「会话」区(标准行:标题/目录/时间/收藏)同屏 bounds 断言(d6-search.png)。
- **I3 ✔ Provider Add 表单**:目录区 Add→六要素全在场(路由 ID+规则提示/显示名/Base URL/双协议 chips/API 密钥「不回显」注记/发现模型空态)→取消→目录行数 8→8 不落库→刷新钮独立工作(logcat:llm/listProviders+listConfigurableProviders 双 RPC 09:22:13)。
- **H3 ✔ dsh-pair 深链**:`--lan` 生成 ocbeacon://pair?url=…248:3080&token=…→am start VIEW→添加服务器对话框:DSH 模式+URL 预填+首次配对辅助区三段文案(dump);logcat 四证链:Pair deep-link received→Home prefill→**pair token exchange: ok**(后台交换成功)→取消不落库。
- **I5 ✔ Host-4199 一轮收发**:4199 模型通道已复活(服务器侧自愈)。无标题会话发「reply with one word: ok」→30.4s 正常完结(轮次 2 台账)+助手回复在场;logcat 零错误。附:4199 上 0 工具轮**有**台账——反证下条 #343 为 DSH 特有路径。
- **新发现登记**:**#343**(DSH 单消息轮台账缺失——流式建行 dsh-t3s1 与完结行 seq-1246 双行均 created==completed 零跨度→durationMs=null→MaybeTurnLedgerRow 门控吞行;Room 直查+4199 对照双证)、**#344**(提问通知正文携带 <system-reminder> 前缀——dumpsys extras android.text 以其开头,净化器未覆盖该实例)、#345(adb 注入 tap 失效态观察,见下)。
- **环境观察(#345 素材)**:两次进入「APP 可 dump 但注入 tap 全灭」态(键事件仍通)——①HOME+am start 克隆任务(t4401)后;②向导输入+连发失败后。均以冷启(debug-entry)/BACK 退桌面后 am start 恢复。疑 MIUI 注入节流;uiautomator 在 Doubang IME 抬起时恒 7 节点失明。已记 #345 待复现定性。
- **操作层经验**:①IME 抬升后发送键恒 (1086,1616)(备忘坐标再次验证),IME 收起后 composer 发送在 (1086,2538);②问题卡契约=选项本地勾选+底部「提交」主按钮(自定义答案走输入框纸飞机),与 QuestionPartContent(消息内嵌渲染)是两套组件;③grep -c 空匹配时「|| echo 0」会拼出 0 加换行 0 的双行假阳性(轮询判定坑);④debug-entry 冷启会重置任务栈与输入态——遇注入失活先冷启;⑤deepseek-official=Insufficient Balance 恒欠费,设备端会话模型一律 zai-coding-cn 组(glm-5.3 可用;flash 变体未单测)。

## 十五、#310 批2+#321 实现与三轮验收收口(2026-09-05)
- **实现**:契约钉死(docs/research/2026-09-05-310-wire-contracts.md)→五子项落地——①subagents/prompt·interruptByParent·list+composer mode 门控(905f25fd/8a1b058b) ②messageFeedback CRUD+CAS 冲突重试+👍/👎 脚部钮(93a6c9f8) ③plan 投影三通道+PlanChip+plan-review 审阅卡(b7822e7e) ④轮次台账行(c7e530da) ⑤@会话源+文件并行候选管线(6a903a42 数据层+753ee6fc UI,主会话亲自实现——连续三次委派失败后)。
- **验收三轮**(报告:docs/acceptance/2026-09-05-310-batch2-and-321.md):轮1 9✔3✘1BLOCKED→**验收驱动根因修复 4 项**(方针步骤4 完整实证):6290102b(空 sessionId 静默跳过——懒创建会话首条消息前)·8faf940c(子会话 durable {kind:subagent} 地址 follow/history 双腿+added 帧父址键误读)·4b463eec(quoted @" 去引号)·098e89a2(host/session-removed 对 durable 子会话=处置广播非删除→降级 Idle;history fold 收编 subagent/model-selection-policy+model/selection log-only 词汇——后者波及普通会话全量 drain 451 次)→轮2 A1/A2 翻盘→轮3 A3/A8 全绿(子会话转录持久+完结回复呈现+不回落空态+二次路由正确+停止链完整)。
- **关卡**:#321 非 UIUX(数据管线修复,文件候选恒空已破,A1/A3 正向证据)→迁出;#310 UIUX→[~] 待人工(域汇总)。
- **方法论沉淀**:执行员「失败聚类+铁证日志」→主 agent 根因裁决→修复→定向重跑=方针修复循环首次完整三轮跑通;「checklist 排序陷阱」教训(空会话状态测 @)入 checklist 前置修订。

## 十三、#327 关卡 + 级联发现 #328/#329(2026-09-05)
- **#327 修复**(commit e8f2461a):onControlValue 按 type 分型(queue/jobs/projection 增量帧镜像为合成帧);红→绿 3 用例。
- **#327 AI 真机验收全绿 10✔/0✘**(checklist+报告:docs/acceptance/2026-09-05-327-queue-badge-chain.md):角标 0→1→2→1→0→空全生命周期·Sheet 列表/计数/三动作·updateQueue 移除(服务器 splice canceled 同刻互证)与插话(「引导至下一轮」next-step 注入互证)·轮末自然消费链·#326 单键交叉回归·0 FATAL。非 UIUX 分类 → AI 证据确凿关卡,卡片迁出。
- **#313 随动**:审计证实主体早已落地(FAB 五入口/QueueSheet/QueueDock 删除/i18n),最后断点即 #327 → 本体验收随 #327 全绿,卡转 [~] 待人工(与 #326 同域)。
- **级联发现 #328(P1,已修 commit 86d8dab2)**:验收执行员实测提问卡 reject success=false + 代理冻结 turn83 → 源码取证:网关 parseRemoteEventRejection 要求 error.name 非空,旧 {message} 单键拒收 → ok:false → waterfall 冻结。修复=error{name:UserQuestionError,code:ASK_CANCELLED}(web questionError 正字法);红→绿;待真机复验。
- **级联发现 #329(P3 仅记录)**:流式期入队走服务器隐形 hold 不产快照(角标不可见)——服务器 inbox 语义,app 端乐观呈现可行性后续评估。
- **方法论回响**:#308 事后定律再中——「未校准的 wire 假设」(注释自曝 error 形态待 E2E 校准)与恒 ok 网关假阳性家族;每张 wire 卡必须服务器源码定音+红测试锁死。

## 十二、#326 单键统一:实现+AI 验收全绿(2026-09-05)
- 实现(commit 555c2ba4):SendStopAreaState 单键状态机(+inputBlocked 参数,web primaryStops 源码取证)+SendStopButton(hasText/inputBlocked 传参)+ChatInputBar 接线;红→绿 12 用例;全量单测过。
- AI 真机验收 10✔/0✘/0BLOCKED(checklist+报告:docs/acceptance/2026-09-05-326-single-send-key.md):四态全验无双键并排·A3 排队(忙中受理→44s 轮末消费)vs A4 steer(busy 中 ClientSendParts 直发)时序可分·A5 blocked=停止键+输入禁用+应答恢复+宿主探针落地·V2 形态/shell 长按回归·0 FATAL。
- 裁定记录:A4「agent 正文含 STEERED」为证据装饰非特性断言(模型 thinking-only 行为,观测附注);A1 并入空文本禁用断言。
- UIUX 卡:转 [~] 待人工验收(清单与 #327 同域汇总)。
- 观测附注(不立卡):uiautomator 对 V2 Markdown 渲染区/部分 a11y 标签 dump 失明(两轮+重进一致,平台已知失明区);发送键 content-desc 间歇 dump 缺失(testTag rid 兜底可用)。

## 十一、#308 AI 真机验收关卡（2026-09-05,方针首跑）

- **三步法执行**:checklist 生成(18 项)→ 纯净审查 subagent 两轮(NEEDS-REVISION 六处修订→PASS)→ 纯净执行 subagent 真机逐项(报告:`docs/acceptance/2026-09-04-308-dsh-approval-wire.md`,checklist+逐项实测记录+收尾数据单文件自包含)。
- **终态:17 ✔ + 1 BLOCKED(A7 环境配置性非缺陷)**。关卡依据:ai-acceptance-workflow §1(非 UIUX 卡,AI 可观测性证据确凿即关闭,2026-09-04 /grill-me 裁决)。
- **关键证据链**:A1 wire 主证(卡→仅一次→+4s 消失+宿主探针 a1)·A3 会话锚定落库(run-as 直读 DataStore sessionId=session-d25241a4)·A4/A5 rule matched 2ms 自动应答无卡+窗口末 6 轮无滞留·A8-revised(切只读档→首条弹卡=A3 规则无跨会话泄漏→新规则锚定 session-fe7b060b→a8c 1ms 命中)·A6 提问 wire 复述「You picked Apple 🍎」·B4 冷启后审批卡重投递在场·全程 FATAL=0。
- **A7 BLOCKED 语义覆盖**:测试会话误在 4199 连接(debug-entry 默认重指,已记 runbook 坑);旧形态规则目录匹配语义由单测+09-04 22:22 probe4 活体证据(同机+prod-3080+同 3 条旧规则)双证作结。
- **环境事实存档**(验收报告收尾区):①debug-entry.sh 默认连 4199(runbook 已补⚠️节);②05:46 adb daemon 崩溃中断 ~1h;③新会话默认权限档=完全访问(A8 原流程前提不成立根因);④「始终允许」有二次确认弹窗。
- **执行层经验**(沉淀给后续卡):B1 判据语言假设缺陷(被测 agent 中文应答「明白了」,语义放行+成因注记);轮询循环注意 120s 工具超时(分批轮询)。
- 卡片已迁出 backlog(P0 节留迁移注记)。