# 365-353-359-uiux-consistency（2026-09-09）

> 状态：进行中
> 关联：backlog #365/#353/#355/#359 · 上一批 docs/journal/2026-09-08-2.md（验收演示批）
> 来源：用户反馈 / grilling / E2E / 顺带发现

## §三 #359 凭据探查+自动输入 + #366/#367 审计统一实现（2026-09-09 00:2X-01:0X）

### #359（调研→实现→实机闭环）
- 调研定稿 `docs/research/2026-09-09-credential-probe-359.md`：可探查面=service.json(4199)//proc environ(V1)/docker logs(dsh012-a5 重生成 token)；**248 边界=宿主零痕迹不可探查**（无配置/无容器/无 history/无监听）。
- 交付 `scripts/cred-probe.sh`（status/v1/dsh012/opencode <port>/boundary-248 五模式）+ 三轮实战修正（credential.active 为 NULL 放宽过滤；0.1.2 拒 --host 0.0.0.0 红线→--network host+127.0.0.1；镜像无 overlay 文件去 --patch）。
- **实机闭环 ✔**：`v1` 模式——起服（pid 674072，口令落盘 600，auth.json 从 V2 凭据库提取 zhipuai key）→ reverse → debug intent 注入 → 「OK: V1-4198 已连接」；wire 冒烟：list 200/create/prompt_async 204/LLM 双消息回流。`dsh012` 模式——容器重建（host-network 127.0.0.1:3081）→ docker logs token 43 字符 → 注入 → 「token 交换成功，cookie 已持久化（365 天）」。commits 02b0ffa0+0a0ed1cc。
- app 侧零改动（debug intent + dsh-pair 机制现成）；248 留用户裁定（boundary-248 报告）。

### #366（UIUX 审计 D8 统一）
- `ServerCapabilities.messageFeedbackSupported` 新位（DSH=true/V1/V2=false，注释锚定 #310②→#366 收敛史）；ChatMessageList:248-250 裸 serverType 特判 → `serverCapabilities.messageFeedbackSupported`；ChatViewModel 种子/清空 combine(serverType)→combine(serverCapabilities)。ServerCapabilitiesDshTest +1（四位面全断言）。commit 见本批。

### #367（UIUX 审计 D10 统一）
- ServerSettingsContent：DshServerConfigSection+DshPluginInventorySection 两 item 移出 `if (agentPresetSupported)` 嵌套（潜伏耦合解体——两区块空数据自门控：forms 空&&!blocked→return / inventory==null→return，行为零变化）。编译 ✔。
