# 365-353-359-uiux-consistency（2026-09-09）

> 状态：进行中
> 关联：backlog #365/#353/#355/#359 · 上一批 docs/journal/2026-09-08-2.md（验收演示批）
> 来源：用户反馈 / grilling / E2E / 顺带发现

<!-- 过程中的取证/验证证据直接写本文件；backlog.md 只留 ≤3 行卡片。 -->

## §〇 批次任务书（2026-09-09 00:10 用户指令）

- **#365**：方向 A——命令执行后转录内插本地合成反馈行（不等服务器事件）。用户补充边界：「原生命令（/compact 等）本就有服务器事件+#323 反馈行兜底；A 只补『受理即知』这层，不依赖服务器」。
- **#360**：方向 C（保持现状）——卡片已在 ba38e8d7 随演示批复盘迁出；本批仅记录裁决，无代码改动。**裁决补录（2026-09-09）**：用户选 C=保持现状（steer 徽标边缘态实践降级，不单独处理）。
- **#353**：直接修复——长按会话行直接弹窗（bottom sheet/对话框）而非 DropdownMenu 选择列表；已归档折叠区保持。卡片曾在 fb9f4d75 误随 #354 迁移丢失，本批重登记。
- **#355**：卡片重登记（同上误迁丢失），本批不做实现（用户指令未列入），保持待办。
- **#359**：先调研后实现——三条重配服务器凭据探查+自动输入（192.168.110.248:248 / V1-4198 / dsh012-a5）。
- **UIUX 一致性调研**：OC Beacon 在 OpenCode V1/V2/DSH 三类服务器中，功能等价模块的 UIUX 是否有差异——代码层 → 测试层 → E2E 脚本层 →（必要时）真机端到端排查校验；异同点统计入 backlog；以最新开发逻辑（提交时间）为标准统一，AI 自行决策；服务器关闭则自行启动。
- **质量门槛**：用户离开期间自主推进，回来时须达可交付质量——真机端到端测试 + ai-acceptance-workflow 三步法 + 全量单测。

## §一 #365 受理即知 + #353 长按直达弹窗（2026-09-09 00:1X-00:5X）

### #365（方向 A，用户 2026-09-09 裁决）
- 实现（commit 5bee330d）：`CommandFeedback.localAccepted` 新态 + `CommandFeedbackFolder.onLocalAcceptance`（local-uuid 占位）；`onRun` 追加前先找**同名最近未转正占位原位升级**（wire 无 execute→run 连接键，commands/execute 不回 commandId——同名+最近是唯一可用配对）；`MiscEventHandler.recordLocalAcceptance` → `EventDispatcher.recordLocalCommandAcceptance` → `ChatRepository.recordCommandAcceptance`（接口新增，eventDispatcher 直通零新依赖）；单点挂 `SessionActionsDelegate.executeCommand` ok 后——**三面（V1/V2/DSH）同构**：聊天面一切命令通道派发（斜杠/面板/review/plan-off）统一经此。CreateDirectoryUseCase 的 mkdir 兜底不经 sessionActions，天然排除（内部动作不产转录行）。
- UI：`CommandFeedbackCard` 新增受理分支——`Icons.Outlined.Schedule` 时钟图标（区别于 run 的 spinner；受理与执行视觉分层）+`command_feedback_accepted`（已受理）标签；i18n ×15 全量补齐。
- 测试：`CommandFeedbackFolderTest` +5（占位追加/同名原位升级保位/异名不消费/双占位 LIFO 消费/done 不触碰占位）；`EventDispatcherCommand323Test` +2（recordLocal→wire run 单卡升级/按会话隔离）。`compileDevDebugKotlin` ✔ 定向单测 ✔（BUILD SUCCESSFUL）。
- 语义边界（用户原话锚定）：「原生命令本就有服务器事件+#323 反馈行兜底；A 只补受理即知这层，不依赖服务器」——DSH 原生命令：受理行→run 升级→done 终态=单卡演化；V1/V2（无 command/run|done 事件面）：受理行即终态痕迹；skill 类斜杠已走消息面（f62e2e54），不在本卡域。

### #353（直接修复，用户裁决）
- 实现（commit ee1b879a）：`SessionRow` 活动行长按 → `ModalBottomSheet` 直达弹窗（详情/重命名/归档，24dp 图标+bodyLarge 行，M3 标准上下文动作形态）；**已归档折叠区保持原 DropdownMenu**（用户「仅折叠区没问题」——该区仅详情单项，下拉即达无列表感）。`sessionRowMenuActions` 纯函数与既有测试零改动（显隐契约不变，只换容器）。编译 ✔。无新文案（复用既有标签）→ 无 i18n 面。

## §二 UIUX 一致性调研·三面审计汇合（代码/测试/E2E 脚本三层并行审计 2026-09-09）

三层背景审计（代码层 23 能力位全清点+UI 条件分类 / 测试层 322 文件覆盖矩阵+18 处钉死差异 / E2E 资产 36 maestro+全部 acceptance 面谱）结论汇合：

### 总判定
架构规则「UI 入口按能力位隐藏，不写服务器类型特判」（ServerConnection.kt:12）在能力位矩阵层被严格贯彻（ServerCapabilitiesDshTest 为事实契约）；**能力位之下的共享后映射 UX（未读/问题卡/通知/markdown 渲染）三面真同构**。真差异集中在：①事件映射层数据可得性不同导致的呈现差（非 UI 层缺陷）；②少数违背能力位约定的裸 serverType 特判；③个别死代码/潜伏耦合。

### 差异清单与处置（D 编号沿用代码层审计）
- **D1 命令反馈三面不一致（DSH 转录卡 vs V1V2 仅 snackbar）**：✅ **本批 #365 已统一**（recordCommandAcceptance 三面单点）——审计在读到未提交工作树时已预见此解。
- **D8 👍/👎 门控走裸 serverType（ChatMessageList.kt:250/ChatViewModel.kt:748-762）**：功能缺口本身合法（OpenCode 无评分域），但违背能力位约定 → 登记 **#366**，本批实现（feedbackSupported 位）。
- **D10 ServerSettingsContent 潜伏耦合（DSH 配置/插件节嵌在 agentPresetSupported 分支内，ServerSettingsContent.kt:99-126）**：今日两比特同值无症状，结构错 → 登记 **#367**，本批实现（解耦嵌套）。
- **D4 V2 时钟域（V2SseMapper 设备钟盖戳 vs V1/DSH 服务器信封钟，2026-08-26 旧实现）**：以最新逻辑（DSH 域一致钟，#338 09-07）为标准 → 登记 **#368** 调研 V2 wire 信封时间可得性后对齐（P3）。
- **D5 后台任务面：V1V2 ShellSheet 历史+详情 vs DSH 仅流内时间线卡；ShellSheet 的 DSH 分支+DshJobSheet 为不可达死代码（SHELL 入口 DSH 永不添加）**：→ 登记 **#369**（死代码清理 P3；DSH jobs 历史面板为可选增强）。
- **D9 V2 QueueSheet 新鲜度（pull-on-open vs DSH push）**：#356 遗留已知项 → 登记 **#370**（轮末自动刷新，P3）。
- **D2 台账步数口径（DSH 工具宿主消息计步 vs V1V2 逻辑轮计步）/ D7 服务器卡片徽标三态样式 / D11 V2 用户消息 echo 形态（summary.body+📎 占位 vs DSH 显式 parts）/ 3 个休眠能力位（runningSessionsFilterSupported/messageDeleteSupported/projectionStatsSupported）处置**：→ 合并登记 **#371**（P3 杂项统一调研）。
- **D3 台账时长「-」/D6 新会话对话框条目源**：既有裁决域（#338 宁缺毋谎；D6=测试钉死的零回归裁决）——不立卡，维持。
- **待真机核验项（并入本批 E2E 清单，不立卡）**：V2 RetryBanner 缺席核实；V1 探测前 null-flicker（backgroundSessions 位 null=T）；DSH CompactionStarted messageId="" 锚定无恙核实；D9 慢拉取下面板陈旧帧。
- **E2E 资产面重大缺口（真机批次吸收）**：V1（4198）**零脚本化 E2E**；maestro 36 流全部锚定旧 4096 通用面——本批真机轮补 V1 面（runbook 配方起服）。

## §三 #359 凭据探查+自动输入 + #366/#367 审计统一实现（2026-09-09 00:2X-01:0X）

### #359（调研→实现→实机闭环）
- 调研定稿 `docs/research/2026-09-09-credential-probe-359.md`：可探查面=service.json(4199)//proc environ(V1)/docker logs(dsh012-a5 重生成 token)；**248 边界=宿主零痕迹不可探查**（无配置/无容器/无 history/无监听）。
- 交付 `scripts/cred-probe.sh`（status/v1/dsh012/opencode <port>/boundary-248 五模式）+ 三轮实战修正（credential.active 为 NULL 放宽过滤；0.1.2 拒 --host 0.0.0.0 红线→--network host+127.0.0.1；镜像无 overlay 文件去 --patch）。
- **实机闭环 ✔**：`v1` 模式——起服（pid 674072，口令落盘 600，auth.json 从 V2 凭据库提取 zhipuai key）→ reverse → debug intent 注入 → 「OK: V1-4198 已连接」；wire 冒烟：list 200/create/prompt_async 204/LLM 双消息回流。`dsh012` 模式——容器重建（host-network 127.0.0.1:3081）→ docker logs token 43 字符 → 注入 → 「token 交换成功，cookie 已持久化（365 天）」。commits 02b0ffa0+0a0ed1cc。
- app 侧零改动（debug intent + dsh-pair 机制现成）；248 留用户裁定（boundary-248 报告）。

### #366（UIUX 审计 D8 统一）
- `ServerCapabilities.messageFeedbackSupported` 新位（DSH=true/V1/V2=false，注释锚定 #310②→#366 收敛史）；ChatMessageList:248-250 裸 serverType 特判 → `serverCapabilities.messageFeedbackSupported`；ChatViewModel 种子/清空 combine(serverType)→combine(serverCapabilities)。ServerCapabilitiesDshTest +1（四位面全断言）。commit 见本批。

### #367（UIUX 审计 D10 统一）
- ServerSettingsContent：DshServerConfigSection+DshPluginInventorySection 两 item 移出 `if (agentPresetSupported)` 嵌套（潜伏耦合解体——两区块空数据自门控：forms 空&&!blocked→return / inventory==null→return，行为零变化）。编译 ✔。

## §四 三步法真机验收 + 失败闭环（2026-09-09 00:45-02:20）

- **步骤1-2（生成→纯净审查）**：checklist v1 被 13 项缺陷打回（type.sh 无 / 键、serial 缺省、reverse 3080 缺、切回路径缺、判据含糊、V2 面缺项、时序无轮询…）→ type.sh 补 /- 键码+v2 修订 → 复审仅剩 A1 判定穷举化一项 → 修后过审（54295282）。
- **步骤3（纯净执行）**：executor subagent 20 项全终态（✔13/✘5），124 份证据 /tmp/acc365/；记录逐项写回 checklist 实测记录区。
- **失败闭环（§2 步骤4 根因修复，commit 3724398d）**：①受理行改**派发时插入**（原 ok 后插入——DSH 19ms 无感、V1/V2 /command 同步挂起数十秒等于「完成才知」）+失败翻 error 终态；②V2 /command 补 text 键（beta-19086 契约 400 Missing key 实证，command+text 齐发 204）；③空会话 hero 整块替换消息区吞命令反馈卡（ChatScreen 空态门控加 commandFeedbackForSession.isEmpty——2026-08-19 pending 卡先例延伸）；④A2 改判（通道三证据全立；技能展开形态=服务器侧存储行为）。
- **r2 重验（主 agent 执机）**：A1-r2 ✔（非空会话单卡 执行中→已完成+摘要锚定；多模态 dense 12 帧证受理相位 <0.8s 亚采样窗口，机制由单测+V1 面实地证）；A3-r2 ✔（V1 /init +2.5s 即「已受理」卡、+22s 终态保持=无事件面设计语义）；A4-r2 ✔（V2 204+已受理卡在场+/init 轮真实运行）；D1-r2 ✔（GLM 5.3 选型后全链路：回复+思考 466ms+台账 1.3s 真实时长——审计 D3 对照证据入库）。
- **环境注记**：V1 服顶层 model 默认未生效（app 记忆服务器级模型 glm-5v-turbo=订阅外，选 Zhipu Coding Plan/GLM 5.3 绕行，curl 1311 实证）；EventEmitter 11-listener 告警=V1 服噪声。
- **横向发现入卡**：#372（三面面板 tap 语义分叉）/ #373（V1 空 scratch 面板 tap 清空+/session//todo 400）。

## §五 批次收尾状态（2026-09-09 02:2X）

| 卡 | 状态 | 证据链 |
|---|---|---|
| #365 受理即知 | 代码+单测+三面真机闭环（5bee330d+3724398d） | checklist A1-A4 r1+r2 + 单测 13 |
| #353 长按直达弹窗 | 三面真机 ✔（ee1b879a） | B1/B1b/B2/B2b/B3/B4 全 ✔（含归档区下拉保持） |
| #366 反馈能力位 | 单测+三面真机 ✔（80c07cb0） | C1/C1b/C1c |
| #367 设置解耦 | 真机 ✔（80c07cb0） | C2 双标题在场 |
| #359 凭据探查 | 实机注入闭环 ✔（02b0ffa0+0a0ed1cc） | V1/dsh012 两路径 OK 输出 |
| #360=C / #361 / #364 | 裁决记录 / 已迁出 | journal §〇+上一批 §六 |
| UIUX 三面审计 | 三层报告汇合 + #366-#373 入册 | journal §二+§四 |

待用户：#365/#353/#359/#366/#367 人工观感尾巴确认后关闭迁 journal；#372/#373 与 P3 池（#368-#371）排期。

## §六 演示验收批（2026-09-09 05:0X 起，用户回归「你演示我验收」A 模式）

**节点 1（#365 DSH 面 /compact 单卡演化）判定（2026-09-09 05:0X，用户两条反馈）：**
- ①设计裁决（→ **#374**）：「压缩应该在同一张卡片里完成所有动作，而不是像现在一样压缩中还多出一个分割线」——已实现（tailSpec suppressByLiveCompactCommand 让位 + 策略测试），待重演确认。
- ②压缩失败（用户观察）：根因取证=**服务器侧 Node 堆 OOM**（web.log FATAL Ineffective mark-compacts near heap limit；OOM worker pid 702661 已死，主进程 pid 986 RSS 1.75GB 存活继续服务）——非 app 缺陷；**app 正确呈现「失败」终态卡**（command/done kind=error 路径实地工作）。处置待用户裁决（重启 prod-3080 会终止本会话服务，需用户选时机）。

**节点 1-重演判定（2026-09-09 05:2X，用户三组反馈 → #375/#376/#377）：**
- ①设计裁决（→ **#375**）：压缩实体应为主对话流内时序位 + 专用可展开 box（进行中 SSE 流式 + 完成后全文，双态展开收起）——尾部钉卡形态否决；#374 让位修复保留为过渡。
- ②重进消失（→ **#376**）：实测证实（重进 dump 0 卡）；机制=command/run|done 折叠仅活窗口（r2 冷启窗口内曾重建；新轮次记录推离窗口后不折叠）。
- ③首次重进重复输出（→ **#377**）：用户观察「第一次重进相同内容两次、二次正常」——seed+REST 合并竞态嫌疑；取证：Room 该会话 24 行含同刻三行结构（seq-1091-1093）。
- ④顺带发现：用户在观察期自发了 calculator 轮（seq-1377「15×2+√(39-10)÷2，乘个 5³」→ 4086.57）——会话台账现示「轮次 3」。
- 取证资产：/tmp/acc365/{d3*.xml,f0.xml,r1e1.xml,r1.db(+wal)}；服务器侧 OOM（前轮）与 adb 掉线重连（mDNS 37471）时间线均在案。

**节点 2 判定（2026-09-09 05:4X，用户二次裁决 → #353 终型，commit 2c255ee6）：**
- 原文：「不是底部弹窗…直接将功能按钮放到会话详情中就可以了呀！功能按钮需要去重（抽屉拉起的有重命名，那么会话详情弹窗就不需要）」「归档与删除：删除有直接接口就不需要归档；归档只在删除没有实际 API 时使用」。
- 实现终型：长按=直达 SessionDetailsDialog（统一活动/归档行）；动作区去重后=复制 ID+重命名+标签+移除动作（deleteSupported→删除 / else archiveSupported→归档，互斥）；已归档行仅复制 ID；DropdownMenu/ModalBottomSheet/sessionRowMenuActions 纯函数+测试退役。

**节点 3 判定（2026-09-09 06:0X，用户裁决）：**
- 评价系统（#366 👍/👎 三面在缺席）**过**。
- 命令反馈卡（/init 已受理）**核心缺陷定性与压缩卡同源**：「应该随着主对话流输出移动，而不是一直固定在底部！！…这些都得改」——尾部钉死是 #323/#365 全族设计缺陷，非个例。
- 处置指令：「结合具体 SSE 输出+退出重进 fetch 数据是否支持→系统性分析→整体重构/重写」→ 立 **#378**（伞卡，统 #375/#376/#377）；样式保留（「整体样式问题不大」）。
- 顺带实证：本轮回 DSH 时 /compact 命令卡随载入重建（窗口内）——#376 机制观察补强。

**节点 4 判定（2026-09-09，设置+服务器面）：**
- （本节点判定当时未单独成段入册；判定内容以 §七 表与 backlog #367/#359 卡过验记录为准：**过**，含 yaml 图形化确认。）

**节点 5 判定（2026-09-09 06:1X-06:2X，#372）：**
- 三面行为实测呈堂：V1 回填（r1+现场）/ V2 直达（r1）/ DSH 直达（现场 tap /compact→已完成卡）。
- 用户裁决：「后端支持回填后加参数就按回填，否则直达」→ 三面后端核证全支持参数（V1/V2 arguments·DSH 整行）→ **统一回填**实现+装机；追加指令「所有命令参数都这么处理+规则持久化+全指令清查」→ 铁律入 `docs/ui-conventions.md` + 派遣点清查入卡。
- 真机重演：tap→回填「/export 」零派遣——**过**；附带指令「后续需系统性根据具体接口修改」→ **#380**。
- 同时登记 **#379**（抽屉手柄统一+fling 不收起，M3 dragHandle 槽位天然支持自定义——用户裁决）。

**节点 6 判定（2026-09-09，#359 尾项 248）：**
- 用户裁决：入 **P4（#381）**——前提=上游远程凭据探知能力（dsh 官方远程 token 发现或其他认证手段）；满足后 cred-probe 接管，不支持再议弃用/人工重配。

## §七 演示批总结（2026-09-09 05:0X-06:3X，六节点全终态）

| 节点 | 判定 | 产出 |
|---|---|---|
| 1 压缩单卡演化 | 两轮反馈→系统性裁决 | #374（让位过渡）/#375（压缩入流重设计）/#376（重进消失）/#377（首进重复）+服务器 OOM 取证 |
| 2 长按直达详情 | 过（二次裁决终型+打磨） | #353 终型×2 轮实现（2c255ee6/ea627baf）|
| 3 受理行+评价 | 评价过；卡片钉底=全族缺陷 | #366 ✔关闭；#378 伞卡（命令卡族系统性重设计）|
| 4 设置+服务器 | 过（含 yaml 图形化确认） | #367/#359 ✔关闭 |
| 5 面板 tap 语义 | 过（回填统一+铁律持久化） | #372 ✔关闭；#379（抽屉手柄）；#380（按接口对齐）|
| 6 248 边界 | 入 P4 | #381 |

- 实现提交链（本演示批）：9262b52a→2c255ee6→ea627baf→daa7c35f→d9c5819d→8f2f445c（+b9ac6086/87786999 判定记录）。
- 方法论注记：本批三起「仪器受限·用户肉眼补位」场景（受理相位亚采样/IME 盲区面板定位/导航串面）——三通道互证规程再次生效；面板定位以 screencap+多模态补偿 dump 盲区为新补充。

## §八 已完结卡片迁入（2026-09-09 演示批过验补迁移）

> 迁入依据：§六/§七 各节点用户判定「过」。补迁移原因：判定发生时迁移动作未当场执行。

### #372 命令选择回填铁律——三面所有指令统一回填可补参（2026-09-09 用户裁决，已持久化 docs/ui-conventions.md） `ui` `command`
- **已实现(2026-09-09)**：ChatInputBar onCommandClick——server 型命令 tap 统一回填 "/name "（废除无 hint 即直达分叉）；client 本地动作（rename 对话框/shell 模式）保持直达；skill 节本就回填——三面一致，待重演验收
- **全指令清查(2026-09-09)**：文本命令选择面唯一=斜杠面板（已统一）；其余派遣点均为专用快捷钮（plan-off chip :731/权限预设切换/review 钮 :561）=铁律例外类；:561 review 特例与 :571 else 分支现仅收 client 型（server 型不再经 onSlashCommand）——review 特例成死代码待清理；V1 空会话 clientCmds 兜底面板（fork/share 等派往服务器被吞）归 #373 邻域
- **真机重演 ✔(2026-09-09)**：DSH 面 tap 面板项→输入框回填「/export 」+零派遣（用户判定「过」，§六 节点5）；底层按接口系统性校准 → **#380**（尾项移交）
- 史：r1 观察 V1 回填/V2·DSH 直达分叉 → 用户裁决「支持加参数就按回填」+「所有命令参数都这么处理+持久化」→ 后端核证三面全支持 → 回填统一+铁律入 ui-conventions；**P3 旧「#372 三面面板 tap 行为不一致」观察卡系裁决前登记，随本卡一并迁出清理**

### #366 👍/👎 反馈门控能力位化——feedbackEnabled 走裸 serverType 特判违背「UI 入口按能力位隐藏」约定（UIUX 三面审计 D8） `refactor` `dsh`
- **已实现(2026-09-09)**：messageFeedbackSupported 位（DSH 独有）+ ChatMessageList/ChatViewModel 两处特判收敛 + 矩阵测试；编译+定向单测 ✔ → journal §三
- 用户判定：评价系统**过**（§六 节点3）

### #367 ServerSettingsContent 潜伏耦合——DSH 配置/插件节嵌在 agentPresetSupported 分支内（UIUX 三面审计 D10） `refactor` `dsh`
- **已实现(2026-09-09)**：两 item 移出嵌套（空数据自门控，行为零变化）；编译 ✔ → journal §三
- 用户判定：**过**（含 yaml 图形化确认，§六 节点4/§七）

### #353 长按直达「会话详情」——动作按钮收进详情+去重+删除/归档互斥（走查反馈② → 2026-09-09 演示二次裁决终型） `ui` `session`
- **终型裁决（2026-09-09 演示）**：「直接将功能按钮放到会话详情中…功能按钮去重（抽屉有重命名则详情不再放）」「删除有直接接口就不需要归档——归档只在删除无 API 的面使用」——长按=直达详情对话框；V1/V2 详情含删除（危险色）、DSH 详情含归档（互斥顶替）；已归档行动作区仅复制 ID
- **已实现终型(2026-09-09, commit 2c255ee6 + 打磨 ea627baf)**：菜单/sheet 中间形态退役（ee1b879a 的 sheet 为中间态）；打磨=按钮间距+历史同步信息整合进详情列；用户判定**过**（§六 节点2/§七）
- 史：走查「直接弹窗而非选择列表」→ sheet 中间态 → 终型直开详情（三次裁决链全记录）

### #359 三条重配服务器凭据探查+自动输入方案调研（走查反馈⑨） `infra`
- **已实现+实机闭环(2026-09-09)**：cred-probe.sh（v1/dsh012 两路径真机注入全绿：V1-4198 连接+LLM 回流 / dsh012-a5 token 交换+cookie 持久化）→ `docs/research/2026-09-09-credential-probe-359.md` + journal §三
- 用户判定：**过**（§六 节点4/§七）；248 边界尾项剥离 → **#381**（P4）

## 修复注记（2026-09-09 journal 章节覆写事故与还原）

- **事故**：演示批期间 journal 追加误用全量覆写——9262b52a/fb7d3f73/87786999/8f2f445c 等提交每次仅保留最新判定段，§〇-§五 及 §六 前序节点段从工作区丢失（各版本仅存于 git 历史：02b0ffa0=§〇-§二 / 80c07cb0=§三 / a992c3ea=§四-§五 / 9262b52a/b9ac6086/fb7d3f73/87786999/8f2f445c=§六各节点）。
- **还原**：本文件按上述提交逐段拼回原文，零改写；节点 4 判定历史上即未单独成段（非覆写丢失），补占位行注明去向。
- **预防**：journal 追加一律用 edit 定位插入，禁用 write 全量替换（AGENTS.md 既有规范重申）。
