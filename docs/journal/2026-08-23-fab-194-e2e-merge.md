# #194 FAB 修复 + 术语批次真机 E2E + 合并 master（2026-08-23）

> 状态：E2E 冒烟全绿（自动化部分）；2 项时间性现象待用户人工验收
> 关联：#194（spec 2026-08-23-fab-slide-overflow-design.md）· #193（术语批次，本批合并载体）
> 设备：houji e69a99d8（1200×2670 @480dpi）· 服务器：Host-4199（V2 beta-17963，adb reverse）
> 构建：app-dev-debug.apk versionCode 1787488794（本地 debug 签名，卸载重装跨签名源后 intent 恢复配置）

## 一、同步（master → terminology-overhaul）

- aba319d2 merge master：唯一冲突 backlog.md **编号撞车**——terminology 侧先行占用 #194–#199（盘点+Tier C 五项），master 又把 #194 给 FAB bug。裁决：master 为主，FAB 保 #194，terminology 六卡顺移 +5 → **#200–#205**，下一编号 #206；FINAL-REPORT/spec/journal 内旧引用同步改。
- backlog-check 通过（编号计数器/本地链接/行数全绿）。

## 二、#194 实现（4 commits）

| commit | 内容 |
|---|---|
| 95ec14dc | D1 容器实测上限（layout 约束 maxHeight，魔法数 160 移除）+ D2 expandShift + D3 动画 + D4 合并 + D5 双 FAB 独立 |
| e2050d1f | 单测算术修正（740 是富余不是溢出） |
| f34a3b23 | **竞态根修**：M3 stagger 入场动画使 item 首帧坐标=折叠态节点顶 → 坐标锚点法作废，改全稳定量：collapsedH + 常量 span(4×44dp+3×4dp=564px) + FabMenuPaddingBottom token(8dp) |
| d3c81893 | **收起动画钳位根修**：collapse 途中节点高度回落经中间值被当折叠基准 → offsetY 永久钳上去（E4e 二次实证）；折叠高改**观测最小值**，coerce 恒用稳定基准；展开/收起动画统一单 LaunchedEffect(expanded) |

关键 M3 1.5.0-alpha26 源码结论（/tmp/m3src 实读）：节点 bottom 对齐容器底；button 钉节点底−16dp；items 从节点顶下排且 stagger 自下而上浮现（首项最后出现）；**折叠态不放置 item 内容**（坐标锚点不可用）；FabMenuPaddingBottom=CloseButtonBetweenSpace=8dp、ListItemBetweenSpace=4dp。

## 三、E2E 证据（真机，dump+截图+logcat 三通道，存 /tmp/e2e-194/）

### 主流程（E1–E3）
- E1 会话列表：渲染/zh 术语（待回答、搜索会话…、切换会话、有未读消息）✓
- E2 进会话：历史渲染、蒙版揭开、FSM Idle、思考完毕/智能体词条 ✓
- E3 发送：POST /prompt 200 → admission → send-seed → MessageUpdated → Busy/Waiting → TextDelta 流式 → completed → SseIdle → Idle ✓（V2 全链 logcat）
- 中断（Tier A 重命名 interruptSession 端到端）：停止按钮 → POST /session/.../interrupt → SSE session.execution.interrupted{reason:user} → L3 回收 ✓
- 堆积面板（FAB 菜单入口）：标题「堆积消息」+ 空态文案（智能体/暂存术语）✓

### FAB（E4，#194 验收清单 1–4 自动化覆盖）
| # | 场景 | 结果 |
|---|---|---|
| 1 | 拖到最顶 | FAB [1044,386]（顶栏底 266，留 40dp 不钻顶栏）✓ |
| 2 | 高位展开 | shift=588（日志：collapsed=192 span=564 H=2026 offsetY=-1810）；items 顶 384 ≈ 容器顶+8dp；四项全可见；按钮被顶到 [974] ✓ |
| 3 | 中低位展开 | 无位移（rest 展开按钮原位）✓ |
| 4 | ⬇ FAB | 顶限 [390] 同边界；菜单 FAB 不动（位移独立）✓ |
| 5 | 收起回位 | 按钮回 [386]（offsetY 停放位保持，d3c81893 修复后）✓ |
| 6 | D4 展开中拖动 | 日志 drag-merge: offsetY=-1810+shift=588；菜单收起、按钮跟手至 [1834] ✓ |

### 回归（E4 附加 + 全程）
- FATAL EXCEPTION 计数 **0**（整个 E2E 会话）✓
- 单测 1895/1895（两轮独立复跑）· i18n-check 675×14 PASSED · compileDevDebugKotlin 每变更过

## 四、待用户人工验收（维度 5：时间性现象，自动化不可覆盖）

| # | 操作路径 | 预期 | 判定 |
|---|---|---|---|
| 1 | 拖 FAB 至高位 → 点开菜单 | items 交错浮现**同时**整体 ~300ms 平滑下滑就位 | 无闪现、无瞬移 |
| 2 | 聊天页聚焦输入框（键盘弹起）→ 拖动/展开 FAB | 无异常跳动，容器收界正确 | 视觉正常 |

## 五、移交

- 本批不新增 backlog 卡；#194 转「待验证」（上述 2 项人工清单）
- M3 升级注意：FloatingActionButtonMenu 实验 API（1.5.0-alpha26）参数面/常量漂移需复核 FabMenuItemSpacingVertical/FabMenuPaddingBottom 两 token
