# 2026-08-22 UI 批次：ModelPicker 复改 + 顶栏菜单 + 堆积/TODO 常驻抽屉

> 批次内容：用户 5 项指令（#151 激活指引口头交付、ModelPicker 对钩/星标、顶栏菜单换位图标、堆积/TODO 主对话抽屉）
> 抽屉设计：grilling 三轮 17 问定案（Q1-Q17），对话记录见会话

## ModelPicker（commit 3bb4a192）

- 对钩从行尾（chevron 后）移到名称后（右对齐簇最左端）——行尾恒为 chevron，选中态不再挪动尾图标
- 星标恢复点击=设置/取消默认模型 toggle（#187 曾收编为二级面板 Switch，按用户要求改回）；面板内 Switch 删除

## ChatTopBar 更多菜单（commit fb50a301）

- 任务转后台从第 5 位提前到第 3 位（原分叉会话位置）；分叉会话移至压缩后
- 图标 FormatListBulleted → PendingActions（与输入组件任务入口一致）

## 堆积/TODO 常驻抽屉（commit 8ff26482）——grilling 定案要点

- **形态**：主对话流模块内覆盖式（底部锚定贴输入组件上沿）；模态 PendingTodoSheet 退役删除；输入组件双角标入口按钮链移除
- **三档**：收起=48dp 标题栏 / 30% / 60%（容器=主对话流模块高度）；标题栏整栏竖拖吸附；点 segment=展开到 30% 并切段（收起只靠拖）
- **标题栏**：左 SegmentedButton 双段（堆积 N / TODO n/m——无 TODO 数据整段隐藏）；右纯图标 ▶继续（PlayArrow）+🗑清空（Delete，带确认框）；TODO 段无操作钮
- **行为**：双空（堆积 0 且 TODO 空）完全隐藏；键盘弹起自动收起（不恢复）；流式不打扰；档位+段位按会话内存级记忆（PendingDrawerMemoryStore 顶层 snapshot map——App 存活期跨会话保留，重启回收起）
- **紧凑化**：堆积行 ~44dp（IconButton 32→28dp、竖向 SM→XS、图标 16→14）；正文仍 2 行截断
- **补偿**：消息列表 contentPadding bottom + inset（最新消息不被标题栏遮挡）；滚到底 FAB 上移让位
- **实现**：Animatable 高度 + 拖拽 snapTo、释放 nearestSnapIndex 吸附（纯函数）；LaunchedEffect(snap, anchors) 响应外部态（键盘收起/segment 展开/容器旋转）
- **测试**：PendingDrawerSnapTest 5 例（锚点计算/各邻域吸附/等距平局取低档/可见性 4 态/常量自洽）；全量 1885/1885 绿

## 追加两轮修正（用户复检反馈）

- **纯覆盖修正（commit 6a8a2041）**：用户指出「之前不是说要覆盖（悬浮）的逻辑吗？现在怎么是挤上去」——初版误加消息列表 contentPadding/FAB 让位补偿（视觉=挤压）。删除补偿：抽屉悬浮盖住消息底部，最新消息被盖是预期（Q2 定案本义）
- **顶栏菜单显隐开关（commit a8e226d1）**：用户需求「顶部导航栏展开后可以设置是否展示堆积消息与todolist的tab」——AppSettings.showPendingTodoDrawer 六层接线（DataStore key show_pending_todo_drawer + SettingsStateDelegate + ChatViewModel.togglePendingTodoDrawer + ChatScreen 门控 + ChatTopBar 菜单项带勾选态）；i18n 670×14 过；1885/1885 绿
- **E2E 基建结论（Popup 焦点属性）**：busy 气泡菜单 focusable=false 不进 uiautomator 树——坐标定位只能像素聚类（文本行簇 y1298-1330=立即发送副题 / y1450-1482=堆积副题 → 堆积行点击点 (900,1455)）；视觉模型坐标提问不可靠（引导性污染+幻报），必须 MCP extract_text 逐簇鉴定或确定性像素分析

## 真机 E2E 终验（2026-08-22，devRelease a8e226d1，全案例矩阵）

16 项全过（对照用户需求逐条）：
1. ✓ 标题栏常驻浮现（「堆积 1」「堆积 2」segment + 右端图标；drain 后双空自动隐藏）
2. ✓ **覆盖式**：60% 展开时消息气泡被抽屉顶边遮挡（MCP 实证）；补偿代码已删（6a8a2041）
3. ✓ 三档吸附：收起 132px / 30%（epsilon 列表可见）/ 60%（MCP「约半屏」）；整栏拖拽生效
4. ✓ 档位记忆（store 按 sessionId keyed）；切回会话被键盘弹起 Q8 收起——Q8 优先于记忆恢复是正确交互
5. ✓ 点 segment 展开（锚点点击→列表出现）
6. ✓ 键盘弹起自动收起（重进会话 dumpsys ime + MCP 双源实证；收起后不自动恢复）
7. ✓ 流式期间抽屉保持不动（busy 全程稳定显示）
8. ✓ TODO 只读/无数据整段隐藏（全程单段）
9. ✓ 顶栏菜单 toggle：菜单第 3 位「堆积/TODO 抽屉」；off→epsilon 在队也完全隐藏；on→恢复
10. ✓ 🗑清空：确认框「将移除 2 条堆积消息」→清除→队列空+chat 无直发+抽屉隐藏；▶继续图标/逻辑就绪（drain 异常兜底场景无法自然 E2E——JVM 单测覆盖语义）
11. ✓ 行高紧凑：图标行簇像素实测行距 123px = 44.7dp ≈ 44dp
12. ✓ 拖拽重排：zeta two 拖到顶 → 任务结束 drain 按**新序 two→one** 发出（重排语义闭环）
13. ✓ 旧入口移除：dump 全 desc 无 a11y_icon_pending_todo
14. ✓ ModelPicker 对钩在名称后（行序=名称→对钩→星标→chevron）
15. ✓ 星标点击 toggle 默认模型：outline→filled→outline 双向
16. ✓ 顶栏菜单换位：堆积/TODO抽屉→任务转后台(提前)→压缩→分叉会话(后移)；图标 PendingActions 统一

全程 0 FATAL。E2E 方法论沉淀：抽屉几何定位=语义树锚点（堆积 N 段节点/快速定位行）+图标行簇像素分析；drain 自动发送与 E2E 操作竞态是主要时序坑（每轮操作须在任务窗口内完成）

## 抽屉样式复改（用户 2026-08-22 第二轮，commits 18bde38a + a4f2ae5c）

- **拉手样式**：Material 抽屉标准小横条（32x4dp 圆角居中）；收起态唯一可见物=拉手（16dp，旧 48dp 标题栏的 1/3）；clipToBounds 裁掉标题栏/列表
- **segment 半宽**：fillMaxWidth(0.5f)（原 weight 全宽）——dump 实证 segment 文字 [300,2037] 右端图标在 x896+（左侧一半为控件区）
- **档位改 20%/80%**（原 30%/60%）；单测期望同步（1885 绿）
- **E2E 发现并修复一个真可用性 bug**：44px 小拉手拖拽 5 轮 0/5 命中——拖拽越过拉手上界即被消息列表滚动接管（手势竞争）；修复=拉手加 detectTapGestures 单击切换 收起↔20%（a4f2ae5c）。真机复验：点拉手展开（堆积 1/mu probe 列表进语义树）→再点收起（段被裁出树）→展开态拖拽到 80%（segment y821≈73-80% 容器）全通
- **布局锚点勘误**：消息 Box 底=输入栏顶≈2370（chip 行 2387 上方有 padding）——收起拉手=[2326,2370] 中心 2348；此前点 2365 落 chip 行误开模型 sheet

## 抽屉圆角（用户 2026-08-22 第三轮，commit 26a6f40f）

- Surface 加 shape=顶部两角 16dp、底部直角（底部抽屉标准形态，底边贴输入栏无缝）
- 真机像素实证弧线：顶+2px 行 x[47,1152]→顶+6px x[43,1156]→顶+12px x[40,1159]——两角逐行向内收窄 ~7px（16dp 弧高）再展开；MCP 分角验证双角 ROUNDED

## 抽屉六项复改 + offset 重构（用户 2026-08-22 第四轮，commits c626a0dc→c291650f→ae3ddaac）

用户六问（segment 高度/按钮右对齐/双段常显置灰角标/收起元素缩小/自定义高度/角标更新）：

- **两个真 bug 定位**：①LaunchedEffect key 含 FloatArray（引用比较恒不等）→每次重组重启 animateTo 与拖拽 snapTo 竞争→释放停在中间位置（116px 非锚点实测）；②Row.height 在收起动画中被 Surface 约束 coerce 压缩→元素缩小消失
- **UI 四项**：SegmentedButton 紧凑 32dp；Spacer(weight) 右对齐操作钮；双段恒展示（堆积/TODO 无数据置灰 enabled=false + Badge 数量角标，TODO 角标=未完成数）；角标随 collectAsStateWithLifecycle 数据流自动更新
- **结构性重构（ae3ddaac）**：高度动画方案补丁过多→重写为 **offset 滑动**（标准 BottomSheet 内部实现）——Surface 固定高=80% 容器（底贴容器底），档位=自全开位置的下移量（收起 offset=full−handle / 半开=full−20%容器 / 全开=0），拖拽只动 offset 无重测量。一揽子消灭：约束压缩（Q4 根治）、a11y 溢出节点、动画竞态中间态
- **真机终验（全六项）**：segment 正常尺寸（MCP"not squished"）；🗑 右对齐；堆积+Badge 1+TODO 置灰常显；拖拽中元素保持尺寸；三档精确吸附（展开 y2067 三轮一致/收起 Surface [2326,2370] 只露 handle 44px/全开像素实测 80% 整）
- 附带发现：offset 方案收起态段节点仍进 a11y 树（被输入栏遮挡但在屏内）——预期行为，验证须用像素/视觉而非 dump 计数

## segment 第五轮微调（用户 2026-08-22，commit f28a80bd）

- Badge 角标真机评审「比文字都大」——弃用，数量改文案后同级小字（「堆积 1」）
- SegmentedButton icon={} 去默认选中 ✔（高亮已足够——用户点名"意义不明"）
- segment 行宽 1/2 → 1/3（fillMaxWidth(0.34f)）
- 真机验证：无✔图标 ✓ 数字纯小字 ✓（无气泡）占比 <1/2 裁宽 ✓ 选中段深色填充高亮 ✓

## segment 第六轮：角标回归（用户 2026-08-22，commit 2cd00b4a）

- 用户问「无法做成角标吗」——可以：BadgedBox（M3 给图标/按钮配角标的正规组件，
  Badge 紧贴 anchor 测量 16dp 级）替代第五轮的独立 Badge（成人版大 pill）与行内小字
- 形态：文字 anchor + 数字角标叠文字右上（count=0 不显）
- 真机验证：角标浮于文字右上 ✓ 直径≈字高（不再比字大）✓ 无✔ ✓ TODO 0 条无角标 ✓
- **第六轮翻车**：BadgedBox 默认 TopEnd 叠 anchor 右上——文字 anchor（~16dp）被 16dp Badge 整颗盖字形；第七轮 offset(6,-4) 外飘仍部分盖字（真机 MCP 两轮实证）
- **根治（e2f82a7f）**：anchor 文字加尾 padding 14dp 撑出空白区——Badge（TopEnd）落在字形右侧空白，零字形重叠；真机 MCP 终验「两字完全可读、角标在右侧空白区」✓
- **用户仍觉难看 → 第八轮终版（3aab0460）**：彻底弃 Badge——GitHub-tab 式行内计数（标签 labelSmall + 数字 10sp 弱化色 onSurfaceVariant，同行底对齐 spacedBy 3dp）。零悬浮零重叠零背景块；真机 MCP 验证「纯小字同行基线、无重叠、clean and minimal like GitHub tab counts」✓

## 第十轮终局重构：贴底工具栏五入口 + 四独立 sheet（用户定案 096fe219）

用户方向修正：「工具栏化只是入口，拉起还是抽屉做」→ 最终定案「样式统一为原 TaskSheet/快速导航同款 ModalBottomSheet，本次写的抽屉全部清理」。

- **ChatBottomToolbar**（恒显示贴底 40dp）：[⬇ | 堆积 N | TODO n | 智能体 n | Shell n]——⬇=原滚到底 FAB 并入（在底时藏）；四入口 GitHub 式行内计数（=运行中/待处理数；0 不显数字但**可点看历史**——用户 Q4 不置灰）
- **四独立 sheet**（SheetScaffold 统一：ModalBottomSheet+75%+标题栏+关闭）：StackedSheet（拖拽/编辑/删/单发+继续清空）/TodoSheet（只读三态）/AgentSheet（TaskSheet Subagents 迁移含历史+前后台/agent/时间/模型徽标/时长走时）/ShellSheet（Shells 迁移含详情视图+输出三级 provider）
- **退役清理**（-1625 行）：PendingTodoDrawer/PendingTodoToolbar/TaskSheet 三组件删除；滚到底 FAB 退役；输入栏任务按钮链（BadgedBox+参数链）；TaskToolbar 开面板参数；顶栏「堆积/TODO 抽屉」设置开关菜单项；PendingDrawerSnapTest
- **真机 E2E**：五入口渲染 ✓；AgentSheet「智能体 (4)」历史列表 ✓；ShellSheet「Shell (0)」空态 ✓；堆积空态引导文案 ✓；TODO 空态 ✓；入队 omega→工具栏「堆积 1」计数刷新 ✓；⬇ 图标在 ✓；清空确认「将移除 1 条」→清除→计数归零 ✓；0 FATAL ✓
- 标题文案：pending_sheet_title「堆积与 TODO」（双 tab 时代）→「堆积消息」×15（096fe219 后补）

## 第十一轮：M3 原生 BottomAppBar（用户定案 3885fb41 + 288bae06）

用户：「我想要的工具栏是 Materials3 中原生的工具栏样式」——弃自拼 TextButton 行。

- **M3 官方 BottomAppBar**（actions 槽 + surfaceContainer 容器色）+ **官方角标模式**
  （BadgedBox→IconButton，M3 文档标准写法）
- 图标化入口：⬇（在底时藏）/ Inbox=堆积 / Checklist=TODO / AccountTree=智能体 /
  Terminal=Shell（后两个沿用 TaskSheet 原 tab 图标，语义连贯）；触达 48dp；
  a11y desc 挂 Icon（角标模式读「堆积消息，3」）
- 计数=运行中/待处理数（0 无角标仍可点看历史）
- 真机验证：四图标钮渲染 ✓ desc 全挂（堆积/TODO/智能体/Shell）✓ 堆积图标角标「1」
  实时显示 ✓ 点入口 sheet 弹出 ✓ 0 FATAL ✓

## 第十二/十三轮：M3 官方 FloatingToolbar + 紧凑档（用户指定 m3.material.io/toolbars）

- 用户给链接 m3.material.io/components/toolbars——目标样式 = **HorizontalFloatingToolbar**（M3 Expressive
  悬浮胶囊工具栏，非 BottomAppBar 满宽带）
- material3 显式升 **1.5.0-alpha26**（BOM 1.4.0 只有 tokens 无实现；maven 查证 alpha26 最新）
- API 勘误：alpha26 实参 = (expanded, modifier, colors, contentPadding, scrollBehavior, shape,
  leadingContent, trailingContent, …elevation, content 尾 lambda)——文档旧名 expandedContent 不存在
  （javap + sources.jar 双核对）
- **紧凑档（第十三轮 2fe36eae，用户反馈「太大了」）**：触达 48→36dp、图标 24→18dp、⬇ 分隔 8→4dp
- 真机验证：悬浮胶囊形态（MCP「floating pill/capsule, matches m3.material.io」）✓ 四图标 ✓
  角标「1」实时 ✓ 紧凑观感（「icons closely packed, ~18dp」）✓ sheet 弹出 ✓ 0 FATAL ✓
- **最小档（第十四轮 0c54aace，用户反馈「还是整体太大」）**：图标 18→14dp、触达 36→28dp、
  contentPadding 显式收至 2/0dp（官方默认给足留白是显大的另一半根因）——MCP 终验
  「much smaller than input bar / tiny ~14dp / appropriately subtle」；堆积+TODO 双 sheet
  弹出回归 ✓ 0 FATAL ✓

## 第十五轮：终局形态 M3 官方 FAB Menu（0b91d771）

- 用户否 FloatingToolbar 最小档（「还是不好」）→ 四方向（A 顶栏/B 单FAB/C 输入栏/D 角落）→
  **用户拍板「做成 fab menu」**；三问定案：右下角悬浮 / 恒显示 / ⬇ 并入菜单；形态 1（M3 官方）
- alpha26 本就带 **FloatingActionButtonMenu 三件套**（Menu+ToggleFloatingActionButton+
  FloatingActionButtonMenuItem；javap+sources.jar 双核对：param 名 button/content/text/…，
  默认 56dp/16dp 角，token FabSmall=40dp）
- 实现 `ChatFabMenu.kt`（ChatBottomToolbar 退役）：
  - 收起 = 40dp 小 FAB（**与退役滚底 SmallFAB 同规格**：containerSize(40,40)+cornerRadius(12,12)+
    20dp 图标；surfaceContainerHigh/onSurface）+ Inbox 图标 + 总数角标（四入口计数和）
  - 展开 = 官方交错动画菜单（horizontalAlignment=End 右对齐）：⬇（在底置灰 surfaceContainerHigh+
    onSurfaceVariant@50%，可滚时 primaryContainer）/堆积/TODO/智能体/Shell，角标挂 icon（BadgedBox）
  - dismissal：外点整屏拦截层（展开时才有）+ BackHandler；点入口先收菜单再开 sheet
  - ChatScreen 仅换调用（BottomCenter→BottomEnd）；四 sheet/ChatToolbarEntry 零改动复用
- i18n：chat_fab_menu_open/close ×15（676 keys PASSED）
- 真机 E2E（houji，devRelease）：FAB 渲染+desc ✓ 展开 5 项右对齐 ✓ ⬇ 置灰(38,43,46)↔
  激活(0,77,100) 像素实锤（MCP 视觉称「未置灰」又误报，像素为准）✓ 点⬇ 收菜单回底 ✓
  外点收起 ✓ 堆积项→sheet ✓ FAB 角标红像素(152,83,80)=「1」✓ 堆积项角标 ✓ IME 上浮 ✓
  busy 队列→自动发送 ✓ 0 FATAL 全程 ✓
- 教训：IME 弹出后输入栏从 y2538 跳 y1616——发送按钮坐标必须 dump 后再点（协议 y1086,1616
  是 IME 开位）；input keyevent 67 批量删除不保证清空（本次残留致拼串误发，测试手法问题）

## 第十六轮：紧凑档 + 配色避撞 + 描边（7687282f + 0a3c07b6）

- 用户三连反馈：①太大 ②颜色与 agent/用户气泡重合 ③要紧凑——查证撞色实锤：FAB 的
  surfaceContainerHigh=agent 气泡色（ChatMessageList:1366）、菜单项默认 primaryContainer=
  用户气泡色（动态色 #004D64）
- **紧凑档**：FAB 40→32dp（图标 20→18dp、圆角 10dp）；菜单项外部 Modifier.height(44dp)
  压掉官方 56dp 最小高（源码 sizeIn(minHeight=FabMenuItemHeight=56dp)），图标 24→18dp、
  titleMedium→labelLarge
- **配色避撞**：FAB 与菜单项统一 secondaryContainer 系（消息流未用）；⬇ 置灰
  surfaceVariant+onSurfaceVariant@45%；FAB 展开随动 secondaryContainer→secondary
- **描边**（用户复改「用带边框的」）：查证 M3 FAB 家族无原生 outlined 变体
  （FloatingActionButtonMenu.kt 源码 0 处 border；Button/Chip 有 Outlined 变体，FAB 没有）
  → 按 OutlinedButton 同法 Modifier.border(1.dp, colorScheme.outline, shape)：
  FAB RoundedCornerShape(10dp)、药丸 RoundedCornerShape(50)
- 踩坑：material3 alpha26 无 minimumInteractiveComponentSize 导出（foundation 与 m3 都没有）
  → 去掉（MenuItem 内部 Surface 已默认 48dp 触达）；containerColor lambda 非 composable
  上下文，色值需外部捕获
- 真机验证：药丸高 128px≈44dp ✓ 描边像素实锤 (138,146,151)=outline 色环在药丸左右缘
  与 FAB 圆环 r≈52px 双双在位 ✓ 背景恰为用户气泡蓝 (0,77,100) 被描边干净分隔 ✓
  MCP 视觉：「描边清晰/区分度显著提高/简洁现代专业」✓ sheet 开合/外点收起回归 ✓ 0 FATAL ✓
- **展开按钮加大（8a66efa9，用户「菜单项 ok、按钮可稍大」）**：官方 Toggle 展开变大模式
  containerSize(36,40)——收起 36dp（图标 18→20dp）→展开 40dp，圆角 11dp 同步；直线扫描
  实锤展开态填充 120px=40.0dp 横纵双轴 ✓ 收起态 108px=36dp ✓ 药丸 44dp 不动 ✓ 0 FATAL ✓
  （MCP 视觉对比例仍有微词 7/10——模型审美仅供参考，用户真机验收为准）

## 第十七轮：⬇ 拆回独立底部居中 FAB（7492af6c）

- 用户定案「滑动到最底部还是在中间吧，大小跟这个按钮一样大」——⬇ 出菜单，
  独立 ChatScrollBottomFab 底部居中，与菜单 FAB 同规格（36dp/圆角 11dp/20dp 图标/
  secondaryContainer+outline 描边；16dp 底距=菜单内部按钮下距，双圆同基线）
- 在底隐藏；isAtBottom .value 读取限制在小组件作用域（沿袭 B-F5 重组隔离）；
  声明在 ChatFabMenu 之前——展开时被外点收起层盖住（点中间先收菜单）
- ChatMessageList 死代码清理：旧 ScrollBottomFab 函数 + hideScrollBottomFab 参数
  + 2 条未用 import（-46 行）；菜单回归四入口（堆积/TODO/智能体/Shell）
- 真机 E2E：在底隐藏 ✓ 上滑出现（居中 x600、与菜单 FAB 同 y2220-2280 基线）✓
  直径 108px=36dp ✓ 同帧色与药丸完全一致 (208,230,242)（动态取色换板致跨帧对比
  误导，同帧为准）✓ 展开态点中间=先收菜单 ✓ 再点=回底后隐藏 ✓ 菜单四入口无 ⬇ ✓
  MCP：「同一组件家族/大小样式一致/下箭头」✓ 0 FATAL ✓
- **点击无效修复（6aa92aa2，用户「点击无效啊」）**：E2E 复现——点击后视口不滚、
  FAB 不消失。logcat 实锤：forceScrollTick 路径的 ForceScrollExecutor 等
  「新消息增长」5s 超时（grew=-1）后才滚——那是**发送后跟随**的执行器，⬇ 按钮
  复用它语义就错了（点按无新消息→死等）。修复：改 `coroutineScope.launch {
  listState.snapToBottom() }` 即时吸附（旧 ScrollBottomFab 同语义）。
  E2E 复验：FAB_BEFORE=1 → 点击 → FAB_AFTER=0（即滚即隐）✓ 0 FATAL ✓
  （教训：拆回旧功能时，连旧的事件路径一起拆——旧 FAB 当年就是 snapToBottom）

## 第十八轮：FAB Menu 回退 M3 原版样式（e64ace76 + 5c8c6b6e）

- 用户问「现在是 M3 原版样式么」——如实对账：16-17 轮的紧凑/换色/描边全是定制，
  逐项列表给出（尺寸/圆角 morph/色/描边/字号/图标 7 项偏离）；给 m3.material.io
  FAB Menu 参考链接。**用户定案「原版的就挺好的呀，这个圆的」**
- e64ace76 全回退：ToggleFloatingActionButton/FloatingActionButtonMenuItem 零定制参数
  （56dp morph 16→28dp + primaryContainer→primary 变色回来、药丸 56dp/titleMedium/
  24dp 图标、无描边）；⬇ 升标准 FloatingActionButton；唯一保留=角标（功能性）
- 5c8c6b6e 尺寸回调（用户「菜单与按钮可以小一点，跟现在的尺寸相近」）：
  **只动尺寸不动色**——containerSize(36,40)、菜单项 height(44dp)、⬇ 36dp；
  morph 动画与官方配色全保留
- 真机 E2E：双 FAB 直径 ~118px≈36dp+阴影 ✓ 中心色 (190,233,255)=primaryContainer
  原版蓝 ✓ 图标节点 72px=24dp 官方尺寸 ✓ 菜单「排队」新文案生效 ✓ snap 即滚 ✓
  MCP 视觉：「蓝绿色系/标准 M3 排版/Close 图标/标准 FAB Menu 观感」✓ 0 FATAL ✓

## 第十九轮：Secondary 配色变体（d659ce46）

- 用户问 FAB Menu 规格页的 12 个颜色角色是什么——讲解成对体系（容器+On）与
  官方三变体（Primary/Secondary/Tertiary），坦白 Primary 默认与用户气泡同角色
  （16 轮撞色根源）；给 A/B/C 三选
- **用户选 B**：Toggle 用官方 API `ToggleFloatingActionButtonDefaults.containerColor(
  secondaryContainer→secondary)`（正规格途径，非手写 lerp）；药丸/⬇ 同步
  secondaryContainer 系；尺寸/排版/动画零改动
- 踩坑：ToggleFloatingActionButton 无 iconColor 参数（该色只在 Modifier.animateIcon
  里）——删掉即可，Surface 自动 contentColorFor(secondaryContainer)=onSecondaryContainer
- 真机 E2E：同帧取证 FAB (208,230,242) vs 用户气泡 (0,77,100) 区分明确 ✓
  MCP：「偏灰柔和第二色系/区分度较好/简洁现代」✓ 0 FATAL ✓

## 第二十/二十一轮：加大+描边 → 双 FAB 规格统一+移左（acd242f4 + ed3ebb1e）

- **第二十轮（acd242f4）**：用户「按钮稍微大一些，最好再加一个边框」——
  Toggle 36→44dp（展开 48dp；色/尺寸 morph 保留，**角 morph 冻结 16dp** 让描边
  形状恒定贴合）、⬇ 36→44dp、药丸 stadium 描边；全族 1dp outline
- **第二十一轮（ed3ebb1e，用户「两个按钮不一样大？细节也不太一样？保持一致」）**：
  像素实测抓到两处真差异——
  ① ⬇ 实渲染 144px=48dp vs 菜单 132px=44dp：普通 FloatingActionButton 内部
     强制 LocalMinimumInteractiveComponentSize(48dp) 最小触达；Toggle 不吃该
     机制。修复：provision 0dp 关闭（FloatingActionButtonMenuItem 源码同款手法）
  ② 图标色不一致：⬇ (53,74,83)=显式 contentColor；菜单 (23,28,31)=落默认
     LocalContentColor（Toggle 无 contentColor 参数）。修复：两 Icon 显式
     tint=onSecondaryContainer
  - ⬇ 移底部左侧（用户要求）：BottomStart + start 16dp=菜单内部横向 padding，
    与右下菜单 FAB 完美镜像
- 终验：双圆 132px=44.0dp 严格同径 ✓ 左缘 48px/右缘 49px=16dp 镜像 ✓
  同色 (208,230,242) ✓ 左 FAB snap 即滚即隐 ✓ 0 FATAL ✓

## #151 GitHub 上报真机调通（5f8c4b2c + 7d1b4f57）

- 用户注册 GitHub App（App ID 4682795，Only on this account，Device Flow）——
  用户误发 PEM 私钥（已提醒删除作废，device flow 用不到）；Client ID/Secret 收齐
- **凭据注入**：build.gradle.kts 从 local.properties 读取（gitignore 保护，提交零凭据
  实证 grep=0）；缺失时空串=禁用态引导不变
- **真机调通过程（教训三连）**：
  ① 「点了没反应」迷宫——Popup 不进 a11y 树/顶栏坐标漂移/键盘遮挡轮番干扰，
     最终 popup dumpsys frame=[727,302][1188,926] 锚定 row4=(950,847)
  ② 仪表化日志实锤点击与凭据都正常，卡在 requestDeviceCode——先见 NPE：
     setBody 未设 Content-Type=form（Ktor 默认 text/plain → GitHub 回错误 JSON →
     双叹解析抛 NPE，Kotlin1.9+ 直接抛 Java NPE 栈完全藏线索）；修复+防御解析
  ③ 修后变 SETTINGS preface not received（h2 被网络中间层干扰）→ GitHub 调用
     改专用 client 强制 HTTP/1.1 + 20s 超时 → 现为干净可读的失败弹窗
- **构建机验证凭据有效**：同 endpoint+body 走代理拿到 user_code=8027-F420 ✓
- **遗留（环境非代码）**：手机直连 github.com 不通（curl 000/h2 干扰/1.1 超时三证）；
  clash 仅监听 127.0.0.1 手机不可达——需用户开 Allow LAN+手机 WiFi 代理，或 VPN/换网

## #151 GitHub 上报全链路打穿（9ec85123 + 0f43acaa，用户开手机 clash 后）

- 手机 clash VPN 模式 → github 直连 302 通；device code 1s 返回（4337-89CB）→ 用户
  浏览器输码授权 → token 加密存储 → 预览 → 提交
- **提交流程连环修复三连（真机日志逐步逼出）**：
  ① 「上报失败」光杆无详情 → GitHubApiError 各分支补 message（401/403/HTTP+body/
     网络）+ 非 2xx 落 AppLogger
  ② 网络错误弹窗显示字面量 `${cause0.message...}` → 自己又犯了转义毒，改字符串拼接
  ③ **致命转义毒（上一会话遗留）**：GitHubApiClient 源码 6 处字面量 `${'$'}`——
     API_BASE/token/repo/issueNumber 全部未插值，URL 乱码致 Ktor 回落 **localhost:80**，
     Bearer 头变字面量 `$token`。全部改正常插值
- **404 → 安装**：URL 修好后 API 返回 404——授权≠安装（GitHub Apps 两步分离），
  用户 Install App 到 LeoNardo-LB/oc-beacon 后即通
- **终态（issue #2 实证）**：search 404 自动降级新建（spec 设计）→ issue 落库含
  机器块（指纹+install_id+设备+版本）+错误日志段+needs-triage 自动标签；指纹恰是
  本次转义 bug 的错误消息归一化——App 把自己踩的坑上报了，行为完全正确
- 教训（写进个人记忆）：通过工具写代码含 `$` 模板时，转义毒（`${'$'}` 字面量入源码）
  会编译通过+运行时静默错——凡 `${` 出现在非模板语义处必查
