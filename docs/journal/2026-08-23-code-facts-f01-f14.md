# 代码事实包 F01-F14 处置（2026-08-23）

> 状态：**已完结**（2026-08-24 用户验收三项通过，卡片已迁入本文件末节）
> 关联：#200 · 台账源 [`docs/terminology/conflicts-master.md`](../terminology/conflicts-master.md) 代码事实区
> 来源：#193 术语批次八路盘点顺带登记的 14 条非注释级事实

## 处置总表

| 号 | 事实（复核实况） | 处置 | commit |
|---|---|---|---|
| F01 | PartSerializer 主映射无 permission/question 分支；**缓存回读 payload 无 type 字段全走字段推断，推断亦缺 message/question 键** → 两类落 Unknown | ✅ 修：主映射补两分支 + 推断补两键（无他类顶层字段冲突核实）+ 回环/typed 单测 ×3 + typeName() 注释同步 | b90cb9b5 |
| F02 | 台账原述"ChatRepository 承载纯 UI 状态"——复核实况：**现役实现早已在 MessageDataDelegate（StateFlow），repo 侧 LRU+接口方法为死路径**（生产 UI 零调用） | ✅ 修：接口/Impl LRU+锁+常量/Fake×2+单测节 全删（Fake 49→47 override，注释同步） | c6ef198e |
| F03 | executeCommand 参数断层两层：ChatRepositoryImpl 不转发 + V1/V2 客户端不进 body（V1 契约 CommandPayload 明列 agent/model/variant/parts） | ✅ 修：接口补可选参数、Impl 转发、双客户端非空进 body（V2 beta-17963 实测容忍——见探测证据） | ab4bd2ea |
| F04 | SessionGrouping.kt 文件头自标死代码（引用旧编号 #194=顺移前 #200）；主代码零调用（normalizedPath 命中均为他处同名局部函数） | ✅ 修：主文件+测试 git rm | 6927faed |
| F05 | WINDOWS_DRIVES_ROOT 全库（含测试）**零引用**，DirectoryPath.DRIVES_ROOT_SENTINEL 单源现存 | ✅ 修：删死常量 | 02cd0ae2 |
| F06 | 双层防抖实锤：SessionSearchBar onValueChange→delay(300)→onSearch→VM _searchQuery.debounce(300) ≈600ms；全库无第三处 300ms | ✅ 修：搜索栏直发逐键上报（删 job/delay/scope + 4 import），VM 单层 300ms 保留 | 8e094a18 |
| F07 | LEVELS 无 FATAL：过滤 chip 精确匹配（entry.level in selectedLevels），选 ERROR 也不含 FATAL，FATAL 仅在无过滤时可见；levelColor 已有 FATAL 分支 | ✅ 修：LEVELS 补 FATAL 档（chip 可选中） | 0c81e2ae |
| F08 | 两处 `\$`/`${'$'}` 转义致字面量打印：MainActivity 分享图计数恒显 `${uris.size}`；Service 轮询失败恒显 `${server.displayName}` 等 | ✅ 修：改真插值 | 8e0b6067 |
| F09 | UI_HIDDEN(20) 恒 ≥ RUNNING_LOW(10)，第二条件永真冗余 | ✅ 修：删冗余分支（行为等价） | c8fa2ea8 |
| F10 | 74→79 个 provider 图标本地映射（models.dev 快照）；**非缺陷**——未命中回退 synthetic 星形图标，属维护观察（双品牌/区域/套餐变体随上游演进需人工同步） | 📋 留档观察，不动 | — |
| F11 | 台账陈旧：注释"46 个方法"→ 已修为 49；复测 `override (suspend )?(fun|val)` = 49，**注释与实况已一致** | 📋 无需修 | — |
| F12 | 前世包名 fixture 实况漂移：非台账所述 FileApiVcsTest 一处，而是 DiffViewTest+GitChangesPanelTest 两文件三处（含断言耦合 onNodeWithText） | ✅ 修：三处同步更正 dev/leonardo/ocbeacon | 88218c17 |
| F13 | 类名 JumpPrefetchStrategy 名实不符（2026-08-21 D-9 移除跳转职责后仅存滚动速度自适应窗口预组合） | ✅ 修：更名 ScrollSpeedPrefetchStrategy（文件/类/实例/KDoc ×4 文件 6 处；调用方零改动） | 200d50a3 |
| F14 | RenderReadinessRegistry 形参 msgId vs 实键 part.id（KT1 已留注释桥接） | ✅ 修：形参更名 partId + KDoc 对齐（调用方均位置参数，零外部改动；他文件 msgId 均为真消息级 id 不动） | b4c0e10a |

## 顺带修复（存量，挡验证路）

**androidTest 编译断裂**（#193 Tier A/B 改名只跑单测未编译 androidTest 的遗漏，F12 验证前置依赖）：

- FakeChatRepository：删已并入 upsertMessages(MergeStrategy) 的 setMessages/mergeMessages/replaceMessages 三遗留 override
- FakeSessionRepository 补 getSessionTodos；FakeSessionStateRepository 补 serverIdFor/hasPendingUserInput
- TaskSheetClickTest → AgentSheetClickTest（TaskSheet 已拆分，改接 AgentSheet 签名）→ commit 0bb0de2d

## V2 字段容忍探测（F03 依据）

假 sessionID POST /api/session/probe-zzz-404/command（Basic auth，本机 beta-17963）：

- 最小体 {"command","arguments"} → 400 "Invalid session ID"
- 全字段体 +agent/model/variant/parts → **同样 400 "Invalid session ID"**（schema 未拒多余字段，校验已达 sessionID 层）→ 补字段安全；V1 侧有 API 参考 CommandPayload 契约背书。

## 验证记录

- :app:compileDevDebugKotlin / compileDevDebugUnitTestKotlin / compileDevDebugAndroidTestKotlin 全 BUILD SUCCESSFUL（每组提交波后复跑）
- :app:testDevDebugUnitTest --rerun 全量 BUILD SUCCESSFUL（2026-08-23，含新增 Part 回环 ×3）
- 行为等价性边界：F03 现有调用方均不传新参（body 与原先逐字节一致）；F05/F09 纯等价删除；F13/F14 编译器保护改名；F01 仅新增分支不触旧路径
- 用户可见变化仅两处：F06 搜索过滤时延 ~600ms→~300ms（更跟手）；F07 诊断页级别过滤新增 FATAL chip

## 用户验证清单（V6 人工）

1. F06：会话列表搜索——连续输入时列表过滤应明显跟手（原约 0.6s 停顿感）
2. F07：设置→诊断→级别过滤——应出现 FATAL chip 且选中后仅剩 FATAL 级条目
3. （顺带回归）工具卡片展开/收起如常（F02 删的是死路径，现役实现在 MessageDataDelegate 未动）

## 遗留

- F10 图标映射维护观察：随 models.dev 演进需人工同步（双品牌 zai↔zhipuai/_cn/_coding_plan 变体）——不立项
- #201-#205 Tier C 五卡按用户指令冻结，等明确开始指令

## 完结迁移（backlog 卡片原文，2026-08-24 用户验收「ok ×3」）

> **#200 盘点代码事实包（F01-F14）：非注释级缺陷与死代码** `refactor` `sse`（2026-08-23 实现完成：11 项修复 + 2 项留档/核实无需修；编译×3 源集 + 全量单测绿；顺带修 androidTest 存量编译断裂——待用户人工验收 F06 搜索跟手/F07 FATAL chip，清单见 journal）
> - PartSerializer 缺 permission/question 分支落 Unknown · executeCommand 死参数（V1/V2 同）· 盘符哨兵双定义 · 搜索防抖 300ms 双层串联 · FATAL 级不可过滤 · 日志 $ 转义 ×2 · 前世包名 fixture · 冗余条件等——详见台账代码事实区
> - → [`docs/terminology/conflicts-master.md`](../terminology/conflicts-master.md) 代码事实区 · 本 journal（处置总表/证据/验证清单）

**完结状态**：用户人工验收三项全过（2026-08-24：①F06 搜索跟手 ②F07 FATAL chip ③工具卡片展开回归；真机 houji versionCode 1787501485 devDebug）。卡片从 backlog 迁入本节。
