# 387 消息层扁平化（2026-09-17）

> 状态：进行中
> 关联：（spec 路径，若有）·（issue 编号，若有）
> 来源：用户反馈 / grilling / E2E / 顺带发现

<!-- 过程中的取证/验证证据直接写本文件；backlog.md 只留 ≤3 行卡片。 -->

## 实施记录（2026-09-17，按 spec 三批次）

### 批1 数据层（已提交 b1b4d393）
- (a) DshEventMapper.mapAssistantMessage 读 `data.message.source.{provider,model}` → Message.Assistant.modelId/providerId。权威实况：本机 DSH 0.1.5 归档 `message.source = {"kind":"model","provider":"opencode-go","model":"deepseek-flash",...}`；web 同源 messageRoute()（dsh-client-ui-chat client.js:6921-6927）。
- (e) usage 全桶：新增 `DshEventMapper.usageTokens`（input/output/total/reasoning/cache.read/cache.write；total 优先 totalTokens，缺席按 input+output+cacheRead+cacheWrite 派生，与 web normalizeUsage chat.js:6931-6964 同构）。`assistant/message` 终态消息落全桶；`assistant/chunk` 的 usage 帧由 Ignored 改为 tokens-only MessageUpdated 写流式宿主（消费端 mergeAssistantMeta 非空合并只写 tokens，宿主 terminate 时被 MessageRemoved 拆除，不污染终态）。
- (f) 删除 metadata.agent → Message.User.agent 漂移映射（V2Mappers.synthetic / SseClientV2 promoted 两处）；删除死组件 MessageMetaInfo.kt + 两个 androidTest。
- 新增 seam：`ui/screens/chat/rowmodel/MessageRowModel.kt`（纯 Kotlin：RowCapabilities[ServerCapabilities 派生] / statusBadgeFor / turnNumberFor / messageRowTail / TurnDetailInput+buildTurnDetailRows / NotificationRowModel / eventIdentityKey+dedupeByEventIdentity）。
- 单测：DshAssistantSourceUsageTest、V2SyntheticAgentDriftTest、MessageRowModelTest。

### 批2 消息层（已提交 b54c55ca / 48294a48 / 397a6156）
- 新增 `MessageSectionScaffold`（扁平三段式布局单点：头部标题栏/正文栏/尾部统计栏，无背景/无边框/无圆角）；`MessageBubble` 增 `flat` 模式委派之——通知层（EventCard/合成通知卡）容器保持不变。
- 用户消息：右对齐 + 最大宽度 82% + 去底色；尾部撤销/复制/「更多」（删除）。
- 智能体三条渲染路径（常规 MessageCardAssistant / ChunkedAssistantMessage / SegmentedAssistantMessage）统一走扁平三段式并抽单点；尾部含 模型名+provider / 第 N 轮 / 耗时（流式 ticker）/ 步数·工具数摘要 / 产出文件行 / 复制 / 从此轮分支 / 更多。
- 头部状态徽标（流式中/已中断/出错，完成态不显示）：新增 `MessageStatusBadgeLabel`；DSH `interrupted` → 已中断（spec 数据层补全 (c)）。
- 「更多」ModalBottomSheet：👍👎（DSH 能力位）+ 复制 Markdown 源码 + 删除消息（core.message.delete）。
- US#22：撤销 UI 层 ×N，装配层 `dedupeByEventIdentity` 按事件身份键收敛并原位更新（syntheticEventIdentityKey：子会话/shell id 优先）。
- US#28：`Message.Assistant.turnNumber`（DSH data.turn）+ merge 保留 + RenderableTurn.serverTurn；第 N 轮服务器优先、客户端锚点兜底。
- 间距：ChatScreen messageSpacing 8→16dp（紧凑 2→8dp）。
- MessageBubble kdoc 旧裁决回写：2026-08-12「三气泡统一容器」被本设计反转（仅角色消息去容器，三段式保留；通知层继续用容器）。

### 待办 / 进行中
- 批3 统计弹窗（(b) sessionStats 呈现 + 逐轮明细列表 + 底部面板化）——子代理实施中。
- 新增 ServerFeatures：COST / MESSAGE_DELETE / TURN_TIMING（能力位派生取代服务器类型分支）。
- i18n：新增 15 英文源键 + 批3 约 15 键，14 语言翻译进行中。
- DSH workflow 降级卡核对（spec Further Notes）：`mapWorkflowRunStart/End` 产 synthetic 文本 → SyntheticNotificationCard → EventCard，**已走统一卡语言**（无需另改）。

## V3 模拟器走查证据（2026-09-17，前台带窗口 AVD Pixel6_Android36）

- 环境：`emulator -avd Pixel6_Android36`（**有窗口前台**，非 headless）+ `adb reverse tcp:4199/tcp:4198`；devDebug 签名包；debug intent 直达会话列表。
- 证据截图：docs/acceptance/2026-09-17-387/（01 统计底部面板；02 用户消息气泡 + assistant 扁平三段式终态）。
- 02 assistant 实测：头部「🤖 智能体 + Build + 2026-09-10 09:36:14」/ 正文全宽无气泡（推理块保留自身容器）/ 尾部「provider 图标 + omen-alpha + 2.4s + 轮次 1 + 1 步 + 0 个工具 + 复制 + 从此轮分支」。
- 01 用户消息实测：右对齐块（最大宽度 82%）+ 去底色；尾部撤销/复制/更多；通知卡（后台命令失败/完成）与消息同列同宽、不缩进、EventCard 容器保留。
- 03 统计面板实测：ModalBottomSheet（拖拽把手 + 关闭）；provider·model、时间戳、上下文进度 147.9k/1.0M(14%)、消息计数、缓存命中 99%、构成 breakdown、Token 明细卡；**各轮次详情**列表（轮次 20/19/18… 倒序、可展开 chevron）。会话级 sessionStats 区在本 V2 会话缺席（DSH 专属投影 → 能力位整区隐藏，符合 US#33）。
- 局限：Maestro CLI 本机未安装 → V2 自动化 flow（maestro/e2e-message-flattening.yaml）未能执行；已改用 adb + 截图走查（V3 等价路径）。本次未跑 connectedAndroidTest（耗时）。
- 用户消息实测发现：V2 服务器 free 模型对极短提示偶发返回 `content=[]` 空 assistant 消息（app 正确不渲染空消息，非回归）——改用含历史内容的会话完成 assistant 侧验证。

## 2026-09-17 用户裁决修订：用户消息保留气泡

- 用户明确指出：「用户消息还是得有气泡，用原来的三段式气泡」——**否决**原 US#2 / L1 规格的「用户消息去底色」。
- 修订后范围：扁平化只作用于**智能体正文**（flat 委派 MessageSectionScaffold）；**用户消息保留原三段式气泡**（MessageBubble：primaryContainer 底色 + AMOLED 描边 + UserBubbleShape），仅在尾部新增「更多」（删除消息，能力位门控）；通知层继续用容器。
- 已同步修订：spec（US#2 / Solution / L1 规格 / 间距节 / Further Notes 旧裁决回写 + 变更说明）、字段盘点底稿开口项结论、MessageBubble 头注释、GitHub Issue #11 body、本 journal。
- 与两端参考实现一致：dsh web / opencode web 均为「用户留气泡、助手平面」。

## 双轴评审结论与修复（2026-09-17）

- Spec 轴：三批次主体忠实落地（L1/L2/统计弹窗/数据层 (a)-(f)/i18n/无越界）；发现 1 项功能违反（US#35 删除门控未接线）+ 2 个 seam 半成品 + 轮次序号进入消息流（与 L1 明文冲突）等。
- Standards 轴：Blocking 1（产出行丢失流式完结门控 → SSE 高度补偿铁律回归）+ Should-fix 7 + Nit 8；规范符合面（M3 原生 / 能力位派生 / PathUtils / AppLogger / i18n 硬规则）无发现。
- 已修：B1 产出行补 `allStepsCompleted` 门控；US#35 删除入口按 core.message.delete 整项隐藏；轮次序号移出消息流；US#9 复制流式常显；删除补确认对话框（启用既有 2 键）；4 孤儿键清 15 语言；统计弹窗稳定身份键；死代码清理；messageRowTail/notificationRowModel seam 接线（主路径 + 分片统计栏 + 通知卡）。
- 未修（登记为后续/接受）：尾部主路径与 ChunkStatsBar ~120 行同构（S1，抽 AssistantTurnTail 的大重构风险高）；MessageMoreSheet 未走 SheetTokens 三件套（S6，动作菜单是否属「主对话抽屉」待 ui-conventions 裁决）；DSH 逐轮 TTFT/速度无数据源（US#26 部分，门控正确隐藏）；N1/N2/N3/N6/N7/N8 排版级。
- 复核：`compileDevDebugKotlin` + 全量单测 + `lintDevDebug`（0 error）+ `assembleDevDebug` 全绿；`i18n-check.sh` PASSED（917 keys × 14 languages）。
- 终态真机复核（证据 02）：用户消息三段式气泡已恢复；助手正文扁平三段式（尾部模型/耗时/步数·工具数 + 复制 + 从此轮分支；轮次序号不再出现在消息流）。

## androidTest 真实化与目标收敛（2026-09-17）

- 修复 androidTest 测试图缺口：build.gradle.kts 补 `kspAndroidTest("com.google.dagger:hilt-compiler:2.60.1")`（此前缺失 → @HiltAndroidTest 的 *_TestComponentDataSupplier 从未生成，全部 Hilt 插桩测试 ClassNotFound）；FakeDomainModule 补 ServerSettingsRepository 绑定 + 新增 FakeServerSettingsRepository（DomainModule 已绑定、测试替换模块漏同步，与 2026-08-16 补 MessageCacheRepository 同类）。
- 修复后 `:app:connectedDevDebugAndroidTest` **首次真正运行**：113 tests / 25 failures。分类：20 例 androidx.compose.ui.test `ComposeNotIdleException`（Idling resource timed out）、1 例 Room `IllegalStateException: A migration from 1 to 9 was required but not found`、其余断言失败。
- 根因定位：`ChatMessageList.kt:1148` 常驻 `LaunchedEffect { while(true){ withFrameNanos{}; PreRenderShiftChannel.drain(listState) } }`（#258 渲染前补偿帧界排空泵）——任何渲染 ChatScreen 的 Compose 测试永不 idle。**属预存在的核心滚动机制（SSE 铁律域），非 #387 引入**，改动需谨慎并用真机复核三铁律。
- 处理：登记 P2 独立卡（androidTest 不可运行：帧泵 idle 超时 + Room 迁移缺失），给出修法方向（给 PreRenderShiftChannel 增加待排空信号使泵空闲挂起，或测试 Local 关泵）。目标描述同步收敛：本目标交付「测试图修复 + #387 全量交付 + 验证 + 文档同步」，预存在帧泵/Room 问题归独立卡。
- 本轮 V1 复核：compileDevDebugKotlin / testDevDebugUnitTest --rerun / lintDevDebug（0 error）/ assembleDevDebug 全绿。

## 消息层 v2 再修（2026-09-17）

用户装包手动测试后提出 7 条反馈，经 3 轮拷问定稿后实施：

- 头部标签栏整体删除（用户与助手都是）：角色图标 / 「用户」「智能体」文字 / agent 标签 / 时间 / 状态徽标全部撤出消息头部（用户裁决：角色文字零信息量）。
- 时间 / 状态徽标 / 逐消息 agent 标签 / 低频动作迁入尾部与「消息详情」弹窗。
- 「更多」`ModalBottomSheet` 抽屉 → 居中 `AlertDialog`（消息详情：上半只读信息、下半动作）；ui-conventions 的 MessageMoreSheet 抽屉例外登记随之撤销。
- 尾部重排：助手 = [状态徽标?][agent 标签?] 模型 · 耗时 · 步数·工具 + 复制 + ⓘ；用户 = 撤销 + 复制 + ⓘ；「从此轮分支」自尾部移入详情弹窗。
- agent 可见性数据驱动：逐消息 `Message.Assistant.agent` 非空（OpenCode）→ 助手尾部显示；为空（DSH 不写该字段）→ 回落到会话级 `Session.agentPreset`，顶部栏标题下方弱色小字显示一次。
- 点 7「统计栏一开始就展示」核对结论：助手尾部流式期本就在渲染（`visible = isStreaming || …`），用户消息迟到属服务器确认后才出现气泡（正常空档）——**本次不改发送路径 / 不加乐观插入**。
- 间距维持 16dp / 紧凑 8dp，不加分隔线（结构可读性依据从「头部角色标签」改为「间距 + 尾部统计栏」）。
- L2 通知层不动。

落点：`MessageSectionScaffold`（砍头部，缩为正文+尾部）、`MessageBubble`（flat 参数瘦身 + `showLabelRow`）、`MessageCardAssistant`（主/Chunked/Segmented 三路径尾部重排 + 详情弹窗）、`MessageCardUser`、`MessageMoreSheet` → `MessageDetailDialog`、`MessageRowModel`（尾部 agentName/detailAvailable + `messageDetailFields` seam）、`ChatTopBar`（会话级 agent）、`ChatScreen` 透传、i18n（新增 4 key、删 2 旧 key，915 keys × 14 语言）。

验证：`compileDevDebugKotlin` / `testDevDebugUnitTest --rerun`（含新增尾部与详情字段断言）/ `compileDevDebugAndroidTestKotlin` / `lintDevDebug`（0 error；注：baseline 有 2 条已失效条目待清理）/ `assembleDevDebug` 全绿；`i18n-check.sh` PASSED 915 keys × 14 languages；模拟器走查见 `docs/acceptance/2026-09-17-387-v2/`。

Spec 与 Issue #11 已同步 v2（`docs/specs/2026-09-12-message-chrome-flattening-design.md`，含「v2 修订记录」节）。

## v2 追加：用户消息 90% 宽 + 思考卡圆角（2026-09-17）

用户复测追加两条：

- 用户消息最大宽度 **90%**：`MessageBubble` 重新引入 `maxWidthFraction`（非 flat 路径作用于 Card；`fillMaxWidth(fraction)` + 外层 `horizontalAlignment=End` 实现右对齐收窄），`MessageCardUser` 传 `0.9f`，分片路径 `ChunkedUserMessage` 用 `Box(contentAlignment=CenterEnd)` + `fillMaxWidth(0.9f)` 同等收窄。
- 思考卡圆角：`ReasoningBlock` 的 `Surface(shape = ShapeTokens.none)` 是全仓库唯一 0dp 圆角卡片；改为 `ShapeTokens.smallMedium`（6dp）对齐 `ToolCardScaffold` 家族。

范围说明：思考卡容器在 spec 的 Out of Scope 里属 #215 卡片层，本次经用户直接指示提前落地；未触碰其它卡片形状。

验证：`compileDevDebugKotlin` / `testDevDebugUnitTest --rerun` / `lintDevDebug`（0 error）/ `assembleDevDebug` 全绿。

## 通知/注入卡文案按服务器特性定制（2026-09-17）

用户指出：DSH 的卡片是「上下文注入」、OpenCode V2 的是「通知」，应按各自特性定制文案。

- **修正错标**：DSH `system/message` 无 `source.kind` 时 mapper 写哨兵 `injectionKind = "system"`，此前落到 `role == "system"` 分支的兜底「工具目录已变更」（OpenCode 专属文案）。改为：`injectionKind != null` 即走注入档（`"system"` / `"context"` / 未知 → 「上下文注入」）；`null`（V2 `role=system`）保留「工具目录已变更」。判定收进 rowmodel 纯函数 `injectionLabelKindFor(kind)`（新增枚举 `InjectionLabelKind`），JVM 可测、不按 ServerType 分支。
- **兜底标签**：`chat_event_generic` 英文「Event」→「Notification」，14 语言同步（zh「事件」→「通知」）。
- **后续卡**：#415 通知/注入卡图标按服务器特性区分；#416 形态分层（DSH 注入更淡，牵动 #215 卡片层）。本轮只动文案（Q3(a)）。

验证：单测新增 `injectionLabelKindFor` 两组断言（显式 kind 专属档；`system`/`context`/未知 → 上下文注入）；`i18n-check.sh` PASSED 915 keys × 14 languages；门禁全绿。

## 批2：V2 系统消息细分 + 卡片层描边（2026-09-17）

- **V2 系统消息细分**：纯函数 `systemNoticeKindFor(text)`（含 tool catalog / tool definitions 语义 → 「工具目录已变更」；其余 → 「通知」）；「通知」档补折叠描述行（原文首行 ≤200 字符）。修正「Today's date is now…」这类日期注入被误标「工具目录已变更」。
- **卡片描边**：`AmoledSurface` 增 `normalBorder` 可选参数 + 新增 `CardStandardBorder`（1dp outline · MEDIUM，EventCard 同语言）；接线 `ToolCardScaffold` / `TodoListCard` / `ToolProgressCard` / `FileCard` / `ReasoningBlock`。AMOLED 仍走 `AmoledDefaultBorder`。属 #215 卡片层范围，经用户直接指示提前落地。
- **验证**：单测 `systemNoticeKindFor` 5 断言；门禁全绿（compile / 全量单测 / lint 0 error / assemble）；模拟器走查 A–E 全通过（日期注入卡→「通知」+描述行、展开全文、工具目录标签保留、工具卡与思考卡 1dp 描边清晰、布局无异常，logcat FATAL=0）。「思考中…」进行态描边未实测（描边为无条件应用，理论覆盖）。

## 收尾：推送 / Issue 同步 / Maestro 环境受限 / DSH 面补测（2026-09-18）

- **推送**：`git push origin master`（e14fcb1a..39f864dd，383 个本地积压提交全部快进推送）；Issue #11 body 与最新 spec 两次同步。
- **Maestro**：`get.maestro.mobile.com` 直连 DNS 不通、本地代理（7897）TLS 握手失败 → 安装放弃；`maestro/e2e-message-flattening.yaml` 更新至 v2 两段式（删 Agent/User 头部断言；新增详情入口 contentDescription 断言、分支文字不应出现在消息流），已提交。
- **DSH 面补测（dsh-pair.sh emulator-5554 配对，token 注入 + cookie 持久化）**：顶部栏标题行内联「智能体 · PTC 模式」（v2 会话级 agent 首次 DSH 面实测通过）；工具卡（Run code）与思考卡 1dp 描边通过；用户气泡约 90% 右对齐通过；logcat 无 FATAL。现存 DSH 会话无上下文注入卡（如实负样本）——「上下文注入」标签由 `injectionLabelKindFor` 单测保证。DSH 思考卡无「· Xs」后缀 = #338 既定行为（时长未知不伪造）。
- 顺带发现：app 中打开「多服务器适配重构」会话即本对话自身的 DSH 实时转录，DSH 流式链路正常。

## 清剩余 P 卡：#410-#417（2026-09-18）

清剩余 P 卡批次（P2 #412 + P3 六张，2026-09-18）：

- **#412 androidTest 可运行**：PreRenderShiftChannel 增「待排空信号」（每列表 CONFLATED Channel；enqueue trySend、新 awaitPending 挂起）——泵由 while(true) 每帧必起改为信号驱动，空闲挂起、时序不变（入队帧 k → 帧 k+1 回调相排空 → 遍首应用）；ChatMessageList 泵改 awaitPending→withFrameNanos→drain。MigrationTest 补全 1→9 迁移链（对齐 DatabaseModule；4_5/5_6/6_7/7_8/8_9 均 CREATE/DROP IF EXISTS 或 DELETE，对手建 v1 库安全）。结果：113 tests，20 例 ComposeNotIdleException 全灭、Room ISE 灭，25→4；附带修复空态断言过时（chat_empty 已改 "Start a session"，断言改按 targetContext 资源取值）。残余 3 例（斜杠补全/@提及/发送路径 waitUntil 超时）为泵修前即存在的独立失败，另立卡。SSE 三铁律复核：泵语义等价（drain 空=早退），走查见下。
- **#414 RenderSupplyCoordinatorTest T11 flake**：固定 delay(150)+delay(100) 改 15s 轮询断言前提（awaitPendingEnqueued 同哲学）——二阶段重析不走 pending 探针，轮询 chunkPlans 块数 > stale 确定性收敛。
- **#413 FAB 遮挡**：LazyColumn contentPadding bottom 8dp → 8dp+76dp（FAB 56dp+底距+呼吸），贴底时末条消息尾部动作不再被压。
- **#415 注入/通知图标区分**：新 InjectionCard 图标 = Icons.Filled.DataObject（{} 上下文语义）；V2 系统通知 = Icons.Outlined.Notifications（铃铛）；工具目录卡保留 Info（历史语义）。
- **#416 注入卡淡形态**：新组件 InjectionCard（左侧 2.5dp 色条 + surfaceContainer 弱底 + smallMedium 圆角、**不描边**，容器语言与 ReasoningBlock 同族）——DSH source.kind 注入与 V2 <system-reminder> 嗅探注入两处接线；V2 工具目录/通知仍走完整 EventCard（描边卡）。
- **#410 尾部单点抽取**：删主路径 statsBar + ChunkStatsBar（约 120 行×2 同构），新私有 AssistantTurnTail 单点（信息簇：徽标→agent→模型→耗时 ticker→摘要；动作簇：复制+详情；产出行完结门控与详情弹窗装配内置），主路径 tailExtra 与分片/分段（isStreaming=false）三处共用。
- **#411 DSH 逐轮 TTFT/解码速度**：权威语义取自本机 dsh-session-stats/dsh-llm 源码（openStep 折叠 + isTokenDelta: assistant-stream.js:185-195）——DshEventMapper 记 step/start 起始（turn/end 清、128 上界）→ assistant/message 时按 stream[] 首 token（text/reasoning-delta 非空文本、tool-call-delta 非空 args 或带 name）派生 ttftMs/decodeMs/decodeTokens 落 Message.Assistant（mergeAssistantMeta 保真）→ ContextDetailDelegate 逐轮聚合（TTFT=轮内首步、速度=Σtokens/Σms）→ 统计弹窗逐轮展开体两行（能力位门控既有）。单测：DshStepTimingTest 4 例 + ContextDetailDelegateTest 1 例。

验证与实测（2026-09-18）：

- 门禁：单测（含 DshStepTimingTest 4 例 + ContextDetailDelegateTest timing 1 例，全量 0 fail）/ lintDevDebug（0 error）/ assembleDevDebug 全绿；connectedDevDebugAndroidTest 114 例 4 失败（原 25：20 idle + Room ISE + 4 本存）→ #417 另立。
- #413 实测（uiautomator 数值）：贴底末条 ⓘ 底边 y≈1019 vs 任务 FAB 顶边 y≈1968——彻底分离。
- #410 回归：V2 会话尾部簇文本（agent 标签 Build/模型/1 步·0 个工具）与思考卡正常。
- DSH 面实测：统计底部面板全区块正常（会话统计/各轮次详情，回归）；**注入卡（插件配置）在现场真实渲染**（InjectionCard 代码路径命中、无崩溃）；新会话轮因服务器 Insufficient Balance 失败 → #411 缺数据路径实测正确（逐轮行在、timing 行整项隐藏，宁缺勿谎）；数值路径未取得活体样本（单测覆盖）。会话「用一句话介绍你自己」由先前失败的走查子代理半程创建（22:19）。
- logcat FATAL/ANR = 0。
