# p1-p2-dev-batch（2026-08-21）

> 状态：进行中
> 关联：docs/specs/2026-08-21-queue-drain-state-compensation-design.md 等（逐项见下）
> 来源：用户指令——P1/P2 待开发项全量开发（三阶段：统一 grilling → 逐项开发+E2E → 期末全面回归）

<!-- 过程中的取证/验证证据直接写本文件；backlog.md 只留 ≤3 行卡片。 -->

## 批次总控

- 目标：P1 #176 #177 #178 #179 #180 #181 #182 #183 #187 #188（#154/#146 grilling 定案延后），P2 无待开发项
- Grilling 定案（2026-08-21，两轮全 frontier）：勾选政策=全验证即勾选迁移+期末人工清单；实施顺序=队列→输入气泡族→subagent 卡片族→ModelPicker→V1 复验；#150 授权自装 V1 服务器
- 勾选政策原文（Q1）：「编译+单测+真机E2E+回归」全链验证即勾选；维度5主观项汇末期清单

## #176 + #177 堆积队列状态补偿 drain（完结，迁移自 backlog）

**spec**：docs/specs/2026-08-21-queue-drain-state-compensation-design.md（三断点根因 + T1/T2/T3 触发器 + 护栏 + 手动入口设计）

**实现**（commit 546c8bc0）：
- PendingMessagePipeline：+drainIfIdle（T1 心跳 5s / T2 onEnqueued / T3 statusFlow Idle 观察三路汇聚；draining map 去重沿用）
- 护栏：Busy/无状态/hasPendingUserInput/serverIdFor=null 一律跳过；POST 失败留队首心跳无限静默重试
- SessionStateService：+serverIdFor/+hasPendingUserInput 只读查询（接口+实现）
- PendingMessageDao/Repository(+Impl)：+observeCounts（GROUP BY）/+sessionIdsWithPending（DISTINCT）
- EventDispatcher init：pipeline.start()（幂等，随首个连接启动）
- ChatViewModel.enqueuePendingMessage：落库后 onEnqueued 即时补偿
- SessionListViewModel/SessionTreeList/SessionRow：pendingCounts + 详情对话框「发送堆积消息（N）」按钮（仅 count>0）
- i18n：session_details_continue_queue ×15（668 keys 校验过）

**JVM 测试**（PendingMessagePipelineTest 6→14 例，全量 1848/1848 绿）：
- t2EnqueueAtIdleDrainsImmediately（#176 TOCTOU 直击）/ t2EnqueueAtBusyDoesNotDrain
- t1HeartbeatRetriesAfterSendFailure（虚拟时间 advanceTimeBy 重试→恢复→delete）
- t3IdleTransitionTriggersDrain（RestValidation 语义）/ pendingUserInputSkipsCompensation
- missingServerOwnershipSkipsCompensation / concurrentTriggersDedupToOneSend（gate 阻塞验证）/ multiSessionCompensationIsIndependent

**真机 E2E 证据**（houji e69a99d8，全新构建，logcat 全程 0 FATAL）：
- E5 回归×2：execution.succeeded → 43ms 内 "queued message sent"（21:10:35，19 chars）；自然路径+T3 并发被 draining map 去重（"drain already in-flight" 单次发送）
- T2/T3/T1 生产日志：enqueue 即时 Idle 检查（"compensation skip (not Idle: Busy)"）；T3 Idle 观察；T1 心跳 5s 稳定节拍（21:31:12→47 连续 5s 间隔）
- 冷启动存活性（进程死亡）：入队 → am force-stop → turn 服务器侧结束/进行中 → 冷启动 relaunch → L4 syncFromRest → 自动 "queued message sent"（21:37:40，26 chars）——#177 核心「app 死后队列自动发送」最严苛形态
- E4 手动入口：队列非空 → 会话列表长按 → 详情对话框出现「发送堆积消息（1）」（i18n 计数格式）→ 点击 → "queued message sent"（21:50:22，21 chars，Busy 态手动放行=服务器 inbox 排队语义）
- 待答护栏实证：pending question 期间 L3 "skip zombie interrupt, keep Busy (waiting)"（21:27:46）+ FSM Busy → drain 正确不抢发

**未覆盖项说明（诚实登记）**：
- #176 的 ms 级 TOCTOU 窗口（turn 恰在入队写库瞬间结束）真机不可确定性复现——T2 逻辑由单测 t2EnqueueAtIdleDrainsImmediately 精确覆盖；真机实证的是其超集（Idle 态任何路径入队即发）
- 严格 RestValidation(Idle)→drain 的服务器侧触发（turn 在 app 死亡窗口内结束）单测覆盖机制（t3IdleTransitionTriggersDrain）；真机冷启动轮因 turn 在复活后结束走了自然路径——机制相同（FSM Idle→drainIfIdle），证据链完整
- 维度 5（观感）：无 UI 变化项（对话框新按钮属功能入口，无动画/手感），无人工验收项

## #178 键盘保持 + #183 分割线留空减半（完结，commit 8a965166）

**#178 取证**：
- 嫌疑①（气泡抢焦点）：实锤——Popup(focusable=true) 抢占窗级焦点 → IME 收起
- 嫌疑②（滚动触发 hide）：证伪——发送路径 forceScroll 走 ForceScrollExecutor 的 requestScrollToItem（程序化瞬时滚动，不置 isScrollInProgress），ChatScreen:391-402 的 hide 只在真实用户滚动 + index 变化时触发
- 修复：focusable=false + BackHandler(enabled = showBusyMenu && busySpinner) 只关气泡

**#183**：MessageCardAssistant 两处（完整气泡 :248 + 分片 chunk :558）vertical padding 6→3dp（compact 3→1.5dp）。

**真机 E2E（像素级，houji e69a99d8，E178 会话）**：
- 菜单打开→键盘带（y1750-2670）与打开前 diff=0.00（纹丝不动）
- 发送（idle 态输入+点发送）→ 键盘带 diff=0.00
- back 序列：①收键盘（系统 IME 先吃 back——Android 标准语义，与 Q11 期望的"先关气泡"顺序相反，实测不可抗：IME 层在 app BackHandler 之前消费）②关菜单（BackHandler 生效，裁剪 OCR 实证立即发送/堆积消息消失）③正常退出聊天页
- turn 自然结束→ busySpinner 变 false → 菜单自动消散（重组关闭）
- 全量单测 1848/1848 绿

**遗留说明（维度 5）**：back 键顺序为「键盘→菜单→退出」（系统 IME 语义）而非用户期望的「菜单→键盘」；所有主流 IM 应用同此行为，覆写需在 view 层拦截 keyevent（与系统对抗），不建议。观感由用户期末清单确认。

## #179 气泡间距（取证完成，待用户证据）

静态取证结论（详见会话记录）：messageSpacing=8dp 自 07-31 未变；分片中段零装饰、首末段装饰与普通气泡同值（14dp/SM、时间戳栏/统计栏同构）；两嫌疑 commit 的唯一视觉变化= 92e2855c 对 ≥3000 字符消息段内单换行空行化（为治滚动卡顿）——表现为「气泡内部变高」而非「气泡之间变大」。已按 Q14 约定带证据回拷用户（截图/会话定位），等待回复中。#183 分割线减半或已缓解观感。

## #180 + #181 + #182 subagent 卡片三件（完结，commit c8be1689）

**#180 取证（两层）**：
- REST 路径：父会话 /message 显示 tool part metadata 在 running 态已有 sessionID（md: {sessionID: 子会话, status: running}）——卡片原代码 `!isRunning` 门控才是跳转不可用的直接根因
- SSE 路径（宿主机 curl /api/event 抓帧实证）：tool.called 无 metadata、tool.success 才有终态；tool.progress 的 metadata 携带 {sessionID: 子会话, status: running}——Running 期推断源
- synthetic 消息同源命名 childID（V2Mappers 注释佐证）→ 一并归一

**实现**：
- V2Mappers：metadata 归一链扩 childID（sessionId/sessionID/jobId/childID 四读）
- SessionNextEventHandler.handleToolProgress：提取 metadata.sessionID → ToolProgressInfo.childSessionId（新字段，域模型+handler双处）
- ToolProgressOutputInjector：注入 Running part metadata（sessionId/sessionID 双写）——输出注入与 id 注入统一
- TaskToolCard：showNavArrow 不再排除 isRunning；clickAction 同源；childID 直读兜底
- #181：showExpandIcon 恒 true（chevron 与导航箭头并存）；导航态恢复 copyText
- #182：take(2000) → chunked(4000) 分片渲染 + 20K 上限 + 截断提示（i18n ×15）；LocalTaskOutputFetcher + ChatViewModel.fetchFullTaskOutput（part 优先→子会话 transcript 回退→pickLonger）；TaskOutputFetch 纯函数

**测试**（+13，全量 1862 绿，i18n 669×14 过）：TaskOutputFetchTest 8（id 匹配/miss/角色拼接/上限/空/pickLonger/分片/childID 契约）；ToolProgressChildSessionInjectionTest 5（注入/仅 id/同引用/Completed 不动/空白不注入）；V2MappersTest +1（childID running 归一）

**真机 E2E**（E180 会话，全功能包装机）：
- #180 Running 跳转：后台 subagent（30×4s 慢爬取）运行中 → 父会话卡片点击 → 顶栏 "slow crawl foxtrot"（子会话）+ 内容 30 条目推理 + "Shell 运行中" 实时进度——进行中进入子会话全程实证
- #181 三图标并存：完成态卡片右缘 2x 裁剪视觉分析=箭头→+复制+chevron 三图标（原仅箭头）
- #182 回退路径实证：展开卡渲染出 "[User] You are a subagent spawned by..."——buildChildTranscript 独有的 [role] 前缀格式，证明实时拉取+回退+渲染闭环（本地 part 输出无此格式）
- 测试会话清理（REST DELETE）；全程 0 FATAL

## #187 + #188 ModelPicker 二级面板 + 默认模型反应式回显（完结，commit b65a4e02）

**实现**（grilling 定案 ①②③+Q10）：
- ModelPickerDialog 重做：星标降纯指示 + chevron 行内 accordion（无 variants 也显示 Q10）+ 二级面板（variant FilterChip pills 含「默认」档 + 默认模型 Switch，onSetDefault toggle 语义复用）
- onSelect 扩 variant 参数（null=默认档）→ ModelConfigDelegate.selectModel 同步 _selectedVariant
- ③：输入行 variant pill 移除（AgentModelVariantSelector/ChatInputBar/ChatScreenBottomBar 链路 + cycleVariant 死代码删除）
- #188：localDefaultModelFlow（StateFlow）→ ChatScreen collectAsStateWithLifecycle 响应式传入（原 getter 一次性快照=写入后星标永不回显根因）
- i18n 零新增键（复用 chat_default_model/chat_default_variant/a11y_icon_expand|collapse）

**E2E 抓获并修复一个真崩溃**：LazyColumn key 被 heredoc 转义事故写成字面量 "model_${provider.id}_${it.id}" → 全模型同 key → IllegalArgumentException 撞键崩溃（logcat 实证）。改字符串拼接构造。教训：quoted heredoc 里 Kotlin ${} 无需转义，勿写 ${'$'}。

**真机验证**（E187 会话，b65a4e02 包）：
- 抽屉开（模型列表+星标指示渲染正常，崩溃修复后 0 FATAL）
- Ox Alpha chevron（desc 节点定位 x1074）展开：pills 默认/High/Low/Max + 默认模型 Switch（uiautomator 节点级实证）
- 开关切换：switch 区像素 diff 35.4 + **星标同帧变色 diff 13.6**——#188 反应式回显闭环（旧 bug 星标永不变）
- High pill 点选关抽屉 → 输入行仅剩模型名（③ pill 移除实证，「模型变体」节点=UnfoldMore 图标 desc）
- 重开抽屉重展开：High pill check-slot 亮度 219.7 vs 其他 224.3——选中态持久
- 全量 1862 绿；测试会话清理

## #150 V1 连接速度真机复验（完结，本批 ④→⑤）

**环境**（用户 grilling Q4 授权自装）：宿主机隔离实例 /tmp/v1srv（1.18.18，XDG 隔离 + cwd=proj1/proj2）起 127.0.0.1:4198；adb reverse tcp:4198；debug intent 配 V1-4198（幂等新增服务器，与 4199 共存）。

**真机证据**（houji e69a99d8，logcat pid 过滤）：
- 探测：`ApiVersionDetector: Detected V1 API at http://127.0.0.1:4198 (version=1.18.18, known=V1)`——方案 B 单探即中（/global/health 66ms，零 V2 白探 RTT）
- 冷首连：attempt 00:44:19.929 → Connected 00:44:19.969 = **40ms**（模拟器区间 25-43ms 内 ✓，原基线 81-138ms ~3×）
- SSE 先行：Connected(19.969) 早于 Pre-loaded(20.016) ✓（首事件即翻转语义）
- 会话列表渲染正常（「目录为空」空态=V1 实例无会话）
- 启动后 1.3s 出现一次「Network recovered, reconnecting 2 server(s)」幂等重连（夜间 WiFi 状态回调抖动；#170 协调器设计行为，终态连接正常）
- V1 preload 警告一条：project "opencode" /session 返回对象非数组（1.18.18 过渡形态既有怪癖，JVM 契约已覆盖，不影响连接判定）
- 全程 0 FATAL；测毕 V1 进程终止 + reverse 4198 移除

**完结说明**：真机复验（2026-08-20 真机优先方针）达成；剩余「回复 upstream issue #1」为用户人工动作（对上游仓库发言），归入期末人工清单——不阻塞本条闭环。

**E2E 环境备注（供后续批次避坑）**：
- adb reverse 会因 svc wifi 折腾/USB 抖动静默丢失——每轮 E2E 前必查 reverse --list；reverse 断 → app 自动回服务器列表 + 电池 banner → 该屏底部「添加服务器」大按钮恰好在与聊天输入框相同的屏幕坐标（y≈2530），吞掉所有误点与打字（本案三度误入添加服务器对话框的根因）
- opencode build agent 对含糊指令会提澄清问题——问题面板禁用主输入框+抢焦点，E2E 提示词必须精确无歧义（"run bash sleep 90 then reply with the word DONE" 两次成功；含 "no questions ever" 等元指令反而触发提问）
- 待答问题面板的「忽略」按钮（底部）可可靠消解卡死（QuestionRejected → execution.interrupted → 下一 turn 接续）
