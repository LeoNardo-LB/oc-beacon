# 384-385 单卡压缩与注入精简卡片（2026-09-10）

> 状态：进行中
> 关联：（spec 路径，若有）·（issue 编号，若有）
> 来源：用户反馈 / grilling / E2E / 顺带发现

<!-- 过程中的取证/验证证据直接写本文件；backlog.md 只留 ≤3 行卡片。 -->

## §一 服务器真值与考古（session-a3be7221 ack-one，asOfSeq 742）

- 事件时间线（89 记录）：turn1-3=ack one/two/three（seq 6-612）→ command/run(613, cmd-ac12d1fd-11, name=compact) → compaction/start(614, cid=490a2f19, sourceCommandId=cmd-ac12d1fd-11 **与 run/done 三处 id 完全一致**) → summary(615) → 载体 user/message(616, source.kind=plugin+compactionId) → compaction/end(617) → command/done(618, kind=success, text='Compacted 8 history items (~5373 tokens).')
- **第 4 轮真相**：压缩后宿主推 context 刷新轮（seq 624 kind=user rpc 触发 + 625 agent-instructions/AGENTS.md + 626 plugin/插件配置 + 627 skill-catalog/技能目录）→ 模型回「收到。工作区约束已就位」——**文本墙=这批注入消息**（source.kind≠user），非压缩载体（616 已被 compactionBoundIds 过滤✓）
- **徽标「2」破案**：ChatTopBar 上下文占用环=projectedTokens/contextWindow 百分比（压缩后 ~2%）——正常工作非缺陷
- 病灶定位：#384=TranscriptPlan.cards() 直接拼接两族+MiscEventHandler 零吸收规则（两 store 同宿主，吸收协调天然合法）；#385=DshEventMapper.mapUserMessage 自注释「注入统一按 user 气泡渲染——差异化展示留给后续」+ #232/#234 EventCard 先例（system 墙→折叠卡）
- RPC 通道勘定：POST /api/session/list（斜杠路由）+ method=端点名 + session/list 用 _request / session/page 用 request + address{kind:session,sessionId}+throughSeq=asOfSeq；响应 records[].event

## §二 #384/#385 修复与实测（commit ce5b167a）

- 实现：CompactionEntry.commandDone + CompactionFolder.onCommandDone + MiscEventHandler 折叠层吸收（start 移除源命令卡/done 路由入 box/run 防御跳过）；Message.User.injectionKind + DshEventMapper 透传 + ChatMessageList EventCard 精简卡分支 + CompactionTranscriptCard 结算行；i18n ×15；单测 7 项新增全绿（CompactionFolder 3+MiscEventHandler 2+DshEventMapper 2）
- #384 四路径全绿：活体单卡（CENSUS[完成] box=1 cmd卡=0 结算='Compacted 8 history items (~5289 tokens).' 在卡内）+同进程重进（+6s box=1）+**跨进程冷启重建（+5s box=1 cmd=0）**+单测
- #385 部分绿：工作区指令/插件配置卡 ✓；skill-catalog 仍墙 ✗（数据层正确——残项见卡）
- 勘误链：MIUI uiautomator dump 陈旧/隐身双坑（打字验证误判+墙文本 dump 不可见）——驱动侧加 fresh_dump 去陈旧；视觉通道降采样 720px 必需（3.3MB 原图 vlm 空响应）
- 徽标破案：顶栏数字=上下文占用环百分比（压缩后 1-2%）非压缩计数

## §三 #385b 深挖链（A 线）——四个根因连环

- **#385b-1 id 碰撞（e9339163）**：DSH seq 仅会话内唯一＋mux 推跨会话事件→裸 seq-N 主键乒乓（两拉库 3 秒间隔行归属翻转实证）→修：seq-{sessionId}-{n}+v8 清存量+测试同步
- **#385b-2 水位缺口**：v8 只清行未重置 session_sync_state→增量同步跳过→早期事件永不回填；v9 补重置（f2868db8 链）
- **#385b-3 迁移崩**：表名误写 session_sync→启动即崩（logcat AndroidRuntime 实证）→勘误 session_sync_state 复活
- **#385b-4 残余（新卡 #386）**：水位重置后早期事件仍不回填 Room（尾页回填、首屏不回填）+流式会话 parts 零入库——重放/持久化管线门控问题，独立立项
- 渲染分支正名：InjCard 日志证明 kind 全透传（plugin/agent-instructions/subagent-settled/agent-message 实测过卡）；墙区零日志=墙为工具卡展开（合法渲染）非注入项
- 环境事故：/tmp tmpfs 100%（109 截图+17 库拉件）→清理至 3%；ack-five 会话模型跑飞（10929 事件读全仓）污染观测——后续演示会话用极简 prompt

## §四 #386 根因与修复

- 根因：MessageStore.upsertMessages 窗口过滤（created>=oldestCached）在「缓存非空但缺早期消息」时把重放回的早期事件整批静默丢弃——过滤防的「写了又被裁循环」只在裁剪边界（缓存≥LIMIT=1000）存在；v8 清库后 dsh-call 幸存行成 oldest 锚→seq-9..12 全滤掉
- 修复：cachedCount<SESSION_MESSAGE_LIMIT 时全量持久化（裁剪边界才过滤）；两处既有 windowSkip 测试补 count=LIMIT 桩 + 新增引导洞自愈用例；全量单测绿
- 实测：ack-four Room seq-9..12 连同 parts+injectionKind 全量回填 ✓；注入卡三 kind（agent-instructions/plugin/skill-catalog）数据+渲染分支双证齐全
- 观测方法论沉淀：盲滚自动化的三类自污染（触点误展开卡/长按文本选区/流式锚底）——像素验证须冷启+清选区+慢滚，或交用户真手（你演示我验收本模）

## 已完结卡片迁入（2026-09-10）

### **#384 DSH /compact 双卡并存——命令卡与压缩 box 同屏两张，违背 #374/#378 单卡承载裁决（演示①用户裁决）** `dsh` `ui` `command` `bug`
  - 演示①实况（2026-09-10 05:2X）：进行中相位=/compact 执行中...命令卡+会话压缩 压缩中...box 两张；完成相位=/compact 已完成命令卡+摘要实体仍两个
  - 根因方向：#378 Phase C 回收 tailSpec suppressByLiveCompactCommand 让位参数后，CommandFeedback（command/run|done 折叠）与 CompactionEntry（compaction/* 折叠）两族无互斥/吸收规则
  - 裁决链：#374「压缩应在同一张卡片里完成所有动作」→#375/#378「流内 box 全程承载」；终态应为 /compact 期间仅 box 一张（状态标签承载执行中/已完成），命令卡让位或并入
  - 证据：/tmp/acc1_inprogress.png、/tmp/acc1_done.png（vision 双帧确认）；头部徽标「2」待解（一次 /compact 后显示 2——修复分析时用服务器真值钉死）
  - 用户裁决升级（2026-09-10 演示批）：「不是说防止双份，而是从根源上杜绝两张卡片，将压缩的 title 与压缩内容放到一张卡片中就行了」——即命令卡实体在折叠层即被压缩实体吸收（sourceCommandId 配对），非渲染层过滤；单卡=title（会话压缩/命令名）+状态+done 结算文本+展开全文
  - 用户裁决升级（2026-09-10 演示批）：「从根源上杜绝两张卡片，将压缩的 title 与压缩内容放到一张卡片中」——命令卡实体在折叠层即被压缩实体吸收（sourceCommandId 配对），非渲染层过滤；单卡=title+状态+done 结算文本+展开全文
  - 迁入依据：2026-09-10 真机演示验收：用户裁决『基本上没问题』——/compact 单卡演化四路径全过（fold 吸收+活体演示 10-50-52 inflight→done 单卡、零命令卡残留）。样式翻新剥离为 #389（backlog.sh migrate 2026-09-10）

### **#385 上下文注入以大段原文渲染——压缩摘要/系统提醒应参考 DSH Web 精简卡片化（演示①用户裁决）** `ui` `dsh` `command` `design`
  - 演示①观察：压缩完成后转录中摘要+保留上下文（available_skills 技能目录全文、system-reminder 标签原文）以大块文本墙呈现（约一屏半），其下才接模型回复——信息噪音大、难扫读
  - 用户指令：「参考 dsh web，做成精简卡片的形式，而不是用一大段话来注入」——DSH Web 对 compacted-summary/system-reminder=紧凑引用块（默认折叠、可展开）
  - 设计面：注入类内容（压缩摘要/system-reminder/技能目录）精简卡片——标题+摘要行+按需展开；正文不默认铺开
  - 实现注意：先钉实体来源（该文本墙是 CompactionTranscriptCard 展开态还是消息面原始 text part 渲染）再定挂点；与 #384 同域同批修
  - 修复批实测（2026-09-10 06:4X，session-a242ed71 ack-four）：agent-instructions→「工作区指令」卡 ✓、plugin→「插件配置」卡 ✓；skill-catalog（seq-12）仍按普通用户气泡渲染大段墙 ✗——Room 数据层正确（payload 含 injectionKind=skill-catalog + Part.Text），渲染层三分支同路径仅此 kind 未命中
  - 伴随异常（同会话实测，待定性）：(a) pending echo 未拆除致 ack four 双气泡（MessageRemoved(pending-rpcId) 疑未命中）；(b) seq-10_text_ord_1（parent-agent 文本）以异 sessionID 挂在 seq-10 下并泄漏进气泡；(c) 该墙文本对 uiautomator 隐身（dump 空白/像素有墙）——渲染载体特殊
  - 证据：/tmp/rd_panel_top.png（三分屏结构）/tmp/rd_trio.png/tmp/acc385.db；分组层 ChatScreen displayItems 每条 user 消息独立成项（无合并）——分支未命中原因待查（疑 ChatMessageList 渲染分发前的其他捕获点或 part 级泄漏）
  - 迁入依据：2026-09-10 真机演示验收：插件配置收起卡（ⓘ 一行小条+可展开）过；技能目录刷新墙气泡（无 kind 标记的服务端注入）剥离为 #387 另行根因修复；今日新会话零注入现象剥离为 #388（backlog.sh migrate 2026-09-10）

### **#386 DSH 会话重放/持久化背离——v8 清库后重进早期事件不回填 Room 且内存件不全（注入项隐形）** `dsh` `data` `storage` `bug`
  - 实测（2026-09-10 08:0X ack-four）：v8 迁移清裸 id 行+水位重置后重进，尾部事件回填（dsh-call-*/seq-671/1440 在库）但 seq-9..12（首轮+三条注入）不入 Room；内存件不全→seq-10/12 注入项 0 部件隐形（渲染分支日志证明 kind 透传正常、seq-11 卡正常）
  - 伴随：ack-five 流式会话 parts 全数不入库（消息行在部件零，UI 靠内存渲染）；疑 HistorySyncManager 分页/持久化门控与 MessageEventHandler 持久路径的某一环节按 seq 窗过滤早期/流式事件
  - 影响面：冷启动离线缓存不完整（在线时入场重放可补内存）；#385 注入卡的 seq-10/12 显示被此卡阻塞
  - 证据：journal 2026-09-10-384-385 §三；/tmp/final_top.png 取证序（已清理，描述在案）
  - 已修复（commit 随 §四）：窗口过滤仅裁剪边界生效；实测 ack-four seq-9..12 全量回填（parts+injectionKind 齐）——待用户验收关闭
  - 迁入依据：2026-09-10 真机演示验收：窗口边界修复使迁移后回填的早期注入事件（插件配置卡等）稳定渲染于转录顶，演示 S1 实见（backlog.sh migrate 2026-09-10）

## §五 节点①真机观演验收（2026-09-10 10:50-10:53，A 模式）

- **彩排制**（用户指令：确认功能没问题之后再演）：r1-r9 共 7 轮内部彩排后才观演，正片零失误。彩排排掉的仪器雷（工具链经验，非 app 缺陷）：uiautomator 陈旧帧坐标（点击落到压缩卡头误触发展开——展开时被压缩历史让位是压缩卡合法语义；观演剧本因此零卡片点击，展开留给用户真手指）；「清除搜索」按钮是 content-desc 而非 text；输入框残留＋type.sh 尾巴滞后（改首过型打字＋5s settle 复核，连续 5 次一次过）；轮次检测改解析「轮次 N ·」最大号（视口只见尾部行，累计不可达）；ESC(111) 在 MIUI IME 有毒（注入全角括号/冻结 accessibility 嫌疑）全程弃用。
- **正片时间线**（截图 /tmp/w_s1_injcard.png、w_s1_wall.png、w_s2_bottom.png、w_s3_colors.png、w_s4_typed.png、w_s4_inflight.png、w_s4_done.png）：S1 [Ack] 3 顶部「ⓘ 插件配置」收起卡＋下滚见技能目录刷新墙气泡（#387 现场）→ S2 一加一底部旧压缩卡（09:20 次）→ S3 name three colors 轮次7 7s → S4 /compact 当面输入：正在压缩上下文：manual → 8s → 上下文已压缩；同一次压缩单卡演化，零命令卡残留。
- **用户裁决**：「基本上没问题」→ #384/#385/#386 三卡验收关闭（依据随迁移写入）。样式反馈定案 **#389**：压缩卡弃分割线形态，以思考卡为容器改文案复用（进行中圈进度条＋「正在压缩上下文…」；完成态「上下文已压缩」＋展开摘要体＋Compacted 结果行）。
- 随验收登记两新卡：**#387**（skill-catalog 刷新注入无 kind 标记→用户气泡墙；需对齐 dsh web 识别机制）、**#388**（同服务进程今日起新会话零注入；推送条件不明，需服务端历史 API 对照 dsh web 定责）。

## §六 #389 压缩卡思考壳统一（2026-09-10 11:1X）

- **#389 实现（同日裁决同日修）**：压缩卡弃 #217 分割线形态——[ReasoningBlock] 通用化（headerOverride/accentOverride/belowHeader 三参，思考路径零行为变化），[CompactionCard]（V1 摘要线/V2 触发线/尾部兜底）与 [CompactionTranscriptCard]（DSH 转录实体）双双借壳，文案统一 chat_compressing_context(_plain)/chat_summarized/chat_session_compact_failed（chat_compaction_* 5 键 ×15 语言退役，i18n-check 884 键全一致）。#384 吸收语义（commandDone 常驻行）、#221 文本锁存、#227 展开表、#375 双态可展、撤销长按全部沿袭。
- **验证**：compileDevDebugKotlin ✓ · 全量单测 ✓ · i18n-check ✓ · 真机（一加一会话既有 2 张完成卡换新装 + 活体 /compact：inflight「正在压缩上下文：manual」+末端旋转加载图标 → 13s 完成，截图 v389_done_state/v389_inflight/v389_done2）· VLM 复核：旧分割线零残留、无外层描边盒、与思考条同款。**服务器类型交互统一铁律**（ui-conventions §1）由此在压缩域闭环：V1/V2/DSH 三认领点单一视觉单一文案。
