# 154b-gist-附件-299-245-验证（2026-09-03）

> 状态：进行中（§一 代码+单测绿；真机 E2E 未做 · §二/§三 未开工）
> 关联：#154（154b 半）· #299 · #245 · spec `docs/specs/2026-08-21-error-report-github-design.md`
> 来源：用户指令（2026-09-03）："154b 可以做的话就开做吧！299 怎么测呢？245 你可以使用真机来进行模拟或端到端测试来复现吗？"

## §一 #154b secret gist 全量日志附件——代码落地（单测绿，E2E 待做）

### 可行性裁定（先决问题）

卡片旧前提「需 App 加 Gists 权限+重新授权」的 **App 形态**核实：注册走的是
GitHub App（`scripts/setup-github-report-app.sh` Stage 1 指向 settings/apps/new，
Issues: Read & write + Device Flow）；代码里 `scope=public_repo` 对 GitHub App
本来就被忽略（权限来自 App 配置）。官方文档（docs.github.com/en/rest/gists/gists）
现口径：**Create a gist 支持 GitHub App user access token**（老社区帖 403 是安装
token/旧状态）。故判定可行，真机 E2E 实测 App user token 的 gist 权限定界
（403 → 降级路径 + 用户在 App 设置补权限/重授权；201 → 直接成）。

### 设计决策（4 条硬规则）

1. **附件永不阻塞正文**：`uploadAttachment` 任何失败（含 403/限流/网络）降级为
   无附件继续上报；401 不在此特判——若 token 真失效，后续 issue/comment 步骤会
   以 Unauthorized 上抛引导重授权（spec §失败处理精神延伸）。
2. **防孤儿 gist**：gist 创建排在 24h 防刷判定**之后**——被防抖掉的重复上报
   不得留下孤儿 gist（单测 `suppressed duplicate within 24h creates no orphan gist`
   锁死 exactly=1）。
3. **截断保尾**：gist 单文件上限 ~1MB 是**字节**，CJK 按 UTF-8 最坏 3 字节/字符
   折算 → `MAX_GIST_CONTENT_CHARS = 300_000`（300K×3=900KB 留余量）；超出保尾
   截断（最新日志诊断价值最高），首行加「（前部已截断…）」标注。
4. **scope 参数双形态覆盖**：device code 请求 scope `public_repo` →
   `gist+public_repo`——GitHub App 形态下仍被忽略（零影响），OAuth App 形态下
   gist 生效（若用户实际注册形态与向导不符也能自愈）。

### 改动清单

- `data/github/GitHubApiClient.kt`：`createSecretGist(token, description, filename,
  content): Result<String>`——POST /gists，`public=false` 即 secret，解析 `html_url`。
- `data/github/ErrorReportService.kt`：顶层 `GistAttachment` 数据类；`report()`
  增 `attachment: GistAttachment? = null` 参数；`Outcome.IssueCreated/Commented`
  增 `gistUrl: String?` 字段（null=未请求或已降级）；`appendGistSection`（正文
  附件段，null 时原样返回）；`truncateGistContent`（internal 可测）。
- `data/github/GitHubDeviceFlowAuth.kt`：scope 双形态（见决策 4）。
- `ui/screens/settings/DiagnosticsViewModel.kt`：`Preview` 增 `attachFullLog=true`；
  `setAttachFullLog`；`buildFullLogAttachment`（提交时现导出——`DiagnosticLogRepository.export`
  同一脱敏管道同一数据面，filename `ocbeacon-diagnostics-<epochMs>.txt`）；
  `gistNote`（Done 消息附件注记——与该 VM 既有 Done/Failed 硬编码中文风格一致，属既有债）。
- `ui/screens/settings/DiagnosticsScreen.kt`：Preview 对话框 Checkbox（默认开）。
- strings ×15：`report_attach_full_log`。

### 验证（V1/V2 自动化层）

- `compileDevDebugKotlin` 绿。
- github 模块测试 28 绿（API 缝 +3：请求形状含 `"public":false`/文件名/内容、
  403→RateLimited、缺 html_url 判失败；服务缝 +7：成功附链接、失败降级不阻塞、
  防抖零孤儿 gist、评论路径带链接、未勾选零调用、超限保尾截断、小内容透传）。
  踩坑两枚已修：`GistAttachment` 是顶层类非嵌套（VM/测试引用改直引）；mockk
  `match{}` 原落在 createIssue 第 2 参（title），gist 链接在 body（第 3 参）。
- **全量 `testDevDebugUnitTest --rerun` BUILD SUCCESSFUL**。
- `i18n-check.sh` PASSED（767 keys × 14 语言一致）。

### 待办（下会话）

1. 真机 E2E：装机（stableRelease 本地 release.jks 原地覆盖装）→ 诊断页上报入口 →
   授权态探测（有 token 直接预览；无 token 走 8 位码——**需用户完成 GitHub 授权，
   代理不得代为授权用户账号**）→ 提交实测：issue 正文含 gist 段与否 + Done 消息
   附件注记。**核心问号：GitHub App user token 对 POST /gists 的真实权限**
   （文档说支持；403 则降级路径生效 + 用户需在 App 设置补权限或重授权）。
   测试后清理测试 issue/gist。
2. V6 用户人工验收（上报 UI 时间性/交互面）。
3. 提交信息：`feat: #154b 上报附加全量日志 secret gist（失败降级不阻塞正文）`。

## §二 #245 真机复现尝试（未开工）

修正方法论要点：历史区**中段**（离两端都远）+ 巨型消息区 + 冷启动进场窗口与
稳态各做 + 慢拖两方向 + 帧级像素差分；此前 8 轮的「冻」样本已勘误为范围尽头
语义（贴底朝更新方向拖=本不该滚）。装机选择：现装 stable 为 R8 release，探针
日志可能被裁——建议 devDebug（dev.leonardo.ocbeacon.dev 可共存安装）带
ScrollDiag/PtrDiag 取证。复现失败→如实报告，卡维持用户侧观察项。

## §三 #299 测试方案（未开工）

残余=观察项+已裁决不做（beforeSeq 游标链并发，风险高二阶）。可自动化部分：
logcat 计时标记（[299-probe]/[dearchive]）+ 长会话进场首渲染/翻旧页延迟实测；
用户侧部分：正常使用中观察长 DSH 会话进场是否还有 ~10s 三点加载体感。
