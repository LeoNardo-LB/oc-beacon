# 第五轮 web 走查记录（清欠账冲刺，2026-09-02）

> 目标：清空四轮后剩余 11 项未实测。截图 shots3-fixture/37-41（42/43 未落盘，对应结构以 DOM 记录为准）。

## 实测成功（5 项）
1. **Lightbox 实锤**（fixture）：点击历史图片 → 全屏 1280×720 dialog 打开 → Esc 关闭 ✓。截图37。
2. **QueueDock 单行直显实锤**（fixture）：忙碌时 composer Enter（dispatch keydown）→ 消息入 dock（「排队测试消息A 04:18」位于 todo 条与运行指示之间）；行悬停仅暴露「复制」——编辑/删除/插话控件在此态不显（Android QueueDock 三动作已是超集，差距方向不变）。截图39-40。
3. **分支按钮实锤**（fixture）：点「在新对话中分支」→ 会话树变化+导航切换（fixture 标题异步生成，(N) 自增形态未锐化）。截图41。
4. **重命名表单实锤**（真实端）：操作菜单→重命名 → dialog=标题输入框（预填当前标题）+取消/重命名双钮 → 取消关闭零变更。表单结构以 DOM 记录为准（42 号截图未落盘）。附带：上轮 onboarding 卡死自愈（第 3 次「继续」后确认态已持久化，本轮刷新不再弹）。
5. **仲裁会话安全打开 + 25 个子代理**（真实端）：非 OOM（19.6K 文本）；谱系控件第二样本（案卷目录会话 11 个 vs 仲裁会话 25 个）。无 jobs 徽标。

## 终判自动化不可达（8 项，均有源码文档兜底）
- 反馈备注编辑器（4 次尝试：提问接管层持续遮蔽完成回复行）
- Think 展开判别（fixture 样本单行，折叠/展开同视觉）
- `+` 命令启动器（composer 工具行实测不可见——源码-实测差异登记，疑空会话 hero 态专属或版本裁剪）
- 拖放附件（dispatchEvent DragEvent 序列不产生草稿轨；合成事件与真实 DnD 有差）
- 时间轴缩放/拖选可视效果（三轮无可见变化）
- jobs popover（全部安全会话无徽标；唯一疑似持有者 github 22小时会话 OOM 禁入）
- 悬停卡（8 次尝试：CUA move ×7 + 事件派发 ×1，均不触发；疑需 trusted pointer）
- QueueDock 编辑/删除/插话控件实操（见上，行悬停不显）

## 附注
- fixture 交互环境报告：demo 循环=提问/审批交替占用 composer；应对=答完提交+循环允许+停止生成三连，可恢复空闲。
