# backlog #309-#313 实现状态审计（2026-09-05，纯净上下文审计员产出）

> 方法：backlog.md + 链接文档 → app/src/main/kotlin 与 app/src/test grep/read + git log 取证（只读）。
> 用途：大批次（2026-09-04 /grill-me 定稿）排期依据——剩余工作按此表执行。

## 结论速览

| 卡 | 状态 | 剩余工作 |
|---|---|---|
| #309 批1 | 代码全落地（4 全+1 半） | 仅 ⑤-a RetryBanner 秒级倒计时（next 字段已在）；补真机 E2E 验收；journal 头部「③④⑤ 待做」已过时需修 |
| #310 批2 | 全未动工 | 全部 5 子项；建议序 ①→②→③→④→⑤（① UI 通道 100% 就绪只缺数据层三方法+发送/停止分流） |
| #311 批3 | 少量底座 | ①-a DSH workspace.archiveSession RPC+归档 UI；①-b 行菜单；①-c 左滑；①-d 多 workspace 全量建模（09-04 裁决升格必做）；② deliverables（先决 #310⑤）；③ skill 工具行+question 行 DSH 适配；④ 等待审批 FSM 增态（先读架构承重规则） |
| #312 S 池 | 全未动工 | 全部 5 子项（⑤ = DSH session.fork messageId 上 wire——DshApiClient.kt:272-275 形参丢弃未传 + 轮尾锚点 UI） |
| #313 FAB 迁移 | 主体落地 | 仅计数角标数据链 = #327 专卡；#327 闭链后本卡随验随迁 |

## 各卡证据（要点）

### #309
- ① goal.complete 第四钮：GoalSheet.kt:180-184 · ChatViewModel.kt:810 · DshApiClientTest.kt:1183（e7409f13）
- ② 压缩事件：DshEventMapper.kt:534-549/:514-527 + 4 用例（e7409f13）
- ③ Full access 确认：ChatScreenBottomBar.kt:121-146/:499-505（418253d5）
- ④ steer 长按：SendStopButton.kt:67/103-105 · DshApiClient.kt:763-766 mode=steer · 13 文件全链（d3a19dde）；载体改单键归 #326，长按语义保留
- ⑤-a 重试：事件链在（DshEventMapper.kt:463-485），RetryBanner.kt:28-64 无秒级倒计时；SessionRetryCard.kt:44 带倒计时组件 0 引用
- ⑤-b continue 钮：全链在（SseEvent.kt:61→…→TurnMaxTokensCard，45a9487e）

### #310（全未动工）
- ① subagent.prompt/interrupt/history grep 0；AgentSheet→子会话 ChatRoute 通道先在
- ② messageFeedback grep 0；feedback/record 仍在忽略目录（DshEventMapper.kt:595）
- ③ Plan grep 0；plan 事件 Ignored
- ④ 台账 grep 0；RenderableTurn 数据在
- ⑤ sessionReferenceResolver/SessionMention grep 0；@path 纯文本

### #311
- ①-a OpenCode 归档 API 在（ManageSessionUseCase.kt:43-46），DSH updateSessionFields=unsupported（DshApiClient.kt:198-202），0 UI 调用方
- ①-b SessionRow.kt:108-110 长按=详情；①-c SwipeToDismiss 全仓 0
- ①-d V011 取 workspace.list 首个 path（DshApiClient.kt:1110/1148-1149），无多行建模
- ③ question 分流在（PartContent.kt:221-259）；skill 行 0
- ④ Asking 在（SessionStatus.kt:19+SessionListStateBuilder.kt:99+SessionRow.kt:120）；PermissionAsked 不进 SessionStatus、FSM 无待审批态

### #312
- ① DateFormatters.kt:11 仅绝对格式；② katex grep 0；③ spill grep 0；④ 命令带图限制 grep 0
- ⑤ V1 wire messageID 在（V1ApiClient.kt:251-254）；DSH fork payload 仅 sessionId（DshApiClient.kt:272-275 messageId 形参丢弃）；SessionActionsDelegate.forkSession:645 无锚点参

### #313
- FAB 五入口 {TODO,AGENT,SHELL,GOAL,QUEUE}（ChatFabMenu.kt:72）；QueueSheet 三动作/只读/steer 门控（QueueSheet.kt:37-70）；QueueDock.kt 已删；i18n ×15 在
- 角标数据链断点 = #327（根因已钉：控制流增量帧被 onControlValue 按 baseline 单形解析丢弃，见 journal 2026-09-04-fix-308-always-326-327 §三）
