## OC Beacon 0.3.1-dev.10 — 2026-08-16

> 版本摘要：13 个问题点全根因修复批次（附件/滚底/任务面板/压缩/默认模型等）+ #120 i18n 合入。

### Fixed

**V2 附件（P0 根治）**
- 修复 V2 发送时附件被静默丢弃（文本发出、图片丢失、无任何提示）
- 修复重进会话后附件缩略图消失（双通道恢复：REST + SSE）

**发送后滚底（dev.9 修复失效的根因修复）**
- dev.9 的等待逻辑是死代码（永不执行），本次重写：等待消息真实到达再锚定底部，fling 惯性中发送也能正确回底

**V2 分页 400（根治）**
- 修复进会话增量同步静默失效与翻页静默终止（cursor 格式不兼容 + 本地构造锚点不可靠）
- 修复「发送后要退出重进才能看到回复」（双自愈：孤儿消息骨架 + 补漏失败重试）

**任务面板**
- 修复进行中任务不显示（轮询结果不触发刷新）
- 修复僵尸「进行中」永不恢复
- 任务列表改为创建时间倒序

**通知**
- 修复按 Home 回桌面后权限/问题通知被静默吞（可能错过权限请求）
- 修复导出失败无任何提示
- 修复日语/中文进入重试状态崩溃（占位符错误）

**其他**
- 修复每次进会话的 PermissionRequest 解析错误日志
- 会话删除后内部映射泄漏清理

### Added

- **压缩气泡**：压缩进行中显示独立透明轮廓气泡（标题栏+进度条）；压缩**完毕后**才弹出成功通知（此前是 HTTP 返回即提示）。V2 服务器的压缩开始事件已接入
- **默认模型**：模型选择器中星标设置/取消默认模型，新会话自动使用（服务器级记忆）
- **执行时长**：任务面板列表项右对齐显示运行时长（运行中实时走秒）
- **思考内容可复制**：思考块右上角复制按钮
- **截断提示**：超长命令输出被服务器截断时显示提示条（含完整输出文件路径）
- **标题栏时间**：消息时间当天显示到秒，非当天显示完整日期

### Changed

- 抽屉/面板高度统一只设 75% 上限（内容少时自然收缩，不再强制撑高）
- 卡片交互职责分离：展开/折叠/跳转由专门按钮承担，卡片本体不再可点击
- #120 文案国际化（15 语言）合入

### 人工验收清单（时间性现象，自动化无法覆盖）

1. 发送消息后视口跟随到底部（含滚到历史中部发送、fling 中发送）
2. 压缩进行中气泡观感（呼吸动画+进度条）与完成后 snackbar 时机
3. 任务运行中时长走秒显示
4. 附件发送后气泡缩略图与重进恢复
5. 模型选择器星标交互
6. 思考块复制按钮反馈（对勾动画）
7. 标题栏时间格式（当天含秒/历史含日期）

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.9...v0.3.1-dev.10)
