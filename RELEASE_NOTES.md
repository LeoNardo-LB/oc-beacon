## OC Beacon 0.3.0-beta.7 — 2026-08-13

> 本版聚焦**跳转定位全面根治**与**后台任务体系完善**：快速导航跳转从"偶发乱跳/定位到回复"到渐进定位精确落点；后台面板升级为**任务面板**（前台/后台标记）；新增顶部菜单**转为后台**入口；转后台提示改为消息流分割线。

### Added

- **任务面板**（原后台面板）：标题改为 Tasks；子代理条目新增**前台/后台标记**（前台 = 阻塞主会话，转后台后自动变后台）
- 顶部导航栏「更多」菜单新增**转为后台**入口（一键批量转后台，对应 TUI ctrl+b——不再依赖工具栏显示条件）
- **转后台提示分割线**：点击转为后台后，消息流显示「— Moved to background —」简洁分割线（替代大段英文系统提示卡片）
- **图标统一**：快速导航改为**地图定位图钉**；任务/后台入口（底部按钮、转后台工具栏、顶部菜单）统一为列表图标
- 快速导航**全量列表**（服务器补全历史 Q）+ loadAround 双向分页定位 + 抽屉式面板
- 后台完成通知**实时推送**（synthetic 独立气泡 + 三色状态 + 发起卡片互相引用）
- 模型选择单行化、后台面板布局优化（分隔线/模型徽标/高度约束）

### Changed

- **跳转定位架构根治**：状态机（Preparing→Measuring→Settling→Displayed）+ loading 蒙版 + 门控/锁单一真相源
- **渐进定位**：目标从视口底部小步逼近顶部（每步 ≤ 半屏 + 区域稳定检测）——彻底消除"一次大滚动 → 内容渐进测量 → 目标被推 → 振荡"
- M3 组件标准化（Card/SuggestionChip/CompactTag）、统一气泡容器 MessageBubble、统一状态图标 TaskStatusIcon

### Fixed

- **跳转定位到回复**：目标 key 前缀匹配（u_/t_ 同 id 歧义根治）
- **跳转乱跳/振荡**：一次定位到最终位置 + 测量稳定判定 4 轮 + 按 key 重定位节流
- **跳转后空白/闪烁**：预组合预热 + 透明门控渲染 + 解析完成信号 await
- **分页死锁**（V2 首次翻页游标格式）+ 上滑加载历史
- **后台 shell 卡 Running 永不结束**（V2 shell.exited 无 sessionID）+ 空壳 user 消息误导显示
- 快速导航合并连续无回复 user、synthetic 误纳、过滤空壳消息

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.0-beta.6...v0.3.0-beta.7)
