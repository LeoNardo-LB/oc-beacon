## OC Beacon 0.3.1-dev.14 — 2026-08-17

> 版本摘要：根治「权限卡片每次进入会话重弹」+ fling 跳底修复。

### Fixed

- **权限卡片反复弹出（根治）**：每次进入会话都弹出之前已点过允许的权限卡——
  根因是权限应答 API 端点契约不符（应答从未到达服务器，服务器侧待处理列表永不清除）。
  已修正为服务器真实契约，并增加失败复核机制（应答失败保留卡片供重试，不再假性消失）
- **全新安装后无法连接服务器**：调试配置激活时生成的服务器 ID 与实际存储不一致
  导致「Server config not found」——已修正（顺带修复版本探测停留在 v1 的问题）
- **滑动跳到底部**：惯性滑动浏览历史时，若上方有新消息到达，视口不再被强制拉回底部

### 人工验收清单

1. 进入挂有权限请求的会话 → 点「仅一次/始终允许」→ 退出重进 → 卡片应不再出现
2. 流式回复期间向上滑浏览历史 → 视口应停在浏览位置
3. 全新安装后 debug 参数激活服务器 → 应直接进入会话列表（无 config not found）

---

完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.13...v0.3.1-dev.14)
