## OC Beacon 0.3.1-dev.7 — 2026-08-15

> 版本摘要：快速定位（导航）三项修复 + 删除会话勘误 + V2 端点全量实测校准。

### Fixed

**快速定位（导航抽屉）**

- **列表只剩 1 条消息**（关键）：本地缓存快照只含用户消息，"连续无回复合并"逻辑在纯用户列表上恒误判——除最后一条外全部被吞掉。修复后完整显示（实测 82 条全部在列）
- **首次打开加载慢**：全量同步从"打开抽屉"移到"进入会话后台预取"——打开抽屉只读本地缓存，秒开
- **新增消息后不更新**：去掉一次性缓存短路，每次打开重新查询——发送的新消息立即可见

**删除会话**

- 上一版误判：`DELETE /api/session/:id` 实测**完全支持**（真实会话 204 + 列表确认删除；此前探测用了不存在的会话 id，把"会话不存在 404"误读为"端点不存在"）。撤回多余的双路探测

### 其他

- V2 服务器端点全量实测校准：rename/interrupt/compact 正常（compact 需带模型参数——App 实现已正确）；**fork 是服务器 bug**（接口注册冲突，任何请求都 400，等服务器升级）；share 确认无端点（菜单已隐藏）

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.6...v0.3.1-dev.7)
