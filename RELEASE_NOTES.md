## OC Beacon 0.3.1-dev.11 — 2026-08-16

> 版本摘要：会话状态显示不对根治（输出中却不显示/重进才见内容变多）+ 三项遗留补全。

### Fixed

**会话状态显示不对（根治，用户反馈）**
- 现象：会话明明输出中，但会话列表页和对话页都不显示"进行中"，退出重进才发现 agent 已输出完毕（内容变多）
- 根因一（正向缺失）：服务器连接中断窗口（后台冻结/信号波动）丢失回合开始事件后，状态机进入不可自愈的死局——而每 5 秒的服务器轮询明明拿到"运行中"却没有回写机制
- 根因二（反向误杀）：上一版引入的任务面板僵尸自愈缺少护栏，可能误杀正在输出的会话（本版修正并补三重护栏：活跃会话绝不因快照缺失强转空闲、未知状态枚举不翻译成空闲、目录参数防御）
- 修复后：中断恢复期内状态自动找回（连续 2 轮确认防抖）；正在流式输出的会话受新鲜度护栏保护

### Added

- **agent 徽标可点击**：点击消息中的 agent 徽标 = 选中该 agent（影响下一次发送）
- **标题栏类型小图标**：用户 👤 / 智能体 🤖 / 后台通知 🔔（与类型文字并列）

### Changed

- androidTest 测试源集修复（5 个模拟仓库接口对齐，恢复可编译）

### 人工验收清单

1. 发送消息后：会话列表该会话显示 Working、对话页有进行中指示、回复流式出现
2. 输出期间切到后台再回：状态应在 10 秒内恢复显示
3. 输出期间查看会话列表：Working 标记在场
4. 点击消息流中 agent 徽标：输入栏 agent 徽标变为所点 agent

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.10...v0.3.1-dev.11)
