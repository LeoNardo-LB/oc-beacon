## OC Beacon 0.3.1-dev.9 — 2026-08-16

> 版本摘要：缺 Q 三层根治 + 七项体验修复（撤回/滚底/计时/内容重复/agent 污染）+ 数据库 v3。

### Fixed

**快速定位缺 Q（根治）**
- 根因：向上翻页加载的更早消息只进内存不落本地库，而导航列表只读本地库——浏览过的消息在导航里永远缺失
- 三层修复：①导航列表 = 本地库 ∪ 内存（结构性保证导航 ⊇ 主对话所见）②进会话预取后与服务器全量对账（原子替换，消除幽灵消息）③浏览过的消息全部落库
- 新增数据库 v3 索引（导航查询长会话毫秒级）

**稳定性与体验**
- **撤回的消息复活**：对齐官方语义——撤回只暂存（stage），真正清理由下一条消息发送时隐式触发；原实现立即提交导致删除事件竞态、消息短暂复活
- **发送后视口不跟随**：发送时消息尚未回显，滚动请求落空——改为等消息实际到达后再锚定底部（在会话中间发送也正确跟随）
- **"会话进行中"图标过早**：从"发送瞬间"改为"消息实际到达服务器后"才显示
- **重进会话后思考计时停住**：恢复的快照是完结态导致计时冻结——会话仍在进行时继续计时
- **内容中段重复**（最后一句话随机重复）：对齐官方"ended 事件是全量值边界，直接覆盖"语义，替代"更长文本胜出"启发式
- **进会话 agent 被子代理污染**（如显示 deep-explore 需手动切回）：对齐官方"只回填 primary agent"防御
- 各类计时刷新间隔 0.1 秒
- 修复崩溃通知在后台线程弹 Toast 导致的二次崩溃

### Changed

- 消息合并策略：text.ended/REST 完结快照直接覆盖流式累积（官方 replayable full-value boundary 语义）

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.8...v0.3.1-dev.9)
