## OC Beacon 0.3.1-dev.1 — 2026-08-15

> 版本摘要：主对话流程四大 bug 根因修复（流式内容缺失/输入框清空/统计栏模型耗时/转后台气泡错乱）+ 历史审计批次全量落地。

### Added

- V2 提问工具适配新 form 协议（V2 服务器上提问卡片可正常弹出、回答与取消）
- 调试通道：adb 一键配置服务器并直达会话列表（debug 构建）

### Changed

- SSE 流式性能优化：解析管线降分配、增量落盘（O(delta) 写入）、日志降噪
- 认证头统一（147 处内联 Authorization 收敛为 auth(conn) 扩展）
- 会话内存热视图上限 1000 条（与本地缓存对齐，长会话不再无界增长）
- 无界容器统一治理：ToolSnapshotCache/目录缓存等全部 LRU 化
- 明文流量白名单化（默认禁 cleartext，仅 localhost/模拟器网段放行）+ R8 保留规则收窄

### Fixed

**主对话流程（本轮重点，全部根因修复 + V1/V2 双版本验证）**

- **流式回复大段缺失、回复完毕/重进后恢复**：断连重连后不再恢复错过的消息（attempt 计数被重置导致恢复逻辑永不触发）+ delta 事件字段缺失时被静默丢弃——两处根因修复，断连场景实测消息完整恢复
- **发送成功后输入框偶发不清空**：清空比对快照误用规范化文本（trim/文件提及拆分后与输入框原文不一致）——改用原始输入文本
- **消息统计栏有概率丢失模型名/耗时（子代理消息高发）**：step.ended 事件覆盖时抹掉了 step.started 写入的模型信息——改为非空字段合并，耗时计算不再归零
- **任务转后台后多出大段"用户回复"（实为子代理输出进了用户气泡）**：子代理/后台完成通知被误渲染为用户消息——现在正确显示为任务完成通知卡片

**其他**

- 终端输入乱序与退出清理（PTY 残留）
- 归档翻页：桶内分页缺陷（部分历史消息永久读不出）+ L3 校验 V2 游标增量补漏
- 多服务器状态隔离（L3 校验打错服务器/isConnected 语义）
- 生命周期：旋转/重建后对话框输入与文件批注保留、崩溃重启退避、低内存主动清理
- V2 part 标识契约（同消息推理/正文不再互相覆盖、REST 与流式不再双份渲染、耗时不再显示为天文数字）
- FileViewer 大文件分页性能、图片解码降采样（大图不再 OOM）
- 通知先查抑制再标记（抑制场景不再静默丢失）、目录浏览缓存失败毒化、设置页并发写

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.0-beta.9...v0.3.1-dev.1)
