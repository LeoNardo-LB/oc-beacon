## OC Beacon 0.3.1-dev.5 — 2026-08-15

> 版本摘要：统计栏去 token 圆环、顶栏上下文指示器常显（session 级权威数据双通道）、思考卡片计时 0.3s。

### Changed

- **消息统计栏精简**：移除 token 占比小圆环（无信息量）——现在只有 agent 徽标、模型图标+模型名、耗时，右对齐复制按钮
- **思考中卡片计时**：刷新间隔 1s → 0.3s（秒数小数进度更顺滑）

### Fixed

- **顶部导航栏上下文指示器随机消失（根治）**：不再依赖消息级 token 数据（会被刷新覆盖/冷启动归零），改用服务器权威的会话级统计——
  - 进入会话即从会话详情加载（V1/V2 通用）
  - V2 服务器实时推送用量更新事件（此前被忽略丢弃），流式期间即时反映
  - 修正计算口径：上下文占用 = 输入+输出+推理（缓存读取为历史累计，不计入——否则会显示 400%+）
  - 修复跨会话数据污染（其他会话的用量事件不再影响当前会话显示）

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.4...v0.3.1-dev.5)
