## OC Beacon 0.3.1-dev.13 — 2026-08-16

> 版本摘要：根治「发送后回复不可见」+ 权限自动允许 + 多项 UI 修复。

### Fixed

- **回复不可见（根治）**：发送消息后切后台/锁屏，再切回时 agent 回复不显示、必须退出会话重进——
  根因是后台期间 SSE 断连窗口丢失流式事件，而服务器仍「运行中」时无任何补漏机制（僵尸运行状态更永不补漏）。
  现在切回应用与 SSE 重连时自动增量对账补漏（不干扰正在进行的流式输出）
- **问题卡片消失**：回答完毕后问题卡片直接消失——服务器以「中断」态收尾时客户端不渲染。
  现在以折叠卡片保留（可展开查看问题与选项）
- **后台任务面板**：执行时长与状态图标重叠——时长移到图标正下方

### Changed

- **思考卡片**：复制改为选中部分文本复制（移除整卡复制按钮）；折叠高度与其他卡片一致（单行）
- **发送后跟随**：诊断加固（滚离底部后发送的跟随链路已实证 5/5 通过）

### Added

- **自动允许权限（设置）**：新开关——开启后对每个权限请求自动应答「始终允许」
  （服务器持久保存规则，同类请求不再询问；仅对可信服务器开启）

---

完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.12...v0.3.1-dev.13)
