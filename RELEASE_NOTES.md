## OC Beacon 0.3.1-dev.16 — 2026-08-20

> 本版为积压问题集中清偿版：修复会话压缩提示、SSE 连接稳定性、V2 兼容性等多项缺陷；提问卡完成 Material 3 原生化重构；附带数据库体积与流畅度优化。

### Added

- 开发构建启用 **StrictMode 主线程违规检测**（仅 debug 构建生效，只记录不致死）。

### Changed

- **提问卡重构为 Material 3 原生样式**：卡片骨架、选项行、多选 tab 全部换用 M3 组件；元信息上移标题栏、选择指示移至右侧；消灭明度锯齿、框中框、字号锯齿等视觉瑕疵；容器语言与其他内嵌卡片统一。
- **权限卡视觉统一**：迁入与其他交互卡一致的骨架与配色。
- **性能与体积优化**：工具调用结果落库改为 500 字符预览（数据库体积降约 90%），推理文本与工具输入同步截断；状态机事件拷贝节流；消息列表并发请求去重；凭据解密记忆化与启动期读取后台化；全库正则预编译。

### Fixed

- **会话压缩提示复活**：V2 服务器压缩完成后正确弹出提示并刷新会话（事件映射与增量文本字段契约修复）。
- **终端打开失败有反馈**：连接失败不再静默，改为弹出错误提示。
- **SSE 连接稳定性**：修复空闲约 40 秒的反复断连（服务器心跳帧被误吞）；修复重连冷却永不结束的死循环。
- **V2 兼容**：长会话历史翻页不可达、会话内切换 Agent 不生效、API key 服务器连接失败均已修复。
- **文件浏览**：修复文件读取通配符端点与跨平台路径推导。
- **token 统计恢复**：消息 token 用量与费用图标不再消失。
- **提问卡数据可靠性**：返回退出后答案不丢失；多选提问支持多个自定义答案；服务器恢复的提问卡作答不再被判为「未作答」；长选项页面可完整滚动到底部。
- **通知**：慢设备不再丢失任务完成通知；提问通知正文改为显示问题本身而非完整提示词。
- **权限自动批准**：自动批准规则从失效到真正生效。
- **空会话**：不再渲染残留的权限卡与提问卡。
- **Markdown 渲染**：修复重复文本链接错位。
- **状态指示**：消除等待回答期间「进行中/空闲」的反复抖动。

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.15...v0.3.1-dev.16)
