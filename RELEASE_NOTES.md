## OC Beacon 0.3.1-dev.19 — 2026-08-21

> 本版聚焦**跳转定位与长会话滚动性能**：快速定位全面提速并根治两类定位卡死；超长消息滚动卡顿与流式输出稳定性大幅改善。

> ⚠️ **BREAKING：更换签名密钥（2026-08-20）**——本版是**新签名首个发布版**。已安装 ≤0.3.1-dev.18（CI 签名包）的设备升级本版需**卸载重装一次**（签名不同无法覆盖安装），代价是重新录入服务器配置。

### Added

- **Baseline Profile**：聊天 UI 热路径 AOT 预编译，首进会话更流畅
- 新装设备免值守装包脚本 `scripts/miui-install.sh`（自动点穿 MIUI 安装弹窗，开发者工具）
- 开发者性能面板：常驻帧监测 + 卡顿相位分解 + 独立悬浮 HUD（dev 调试构建）

### Changed

- **快速定位引擎重构**：内部定位由反射调用改为官方 Compose API（升级 Compose 不再受内部字段变更影响）；用户提问跳转不再等待预解析，**直通定位，最多快 ~2.5 秒**
- 超长消息（数千字以上）滚动体验：块级分片渲染 + 解析移出主线程 + 预取窗口速度自适应——长会话 fling 不再出现巨帧卡顿

### Fixed

- **向前定位到最新提问卡 5 秒**：接近列表末端时定位循环空转超时——现检测到滚动边界即收场，**5.2s → 1.1s**
- **定位过程中偶发数据加载打架**：跳转期间自动翻页误触发导致定位反复修正、蒙版多挂数秒——已在触发点直读跳转状态机根除
- 定位到分片渲染的长消息时**落点错位 / 显示不完整气泡**：分片锚点映射修正
- 任务完成通知卡片的**「定位」按钮偶发超时失败**：分片消息查找适配
- 跳转后瞬间**气泡叠放/视口内裂变**：分片提交门控覆盖跳转稳定窗口
- **TalkBack 两个可达性问题**：Markdown 代码块点击、压缩卡片长按动作补语义
- SSE 断线重连残留协程**内存泄漏**（服务销毁后仍持有引用）
- 离线瞬断窗口打开终端**落错目录**：目录兜底重拉
- V1 服务器**连接偏慢**：探测排序 + SSE 先行并行预加载（#150）

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.18...v0.3.1-dev.19)