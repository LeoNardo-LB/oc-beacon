## OC Beacon 0.3.1-dev.6 — 2026-08-15

> 版本摘要：基于 opencode V1/V2 TUI/Web 源码系统性调研（11 域）的全量对齐修复批次——稳定性专项。

### Fixed

**稳定性（调研根因修复）**

- **会话被莫名打断**：客户端自动"僵尸解除"会误杀正在等待后台任务的主会话——已对齐官方语义（官方客户端从不在用户无操作时打断会话）彻底移除自动打断
- **agent 徽标跳变**（Build ↔ deep-explore）：turn 徽标锁定该轮首条消息的 agent（官方：每条消息 agent 写入一次永不改写）
- **顶部上下文指示器数值异常/压缩后不回落**：改用官方口径——最后一条带 token 的消息快照直接覆盖（含缓存读取），压缩后自然回落
- **消息"永不完成"**：step.ended 事件现在正确标记完成时间与结束原因（官方语义）
- **工具运行中无输出**：工具进度事件此前被整体丢弃——现在实时显示（bash 执行过程可见）
- **撤销(undo)回退到会话开头**：取错消息的顺序 bug——现在正确回退最后一条用户消息
- **思考卡片/统计栏计时**：0.3s 刷新，秒数带小数

**V2 协议兼容（双契约防御——服务器协议演进期）**

- 发送消息：新版包裹格式优先，旧版自动降级
- 提问回复/取消：question.v2 新端点优先，form 旧端点降级；新增 question.v2 事件映射
- 删除会话：双路探测（当前服务器版本暂不支持删除，已在服务器端登记为协议缺口）
- 会话错误（挂后台任务失败）现在会产生未读红点；会话跨目录移动后列表分组自动更新

### Changed

- **用户消息不再渲染 Markdown**（对齐官方 TUI）：所见即所得，避免下划线变斜体等意外转换
- **压缩会话**：分割线升级为可展开卡片——点击查看摘要全文；压缩完成后立即显示（无需重进会话）
- 统计栏移除 token 小圆环（保留 agent 徽标/模型/耗时/复制按钮）
- 事件序号（durable.seq）丢失检测接线——为断线精确补偿铺路
- V2 模型 variant（思考档位 low/high/max）数据链修复——variant 切换在 V2 可用

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.5...v0.3.1-dev.6)
