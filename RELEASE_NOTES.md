## OC Beacon 0.3.1-dev.17 — 2026-08-20

> 本版为功能版：agent 回复期间新增**堆积消息**（暂存本地、turn 自然结束后自动发送）与**TODO 面板**，彻底改变忙碌时发送按钮的交互方式。

### Added

- **忙碌时点发送弹出气泡菜单**：agent 回复中且输入框有内容时，点击发送按钮（转圈态）不再直接中断，而是弹出两项选择——「立即发送」（服务器排队、消息立即可见、不中断当前回复）与「堆积消息」（暂存本地，本轮自然结束后自动发送一条）。输入框为空时点击仍是直接停止。
- **堆积消息队列**：可无限堆积多条；每个 turn 自然结束后自动发送队首 1 条（手动停止、出错、退出App 均不发送）；跨重启持久保留；在面板中可编辑、删除、立即发送单条，支持长按拖拽排序与一键清空。
- **堆积与 TODO 面板**：输入栏第一行新增双角标图标入口（上角标=堆积条数，下角标=待办数）。面板含堆积清单与 agent 的 **TODO 计划列表**（只读实时镜像，显示完成进度）——TODO 页签在暂不支持该功能的服务器上自动隐藏。
- **「继续」按钮**：会话空闲且队列非空时，可在面板中手动放行队首 1 条，无需等待下一次回复结束。

### Fixed

- **任务面板换用新图标**（时钟+清单样式），原列表图标让给堆积/TODO 面板入口，双角标避免混淆。
- 修复堆积面板删除条目后列表短暂显示旧行的问题。

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.16...v0.3.1-dev.17)
