## OC Beacon 0.3.0 — 2026-08-29

> 本版把 **shell 命令正式纳入主对话流**（时间线化 + 默认展开 + 代码块呈现），并完成滚动与渲染稳定性的机制级重做：渲染前补偿体系统一、pausable prefetch 崩溃根因修复。0.3.0 线自 beta.1 以来历经 9 个测试版打磨，此为首个正式版。

### Added
- **Shell 命令卡时间线化**：用户发起的 shell 命令（! 前缀 / 服务器发起）作为主对话内容按时间序渲染，不再挤在贴底横幅；跨进程持久化，重启不丢
- **Shell 卡默认展开 + 代码块呈现**：命令完成卡默认展开，输出按等宽代码块逐行保留（对齐 TUI 形态）
- **对话流事件卡统一**：子智能体完成、shell 完成、目录变更三类事件共用统一事件卡（标签行 / 命令预览 / 严重度配色 / 展开两段式）
- **连续同内容 shell 卡去重**：重复命令自动折叠为首张 + ×N 计数
- **双 FAB 交互**：滚动到底与功能菜单两颗 FAB 支持沿屏幕边缘拖动停靠、等高贴边
- **错误上报 GitHub 化**：Diagnostics 一键上报到 GitHub Issue（GitHub 设备流认证 + 指纹查重防刷 + 错误信息可读化）
- **双方言 shell 加固**：V2 schema 漂移双形态容错、V1 同步阻塞超时 + 409 退避重试、truncated 输出全量续读

### Changed
- **渲染前补偿体系统一**：展开 / 流式 / 压缩导致的列表高度变化全部在渲染前移动视窗，全程无可见补偿动画
- **限速 fling**：视口自适应限帧位移，高速穿越未组合区域不再瞬移、不下跳
- **prefetch 机制级加固**：关闭 pausable 预组合（绕开框架缺陷 331365999），预组合走整 item 旧路径
- **滚动巨帧根治**：长消息分片 + 解析后台化，慢拖与快甩的帧预算双优化
- **V1/V2 压缩形态统一**：压缩消息归一化为轮次分割线呈现
- **术语体系落地**：CONTEXT.md 46 词条 + 15 语言显示词全量对齐（会话 / 轮次 / 堆积消息 / 子智能体 / 撤销 / 压缩……）

### Fixed
- **fling 中 FATAL 崩溃**：pausable prefetch 组栈失衡（框架缺陷 331365999 家族，IntStack.peek2 AIOOBE）——机制级根因修复，预组合不再走 pausable 路径
- **shell 卡半截显示**：markdown 状态流 Loading 首帧 + 揭示竞态导致卡片停在半截高度——verbatim 直渲染从构造上根除
- **历史 shell 卡输出体空白**：Room 持久化与三级输出续读链路核实完整
- **GitHub 上报系列修复**：API 模板转义、查重搜索限定词、设备流凭据编码
- 思考块 -18px 漂移、发送抖动、fling 下跳等多项真机回归修复

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.2.0...v0.3.0)
