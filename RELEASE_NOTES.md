## OC Beacon 0.3.2-dev.1 — 2026-08-24

> 0.3.2 版本线首个开发预览：承接 0.3.1-beta 之后的界面术语全量统一（15 语言）、错误上报 GitHub 集成增强，以及思考计时、FAB、一键已读等十余项修复。泡测稳定后晋升 0.3.2-beta。

### Added

- **GitHub App 注册向导**：设置中可按引导逐步注册 GitHub App，错误上报功能开箱即用，不再需要手工配置凭据。
- **上报 issue 标题区分度**：自动生成的 GitHub issue 标题现在带分类前缀 + 内容摘要 + 指纹签名，重复上报的问题一眼可辨。

### Changed

- **界面术语全量统一（15 语言）**：显示词与术语表对齐——智能体（原 Assistant）、会话、目录（原文件夹）、堆积消息、子智能体、轮次完成通知等；中英文界面均受影响。
- **「自动展开工具卡」设置自动迁移**：设置键升级后旧值自动保留，无需重新设置。
- **搜索更跟手**：会话搜索防抖从约 600ms 降到 300ms，逐键响应。
- **等待确认标记更省资源**：自适应节流（60 秒窗口），相关日志与网络请求削减约 92%。

### Fixed

- **思考计时滑动归零**：滑出视口再滑回，思考时长不再重置归零；历史残留思考卡静态显示不再反复跳变。
- **FAB 滑动展开溢出**：悬浮按钮滑动展开不再越界，收起动画不再拖拽停驻偏移。
- **一键已读跨服务器误伤**：标记全部已读只作用于当前服务器的会话，不再被其他服务器的时间偏差干扰（单服务器用户无感知，行为更正确）。
- **中断标记**：被中断的轮次现在正确显示中断原因标记，不再静默丢失。
- **历史会话权限/提问卡降级**：从缓存恢复历史会话时，权限与提问卡片不再退化为「未知工具」。
- **发送参数断层**：会话中指定模型/智能体发送时，参数现在真正随请求生效。
- **诊断日志 FATAL 过滤**：诊断屏级别过滤补上 FATAL 档，可单独筛选致命错误。

---
完整变更记录：[Full Changelog](https://github.com/LeoNardo-LB/oc-beacon/compare/v0.3.1-dev.22...v0.3.2-dev.1)
